---
phase: quick-260726-m3a
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - landing/src/pages/Privacy.jsx
autonomous: true
requirements: [PRIVACY-RETENTION-ACCURACY]

must_haves:
  truths:
    - "The /privacy retention bullet accurately states deleted trips are hidden from the app and excluded from sync/restore but the backend record persists until full account deletion is requested"
    - "The 'deleted trips are removed from your cloud backup' claim no longer appears anywhere in the file"
    - "The Last updated date reads 'July 26, 2026'"
  artifacts:
    - path: "landing/src/pages/Privacy.jsx"
      provides: "Corrected retention wording + updated LAST_UPDATED constant"
      contains: "July 26, 2026"
  key_links:
    - from: "landing/src/pages/Privacy.jsx line ~134 <LI>"
      to: "backend/functions/src/handlers/delete-trip.ts soft-delete behavior"
      via: "text disclosure matching server ground truth (no hard delete, record retained)"
      pattern: "excluded from"
---

<objective>
Correct one inaccurate claim in the public privacy policy (landing/src/pages/Privacy.jsx) so its "Data retention and your choices" section truthfully reflects the backend's soft-delete behavior, and bump the Last updated date.

Purpose: The /privacy URL is pasted into the Play Console Data Safety form and store listing. The current line "deleted trips are removed from your cloud backup" is false — DELETE /trips/:tripId only sets `deleted: true` (soft delete), never hard-deletes the Firestore document, and there is no purge/TTL/scheduled cleanup. An inaccurate data-safety disclosure is a compliance risk.

Output: Updated landing/src/pages/Privacy.jsx (text-only change to one bullet + one date constant). No code behavior changes. No app/emulator/backend involvement.
</objective>

<execution_context>
@/Users/coolman/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@landing/src/pages/Privacy.jsx

<ground_truth>
From backend/functions/src/handlers/delete-trip.ts (already verified during security review):
- DELETE /trips/:tripId performs `ref.update({ deleted: true, deletedAt, serverUpdatedAt })`.
- There is NO `.delete()` call — the document is NEVER hard-deleted (see the handler's own doc comment, line 19-20).
- A grep across backend/functions/src for purge/TTL/onSchedule/scheduler returns zero hits — no scheduled cleanup job exists.
- restore-trips.ts filters with `.where('deleted','==',false)` — deleted trips are excluded from restore/sync, not erased.
- The Firestore document persists indefinitely until the user requests full account deletion via email (the existing last bullet in the same <UL> block).
</ground_truth>

<interfaces>
Existing helper components defined at top of Privacy.jsx (reuse — do not create new styles):
- <LI>{children}</LI>   — list item, styled
- <Strong>{children}</Strong> — bold emphasis, styled
- <UL>{children}</UL>   — unordered list wrapper
Constant to update: `const LAST_UPDATED = 'July 25, 2026'` (line 13)
Target bullet: line 134, inside the "Data retention and your choices" <UL> block.
Adjacent bullets for tone reference: "You can clear your Home and Office locations at any time from Settings." / "To request deletion of your account and all associated cloud data, email us at ..."
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Correct retention bullet + bump Last updated date</name>
  <files>landing/src/pages/Privacy.jsx</files>
  <action>
Make exactly two edits, nothing else in the file.

1. Line 13: change `const LAST_UPDATED = 'July 25, 2026'` to `const LAST_UPDATED = 'July 26, 2026'` (matches existing 'Month DD, YYYY' style; today is 2026-07-26).

2. Line 134: replace the inaccurate bullet:
   `<LI>You can delete any trip in the app; deleted trips are removed from your cloud backup.</LI>`
   with wording that matches the server ground truth AND the surrounding list's tone. Use the existing <Strong> helper for emphasis consistent with other bullets. Suggested replacement (adjust prose to match tone, keep it a single <LI>):

   `<LI>You can delete any trip in the app. Deleting a trip removes it from your device and excludes it from cloud sync and restore, so it no longer appears on any device. The underlying backup record is retained on our backend rather than immediately erased; to have it and all associated cloud data permanently removed, request full account deletion below.</LI>`

   Requirements for the replacement text:
   - MUST NOT claim the trip is "removed from your cloud backup" or otherwise imply immediate cloud erasure.
   - MUST convey: hidden from app + excluded from sync/restore, but backend record persists until full account deletion is requested.
   - MUST use the already-defined <LI>/<Strong> helpers — introduce no new inline styles or components.
   - Keep it a single <LI> element within the existing <UL>.

Do NOT modify any other line, section, or claim in the file. Every other disclosure (account info, precise location, trip data, preferences, sharing, security/HTTPS/Keystore, children's clause, offline/local-only) was independently verified accurate and must stay byte-for-byte unchanged.
  </action>
  <verify>
    <automated>cd /Users/coolman/bizzle/landing && grep -q "July 26, 2026" src/pages/Privacy.jsx && ! grep -q "removed from your cloud backup" src/pages/Privacy.jsx && grep -q "excluded from" src/pages/Privacy.jsx && echo PASS</automated>
  </verify>
  <done>
- `LAST_UPDATED` reads 'July 26, 2026'.
- The string "removed from your cloud backup" no longer appears in the file.
- The retention bullet accurately describes soft-delete (hidden/excluded from sync-restore, backend record retained until account deletion).
- No other section of Privacy.jsx changed.
- grep verify command prints PASS.
  </done>
</task>

</tasks>

<verification>
- `grep -n "July 26, 2026" landing/src/pages/Privacy.jsx` returns line 13.
- `grep -n "removed from your cloud backup" landing/src/pages/Privacy.jsx` returns nothing.
- `git diff landing/src/pages/Privacy.jsx` shows exactly two logical changes: the date constant and the one retention <LI>. No other hunks.
- (Optional, if landing dev deps installed) `cd landing && npx eslint src/pages/Privacy.jsx` reports no new errors — JSX still valid.
</verification>

<success_criteria>
- Privacy policy retention wording matches backend soft-delete ground truth (no false "removed from cloud backup" claim).
- Last updated date bumped to July 26, 2026.
- Change is text-only; no other privacy disclosures altered.
</success_criteria>

<output>
After completion, create `.planning/quick/260726-m3a-fix-privacy-policy-retention-wording-to-/260726-m3a-SUMMARY.md`
</output>
