# v0.1 — Consolidated Device Verification Checklist

**Generated:** 2026-06-01
**Purpose:** Single on-device checklist covering every open human-UAT scenario and
un-signed-off verification item blocking the v0.1 milestone close. Run these on a
**real Android 14 device** and record pass/issue for each. When done, re-run
`/gsd-complete-milestone` — items confirmed here let the matching `human_needed`
verifications and `partial` UAT files be flipped to passed.

**Source files** (results should be back-ported into these after testing):
- `phases/02-core-tracking/02-HUMAN-UAT.md` + `02-DEVICE-CHECKLIST.md`
- `phases/04-trip-history/04-HUMAN-UAT.md`
- `phases/05-stats-analytics/05-VERIFICATION.md` (human_verification block)
- `phases/06-dashboard/06-VERIFICATION.md` (human_verification block — ⚠ partly stale, see Group E)
- `phases/07-polish-notifications/07-VERIFICATION.md` (human_verification block)
- `phases/09-authentication/09-HUMAN-UAT.md` + `09-UAT.md`
- `phases/10-backend-infrastructure/10-VERIFICATION.md` (human_verification block)
- `phases/11-sync-engine/11-VERIFICATION.md` (human_verification block)

> **Already cleared (no device work needed):** Phase 01 (`passed`), Phase 02
> verification (`pass`), Phase 03 HUMAN-UAT (its open items fold into Group B/C
> below), Phase 08 (fully verified + reconciled this session).

---

## One-time setup (prerequisites)

- [ ] Real Android 14 device with USB debugging enabled; `adb devices` lists it
- [ ] App installed from a fresh `flutter install` / `flutter run`
- [ ] **For Groups G–I (auth/backend/sync) only:**
  - [ ] `kGoogleServerClientId` in `lib/config/constants.dart` set to the real Web OAuth client ID from the Firebase Console
  - [ ] `android/app/google-services.json` present (real Firebase project)
  - [ ] Cloud Functions deployed (`firebase deploy --only functions`) — confirm `10-DEPLOY.md`
  - [ ] A test Google account available for sign-in

---

## Group A — Core Tracking (Phase 02) — 11 scenarios
*Satisfies the remaining device items behind `02-HUMAN-UAT.md`. Full steps in `02-DEVICE-CHECKLIST.md`.*

- [ ] **A1. Soft-deny location** — Fresh install; Start commute prompts for location; Deny returns to home with no crash
- [ ] **A2. Grant fine location from Start** — Second Start succeeds; tracking renders with `foregroundOnly` banner (background not yet granted)
- [ ] **A3. Upgrade to background location** — Start (after fine granted) prompts ACCESS_BACKGROUND_LOCATION; accepting removes the banner
- [ ] **A4. Record a normal trip (≥30s, ≥100m)** — Tiles tick live; Stop persists a Drift row (direction=unknown); "Trip saved" snackbar; shade clears
- [ ] **A5. Short-trip discard (D-10)** — A <30s or <100m trip shows "Trip too short to save" and does NOT persist
- [ ] **A6. Background + screen off keeps recording** — Home + lock doesn't stop recording; foreground notification stays visible; tiles continue on return
- [ ] **A7. Notification Stop button** — Stop action in the shade stops recording like the in-app button
- [ ] **A8. Single notification (D-14)** — Exactly ONE shade notification throughout tracking, never two
- [ ] **A9. App kill + relaunch** — Force-stop during tracking then relaunch → clean home, no ghost trip, no stale notification, no crash
- [ ] **A10. Foreground service type = location** — `adb shell dumpsys activity services com.example.traevy | grep foregroundServiceType` shows `location`
- [ ] **A11. Permanent deny + settings CTA (D-09)** — "Don't ask again" → Start shows "Open settings" CTA → opens system app-settings

## Group B — Trip Management (Phase 03) — now testable via Phase 4 trip cards
*These were `blocked` in `03-HUMAN-UAT.md` only because trip cards didn't exist yet. They do now.*

- [ ] **B1. Edit trip full flow** — From a trip card → Edit; change direction + adjust times; Save dismisses with "Trip updated"
- [ ] **B2. End-before-start validation** — Set end ≤ start → inline error "End time must be after start time." in error color; Save disabled
- [ ] **B3. Delete from trip card** — more_vert → Delete trip → confirm dialog → trip removed + "Trip deleted" snackbar; list updates reactively
- [ ] **B4. Phase 2 backfill** *(only if upgrading a build that had direction='unknown' trips)* — backfill runs once; no unknown-direction trips remain

## Group C — Trip History & Detail (Phase 04) — 5 scenarios

- [ ] **C1. History list layout** — Sticky date headers pin while scrolling; cards show direction arrow, distance, duration, departure time; "No trips yet" empty state
- [ ] **C2. Calendar markers + date filter** — Calendar toggle switches to TableCalendar; days with trips show dot markers; tapping a date filters the sub-list
- [ ] **C3. Trip card → detail nav** — Tapping a card opens TripDetailScreen; stats match the tapped trip
- [ ] **C4. GPS trip map** — Detail shows flutter_map with OSM tiles + drawn polyline; page scroll passes through the map (IgnorePointer)
- [ ] **C5. Two-step delete** — more_vert sheet (Edit / Delete) → Delete → confirm dialog → reactive removal + snackbar *(same as B3 — verify once)*

## Group D — Stats (Phase 05) — 3 verification items

- [ ] **D1. Empty state** — Stats with no trips: all 5 cards visible with "—" placeholders; trend chart flat at y=0
- [ ] **D2. Non-zero week total** — Record ≥1 GPS trip → Stats "This week" shows the formatted duration; no error state
- [ ] **D3. Best/Worst chip contrast (WCAG 1.4.1)** — Best chip has trending_down icon, worst has trending_up; distinguishable without color alone

## Group E — Dashboard (Phase 06) — ⚠ spec partly SUPERSEDED by Phase 8
*Phase 8 replaced the FAB/separate-tracking-screen/InProgressCard/AppBar-icons model with the hero card + MainShell tabs + in-place recording. Test the **current** behavior; the original wording is obsolete where noted.*

- [ ] **E1. Dashboard boots** — App opens to the dashboard; today's date in header; weekly loss card visible; "no commutes today" empty state when applicable
- [ ] **E2. Start from hero (SUPERSEDED: was "FAB Start commute")** — Tapping the hero START begins recording **in place** (no nav to a separate screen); permission dialog if ungranted
- [ ] **E3. Active state in hero (SUPERSEDED: was "InProgressCard")** — While recording, the hero shows RECORDING badge + elapsed + distance/speed/stuck; rest of dashboard stays scrollable/usable
- [ ] **E4. Tab navigation (SUPERSEDED: was "AppBar history/stats icons")** — MainShell tabs (Trips, Stats, Settings) switch correctly and recording continues
- [ ] **E5. Manual entry (+)** — The add-trip entry opens the ManualEntrySheet modal
- [ ] **E6. Weekly card tap** — Tapping the weekly loss/summary card navigates to Stats

## Group F — Polish & Notifications (Phase 07) — 4 verification items

- [ ] **F1. Instant dark mode** — Settings → Dark toggles theme instantly across all screens, no restart
- [ ] **F2. Theme persists + weekly summary** — Toggle Weekly summary ON, kill+relaunch: theme persists (Drift); weekly notification fires Sunday 6 PM (or simulated next Sunday)
- [ ] **F3. Daily reminder fires** — Daily reminder ON, set time +1 min, wait: notification fires; weekend-only vs weekday-only behavior correct
- [ ] **F4. Manual trip with traffic flows to stats** — Submit manual entry with Time in traffic = 0:30, Distance = 15.5 km → saves; Stats weekly traffic total INCLUDES it (not excluded) *(validates the bug fixed in 0b225a2 on a device)*

## Group G — Authentication (Phase 09) — merged HUMAN-UAT + UAT (deduped)

- [ ] **G1. Cold start smoke** — Kill + fresh launch: boots without errors; brief splash; settles on onboarding (signed out) or dashboard (session restored); no red screen/hang
- [ ] **G2. Google Sign-In** — Onboarding "Continue with Google" → native account picker → credential exchanged with Firebase → non-null ID token → one-time SignInSuccessScreen
- [ ] **G3. Success screen is one-time** — After "Let's go", kill+relaunch goes straight to dashboard (no confirmation screen again)
- [ ] **G4. Existing trips tagged to account** — Trips recorded before sign-in are retained and associated to your account after first sign-in (none lost/duplicated)
- [ ] **G5. Session persists across restart** — After sign-in, kill+relaunch stays signed in (no re-prompt); ID token present in `flutter_secure_storage`
- [ ] **G6. Settings Account section** — Shows "Sign in to back up" when signed out; shows account name/email when signed in
- [ ] **G7. Sign-in sheet cancel is safe** — Dismissing/cancelling the sign-in sheet returns you where you were, no crash, still signed out
- [ ] **G8. Degrade-to-guest** — Build without `google-services.json` (or init fails): app launches as guest; sign-in buttons disabled/dimmed; no crash

## Group H — Backend Infrastructure (Phase 10) — 1 live round-trip
*Folds with Group I (E2E sync exercises the same endpoints). Handler code already proven by the 48-test emulator suite; this is the live-token confirmation.*

- [ ] **H1. Live happy-path round-trip** — With a real ID token: POST /trips/sync → 200 + syncedIds, doc appears under Firestore `trips/` (userId forced to uid, deleted:false); GET /trips/restore returns it; DELETE soft-deletes (deleted:true, doc still present)

## Group I — Sync Engine (Phase 11) — 3 live scenarios

- [ ] **I1. Background sync to live backend** — Signed-in user records/edits/deletes a trip → sync_queue drains to Firestore → Settings "Cloud sync" row settles on "All synced"
- [ ] **I2. Connectivity + resume triggers** — Offline→online and background→resume each auto-drains pending rows once (no tight loop)
- [ ] **I3. Cloud restore round-trip** — "Restore from cloud" downloads Firestore trips, inserts new into Drift (skips duplicates), shows "Restored N trips" / "Already up to date"

---

## After testing

1. Back-port each result into the source UAT/VERIFICATION files (pass / issue + notes).
2. For any **issue**, capture it via `/gsd-capture` or `/gsd-debug` before close.
3. Re-run `/gsd-complete-milestone`:
   - Groups passing → matching `human_needed` verifications + `partial` UAT files flip to passed.
   - Any remaining → acknowledge as deferred at close, or fix first.

**Coverage map:** A→Phase 02 · B→Phase 03 · C→Phase 04 · D→Phase 05 · E→Phase 06 · F→Phase 07 · G→Phase 09 · H→Phase 10 · I→Phase 11. (Phase 01 & 08 already signed off.)
