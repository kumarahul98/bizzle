---
phase: 06-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - lib/config/constants.dart
  - lib/features/dashboard/providers/dashboard_providers.dart
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "All 10 Phase 6 dashboard string constants exist in constants.dart under a Phase 6 block with exact names and values from UI-SPEC"
    - "todaysTripSummariesProvider is a manual Provider<AsyncValue<List<TripSummary>>> that filters allTripSummariesProvider to today's local date"
    - "Provider uses whenData pattern so loading/error states propagate correctly"
    - "dashboard_providers_test.dart turns GREEN after this plan"
  artifacts:
    - path: "lib/config/constants.dart"
      provides: "Phase 6 Dashboard constants block"
      contains: "kDashboardFabIdleLabel"
    - path: "lib/features/dashboard/providers/dashboard_providers.dart"
      provides: "todaysTripSummariesProvider manual Riverpod provider"
      exports:
        - "todaysTripSummariesProvider"
  key_links:
    - from: "lib/features/dashboard/providers/dashboard_providers.dart"
      to: "lib/features/trips/providers/history_providers.dart"
      via: "ref.watch(allTripSummariesProvider)"
      pattern: "allTripSummariesProvider"
    - from: "lib/features/dashboard/widgets/weekly_summary_card.dart"
      to: "lib/config/constants.dart"
      via: "kDashboardWeeklySummaryTitle import"
      pattern: "kDashboardWeeklySummary"
---

<objective>
Add all Phase 6 string constants to constants.dart and implement the todaysTripSummariesProvider derived provider.

Purpose: Foundation layer — widget files in Plan 03 and DashboardScreen in Plan 04 depend on these constants and this provider. Getting this right first prevents constants.dart churn across later plans.
Output: 10 new constants in constants.dart, todaysTripSummariesProvider in dashboard_providers.dart, dashboard_providers_test.dart GREEN.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/.planning/PROJECT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/06-dashboard/06-CONTEXT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/06-dashboard/06-UI-SPEC.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/06-dashboard/06-PATTERNS.md

<interfaces>
<!-- Key contracts extracted from the codebase. -->

From lib/config/constants.dart (end of file, Phase 5 block pattern):
```dart
// ---------------------------------------------------------------------------
// Phase 5: Stats & Analytics
// ---------------------------------------------------------------------------

/// FAB label when tracking is idle (D-03).
const String kStatsHomeButtonLabel = 'View stats';
// ... (note: kStatsHomeButtonLabel is safe to REMOVE — only used in home_screen.dart which is deleted in Plan 04)
```

From lib/features/stats/providers/stats_providers.dart (exact provider pattern to mirror):
```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:traevy/database/daos/trips_dao.dart';
import 'package:traevy/features/trips/providers/history_providers.dart';

final Provider<AsyncValue<StatsSummary>> statsSummaryProvider =
    Provider<AsyncValue<StatsSummary>>(
  (ref) {
    final asyncTrips = ref.watch(allTripSummariesProvider);
    return asyncTrips.whenData(
      (trips) => computeStatsSummary(trips, DateTime.now()),
    );
  },
  name: 'statsSummaryProvider',
);
```

From lib/features/trips/providers/history_providers.dart:
```dart
final StreamProvider<List<TripSummary>> allTripSummariesProvider =
    StreamProvider<List<TripSummary>>(
  (ref) => ref.watch(tripsDaoProvider).watchAllSummaries(),
  name: 'allTripSummariesProvider',
);
```

From lib/features/trips/screens/history_screen.dart (date comparison pattern):
```dart
// groupTripsByDate uses:
final local = trip.startTime.toLocal();
final date = DateTime(local.year, local.month, local.day);
// compared against:
final today = DateTime(now.year, now.month, now.day);
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 6 constants block to constants.dart</name>
  <files>lib/config/constants.dart</files>
  <read_first>
    - lib/config/constants.dart — full file; understand current end-of-file position, existing Phase 5 block format (lines 241–310), and confirm kStatsHomeButtonLabel is present (to remove it, since home_screen.dart is deleted in Plan 04)
    - .planning/phases/06-dashboard/06-UI-SPEC.md — "Constants Required" section (exact constant names and values — the locked list)
    - .planning/phases/06-dashboard/06-PATTERNS.md — "lib/config/constants.dart (config, append)" section
  </read_first>
  <behavior>
    - After append: `grep "kDashboardFabIdleLabel" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardFabActiveLabel" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardTodaySectionLabel" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardEmptyStateLabel" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardErrorMessage" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardInProgressLabel" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardWeeklySummaryTitle" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardInTrafficLabel" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardTripCountSingular" lib/config/constants.dart` returns a match
    - After append: `grep "kDashboardTripCountPlural" lib/config/constants.dart` returns a match
  </behavior>
  <action>
Read `lib/config/constants.dart` in full first. Then append a new block at the very bottom of the file.

The block MUST use these EXACT constant names and values from the UI-SPEC (they are locked and the test file in Plan 01 references them by name):

```dart
// ---------------------------------------------------------------------------
// Phase 6: Dashboard
// ---------------------------------------------------------------------------

/// FAB label when tracking is idle (D-03).
const String kDashboardFabIdleLabel = 'Start commute';

/// FAB label when tracking is active (D-03).
const String kDashboardFabActiveLabel = 'Go to tracking';

/// Section heading above today's trip list (D-02).
const String kDashboardTodaySectionLabel = 'Today';

/// Empty-state label shown when no trips exist today (D-05).
const String kDashboardEmptyStateLabel = 'No commutes yet today';

/// Error-state label shown when the trips provider is in an error state.
const String kDashboardErrorMessage = 'Could not load trips.';

/// In-progress card title label (D-04).
const String kDashboardInProgressLabel = 'In progress';

/// Weekly summary card title label (D-06).
const String kDashboardWeeklySummaryTitle = 'This week';

/// Weekly summary traffic row label (D-06, Claude's discretion).
const String kDashboardInTrafficLabel = 'In traffic';

/// Trip count label when today has exactly one trip (D-06 pluralization).
const String kDashboardTripCountSingular = '1 trip';

/// Trip count label template when today has multiple trips.
/// Build the full string at call site: '$count trips'
/// using [kDashboardTripCountPlural] as the suffix.
const String kDashboardTripCountPlural = 'trips';
```

DO NOT remove `kStatsHomeButtonLabel` in this plan — it is still referenced by `home_screen.dart` which exists until Plan 04. Plan 04 will remove it after deleting `home_screen.dart`.

After editing, run `flutter analyze lib/config/constants.dart` to confirm no lint errors.
  </action>
  <verify>
    <automated>grep -c "kDashboard" /Users/coolman/Documents/Projects/bizzle/lib/config/constants.dart</automated>
  </verify>
  <done>grep returns 10 or more matches (one per constant). flutter analyze lib/config/constants.dart passes with 0 errors. All constant names match UI-SPEC exactly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create dashboard_providers.dart with todaysTripSummariesProvider</name>
  <files>lib/features/dashboard/providers/dashboard_providers.dart</files>
  <read_first>
    - lib/features/stats/providers/stats_providers.dart — exact provider declaration pattern: manual Provider, ref.watch, whenData, name: parameter
    - lib/features/trips/providers/history_providers.dart — allTripSummariesProvider type: StreamProvider&lt;List&lt;TripSummary&gt;&gt;; import path for TripSummary
    - lib/features/trips/screens/history_screen.dart — date comparison pattern using .toLocal() and DateTime(y, m, d) equality (look for groupTripsByDate or similar in history_providers.dart)
    - .planning/phases/06-dashboard/06-PATTERNS.md — "dashboard_providers.dart (provider, transform)" section with full file pattern
  </read_first>
  <behavior>
    - Provider is declared as `final Provider<AsyncValue<List<TripSummary>>> todaysTripSummariesProvider`
    - Uses `ref.watch(allTripSummariesProvider)` to get the upstream stream value
    - Uses `asyncTrips.whenData(...)` to filter — loading/error states propagate automatically
    - Date comparison: `trip.startTime.toLocal()` to get local time, then `DateTime(local.year, local.month, local.day) == DateTime(now.year, now.month, now.day)`
    - Provider has `name: 'todaysTripSummariesProvider'` parameter
    - No `@riverpod` annotation (manual provider only per project rules)
    - All public members have `///` doc comments per very_good_analysis
  </behavior>
  <action>
Create `lib/features/dashboard/providers/dashboard_providers.dart`.

This file creates the directory `lib/features/dashboard/providers/` (create it if it does not exist).

File content pattern (adapt from stats_providers.dart):

```dart
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:traevy/database/daos/trips_dao.dart';
import 'package:traevy/features/trips/providers/history_providers.dart';

/// Trips whose [TripSummary.startTime] (converted to local time) falls
/// on today's calendar date. Derived from [allTripSummariesProvider]
/// so no duplicate Drift subscription is opened.
///
/// Returns the same [AsyncValue] states (loading/error/data) as the
/// upstream provider.
final Provider<AsyncValue<List<TripSummary>>> todaysTripSummariesProvider =
    Provider<AsyncValue<List<TripSummary>>>(
  (ref) {
    final asyncTrips = ref.watch(allTripSummariesProvider);
    return asyncTrips.whenData((trips) {
      final now = DateTime.now();
      final today = DateTime(now.year, now.month, now.day);
      return trips.where((trip) {
        final local = trip.startTime.toLocal();
        final date = DateTime(local.year, local.month, local.day);
        return date == today;
      }).toList();
    });
  },
  name: 'todaysTripSummariesProvider',
);
```

Use ONLY `package:traevy/...` absolute imports — no relative imports.
All imports must be sorted alphabetically (very_good_analysis enforces this via directives_ordering).

After creating the file:
1. Run `flutter analyze lib/features/dashboard/providers/dashboard_providers.dart` — must pass with 0 errors.
2. Run `flutter test test/unit/features/dashboard/dashboard_providers_test.dart` — the 3 import errors on dashboard_providers should now resolve; tests may still fail if assertion logic needs tuning but the compile error must be gone.
  </action>
  <verify>
    <automated>flutter analyze lib/features/dashboard/providers/dashboard_providers.dart && flutter test test/unit/features/dashboard/dashboard_providers_test.dart 2>&1 | tail -5</automated>
  </verify>
  <done>flutter analyze returns 0 issues. dashboard_providers_test.dart either passes all 4 tests or fails only on assertion logic (not on missing import). File exports todaysTripSummariesProvider. grep confirms `whenData` and `toLocal()` are present in the file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Drift stream → Riverpod provider | allTripSummariesProvider emits List&lt;TripSummary&gt; from local SQLite — trusted local source, no network involved |
| Provider → widget | todaysTripSummariesProvider filters in memory — no user input, no injection surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Information Disclosure | todaysTripSummariesProvider | accept | Provider reads from Drift (local DB, user's own data). No user_id filter needed until Phase 8 auth — kDefaultUserId is used in Phase 1–7. Risk is zero in single-user local-first context. |
| T-06-04 | Tampering | constants.dart string values | accept | String constants are compile-time literals — not mutable at runtime; no injection risk. |
</threat_model>

<verification>
- `grep -c "kDashboard" lib/config/constants.dart` → 10 or more
- `ls lib/features/dashboard/providers/dashboard_providers.dart` → file exists
- `grep "todaysTripSummariesProvider" lib/features/dashboard/providers/dashboard_providers.dart` → match found
- `grep "whenData" lib/features/dashboard/providers/dashboard_providers.dart` → match found
- `flutter analyze lib/config/constants.dart lib/features/dashboard/providers/dashboard_providers.dart` → 0 issues
- `flutter test test/unit/features/dashboard/` → tests run (compile error on dashboard_providers resolved)
</verification>

<success_criteria>
- constants.dart has 10 new kDashboard* constants with exact names and values from UI-SPEC
- dashboard_providers.dart exports todaysTripSummariesProvider as a manual Provider (no @riverpod)
- todaysTripSummariesProvider filters using .toLocal() date comparison (same pattern as history_providers.dart groupTripsByDate)
- flutter analyze passes on both modified/created files
- dashboard_providers_test.dart compiles and tests run (RED → at minimum compile-GREEN)
</success_criteria>

<output>
After completion, create `.planning/phases/06-dashboard/06-02-SUMMARY.md`
</output>
