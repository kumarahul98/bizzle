---
phase: 20-first-run-login-skip
plan: 02
type: execute
wave: 1
depends_on: [20-01]
files_modified:
  - lib/features/dashboard/widgets/home_header.dart
  - lib/features/dashboard/widgets/guest_connection_indicator.dart
  - lib/config/constants.dart
  - test/widget/features/dashboard/guest_connection_indicator_test.dart
  - test/widget/features/settings/settings_screen_test.dart
autonomous: true
requirements: [AUTH-04]
user_setup: []

must_haves:
  truths:
    - "While in guest (skipped/local-only) mode the dashboard header shows a small, unobtrusive `cloud_off` 'not connected' indicator — and it is ABSENT once signed in (SC#3, D-06)."
    - "The indicator is non-nagging: it is a passive icon in the always-visible HomeHeader, NOT a snackbar/popup/toast, and does not block or interrupt the UI (SC#3, D-06)."
    - "Tapping the indicator opens the existing sign-in CTA (the sign_in_sheet) so a guest can sign in to back up trips without leaving the dashboard (SC#3, D-06)."
    - "Settings presents a clear 'Sign in to back up' entry while guest and the signed-in identity once signed in — the existing Phase 9/11 Account section behaviour is preserved and verified (SC#3, D-07)."
  artifacts:
    - path: "lib/features/dashboard/widgets/guest_connection_indicator.dart"
      provides: "GuestConnectionIndicator — ConsumerWidget showing cloud_off only when AuthGuest, tapping opens showSignInSheet; renders nothing (SizedBox.shrink) when signed-in/loading"
      contains: "class GuestConnectionIndicator"
    - path: "lib/features/dashboard/widgets/home_header.dart"
      provides: "HomeHeader becomes a ConsumerWidget hosting GuestConnectionIndicator next to the avatar"
      contains: "GuestConnectionIndicator"
    - path: "test/widget/features/dashboard/guest_connection_indicator_test.dart"
      provides: "Guest shows indicator + tap opens sheet; signed-in hides it"
      min_lines: 40
  key_links:
    - from: "lib/features/dashboard/widgets/guest_connection_indicator.dart"
      to: "authStateProvider + showSignInSheet"
      via: "ref.watch(authStateProvider) sealed switch + onTap → showSignInSheet(context)"
      pattern: "authStateProvider|showSignInSheet"
    - from: "lib/features/dashboard/widgets/home_header.dart"
      to: "GuestConnectionIndicator"
      via: "widget composition in the header Row"
      pattern: "GuestConnectionIndicator"
---

<objective>
Add the non-nagging guest "not connected" indicator with a sign-in CTA, and verify the Settings account entry stays correct across guest / signed-in (AUTH-04, SC#3, D-06/D-07).

Purpose: A skipped/local-only user must be able to SEE that their trips aren't backed up — calmly, without nagging — and have a one-tap path to sign in. The always-visible dashboard HomeHeader (the avatar-circle area) is the single surface present in guest mode, so the indicator lives there (Claude's Discretion resolved: dashboard header over shell app bar — MainShell has no app bar; the dashboard HomeHeader is the natural home). Tapping it reuses the existing `sign_in_sheet`. Settings already has the state-aware Account section (Phase 9/11) showing "Sign in to back up" while guest — this plan verifies it with a test and adds nothing redundant.

Output: a `GuestConnectionIndicator` widget wired into HomeHeader, a widget test proving guest-visible / signed-in-hidden / tap-opens-sheet, and a settings test asserting the guest "Sign in" entry. Depends on 20-01 only because the gate must already route a guest into MainShell for the indicator to be reachable (no file overlap — 20-01 does not touch these files).
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-first-run-login-skip/20-CONTEXT.md

<interfaces>
<!-- Extracted from the codebase — implement against these, do NOT re-explore. -->

HomeHeader (lib/features/dashboard/widgets/home_header.dart) is TODAY a StatelessWidget: a Row with [date+greeting Column, Spacer, 36×36 avatar circle]. It uses TraevyTokensExt + TraevyFonts. Convert it to a ConsumerWidget so it can read auth state, and place the GuestConnectionIndicator immediately LEFT of (or replacing the role of) the avatar circle — keep the avatar; add the indicator beside it with a small gap. Keep HomeHeader <100 lines (extract the indicator to its own file — done below).

AuthState (lib/features/auth/models/auth_state.dart): sealed AuthLoading / AuthGuest / AuthSignedIn — switch exhaustively, NEVER a default. The indicator shows ONLY for AuthGuest; AuthSignedIn and AuthLoading render `SizedBox.shrink()` (loading must not flash the indicator).

authStateProvider (lib/features/auth/providers/auth_providers.dart): `NotifierProvider<AuthStateNotifier, AuthState>`.

showSignInSheet (lib/features/auth/widgets/sign_in_sheet.dart): `Future<void> showSignInSheet(BuildContext context)` — the existing bottom sheet with the Google CTA + copy ("Back up your commutes" / kCopySignInSheetSubtext). REUSE it as-is for the tap action (D-06 says reuse if suitable — it is).

GuestConnectionIndicator (NEW — lib/features/dashboard/widgets/guest_connection_indicator.dart):
```dart
class GuestConnectionIndicator extends ConsumerWidget {
  const GuestConnectionIndicator({super.key});
  @override
  Widget build(BuildContext context, WidgetRef ref) {
    final auth = ref.watch(authStateProvider);
    return switch (auth) {
      AuthGuest() => IconButton(
          icon: const Icon(Icons.cloud_off_outlined),
          tooltip: kCopyGuestNotConnectedTooltip,
          iconSize: 20,
          // dim/muted colour from TraevyTokensExt.textMuted so it's calm
          onPressed: () => showSignInSheet(context),
        ),
      AuthLoading() || AuthSignedIn() => const SizedBox.shrink(),
    };
  }
}
```
Use TraevyTokensExt for the muted colour (consistent with the avatar's textMuted styling). NON-NAGGING: it is a passive IconButton, NO snackbar/dialog auto-shown — interaction is user-initiated only (D-06).

Constants to ADD to lib/config/constants.dart (no hardcoded strings):
- `const String kCopyGuestNotConnectedTooltip = 'Not connected — sign in to back up';`
(The sign-in sheet copy already exists: kCopySignInSheetHeadline / kCopySignInSheetSubtext.)

Settings Account section (lib/features/settings/screens/settings_screen.dart `_AccountSection`) ALREADY (Phase 9/11) switches on authStateProvider: AuthSignedIn → AccountRow + CloudSyncRow + RestoreRow + Sign out; guest/loading → a single SettingsRow(label: kCopySettingsGuestSignIn='Sign in to back up', onTap: showSignInSheet). D-07 is therefore satisfied by existing code — this plan VERIFIES it with a test, it does NOT modify settings_screen.dart.

Test fakes available: test/widget/features/onboarding/onboarding_screen_test.dart has _FakeAuthNotifier (extends AuthStateNotifier, returns a fixed AuthState) + firebaseReadyProvider override=false to avoid platform channels. test/widget/features/settings/settings_screen_test.dart has _FakeUserPreferencesDao + ProviderScope override + _pumpScreen patterns to reuse.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: GuestConnectionIndicator wired into HomeHeader</name>
  <read_first>
    - lib/features/dashboard/widgets/home_header.dart (current StatelessWidget Row layout, token/font usage)
    - lib/features/auth/models/auth_state.dart (sealed variants)
    - lib/features/auth/providers/auth_providers.dart (authStateProvider)
    - lib/features/auth/widgets/sign_in_sheet.dart (showSignInSheet signature + behaviour)
    - lib/config/theme.dart (TraevyTokensExt.textMuted)
    - lib/config/constants.dart (where to add kCopyGuestNotConnectedTooltip; existing sign-in sheet copy)
  </read_first>
  <action>
    Create lib/features/dashboard/widgets/guest_connection_indicator.dart as a ConsumerWidget exactly per the interfaces block: watch authStateProvider, render a calm muted `Icons.cloud_off_outlined` IconButton (iconSize 20, tooltip kCopyGuestNotConnectedTooltip, muted colour via TraevyTokensExt.textMuted) ONLY for AuthGuest, and `SizedBox.shrink()` for AuthLoading | AuthSignedIn (exhaustive switch, no default). onPressed → `showSignInSheet(context)`. Dartdoc citing SC#3/D-06: non-nagging passive indicator, user-initiated CTA, hidden once signed in.

    Add `const String kCopyGuestNotConnectedTooltip = 'Not connected — sign in to back up';` to lib/config/constants.dart near the sign-in copy constants.

    Convert lib/features/dashboard/widgets/home_header.dart to a ConsumerWidget (build(context, ref)) and place `const GuestConnectionIndicator()` in the header Row just before the existing avatar circle (small SizedBox(width: 4) gap). Do NOT remove the avatar. Keep the file <100 lines (the indicator lives in its own file, so HomeHeader stays small). Keep const constructors where possible. No behavioural change when signed in (indicator is shrink()).
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter analyze lib/features/dashboard/ && grep -q "class GuestConnectionIndicator" lib/features/dashboard/widgets/guest_connection_indicator.dart && grep -q "showSignInSheet" lib/features/dashboard/widgets/guest_connection_indicator.dart && grep -q "GuestConnectionIndicator" lib/features/dashboard/widgets/home_header.dart && grep -q "kCopyGuestNotConnectedTooltip" lib/config/constants.dart</automated>
  </verify>
  <done>GuestConnectionIndicator exists, shows cloud_off only in guest mode and shrink() otherwise, taps to showSignInSheet; HomeHeader is a ConsumerWidget hosting it beside the avatar; tooltip constant added; analyze clean.</done>
</task>

<task type="auto">
  <name>Task 2: Widget tests — indicator visibility/tap + settings guest entry</name>
  <read_first>
    - test/widget/features/onboarding/onboarding_screen_test.dart (_FakeAuthNotifier + firebaseReadyProvider override; reuse to script AuthGuest vs AuthSignedIn)
    - test/widget/features/settings/settings_screen_test.dart (_FakeUserPreferencesDao + ProviderScope override + _pumpScreen; extend for the guest Account-row assertion)
    - lib/features/dashboard/widgets/guest_connection_indicator.dart (the widget under test)
    - lib/features/settings/screens/settings_screen.dart (_AccountSection guest branch: kCopySettingsGuestSignIn row)
  </read_first>
  <action>
    Create test/widget/features/dashboard/guest_connection_indicator_test.dart, reusing _FakeAuthNotifier + firebaseReadyProvider=false override (no platform channels):
      1) "guest mode shows the cloud_off indicator" — override authStateProvider with AuthGuest, pump GuestConnectionIndicator in a MaterialApp+theme, expect `find.byIcon(Icons.cloud_off_outlined)` is present.
      2) "signed-in hides the indicator" — override with AuthSignedIn(...), expect the icon is absent (find SizedBox.shrink / no cloud_off icon).
      3) "loading hides the indicator" — override with AuthLoading, expect absent (no-flash).
      4) "tapping the indicator opens the sign-in sheet" — guest mode, tap the IconButton, pumpAndSettle, expect the sheet content (e.g. `find.text(kCopySignInSheetHeadline)` or the GoogleContinueButton) is shown. Keep firebaseReady=false so the sheet renders its disabled-button degrade path without invoking real sign-in.
    Wrap in a theme that provides TraevyTokensExt (mirror how onboarding/settings tests build their MaterialApp with buildLightTheme()).

    Extend test/widget/features/settings/settings_screen_test.dart with a test asserting the guest Account entry (D-07): with authStateProvider overridden to AuthGuest (and the existing prefs/firebaseReady overrides), pump SettingsScreen and expect `find.text(kCopySettingsGuestSignIn)` is present; assert the signed-in-only rows (e.g. Sign out / kCopySettingsSignOut) are ABSENT in guest mode. (If the existing suite already covers the signed-in Account row, do not duplicate — only add the guest-entry assertion if missing.)

    All assertions automated.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter test test/widget/features/dashboard/guest_connection_indicator_test.dart test/widget/features/settings/settings_screen_test.dart</automated>
  </verify>
  <done>The indicator test proves guest-visible / signed-in-hidden / loading-hidden / tap-opens-sheet; the settings test proves the guest "Sign in to back up" entry is present (and sign-out absent) in guest mode — D-06/D-07 verified.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| guest UI ↔ user awareness | The indicator is the only signal a guest gets that trips aren't backed up. If it nags (snackbar/popup) it degrades UX; if it's absent the user is unaware (SC#3). |
| indicator tap ↔ sign-in flow | Tapping must lead to the real sign-in CTA, not a dead end. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-05 | Denial of service (UX nagging) | guest connection indicator | mitigate | The indicator is a passive IconButton in the always-visible header — NO auto-shown snackbar/dialog/toast. The sign-in sheet opens only on user tap. The widget test asserts it is an icon (not a snackbar) and that tap (not auto) opens the sheet (D-06). |
| T-20-06 | Information disclosure | indicator / sheet logging | accept | No new logging; showSignInSheet's existing security invariants (never log token/credential) are unchanged. The indicator reads only the sealed AuthState (no PII). |
| T-20-07 | Tampering | indicator state correctness | mitigate | The indicator switches exhaustively on the sealed AuthState (no default) so a future variant is a compile error; AuthSignedIn and AuthLoading both render shrink(), asserted by tests, so a signed-in user never sees a stale "not connected" badge. |
</threat_model>

<verification>
- `flutter analyze lib/features/dashboard/` reports no new issues.
- `flutter test test/widget/features/dashboard/guest_connection_indicator_test.dart test/widget/features/settings/settings_screen_test.dart` is green: indicator visible only in guest, hidden when signed-in/loading, tap opens the sign-in sheet; settings shows the guest "Sign in to back up" entry.
- The full existing `flutter test` suite stays green — HomeHeader became a ConsumerWidget (any existing HomeHeader/dashboard widget test that pumps it must now run under a ProviderScope; update those if present).
</verification>

<success_criteria>
- SC#3: In skipped/local-only mode the dashboard shows a calm `cloud_off` "not connected" indicator (no nagging), and tapping it opens the sign-in CTA; once signed in the indicator disappears. (Sync being disabled in guest mode is already enforced by the Phase 11 engine's AuthSignedIn gate — unchanged.)
- D-06 implemented: unobtrusive header indicator, no persistent snackbars, reuses sign_in_sheet.
- D-07 verified: Settings presents the guest "Sign in to back up" entry and the signed-in identity once signed in (existing behaviour, now test-covered).
</success_criteria>

<output>
NO SUMMARY file for this phase (per phase instruction). Report completion to the orchestrator directly.
</output>
</content>
