---
phase: 10-backend-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/firebase.json
  - backend/.firebaserc
  - backend/firestore.rules
  - backend/firestore.indexes.json
  - backend/functions/package.json
  - backend/functions/tsconfig.json
  - backend/functions/.gitignore
  - backend/functions/jest.config.js
  - backend/functions/src/index.ts
  - backend/functions/src/types/trip.ts
  - backend/functions/src/utils/auth.ts
  - backend/functions/src/utils/firestore.ts
  - backend/functions/src/utils/validation.ts
  - backend/functions/src/utils/response.ts
  - backend/functions/src/utils/__tests__/validation.test.ts
  - backend/functions/src/utils/__tests__/auth.test.ts
autonomous: true
requirements: [BACK-02, BACK-03, BACK-04]
user_setup: []

must_haves:
  truths:
    - "backend/functions builds clean (npm install && npm run build, zero TS errors, no any)"
    - "The api function boots on the Functions emulator and GET /health returns 200 {status:'ok'}"
    - "Firestore Security Rules deny all direct client access (deny-all match)"
    - "firebase.json wires both firestore.rules AND firestore.indexes.json (indexes target exists)"
    - "The shared Trip and TripDoc types exist and mirror the Drift trip fields (Phase 10 to 11 contract)"
    - "auth.ts extracts the Bearer token and verifies it; missing/invalid throws a typed AuthError"
    - "validation.ts zod schemas accept valid trip payloads and reject malformed ones; trips array is capped at 1000"
    - "id and tripIdParam are validated as UUIDs (z.string().uuid()) per D-09"
    - "Util unit tests pass (zod accept/reject incl. array-cap, Bearer-header parsing) with no emulator"
  artifacts:
    - path: "backend/firebase.json"
      provides: "functions + firestore(rules + indexes) + emulators block (auth 9099, functions 5001, firestore 8080, ui 4000, singleProjectMode:true)"
      contains: "singleProjectMode"
    - path: "backend/.firebaserc"
      provides: "default project travey-298a7"
      contains: "travey-298a7"
    - path: "backend/firestore.rules"
      provides: "deny-all rule"
      contains: "allow read, write: if false"
    - path: "backend/firestore.indexes.json"
      provides: "Firestore indexes target — composite index for restore query (userId==, deleted==)"
      contains: "indexes"
    - path: "backend/functions/package.json"
      provides: "node20 engines, pinned deps (matching 10-RESEARCH verified table), build/lint/serve/test scripts"
      contains: "firebase-functions"
    - path: "backend/functions/tsconfig.json"
      provides: "strict TS, CommonJS, outDir lib, rootDir src"
      contains: "strict"
    - path: "backend/functions/src/index.ts"
      provides: "admin initializeApp + exported Express app + api onRequest + GET /health"
      exports: ["app", "api"]
    - path: "backend/functions/src/types/trip.ts"
      provides: "Trip type (mirrors Drift) + Firestore TripDoc type (server metadata)"
      exports: ["Trip", "TripDoc"]
    - path: "backend/functions/src/utils/auth.ts"
      provides: "Bearer extraction + verifyIdToken + typed AuthError"
      exports: ["extractBearerToken", "verifyAuth", "AuthError"]
    - path: "backend/functions/src/utils/firestore.ts"
      provides: "getFirestore + FirestoreDataConverter<TripDoc> + tripsCollection()"
      exports: ["tripsCollection", "tripConverter"]
    - path: "backend/functions/src/utils/validation.ts"
      provides: "zod tripSchema (uuid id), syncTripsBody (.min(1).max(1000)), tripIdParam (uuid), kMaxSyncBatchTrips"
      exports: ["tripSchema", "syncTripsBody", "tripIdParam", "kMaxSyncBatchTrips"]
    - path: "backend/functions/src/utils/response.ts"
      provides: "consistent { statusCode, body: { data?, error? } } helper"
      exports: ["sendSuccess", "sendError"]
  key_links:
    - from: "backend/functions/src/index.ts"
      to: "Express app"
      via: "onRequest(app)"
      pattern: "onRequest\\("
    - from: "backend/functions/src/utils/firestore.ts"
      to: "Firestore trips collection"
      via: "FirestoreDataConverter<TripDoc>"
      pattern: "withConverter"
    - from: "backend/functions/src/utils/auth.ts"
      to: "Firebase Admin Auth"
      via: "getAuth().verifyIdToken"
      pattern: "verifyIdToken"
    - from: "backend/firebase.json"
      to: "backend/firestore.indexes.json"
      via: "firestore.indexes config key"
      pattern: "firestore.indexes.json"
---

<objective>
Create the self-contained `backend/` Firebase project and ALL shared infrastructure for
the three trip endpoints, with NO endpoint business logic yet. This plan establishes the
scaffold (firebase.json, .firebaserc, deny-all rules, firestore indexes, package.json,
tsconfig), the Phase 10 to 11 type contract (`Trip` / `TripDoc`), and the four shared utils
(auth, firestore, validation, response) that the real `/trips/*` handlers in Plan 02
will consume.

The Express router in this plan mounts ONLY a minimal `GET /health` route so the
`api` function builds and boots clean on the emulator on its own. The three real
`/trips` routes are added in Plan 02.

Purpose: A clean, bootable foundation removes all scaffolding and contract risk from
Plan 02, which then adds three handlers against fixed interfaces (no scavenger hunt).
This plan directly satisfies success criterion 5 (deny-all rules) and lays the
auth/validation foundation for criteria 1-4.

Output: A `backend/` directory that installs, builds clean (strict TS, no `any`),
boots on the Firebase Emulator Suite, serves `GET /health` returning 200, ships deny-all
Firestore rules + a firestore.indexes.json target, and has green unit tests for the
pure-logic utils.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-backend-infrastructure/10-CONTEXT.md
@.planning/phases/10-backend-infrastructure/10-RESEARCH.md
@CLAUDE.md
@lib/database/tables/trips_table.dart

<notes>
- Version pins and patterns below come from `10-RESEARCH.md` (npm-registry-VERIFIED
  2026-06-01) plus the locked CONTEXT decisions (D-01..D-15) and CLAUDE.md backend rules.
  RESEARCH.md is authoritative for every dependency version — use the EXACT pins in Task 1
  (they match the RESEARCH Standard Stack table). Each `@types/X` is kept on the SAME major
  as its runtime package X (the RESEARCH-flagged anti-pattern is mixing majors, e.g. express 5
  with @types/express 4, or supertest 7 with @types/supertest 6).
- **Node runtime (per RESEARCH D-02 / Open Q2 / Pitfall 9):** deploy runtime is
  `engines.node: "20"` (locked D-02). Node 20 is a SUPPORTED but DEPRECATED Cloud Functions
  runtime — Firebase and firebase-admin 13 recommend **Node 22**. Deploy with 20 per the lock;
  if a future deploy is blocked, surface the Node 22 recommendation to the user (L1 callout).
  Local Node (25) builds/tests fine — the runtime pin is independent of the local interpreter.
- This is a GREENFIELD backend: no `backend/`, `functions/`, or `firestore.rules` exist.
- DO NOT touch the repo-root `firebase.json` — that is FlutterFire's app config (D-01).
  The backend gets its own config dir; all paths below are under `backend/`.
- All commands in <verify> blocks run from `backend/functions/` (or `backend/` for
  emulator commands) as noted in each command.
</notes>

<interfaces>
<!-- Canonical Drift trip fields the Trip / TripDoc / zod schema MUST mirror.
     Source: lib/database/tables/trips_table.dart. Dart camelCase maps to snake_case
     SQL; the Phase 11 client sends camelCase JSON, so the TS Trip type is camelCase. -->

From lib/database/tables/trips_table.dart (canonical trip fields):
  id                 : String   (UUID v4, primary key, client-generated)
  userId             : String   (Firebase uid; server FORCES this — D-08)
  startTime          : DateTime (UTC) -> ISO 8601 UTC string in JSON/Firestore (D-10)
  endTime            : DateTime (UTC) -> ISO 8601 UTC string
  durationSeconds    : int
  distanceMeters     : double   (Drift real)
  routePolyline      : String?  (nullable — manual entries have none)
  direction          : String   ("to_office" | "to_home")
  timeMovingSeconds  : int
  timeStuckSeconds   : int
  isManualEntry      : bool     (default false)
  createdAt          : DateTime (UTC) -> ISO 8601 UTC string
  updatedAt          : DateTime (UTC) -> ISO 8601 UTC string

Server metadata added ONLY on the Firestore document (TripDoc, D-10/D-11):
  deleted          : boolean (default false)
  deletedAt        : Timestamp | null
  serverUpdatedAt  : Timestamp  (FieldValue.serverTimestamp() on every write)

Firebase Functions v2 Express pattern (RESEARCH Pattern 1, npm-verified):
  import { onRequest } from "firebase-functions/v2/https";
  import { setGlobalOptions } from "firebase-functions/v2";
  setGlobalOptions({ region: "us-central1" });   // D-02
  export const api = onRequest(app);              // single function wraps the Express app
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend scaffold — firebase config, deny-all rules, indexes, package.json, tsconfig, jest.config (owner)</name>
  <files>backend/firebase.json, backend/.firebaserc, backend/firestore.rules, backend/firestore.indexes.json, backend/functions/package.json, backend/functions/tsconfig.json, backend/functions/.gitignore, backend/functions/jest.config.js</files>
  <action>
Create the self-contained Firebase project under `backend/` (D-01). Do NOT modify the
repo-root `firebase.json`.

1. `backend/firebase.json` — JSON with (RESEARCH Pattern 8):
   - `functions`: { "source": "functions", "predeploy": ["npm --prefix \"$RESOURCE_DIR\" run build"] }
   - `firestore`: { "rules": "firestore.rules", "indexes": "firestore.indexes.json" }
     (BOTH keys required — the `indexes` key is MANDATORY per RESEARCH Pattern 8 / M2.)
   - `emulators` (D-14): auth { "port": 9099 }, functions { "port": 5001 },
     firestore { "port": 8080 }, ui { "enabled": true, "port": 4000 },
     and `"singleProjectMode": true` at the emulators-block level.

2. `backend/.firebaserc` — { "projects": { "default": "travey-298a7" } } (D-01).

3. `backend/firestore.rules` — deny-all (D-13, satisfies success criterion 5):
   ```
   rules_version = '2';
   service cloud.firestore {
     match /databases/{database}/documents {
       match /{document=**} {
         allow read, write: if false;
       }
     }
   }
   ```

4. `backend/firestore.indexes.json` — ship the Firestore indexes target (RESEARCH
   Pattern 8 / Pitfall 5 / M2). The restore query is
   `where('userId','==',uid).where('deleted','==',false)` — two equality filters that
   Firestore may require a COMPOSITE index for in prod (the emulator is lenient and will
   pass without it, so this must be declared proactively). Define the composite index:
   ```json
   {
     "indexes": [
       {
         "collectionGroup": "trips",
         "queryScope": "COLLECTION",
         "fields": [
           { "fieldPath": "userId", "order": "ASCENDING" },
           { "fieldPath": "deleted", "order": "ASCENDING" }
         ]
       }
     ],
     "fieldOverrides": []
   }
   ```
   NOTE: this index is deployed in the post-execution manual deploy step (see Plan 03's
   deploy note); shipping it here means the restore query won't fail with
   FAILED_PRECONDITION in prod even though the emulator can't surface that error.

5. `backend/functions/package.json` — use these EXACT pinned versions, reconciled to the
   `10-RESEARCH.md` npm-VERIFIED Standard Stack table (do not bump; each `@types/X` is on the
   SAME major as runtime X):
   - "engines": { "node": "20" }  (locked D-02; Node 22 recommended — see L1 callout)
   - "main": "lib/index.js"
   - dependencies (RESEARCH Core table):
     - "firebase-functions": "^7.2.5"
     - "firebase-admin": "^13.10.0"
     - "express": "^5.2.1"
     - "zod": "^4.4.3"
   - devDependencies (RESEARCH Dev table; types match runtime majors):
     - "typescript": "^5.5"   (RESEARCH A4/Open Q6: pin TS 5.x for ts-jest/eslint stability,
       NOT TS 6, even though 6.0.3 is npm-latest)
     - "jest": "^30.4.2"
     - "ts-jest": "^29.4.11"
     - "@types/jest": "^30.0.0"        (matches jest major 30)
     - "@types/express": "^5.0.6"      (matches express major 5)
     - "supertest": "^7.2.2"
     - "@types/supertest": "^7.2.0"    (matches supertest major 7 — fixes the prior 6/7 mismatch)
     - "@types/node": "^20"
   - scripts:
     - "build": "tsc"
     - "lint": "tsc --noEmit"
     - "serve": "npm run build && firebase emulators:start --only functions,firestore,auth"
     - "test": "firebase emulators:exec --only firestore,auth \"jest\""
     - "test:unit": "jest --selectProjects unit"
   NOTE: this plan only adds pure-logic util tests (run via `test:unit`, which selects the
   `unit` Jest project). The emulator `test` script is the canonical entrypoint; Plan 03
   EXTENDS jest.config.js with an `integration` project (it must NOT overwrite this config —
   see step 7 + BLOCKER-2). `jest.config.js` is OWNED by this plan; Plan 03 only adds a project.

6. `backend/functions/tsconfig.json` (D-03, Functions v2 — CommonJS):
   { "compilerOptions": { "module": "commonjs", "target": "es2021", "lib": ["es2021"],
     "strict": true, "noImplicitAny": true, "esModuleInterop": true,
     "skipLibCheck": true, "outDir": "lib", "rootDir": "src",
     "sourceMap": true, "noUnusedLocals": true, "noUnusedParameters": true },
     "include": ["src"], "exclude": ["lib", "node_modules"] }

7. `backend/functions/.gitignore` — ignore `node_modules/` and the build output `lib/`
   (do NOT commit compiled JS; the Flutter app's repo-root `lib/` is unrelated and unaffected).

8. `backend/functions/jest.config.js` — ts-jest with a `projects` ARRAY (this shape is
   load-bearing: Plan 03 EXTENDS it by appending an `integration` project — it must NOT be
   replaced with a flat config, see BLOCKER-2). Define a `unit` project matching the
   pure-logic util tests under `src/utils/__tests__/**`:
   ```js
   module.exports = {
     projects: [
       {
         displayName: 'unit',
         preset: 'ts-jest',
         testEnvironment: 'node',
         testMatch: ['<rootDir>/src/utils/__tests__/**/*.test.ts'],
       },
       // Plan 03 appends an `integration` project here (test/**/*.test.ts, emulator
       // setupFiles, maxWorkers:1). It MUST keep this `unit` project intact so
       // `npm run test:unit` (--selectProjects unit) still discovers the util tests.
     ],
   };
   ```
  </action>
  <verify>
    <automated>cd backend/functions && npm install</automated>
    <automated>cd backend && grep -q "if false" firestore.rules && grep -q "singleProjectMode" firebase.json && grep -q "firestore.indexes.json" firebase.json && grep -q "travey-298a7" .firebaserc && node -e "require('./firebase.json')" && node -e "require('./firestore.indexes.json')"</automated>
  </verify>
  <done>
`npm install` succeeds with the RESEARCH-verified version pins (firebase-functions ^7,
firebase-admin ^13, express ^5 + @types/express ^5, zod ^4, jest ^30 + @types/jest ^30,
ts-jest ^29, supertest ^7 + @types/supertest ^7, typescript ^5.5); deny-all rule present in
firestore.rules; firestore.indexes.json exists and is valid JSON with the trips composite
index; firebase.json is valid JSON wiring BOTH firestore.rules and firestore.indexes.json
plus the emulators block (singleProjectMode:true, ports 9099/5001/8080/4000); .firebaserc
default project is travey-298a7; package.json engines node 20; jest.config.js uses a
`projects` array with a `unit` project. (TypeScript build is validated in Task 2 once src/ exists.)
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Shared contract types + four utils + index.ts with GET /health</name>
  <files>backend/functions/src/types/trip.ts, backend/functions/src/utils/response.ts, backend/functions/src/utils/auth.ts, backend/functions/src/utils/validation.ts, backend/functions/src/utils/firestore.ts, backend/functions/src/index.ts</files>
  <behavior>
    - extractBearerToken("Bearer abc.def.ghi") returns "abc.def.ghi"
    - extractBearerToken(undefined) / "" / "abc" / "Basic x" throws AuthError
    - verifyAuth(req): missing/invalid/expired token throws typed AuthError; valid returns uid (verifyIdToken mocked in unit tests)
    - tripSchema accepts a fully-valid Trip JSON (UUID id, camelCase, ISO strings, nullable routePolyline)
    - tripSchema rejects: missing id, NON-UUID id, bad direction value, non-ISO startTime, wrong types
    - tripSchema treats userId as optional/ignored (server forces it — D-08)
    - syncTripsBody accepts { trips: [validTrip, ...] } for 1..1000 trips; rejects { trips: "x" }, missing trips, EMPTY trips array (0), and MORE THAN 1000 trips
    - tripIdParam accepts a valid UUID tripId; rejects a non-UUID / empty string
    - sendSuccess/sendError produce { statusCode, body: { data?|error? } } and set the matching HTTP status
    - GET /health returns 200 { status: 'ok' }
  </behavior>
  <action>
Build the contract + utils in interface-first order. Strict TS, NO `any`
(CLAUDE.md). One concern per file (D-05, CLAUDE.md). Verify -> validate -> trust ordering
is established here even though handlers come in Plan 02.

1. `src/types/trip.ts` — the Phase 10 to 11 CONTRACT (D-10). Export:
   - `type Direction = 'to_office' | 'to_home'`.
   - `interface Trip` — camelCase fields exactly as in <interfaces>:
     id, userId, startTime, endTime, durationSeconds, distanceMeters,
     routePolyline (string | null), direction (Direction), timeMovingSeconds,
     timeStuckSeconds, isManualEntry, createdAt, updatedAt. Timestamps are
     ISO 8601 UTC `string`s (D-10).
   - `interface TripDoc extends Trip` — adds server metadata: `deleted: boolean`,
     `deletedAt: Timestamp | null`, `serverUpdatedAt: Timestamp`. Import
     `Timestamp` from `firebase-admin/firestore`.

2. `src/utils/response.ts` (D-06) — export `sendSuccess<T>(res, statusCode, data: T)`
   and `sendError(res, statusCode, error: string)`. Each calls `res.status(statusCode)`
   then `.json({ statusCode, body: { data } })` or `{ statusCode, body: { error } }`.
   Never leak stack traces/tokens. Type `res` as `express.Response`.

3. `src/utils/auth.ts` (D-07) — the verify foundation. Export:
   - `class AuthError extends Error` with a public `statusCode = 401` and a
     readable message (never echo the token).
   - `extractBearerToken(header: string | undefined): string` — pure function that
     splits the `Authorization` header, requires the `Bearer ` scheme, throws
     `AuthError` on missing/malformed. Keep this exported and Admin-SDK-free so it is
     unit-testable.
   - `async verifyAuth(req: express.Request): Promise<string>` — calls
     `extractBearerToken(req.headers.authorization)`, then
     `getAuth().verifyIdToken(token)` (import `getAuth` from `firebase-admin/auth`),
     returns `decoded.uid`. Catch verify failures and rethrow as `AuthError`.

4. `src/utils/validation.ts` (D-07, RESEARCH Pattern 9) — zod schemas. Export:
   - `kMaxSyncBatchTrips = 1000` — named constant for the sync-array DoS cap (M1, no
     magic numbers per CLAUDE.md). Plan 02's sync handler imports/relies on this bound.
   - `tripSchema` — z.object mirroring `Trip`:
     - `id` — **`z.string().uuid()`** (D-09: doc id == client trip UUID; RESEARCH Pattern 9.
       Do NOT downgrade to `.min(1)` — the locked data model requires UUID doc ids).
     - timestamps `z.string().datetime()`;
     - `direction` `z.enum(['to_office','to_home'])`;
     - `routePolyline` `z.string().nullable()`; `distanceMeters` `z.number()`;
       integer seconds fields `z.number().int()`; `isManualEntry` `z.boolean()`;
     - `userId` `z.string().optional()` (ignored — D-08).
   - `syncTripsBody` — **`z.object({ trips: z.array(tripSchema).min(1).max(kMaxSyncBatchTrips) })`**
     (D-12 + M1 DoS cap, RESEARCH Open Q4 / Security Domain). `.min(1)` rejects an empty
     batch; `.max(1000)` bounds the request body so an unbounded sync payload is rejected
     with 400 rather than accepted.
   - `tripIdParam` — **`z.object({ tripId: z.string().uuid() })`** (D-09 / RESEARCH Pattern 9:
     the path param is a trip UUID; reject non-UUID ids — do NOT use `.min(1)`).
   Export inferred types via `z.infer` where useful for handlers.

5. `src/utils/firestore.ts` (D-09/D-10) — export:
   - `tripConverter: FirestoreDataConverter<TripDoc>` (typed `toFirestore` /
     `fromFirestore`). Import `FirestoreDataConverter`, `getFirestore` from
     `firebase-admin/firestore`.
   - `tripsCollection()` — returns `getFirestore().collection('trips')
     .withConverter(tripConverter)` (top-level `trips`, doc id = trip UUID).

6. `src/index.ts` — boot wiring:
   - `import { initializeApp } from 'firebase-admin/app'; initializeApp();`
   - `import { setGlobalOptions } from 'firebase-functions/v2';
     setGlobalOptions({ region: 'us-central1' });` (D-02)
   - `import { onRequest } from 'firebase-functions/v2/https';`
   - `const app = express(); app.use(express.json());`
   - Mount ONLY `app.get('/health', (_req, res) => { res.status(200).json({ status: 'ok' }); });`
     (the three /trips routes come in Plan 02).
   - `export { app };` (exported separately so Plan 02's supertest imports it without
     the Functions wrapper).
   - `export const api = onRequest(app);`
  </action>
  <verify>
    <automated>cd backend/functions && npm run build</automated>
    <automated>cd backend/functions && grep -q "FirestoreDataConverter<TripDoc>" src/utils/firestore.ts && grep -q "verifyIdToken" src/utils/auth.ts && grep -q "uuid()" src/utils/validation.ts && grep -q "max(kMaxSyncBatchTrips)" src/utils/validation.ts && grep -q "onRequest(app)" src/index.ts && grep -q "status: 'ok'" src/index.ts</automated>
    <automated>cd backend && firebase emulators:exec --only functions,firestore,auth 'curl -sf -o /dev/null -w "%{http_code}" http://127.0.0.1:5001/travey-298a7/us-central1/api/health | grep -q 200'</automated>
  </verify>
  <done>
`npm run build` compiles clean with zero TS errors and no `any`. The emulator boots and
`GET /health` returns HTTP 200 with `{ status: 'ok' }`. validation.ts uses `z.string().uuid()`
for `id` and `tripIdParam` and caps the sync array at `kMaxSyncBatchTrips` (1000) via
`.min(1).max()`; firestore.ts exposes a `FirestoreDataConverter<TripDoc>` and
`tripsCollection()`; auth.ts verifies the ID token; index.ts exports both `app` and `api`.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Unit tests for pure-logic utils (validation incl. array-cap + bearer parsing)</name>
  <files>backend/functions/src/utils/__tests__/validation.test.ts, backend/functions/src/utils/__tests__/auth.test.ts</files>
  <behavior>
    validation.test.ts:
      - tripSchema.parse(validTrip) succeeds (UUID id, ISO timestamps, direction enum, null routePolyline)
      - tripSchema rejects: missing id, NON-UUID id ("not-a-uuid"), direction "sideways", non-ISO startTime, distanceMeters as string, non-int durationSeconds
      - tripSchema accepts a trip with userId omitted (server forces it)
      - syncTripsBody accepts { trips: [validTrip] } (1 trip) and { trips: [1000 valid trips] }
      - syncTripsBody REJECTS { trips: [] } (0 trips — .min(1)) and { trips: [1001 valid trips] } (.max(1000) DoS cap — M1)
      - syncTripsBody rejects { trips: "x" } and {} (missing trips)
      - tripIdParam accepts { tripId: <valid uuid v4> }; rejects { tripId: "not-a-uuid" } and { tripId: "" }
    auth.test.ts:
      - extractBearerToken("Bearer t.o.k") === "t.o.k"
      - extractBearerToken(undefined) throws AuthError (statusCode 401)
      - extractBearerToken("token-without-scheme") throws AuthError
      - extractBearerToken("Basic xyz") throws AuthError
      - verifyAuth: mock firebase-admin/auth getAuth().verifyIdToken to resolve {uid:'u1'} -> returns 'u1'
      - verifyAuth: mock verifyIdToken to reject -> throws AuthError (not the raw error)
  </behavior>
  <action>
Write Jest unit tests covering ONLY the pure-logic utils — no emulator, no real Firestore
(D-15 happy-path/ownership emulator tests for handlers come in Plan 02/03). These run under
the `unit` Jest project (`npm run test:unit`).

1. `src/utils/__tests__/validation.test.ts` — import `tripSchema`, `syncTripsBody`,
   `tripIdParam`, `kMaxSyncBatchTrips` from `../validation`. Build a `validTrip` fixture
   matching the `Trip` contract (a valid UUID v4 `id`, ISO 8601 UTC strings,
   `direction: 'to_office'`, `routePolyline: null`, integer seconds, numeric distance).
   Assert accept/reject cases listed in <behavior> using `.safeParse(...).success` true/false.
   For the DoS-cap cases (M1): build an array of 1000 valid trips (assert `.success === true`)
   and an array of 1001 valid trips (assert `.success === false`); also assert the empty
   array `{ trips: [] }` is rejected. Use `kMaxSyncBatchTrips` to size the arrays so the test
   tracks the constant.

2. `src/utils/__tests__/auth.test.ts` — import `extractBearerToken`, `verifyAuth`,
   `AuthError` from `../auth`. For `extractBearerToken`, assert the pure cases. For
   `verifyAuth`, mock `firebase-admin/auth` with `jest.mock('firebase-admin/auth', ...)`
   so `getAuth().verifyIdToken` is controllable: one test resolves `{ uid: 'u1' }` and
   asserts the returned uid; one rejects and asserts an `AuthError` is thrown (the raw
   error is never surfaced). Build a minimal fake `req` with
   `{ headers: { authorization: 'Bearer good.token' } }`.

Do not test framework boilerplate — only the schema rules (incl. the UUID + array-cap
guarantees) and the parsing/verification logic (CLAUDE.md: test what matters).
  </action>
  <verify>
    <automated>cd backend/functions && npm run test:unit</automated>
  </verify>
  <done>
`npm run test:unit` passes: all zod accept/reject cases green (incl. NON-UUID id rejected,
empty array rejected, 1000 accepted, 1001 rejected — the M1 DoS cap), all bearer-header
parsing cases green, and the mocked `verifyAuth` success + failure cases green. No emulator
required for this task.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client to api (HTTPS Cloud Function) | Untrusted client request (Authorization header + JSON body) crosses into the backend. This plan builds the verification/validation gates the boundary depends on; handlers wire them in Plan 02. |
| api to Firestore | Admin SDK writes; clients are denied by deny-all rules (this plan). |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing | Authorization header / verifyAuth (auth.ts) | mitigate | `verifyAuth` requires a `Bearer` token and calls `getAuth().verifyIdToken`; missing/invalid/expired throws `AuthError` (401). Foundation for all handlers (D-07). |
| T-10-02 | Tampering | Request body (validation.ts) | mitigate | zod `tripSchema`/`syncTripsBody`/`tripIdParam` validate every field at handler entry before any write (D-07). `id`/`tripIdParam` are `z.string().uuid()` (D-09) so malformed doc ids are rejected. |
| T-10-03 | Information disclosure | Direct Firestore access from client | mitigate | `firestore.rules` deny-all (`allow read, write: if false`); only the Admin SDK reads/writes (D-13, success criterion 5). |
| T-10-04 | Information disclosure | Error responses (response.ts / AuthError) | mitigate | `sendError` returns only a string message; `AuthError` never echoes the token or stack trace (D-06). |
| T-10-05 | Elevation of privilege | Client-supplied userId | mitigate | `tripSchema.userId` is optional and ignored; the server forces the token uid on write (D-08). Schema established here; enforcement lands in Plan 02 handlers. |
| T-10-06 | Denial of service | Unbounded sync batch (validation.ts) | mitigate | `syncTripsBody` caps the trips array at `kMaxSyncBatchTrips = 1000` via `.min(1).max(1000)` (M1, RESEARCH Open Q4) so an oversized request body is rejected with 400 before any Firestore work; per-commit cost is further bounded by ≤500 chunking in Plan 02. |
| T-10-07 | Repudiation | Audit of writes | accept | `serverUpdatedAt` (FieldValue.serverTimestamp) is added to `TripDoc` for write audit; full audit logging is out of scope for v0.1. |
</threat_model>

<verification>
Phase-level checks for this plan:

1. `cd backend/functions && npm install && npm run build` — clean install (RESEARCH-verified
   pins) and compile, zero TS errors, no `any` (strict mode enforces).
2. `cd backend && firebase emulators:exec --only functions,firestore,auth 'curl -sf http://127.0.0.1:5001/travey-298a7/us-central1/api/health'`
   — emulator boots and `GET /health` returns 200 `{status:'ok'}`.
3. `cd backend && grep -q "allow read, write: if false" firestore.rules` — deny-all
   rules present (success criterion 5).
4. `cd backend && grep -q "firestore.indexes.json" firebase.json && node -e "require('./firestore.indexes.json')"`
   — indexes target wired and valid (M2; composite index for the restore query shipped).
5. `cd backend/functions && npm run test:unit` — pure-logic util tests green
   (zod accept/reject incl. UUID + array-cap, bearer parsing, mocked verifyAuth).
</verification>

<success_criteria>
- `backend/` is a self-contained Firebase project (own firebase.json/.firebaserc/rules/indexes);
  repo-root firebase.json untouched.
- Dependency pins match the `10-RESEARCH.md` npm-verified table (firebase-functions ^7,
  firebase-admin ^13, express ^5 + @types/express ^5, zod ^4, jest ^30 + @types/jest ^30,
  ts-jest ^29, supertest ^7 + @types/supertest ^7, typescript ^5.5); each `@types/X` on the
  same major as runtime X.
- `npm install && npm run build` succeeds with strict TS and no `any`.
- The `api` function boots on the Functions emulator; `GET /health` returns 200.
- `firestore.rules` denies all direct client access (success criterion 5 satisfied).
- `firebase.json` wires both `firestore.rules` and `firestore.indexes.json`; the indexes file
  ships the composite index for the restore query (M2).
- `Trip` / `TripDoc` contract types and the four utils (auth, firestore, validation,
  response) exist with the exported surface listed in must_haves — `id`/`tripIdParam` validated
  as UUIDs, sync array capped at 1000 — ready for Plan 02 to mount the three `/trips` handlers.
- Util unit tests pass with no emulator.
</success_criteria>

<output>
After completion, create `.planning/phases/10-backend-infrastructure/10-01-SUMMARY.md`
</output>
</content>
</invoke>
