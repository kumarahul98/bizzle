---
phase: 25-interrupted-trip-recovery
plan: 02
type: execute
wave: 2
depends_on: [25-01]
files_modified:
  - lib/features/tracking/services/tracking_service_controller.dart
  - lib/features/tracking/services/tracking_event_source.dart
  - lib/features/tracking/services/fbs_tracking_event_source.dart
  - lib/features/tracking/services/main_isolate_tracking_engine.dart
autonomous: true
requirements: [TRACK-13]

must_haves:
  truths:
    - "Tracking engine can be started with an existing trip state"
  artifacts:
    - path: "lib/features/tracking/services/tracking_event_source.dart"
      provides: "Updated start() signature accepting initial state"
  key_links:
    - from: "lib/features/tracking/services/main_isolate_tracking_engine.dart"
      to: "lib/features/tracking/services/trip_accumulator.dart"
      via: "TripAccumulator.restore() inside start()"
---

<objective>
Update the tracking service layers to accept an interrupted trip's state on `start()` so the `TripAccumulator` can be restored seamlessly.
Purpose: Satisfy D-03 (Resume Behavior) so the engine continues tracking exactly where it left off.
Output: Plumbed `start()` methods across the tracking event sources and controller.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-interrupted-trip-recovery/25-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Update TrackingEventSource interface</name>
  <files>lib/features/tracking/services/tracking_event_source.dart</files>
  <read_first>Review TrackingEventSource methods.</read_first>
  <action>
    Modify `TrackingEventSource.start` to accept `{Map<String, dynamic>? initialAccumulatorState}`.
  </action>
  <verify>
    <automated>flutter analyze lib/features/tracking/services/tracking_event_source.dart</automated>
  </verify>
  <acceptance_criteria>
    - Interface is updated.
    - start() accepts an optional Map<String, dynamic>? initialAccumulatorState.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement Main Isolate Recovery</name>
  <files>lib/features/tracking/services/main_isolate_tracking_engine.dart, test/features/tracking/services/main_isolate_tracking_engine_test.dart</files>
  <read_first>Review MainIsolateTrackingEngine.start() method.</read_first>
  <action>
    Update `MainIsolateTrackingEngine.start()` to accept `initialAccumulatorState`.
    If provided, use `TripAccumulator.restore(initialAccumulatorState)` instead of `TripAccumulator(startedAt: ...)`.
  </action>
  <verify>
    <automated>flutter test test/features/tracking/services/main_isolate_tracking_engine_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Main isolate engine restores state seamlessly.
    - When start() is called with initialAccumulatorState, the internal TripAccumulator is initialized via TripAccumulator.restore() instead of new.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Implement Background Isolate Recovery (fbs)</name>
  <files>lib/features/tracking/services/fbs_tracking_event_source.dart</files>
  <read_first>Review background isolate communication mechanism.</read_first>
  <action>
    Update `FbsTrackingEventSource.start()` to accept `initialAccumulatorState`.
    Pass this state map via `service.invoke('start', {'initialState': initialAccumulatorState})` (or however the start signal is passed).
    Update the background isolate's entry point (which likely lives in the same file or a nearby `fbs_entry.dart`) to receive `initialState` and use `TripAccumulator.restore()` if present.
  </action>
  <verify>
    <automated>flutter analyze lib/features/tracking/services/fbs_tracking_event_source.dart</automated>
  </verify>
  <acceptance_criteria>
    - Background isolate restores state across the channel.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Update TrackingServiceController</name>
  <files>lib/features/tracking/services/tracking_service_controller.dart, test/features/tracking/services/tracking_service_controller_test.dart</files>
  <read_first>Review TrackingServiceController.start() method.</read_first>
  <action>
    Update `TrackingServiceController.start()` to accept `{Map<String, dynamic>? initialAccumulatorState}`.
    Pass this argument down to `_source.start(initialAccumulatorState: initialAccumulatorState)`.
  </action>
  <verify>
    <automated>flutter test test/features/tracking/services/tracking_service_controller_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Controller plumbs the state to the engine.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Isolate Channel | State Map crosses from main isolate to background isolate (fbs). Must only contain primitive-compatible types. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-03 | Denial of Service | `TripAccumulator.restore` | mitigate | Ensure parsing exceptions in the background isolate are caught and logged so a malformed state doesn't crash the background process permanently. |
</threat_model>

<verification>
Unit tests ensure state is properly forwarded and parsed.
</verification>

<success_criteria>
The engine can be started using the restored state map.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/25-interrupted-trip-recovery/25-02-SUMMARY.md`

<output>
Create `.planning/phases/25-interrupted-trip-recovery/25-02-SUMMARY.md` when done
</output>
