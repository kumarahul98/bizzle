---
phase: 25-interrupted-trip-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/tracking/services/trip_state_persister.dart
  - lib/features/tracking/services/trip_accumulator.dart
autonomous: true
requirements: [TRACK-13]

must_haves:
  truths:
    - "Active trip state is durably saved to disk on every update"
    - "Clean stops delete the active trip file"
  artifacts:
    - path: "lib/features/tracking/services/trip_state_persister.dart"
      provides: "File I/O for active_trip.json"
  key_links:
    - from: "lib/features/tracking/services/trip_accumulator.dart"
      to: "lib/features/tracking/services/trip_state_persister.dart"
      via: "saving state on addSample, pause, and resume"
---

<objective>
Implement the durable persistence layer for active tracking state so that an OS kill does not lose the trip data.
Purpose: Satisfy D-01 of Phase 25 by continuously persisting the accumulator's state to disk.
Output: `TripStatePersister` service and serialization logic in `TripAccumulator`.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-interrupted-trip-recovery/25-CONTEXT.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create TripStatePersister</name>
  <files>lib/features/tracking/services/trip_state_persister.dart, test/features/tracking/services/trip_state_persister_test.dart</files>
  <read_first>Review path_provider and dart:io usage in the project.</read_first>
  <action>
    Create `TripStatePersister` with methods `saveState(Map<String, dynamic>)`, `Map<String, dynamic>? loadState()`, and `clear()`.
    Use `path_provider`'s `getApplicationDocumentsDirectory()` and `dart:io` `File` to write to `active_trip.json`.
    Since this will be called from both main and background isolates, ensure file operations are robust (e.g. use `writeAsString` / `readAsString`).
  </action>
  <verify>
    <automated>flutter test test/features/tracking/services/trip_state_persister_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Persister can save, load, and clear the JSON file.
    - saveState(map) writes JSON to active_trip.json in the app documents directory.
    - loadState() returns the parsed map or null if file does not exist.
    - clear() deletes the file.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TripAccumulator Serialization</name>
  <files>lib/features/tracking/services/trip_accumulator.dart, test/features/tracking/services/trip_accumulator_test.dart</files>
  <read_first>Review TripAccumulator internal state fields.</read_first>
  <action>
    Update `TripAccumulator` to add `Map<String, dynamic> dumpState()` and a factory/constructor `TripAccumulator.restore(Map<String, dynamic> state)`.
    Ensure all fields necessary for a seamless resume are serialized: `_distanceMeters`, `_timeMovingSeconds`, `_timeStuckSeconds`, `_samples` (as list of lat/lng/timestamp/accuracy/speed/heading/altitude), `_breaks`, `_isPaused`, `_currentPauseStart`, `_accumulatedPausedSeconds`, `startedAt`, `_tripId`, `_lastAccepted`, `_lastAcceptedAt`.
    *Note: D-01 references TripSnapshot, but to fully resume we must persist the accumulator's state.*
  </action>
  <verify>
    <automated>flutter test test/features/tracking/services/trip_accumulator_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Accumulator can export and import its full internal state losslessly.
    - dumpState() returns a map containing all internal state (samples, breaks, distances, paused fields, etc).
    - TripAccumulator.restore(map) initializes an accumulator with the exact same state as before.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Wire persistence into Accumulator</name>
  <files>lib/features/tracking/services/trip_accumulator.dart</files>
  <read_first>Review TripAccumulator lifecycle methods.</read_first>
  <action>
    Inject `TripStatePersister` into `TripAccumulator` (or instantiate it).
    Update `addSample()`, `pause()`, and `resume()` to fire-and-forget `persister.saveState(dumpState())`.
    Update `finalize()` to call `persister.clear()` so a clean stop leaves no interrupted trip file.
  </action>
  <verify>
    <automated>flutter test test/features/tracking/services/trip_accumulator_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Accumulator saves state on every update and clears it on stop.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| File System | `active_trip.json` contains location history (PII). App documents directory is sandboxed by iOS/Android so it's protected from other apps. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-01 | Information Disclosure | `TripStatePersister` | mitigate | Write file to the private app documents directory (via path_provider) to ensure OS-level sandbox protection. |
| T-25-02 | Tampering | `TripAccumulator.restore` | mitigate | Add basic schema validation to `restore()` to prevent crashes on malformed JSON; discard gracefully if corrupt. |
</threat_model>

<verification>
File I/O operations and serialization are verified by automated tests.
</verification>

<success_criteria>
`TripAccumulator` durably backs up its state to `active_trip.json` continuously, and cleans up on `finalize()`.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/25-interrupted-trip-recovery/25-01-SUMMARY.md`

<output>
Create `.planning/phases/25-interrupted-trip-recovery/25-01-SUMMARY.md` when done
</output>
