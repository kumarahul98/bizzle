---
phase: 25-interrupted-trip-recovery
plan: 03
type: execute
wave: 3
depends_on: [25-02]
files_modified:
  - lib/config/constants.dart
  - lib/features/tracking/state/tracking_state.dart
  - lib/features/tracking/providers/tracking_providers.dart
  - lib/features/tracking/widgets/recovery_prompt_dialog.dart
  - lib/core/widgets/main_shell.dart
autonomous: true
requirements: [TRACK-13]

must_haves:
  truths:
    - "User is prompted to resume or discard on launch if an interrupted trip exists"
    - "Tracking resumes seamlessly if user taps Resume"
  artifacts:
    - path: "lib/features/tracking/widgets/recovery_prompt_dialog.dart"
      provides: "Modal prompt offering Resume/Discard options"
  key_links:
    - from: "lib/core/widgets/main_shell.dart"
      to: "lib/features/tracking/providers/tracking_providers.dart"
      via: "Listening for TrackingInterrupted state"
---

<objective>
Wire the detection of interrupted trips on app launch and present the recovery dialog to the user.
Purpose: Complete D-02 and D-04, tying the UI to the backend recovery logic.
Output: UI dialog, `TrackingInterrupted` state, and Notifier integration.
</objective>

<execution_context>
@$HOME/.gemini/gsd-core/workflows/execute-plan.md
@$HOME/.gemini/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/25-interrupted-trip-recovery/25-CONTEXT.md
@.planning/phases/25-interrupted-trip-recovery/25-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add UI Constants</name>
  <files>lib/config/constants.dart</files>
  <read_first>Review 25-UI-SPEC.md for dialog copy.</read_first>
  <action>
    Add the constants specified in 25-UI-SPEC.md:
    `kRecoveryDialogTitle = 'Interrupted Commute'`,
    `kRecoveryDialogBody = 'Traevy stopped unexpectedly during your last commute. Do you want to resume recording or discard it?'`,
    `kRecoveryResumeAction = 'Resume'`,
    `kRecoveryDiscardAction = 'Discard'`.
  </action>
  <verify>
    <automated>flutter analyze lib/config/constants.dart</automated>
  </verify>
  <acceptance_criteria>
    - Constants added.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TrackingState and Notifier</name>
  <files>lib/features/tracking/state/tracking_state.dart, lib/features/tracking/providers/tracking_providers.dart, test/features/tracking/providers/tracking_providers_test.dart</files>
  <read_first>Review TrackingState and TrackingNotifier classes.</read_first>
  <action>
    Add `TrackingInterrupted` to the sealed `TrackingState` hierarchy, holding `Map<String, dynamic> snapshot` (the raw map).
    Update `TrackingNotifier._attach()` (or `build()`) to check `TripStatePersister.loadState()`. If non-null, log the detection of the interrupted trip, then set state to `TrackingInterrupted(snapshot)`. Wait, since `loadState()` might be async if using file I/O, you might need to handle the Future cleanly (e.g. initial state is `TrackingIdle`, then flips to `TrackingInterrupted` when load completes, or make it async in a setup method).
    Implement `resumeInterruptedTrip()`: take the snapshot, transition to `TrackingStarting`, and call `controller.start(initialAccumulatorState: snapshot)`.
    Implement `discardInterruptedTrip()`: call `persister.clear()` and transition to `TrackingIdle`.
  </action>
  <verify>
    <automated>flutter test test/features/tracking/providers/tracking_providers_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - Notifier handles detection, logging, resumption, and discard.
    - TrackingNotifier on init checks TripStatePersister. If state found, logs the detection and sets state to TrackingInterrupted.
    - resumeInterruptedTrip() calls controller.start(initialState) and sets state to TrackingStarting.
    - discardInterruptedTrip() calls persister.clear() and sets state to TrackingIdle.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: RecoveryPromptDialog</name>
  <files>lib/features/tracking/widgets/recovery_prompt_dialog.dart</files>
  <read_first>Review Flutter AlertDialog documentation.</read_first>
  <action>
    Create `RecoveryPromptDialog` extending `StatelessWidget`.
    Follow 25-UI-SPEC.md: use `AlertDialog`, `kRecoveryDialogTitle`, `kRecoveryDialogBody`.
    Actions: Resume (calls `TrackingNotifier.resumeInterruptedTrip()`) and Discard (calls `TrackingNotifier.discardInterruptedTrip()` using destructive color).
    Ensure the dialog is `barrierDismissible: false` if possible (this is usually handled in `showDialog`, but can also enforce via `WillPopScope`/`PopScope`).
  </action>
  <verify>
    <automated>flutter analyze lib/features/tracking/widgets/recovery_prompt_dialog.dart</automated>
  </verify>
  <acceptance_criteria>
    - Dialog is implemented correctly per spec.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: MainShell Wiring</name>
  <files>lib/core/widgets/main_shell.dart</files>
  <read_first>Review MainShell widget and trackingStateProvider.</read_first>
  <action>
    In `MainShell` (or whatever top-level screen is appropriate), add a `ref.listen<TrackingState>` to `trackingStateProvider`.
    If the state transitions to `TrackingInterrupted`, call `showDialog(barrierDismissible: false, builder: (_) => const RecoveryPromptDialog())`.
    When the state transitions away from `TrackingInterrupted`, ensure the dialog is dismissed (e.g., using `Navigator.pop` if it's still showing).
  </action>
  <verify>
    <automated>flutter analyze lib/core/widgets/main_shell.dart</automated>
  </verify>
  <acceptance_criteria>
    - Dialog automatically pops up when an interrupted trip is detected.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI Event | User choice to discard deletes the stored file permanently. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-25-04 | Repudiation | `discardInterruptedTrip` | mitigate | Explicit user action required to discard; dialog cannot be dismissed outside of the buttons (`barrierDismissible: false`). |
</threat_model>

<verification>
App flows tested via widget tests for the dialog and notifier tests for the state machine.
</verification>

<success_criteria>
App launches with the recovery prompt if killed mid-trip, and correctly honors the user's choice.
</success_criteria>

## Artifacts this phase produces
- `.planning/phases/25-interrupted-trip-recovery/25-03-SUMMARY.md`

<output>
Create `.planning/phases/25-interrupted-trip-recovery/25-03-SUMMARY.md` when done
</output>
