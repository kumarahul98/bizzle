---
phase: 04-trip-history
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/features/trips/history_grouping_test.dart
  - test/unit/shared/formatters_test.dart
  - test/widget/features/trips/history_screen_test.dart
  - test/widget/features/trips/trip_detail_screen_test.dart
autonomous: true
requirements:
  - HIST-01
  - HIST-02
  - HIST-03
must_haves:
  truths:
    - "Four stub test files exist and compile cleanly with flutter test (skipped tests are green)"
    - "All stub tests are skipped (not failing), allowing later tasks to fill them in TDD-style"
    - "Test scaffold imports the correct package paths for the production files they will cover"
  artifacts:
    - path: "test/unit/features/trips/history_grouping_test.dart"
      provides: "Unit test stubs for groupTripsByDate and formatDateHeader (HIST-01)"
      contains: "group('groupTripsByDate'"
    - path: "test/unit/shared/formatters_test.dart"
      provides: "Unit test stubs for formatDuration, formatDistance, decodedToLatLng (HIST-01, HIST-03)"
      contains: "group('formatDuration'"
    - path: "test/widget/features/trips/history_screen_test.dart"
      provides: "Widget test stubs for HistoryScreen list, calendar, empty states (HIST-01, HIST-02)"
      contains: "group('HistoryScreen'"
    - path: "test/widget/features/trips/trip_detail_screen_test.dart"
      provides: "Widget test stubs for TripDetailScreen loading, not-found, manual trip, stats (HIST-03)"
      contains: "group('TripDetailScreen'"
  key_links:
    - from: "test/unit/features/trips/history_grouping_test.dart"
      to: "lib/features/trips/providers/history_providers.dart"
      via: "import 'package:traevy/features/trips/providers/history_providers.dart'"
      pattern: "package:traevy/features/trips/providers/history_providers"
    - from: "test/unit/shared/formatters_test.dart"
      to: "lib/shared/utils/formatters.dart"
      via: "import 'package:traevy/shared/utils/formatters.dart'"
      pattern: "package:traevy/shared/utils/formatters"
---

<objective>
Create Wave 0 stub test files for all Phase 4 behaviors. These stubs compile and pass immediately
(via skip()) so the test runner is green before any production code exists. Subsequent plans fill in
real assertions after implementing the production code.

Purpose: Nyquist compliance — every task in Waves 1–3 has an automated verify command pointing to
an existing file. Without these stubs, Wave 1+ tasks would fail their verify steps with
"file not found" errors.

Output: 4 test files (2 unit, 2 widget) that skip all cases with a `// TODO: implement` comment.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/.planning/PROJECT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/ROADMAP.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/04-trip-history/04-VALIDATION.md

<interfaces>
<!-- Existing test scaffold patterns to replicate exactly -->

From test/unit/features/trips/direction_label_service_test.dart (analog for unit tests):
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:traevy/config/constants.dart';
import 'package:traevy/features/trips/services/direction_label_service.dart';

void main() {
  group('DirectionLabelService', () {
    test('...', () { ... });
  });
}
```

From test/unit/shared/polyline_codec_test.dart (analog for shared utils unit tests):
```dart
import 'package:flutter_test/flutter_test.dart';
import 'package:traevy/shared/utils/polyline_codec.dart';

void main() {
  group('encodePolyline', () {
    test('returns empty string for empty input', () {
      expect(encodePolyline(const []), isEmpty);
    });
  });
}
```

From test/widget/features/trips/edit_trip_sheet_test.dart (analog for widget tests):
```dart
import 'package:drift/drift.dart' hide isNotNull, isNull;
import 'package:drift/native.dart';
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:traevy/config/constants.dart';
import 'package:traevy/database/daos/trips_dao.dart';
import 'package:traevy/database/database.dart';
import 'package:traevy/database/providers.dart';
import 'package:uuid/uuid.dart';

void main() {
  group('EditTripSheet', () {
    late AppDatabase db;
    setUp(() {
      db = AppDatabase(DatabaseConnection(NativeDatabase.memory(), closeStreamsSynchronously: true));
    });
    tearDown(() async => db.close());
    // ...
  });
}
```

From lib/database/daos/trips_dao.dart:
```dart
class TripSummary {
  const TripSummary({
    required this.id,        // String (UUID)
    required this.startTime, // DateTime (UTC)
    required this.endTime,   // DateTime (UTC)
    required this.durationSeconds,  // int
    required this.distanceMeters,   // double
    required this.direction,        // String ('to_office' | 'to_home' | 'unknown')
    required this.timeMovingSeconds, // int
    required this.timeStuckSeconds,  // int
    required this.isManualEntry,    // bool
  });
}

// TripRow has the same fields PLUS:
//   routePolyline: String (encoded polyline)
//   userId: String
//   createdAt: DateTime
//   updatedAt: DateTime
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create unit test stubs — history_grouping_test and formatters_test</name>
  <files>
    test/unit/features/trips/history_grouping_test.dart
    test/unit/shared/formatters_test.dart
  </files>
  <read_first>
    - test/unit/features/trips/direction_label_service_test.dart (unit test analog — copy structure)
    - test/unit/shared/polyline_codec_test.dart (shared utils unit test analog — copy structure)
    - .planning/phases/04-trip-history/04-VALIDATION.md (HIST-01 and HIST-03 test coverage list)
    - .planning/phases/04-trip-history/04-RESEARCH.md (groupTripsByDate and formatters signatures)
  </read_first>
  <behavior>
    history_grouping_test.dart stubs:
    - Test 1 (skip): groupTripsByDate returns empty map for empty input
    - Test 2 (skip): groupTripsByDate groups trips by local date (same UTC day, same local date)
    - Test 3 (skip): groupTripsByDate strips time from key (keys are date-only DateTime objects)
    - Test 4 (skip): groupTripsByDate preserves newest-first order within each group
    - Test 5 (skip): formatDateHeader returns kHistoryDateToday for today
    - Test 6 (skip): formatDateHeader returns kHistoryDateYesterday for yesterday
    - Test 7 (skip): formatDateHeader returns 'Mon 21 Apr' format for older dates

    formatters_test.dart stubs:
    - Test 1 (skip): formatDuration(0) returns '0 min'
    - Test 2 (skip): formatDuration(2700) returns '45 min' (under 60 min)
    - Test 3 (skip): formatDuration(3600) returns '1h 00min' (exactly 60 min)
    - Test 4 (skip): formatDuration(4320) returns '1h 12min' (over 60 min)
    - Test 5 (skip): formatDistance(0) returns '0.0 km'
    - Test 6 (skip): formatDistance(12400) returns '12.4 km'
    - Test 7 (skip): decodedToLatLng('') returns empty list
    - Test 8 (skip): decodedToLatLng with valid polyline returns correct LatLng list
  </behavior>
  <action>
    Create test/unit/features/trips/history_grouping_test.dart:
    - Imports: flutter_test/flutter_test.dart, traevy/config/constants.dart
    - Also import traevy/features/trips/providers/history_providers.dart (where groupTripsByDate will live) and traevy/shared/utils/formatters.dart (where formatDateHeader will live)
    - Use `markTestSkipped('TODO: implement')` pattern to skip each test while keeping groups parseable
    - group('groupTripsByDate', ...) with 4 skipped tests
    - group('formatDateHeader', ...) with 3 skipped tests

    Create test/unit/shared/formatters_test.dart:
    - Imports: flutter_test/flutter_test.dart, traevy/shared/utils/formatters.dart
    - The decodedToLatLng test also imports package:latlong2/latlong2.dart
    - group('formatDuration', ...) with 4 skipped tests
    - group('formatDistance', ...) with 2 skipped tests
    - group('decodedToLatLng', ...) with 2 skipped tests

    IMPORTANT: Use `markTestSkipped('TODO: implement')` inside each test body — NOT `skip:` parameter on test(). This ensures the test runs and passes (as skipped) rather than being ignored entirely.

    The imports reference files that do not exist yet (history_providers.dart, formatters.dart). This is intentional — the imports will compile once Wave 1 creates those files. For now, comment out the import lines with // TODO: uncomment after Wave 1 to allow the stubs to compile immediately without the production files.

    Actually — do NOT comment out imports. Instead, structure the files so the imports are present but the test bodies use markTestSkipped. Dart will fail to compile if imported symbols don't exist. Use a workaround: put the imports inside a conditional or comment block.

    Correct approach: stub tests should import only flutter_test. The production symbol imports (groupTripsByDate, formatters) are added when Wave 1 creates the files and Wave 2 fills in the real assertions. For Wave 0, use `test('groupTripsByDate groups by date', () => markTestSkipped('Wave 1: implement groupTripsByDate first'))` with NO import of the production module.
  </action>
  <verify>
    <automated>flutter test test/unit/features/trips/history_grouping_test.dart test/unit/shared/formatters_test.dart --reporter expanded 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/unit/features/trips/history_grouping_test.dart exists
    - test/unit/shared/formatters_test.dart exists
    - `flutter test test/unit/features/trips/history_grouping_test.dart` exits 0 (all tests skipped = pass)
    - `flutter test test/unit/shared/formatters_test.dart` exits 0 (all tests skipped = pass)
    - Each file contains at least 3 test() calls
    - No import references production files that don't exist yet (history_providers.dart, formatters.dart not created until Wave 1)
  </acceptance_criteria>
  <done>Both unit test stub files compile and exit 0 (all tests skipped).</done>
</task>

<task type="auto">
  <name>Task 2: Create widget test stubs — history_screen_test and trip_detail_screen_test</name>
  <files>
    test/widget/features/trips/history_screen_test.dart
    test/widget/features/trips/trip_detail_screen_test.dart
  </files>
  <read_first>
    - test/widget/features/trips/edit_trip_sheet_test.dart (widget test analog — copy scaffold structure exactly)
    - .planning/phases/04-trip-history/04-VALIDATION.md (full list of HIST-01, HIST-02, HIST-03 widget test cases)
    - .planning/phases/04-trip-history/04-PATTERNS.md (widget test scaffold section)
  </read_first>
  <action>
    Create test/widget/features/trips/history_screen_test.dart:
    - Imports: drift/drift.dart (hiding isNotNull, isNull), drift/native.dart, flutter/material.dart, flutter_riverpod, flutter_test, traevy/database/database.dart, traevy/database/providers.dart, uuid/uuid.dart
    - DO NOT import HistoryScreen yet (it doesn't exist) — comment out with // TODO: import after Wave 2
    - group('HistoryScreen', ...) containing:
      * setUp/tearDown using in-memory NativeDatabase (exact pattern from edit_trip_sheet_test.dart)
      * makeSummary() factory helper returning a TripSummary (copy from edit_trip_sheet_test.dart exactly, same fields)
      * 5 skipped tests using markTestSkipped:
        1. 'renders trip cards grouped under date headers'
        2. 'shows empty state when no trips'
        3. 'calendar view shows event marker on days with trips'
        4. 'tapping a calendar date filters the sub-list'
        5. 'tapping a trip card navigates to detail screen'

    Create test/widget/features/trips/trip_detail_screen_test.dart:
    - Same scaffold: imports, setUp/tearDown, in-memory DB
    - DO NOT import TripDetailScreen yet — comment out with // TODO: import after Wave 2
    - group('TripDetailScreen', ...) containing:
      * setUp/tearDown with NativeDatabase.memory()
      * makeSummary() and makeGpsTrip() factory helpers (makeGpsTrip includes routePolyline = '_p~iF~ps|U')
      * 4 skipped tests:
        1. 'shows CircularProgressIndicator while loading'
        2. 'shows Trip not found for invalid id'
        3. 'manual trip hides map and shows Manually entered badge'
        4. 'GPS trip shows all six stat rows'

    Apply the SAME approach as Task 1 for import safety: do not import screen classes that don't exist yet. The stub files compile because they only use flutter_test and drift types (which exist already).

    The makeSummary helper MUST be identical to edit_trip_sheet_test.dart's version — same field values:
    ```dart
    TripSummary makeSummary({String direction = kDirectionToOffice}) {
      final start = DateTime.utc(2026, 1, 1, 8);
      final end = DateTime.utc(2026, 1, 1, 9);
      return TripSummary(
        id: const Uuid().v4(),
        startTime: start,
        endTime: end,
        durationSeconds: 3600,
        distanceMeters: 0,
        direction: direction,
        timeMovingSeconds: 0,
        timeStuckSeconds: 0,
        isManualEntry: false,
      );
    }
    ```
  </action>
  <verify>
    <automated>flutter test test/widget/features/trips/history_screen_test.dart test/widget/features/trips/trip_detail_screen_test.dart --reporter expanded 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - test/widget/features/trips/history_screen_test.dart exists and contains `group('HistoryScreen'`
    - test/widget/features/trips/trip_detail_screen_test.dart exists and contains `group('TripDetailScreen'`
    - `flutter test test/widget/features/trips/history_screen_test.dart` exits 0
    - `flutter test test/widget/features/trips/trip_detail_screen_test.dart` exits 0
    - setUp uses `NativeDatabase.memory()` with `closeStreamsSynchronously: true`
    - tearDown calls `db.close()`
    - No imports of HistoryScreen or TripDetailScreen (not yet created)
  </acceptance_criteria>
  <done>Both widget test stub files compile and exit 0 (all tests skipped).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test ↔ Production | Test files import production modules — incorrect imports could mask missing implementations |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Tampering | Test stub imports | accept | Wave 0 stubs intentionally omit production imports; Wave 2 adds them when production files exist — this is documented and deliberate, not a security threat |
</threat_model>

<verification>
Run the full existing test suite to confirm Wave 0 stubs do not break anything:

```bash
flutter test
```

All tests (including new stubs) must exit 0.
</verification>

<success_criteria>
- `flutter test test/unit/features/trips/history_grouping_test.dart` exits 0
- `flutter test test/unit/shared/formatters_test.dart` exits 0
- `flutter test test/widget/features/trips/history_screen_test.dart` exits 0
- `flutter test test/widget/features/trips/trip_detail_screen_test.dart` exits 0
- `flutter test` (full suite) exits 0
- No existing tests broken
</success_criteria>

<output>
After completion, create `.planning/phases/04-trip-history/04-01-SUMMARY.md`
</output>
