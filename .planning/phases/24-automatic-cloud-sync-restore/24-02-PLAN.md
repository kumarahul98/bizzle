---
phase: 24-automatic-cloud-sync-restore
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/config/constants.dart
  - lib/features/main/screens/main_shell.dart
autonomous: true
requirements: [SYNC-04]
must_haves:
  truths:
    - Upon signing in, the app automatically triggers a cloud restore exactly once per session.
    - A toast notification informs the user about the restore progress and outcome.
  artifacts:
    - path: lib/features/main/screens/main_shell.dart
      provides: Auto-restore trigger via auth listener
  key_links:
    - from: lib/features/main/screens/main_shell.dart
      to: lib/sync/restore_controller.dart
      via: ref.read(restoreControllerProvider.notifier).restore()
---

<objective>
Implement the auto-restore trigger upon successful sign-in.

Purpose: Automatically fetches the user's trips from the cloud when they sign in, removing the need for manual restoration.
Output: Auth listener in MainShell triggering RestoreController.
</objective>

## Artifacts this phase produces
- `kAutoRestoreInProgress`, `kAutoRestoreError` constants in `lib/config/constants.dart`
- `_hasRunAutoRestoreForCurrentSession` boolean state variable in `MainShell` state class
- `AutoRestoreToast` (inline SnackBar invocation logic in `MainShell`)

<tasks>
<task type="auto">
  <name>Task 1: Add Auto-Restore Trigger in MainShell</name>
  <read_first>
    - lib/features/main/screens/main_shell.dart
    - lib/features/auth/providers/auth_providers.dart
    - lib/sync/restore_controller.dart
  </read_first>
  <action>
    Add constants `kAutoRestoreInProgress` and `kAutoRestoreError` in `lib/config/constants.dart`.
    In `MainShell`'s `initState` or `ref.listen` block, add a listener on `authStateProvider`.
    Create a local state variable `_hasRunAutoRestoreForCurrentSession = false`.
    When the auth state transitions to `AuthSignedIn` AND `_hasRunAutoRestoreForCurrentSession` is false:
    1. Set `_hasRunAutoRestoreForCurrentSession = true`.
    2. Call `SyncEngine.pauseUploads()` (or acquire an upload lock) to prevent Phase 20 guest trip upload/backfill queue processing.
    3. Show a `SnackBar` with `kAutoRestoreInProgress`.
    4. Call `ref.read(restoreControllerProvider.notifier).restore()`.
    5. Listen to the result: upon completion (success, error, or conflict resolution done), call `SyncEngine.resumeUploads()` (or release the lock) so that local-only trips are uploaded ONLY after download-detect-reconcile completes.
    6. Update the `SnackBar` with `kAutoRestoreResultTemplate`, `kAutoRestoreUpToDate`, or `kAutoRestoreError`.
  </action>
  <acceptance_criteria>
    `MainShell` triggers `restore()` when `AuthSignedIn` is emitted, explicitly acquiring a lock or pausing uploads so restore finishes before any uploads, showing a `SnackBar`, and it does not trigger again on subsequent rebuilds.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/main/screens/main_shell_test.dart</automated>
  </verify>
  <done>Sign-in triggers exactly one restore attempt with toast feedback.</done>
</task>
</tasks>
