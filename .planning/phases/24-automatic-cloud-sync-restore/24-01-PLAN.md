---
phase: 24-automatic-cloud-sync-restore
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/config/constants.dart
  - lib/sync/sync_engine.dart
  - lib/features/dashboard/widgets/sync_stuck_banner.dart
  - lib/features/dashboard/screens/dashboard_screen.dart
autonomous: true
requirements: [SYNC-05]
must_haves:
  truths:
    - Failed sync items are automatically re-attempted without user action but without spamming the server.
    - The user sees a visible banner when sync items are genuinely stuck.
  artifacts:
    - path: lib/features/dashboard/widgets/sync_stuck_banner.dart
      provides: Stuck item visual notification
  key_links:
    - from: lib/features/dashboard/screens/dashboard_screen.dart
      to: lib/features/dashboard/widgets/sync_stuck_banner.dart
      via: SyncStatus check
---

<objective>
Implement time-gated auto-retry for failed sync items and surface a stuck-item banner.

Purpose: Makes sync resilient by recovering from transient failures automatically, while keeping the user informed if intervention is needed.
Output: SyncEngine with time-gated retry, SyncStuckBanner widget.
</objective>

## Artifacts this phase produces
- `kFailedAutoRetryWindow` constant in `lib/config/constants.dart`
- `kSyncStuckBannerMessage`, `kSyncStuckBannerAction`, `kSyncStuckBannerDismiss` constants in `lib/config/constants.dart`
- `SyncStuckBanner` widget class in `lib/features/dashboard/widgets/sync_stuck_banner.dart`
- `_lastAutoRetry` nullable DateTime variable in `SyncEngine` class

<tasks>
<task type="auto">
  <name>Task 1: Implement Time-Gated Auto-Retry</name>
  <read_first>
    - lib/config/constants.dart
    - lib/sync/sync_engine.dart
  </read_first>
  <action>
    Add `kFailedAutoRetryWindow = Duration(hours: 4)` to `lib/config/constants.dart`.
    In `SyncEngine`, add an in-memory `DateTime? _lastAutoRetry` variable.
    Modify the connectivity-restored and app-resume triggers to call `retryFailed()` only if `_lastAutoRetry` is null or `DateTime.now().difference(_lastAutoRetry!) > kFailedAutoRetryWindow`.
    When `retryFailed()` executes, set `_lastAutoRetry = DateTime.now()`.
    Ensure the immediate sync-on-finish (post-save rising edge) remains unchanged.
    Explicitly write a regression test in `sync_engine_test.dart` to verify that the immediate sync-on-finish (rising edge) behavior still fires correctly, as mandated by D-08.
  </action>
  <acceptance_criteria>
    `test/sync/sync_engine_test.dart` passes and proves `retryFailed` is time-gated (called at most once per 4 hours for automated triggers) AND includes the required regression test proving immediate sync-on-finish fires correctly.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/sync/sync_engine_test.dart</automated>
  </verify>
  <done>Auto-retry triggers are gated by `kFailedAutoRetryWindow`.</done>
</task>

<task type="auto">
  <name>Task 2: Create and Wire SyncStuckBanner</name>
  <read_first>
    - lib/features/dashboard/screens/dashboard_screen.dart
    - lib/features/tracking/widgets/permission_banner.dart
  </read_first>
  <action>
    Add constants `kSyncStuckBannerMessage`, `kSyncStuckBannerAction`, and `kSyncStuckBannerDismiss` to `lib/config/constants.dart` matching the UI-SPEC.
    Create `SyncStuckBanner` in `lib/features/dashboard/widgets/sync_stuck_banner.dart` using `MaterialBanner` (patterned after `PermissionBanner`).
    Wire it into `DashboardScreen` so it appears below the app bar when `ref.watch(syncStatusProvider)` is `SyncFailed` and the auto-retry window is exhausted.
    The banner's action button should navigate to Settings to manually retry, and dismiss should hide it for the session.
  </action>
  <acceptance_criteria>
    `flutter analyze` is clean. The `SyncStuckBanner` renders correctly when `SyncFailed` is the current status and auto-retry is exhausted.
  </acceptance_criteria>
  <verify>
    <automated>flutter test test/features/dashboard/screens/dashboard_screen_test.dart</automated>
  </verify>
  <done>Banner appears on Dashboard only when sync is failed and stuck.</done>
</task>
</tasks>
