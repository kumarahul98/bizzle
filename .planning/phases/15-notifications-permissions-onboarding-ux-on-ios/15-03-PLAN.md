---
phase: 15-notifications-permissions-onboarding-ux-on-ios
plan: 03
type: execute
wave: 2
depends_on: ["15-02"]
files_modified:
  - lib/notifications/notification_service.dart
  - lib/app.dart
  - lib/features/tracking/services/tracking_notification_service.dart
  - lib/features/tracking/services/tracking_service_controller.dart
requirements: [IOS-10, IOS-11, IOS-14]
autonomous: true
must_haves:
  truths:
    - "iOS notification permission is requested contextually (~7 days since first trip, or earlier when a departure reminder is enabled), never on cold start, and never gates tracking Start"
    - "showRecording() posts nothing on iOS (Platform.isAndroid gate, defence-in-depth at service + controller)"
    - "The Android Active-commute notification shows a two-line live-stats body (elapsed / distance / moving / stuck) with the channel id, notification id, and foreground-service binding unchanged"
  artifacts:
    - path: "lib/notifications/notification_service.dart"
      provides: "requestIOSNotificationPermission() contextual helper"
      contains: "requestIOSNotificationPermission"
    - path: "lib/features/tracking/services/tracking_notification_service.dart"
      provides: "Platform.isAndroid gate + two-line enriched body"
      contains: "Platform.isAndroid"
  key_links:
    - from: "lib/features/tracking/services/tracking_notification_service.dart"
      to: "kTrackingNotificationBodyLine1Template + Line2Template"
      via: "_renderBody produces line1\\nline2"
      pattern: "kTrackingNotificationBodyLine"
    - from: "lib/notifications/notification_service.dart"
      to: "IOSFlutterLocalNotificationsPlugin.requestPermissions"
      via: "resolvePlatformSpecificImplementation"
      pattern: "requestPermissions"
---

<objective>
Deliver the iOS notification-decoupling thread (IOS-10), the phantom-notification suppression (IOS-11), and the Android notification parity enrichment (IOS-14). Pure Dart — no native code. Depends on Plan 02 for the shared formatters and the Phase 15 constants.

Three concerns:
1. Add `requestIOSNotificationPermission()` to NotificationService and trigger it contextually: ~7 days since the first recorded trip (the natural weekly-summary anchor), or earlier when the user enables a departure reminder (D-07 edge). Never on cold start; never a tracking gate.
2. Add the `Platform.isAndroid` defence-in-depth guard inside `showRecording()` (D-11) — the controller gate (already at line ~119) is the primary guard; the service guard makes the code self-documenting and belt-and-suspenders.
3. Enrich the Android Active-commute notification body to a two-line layout (elapsed / distance / moving / stuck) using the Plan 02 formatters + templates, with the D-14 channel/id/foreground-service invariants preserved (IOS-14).

Purpose: iOS notifications fire after a contextual ask without ever blocking Start; iOS posts no phantom tracking notification; Android matches the iOS Live Activity's live stats.
Output: 3 modified files, GREEN notification + tracking-notification tests.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-PATTERNS.md
@.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-RESEARCH.md

# Existing code to read before modifying:
@lib/notifications/notification_service.dart
@lib/features/tracking/services/tracking_notification_service.dart
@lib/features/tracking/services/tracking_service_controller.dart

<interfaces>
<!-- From 15-PATTERNS.md + 15-RESEARCH.md. Plan 02 already added formatElapsed, formatStuck, and the templates. -->
- Notification permission request (PATTERNS lines 418-441):
  `final ios = _plugin.resolvePlatformSpecificImplementation<IOSFlutterLocalNotificationsPlugin>(); await ios?.requestPermissions(alert: true, badge: true, sound: true);`
  Guard with `if (defaultTargetPlatform != TargetPlatform.iOS) return;`. Place after cancelReminder(), before private helpers. Wrap in try/on Exception with debugPrint (matches initialize() pattern).
- Contextual trigger anchor (RESEARCH §1, Assumption A1): 7 days since the OLDEST recorded trip's startTime AND notification permission currently undetermined → request once. Persist a one-time flag (user_preferences or a shared-pref key) to avoid re-requesting. Also request when a departure reminder is enabled before the 7-day mark.
- showRecording() service guard (PATTERNS lines 201-210): `import 'dart:io' show Platform;` then `if (!Platform.isAndroid) return;` at the top of showRecording(). dart:io Platform is acceptable here (never unit-tested in isolation; controller gate is the testable defaultTargetPlatform guard).
- Enriched body (PATTERNS lines 221-242): _renderBody returns `'$line1\n$line2'` where line1 = kTrackingNotificationBodyLine1Template with {elapsed}=formatElapsed(...), {km}=(meters/1000).toStringAsFixed(1); line2 = kTrackingNotificationBodyLine2Template with {moving}=formatStuck(timeMovingSeconds), {stuck}=formatStuck(timeStuckSeconds). BigTextStyleInformation receives the two-line string. summary = "{km} km · {elapsed}" per UI-SPEC Surface E.
- D-14 invariants (must NOT change): kTrackingNotificationChannelId, kTrackingNotificationId, ongoing:true, autoCancel:false, onlyAlertOnce:true, Importance.low, Priority.low, AndroidConfiguration.foregroundServiceNotificationId (Pitfall 3).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Contextual iOS notification permission request (IOS-10)</name>
  <files>lib/notifications/notification_service.dart, lib/app.dart</files>
  <behavior>
    - requestIOSNotificationPermission() resolves IOSFlutterLocalNotificationsPlugin and calls requestPermissions(alert:true, badge:true, sound:true) on iOS only.
    - No-op on non-iOS (defaultTargetPlatform guard).
    - Errors are caught and logged, never thrown (matches initialize() pattern).
  </behavior>
  <action>
Read notification_service.dart. Add the public `requestIOSNotificationPermission()` method after cancelReminder() and before the private helpers block (PATTERNS lines 418-434). Guard with `if (defaultTargetPlatform != TargetPlatform.iOS) return;`, resolve IOSFlutterLocalNotificationsPlugin, call requestPermissions(alert/badge/sound), wrap in try/on Exception catch with debugPrint (PATTERNS lines 436-441). The Darwin init already has requestAlertPermission:false (defers to this flow — do not change it).

Wire the contextual trigger (D-07): add the public method `maybeRequestNotificationPermissionForUsage()` in notification_service.dart that requests once when (a) the oldest recorded trip's startTime is ≥7 days ago AND notification permission is undetermined, OR (b) it is called from the departure-reminder enable path. Persist a one-time flag (user_preferences or a dedicated pref key) so it never re-asks. The method is no-op on non-iOS (defaultTargetPlatform guard) and never throws.

**Concrete call sites (NOT deferred — both are real steps in this task):**
- **(a) Root post-launch hook in `lib/app.dart`:** in the same widget build where `ref.watch(syncEngineProvider)` is eagerly read at app root (app.dart:46 — the established "run-at-root" seam, mirroring how SyncEngine registers its own `AppLifecycleListener(onResume:)` at sync_engine.dart:331), add a fire-and-forget `unawaited(ref.read(notificationServiceProvider).maybeRequestNotificationPermissionForUsage().catchError((Object _) {}))`. Run it from a post-frame callback (`WidgetsBinding.instance.addPostFrameCallback`) so it never blocks the first frame. App-launch cadence is sufficient for a ~7-day anchor.
- **(b) Departure-reminder enable path:** call `maybeRequestNotificationPermissionForUsage()` from wherever the departure reminder is enabled (the scheduleReminder path), so a reminder enabled before the 7-day mark still gets permission.

NEVER call from cold-start `initialize()` unconditionally.
  </action>
  <verify>
    <automated>flutter test test/unit/notifications/notification_service_test.dart --no-pub 2>&1 | tail -8 && grep -q "maybeRequestNotificationPermissionForUsage" lib/app.dart && echo OK</automated>
  </verify>
  <done>requestIOSNotificationPermission() GREEN; maybeRequestNotificationPermissionForUsage() requests once on the 7-day anchor or on reminder-enable; it is invoked from lib/app.dart's root post-frame hook (grep-confirmed) AND the reminder-enable path; no cold-start request; never gates Start.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Platform.isAndroid gate + Android two-line enriched body (IOS-11, IOS-14)</name>
  <files>lib/features/tracking/services/tracking_notification_service.dart, lib/features/tracking/services/tracking_service_controller.dart</files>
  <behavior>
    - showRecording() returns immediately (posts nothing) when !Platform.isAndroid (iOS).
    - _renderBody output is two lines using the templates; line1 has elapsed+km, line2 has moving+stuck.
    - kTrackingNotificationChannelId, kTrackingNotificationId unchanged; ongoing/autoCancel/onlyAlertOnce unchanged.
  </behavior>
  <action>
IOS-11: add `import 'dart:io' show Platform;` to tracking_notification_service.dart and `if (!Platform.isAndroid) return;` as the first statement of showRecording() (PATTERNS lines 201-210). Confirm the existing controller gate at tracking_service_controller.dart line ~119 (`if (defaultTargetPlatform != TargetPlatform.iOS) { await _notifications.showRecording(); }`) remains — it is the primary, testable guard. Do not duplicate or remove it.

IOS-14: rewrite _renderBody (~lines 252-264) to build the two-line string per the interfaces block — line1 from kTrackingNotificationBodyLine1Template ({elapsed}=formatElapsed, {km}=toStringAsFixed(1)), line2 from kTrackingNotificationBodyLine2Template ({moving}=formatStuck(timeMovingSeconds), {stuck}=formatStuck(timeStuckSeconds)), returning `'$line1\n$line2'`. Pass it to the existing BigTextStyleInformation (line ~178, unchanged call). Set the BigTextStyle summary to "{km} km · {elapsed}" per UI-SPEC Surface E. Keep title template unchanged. DO NOT introduce any new channel id or notification id, and DO NOT touch the AndroidConfiguration.foregroundServiceNotificationId or the ongoing/autoCancel/onlyAlertOnce flags (Pitfall 3 — single-shade-entry contract).
  </action>
  <verify>
    <automated>flutter test test/unit/features/tracking/tracking_notification_service_test.dart --no-pub</automated>
  </verify>
  <done>showRecording() no-ops on iOS; Android body is two lines with parity stats; channel/notification ids and foreground-service binding unchanged; tests GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| app → OS notification system | Notification permission scope and posting decisions cross into the OS notification subsystem |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-05 | Elevation of Privilege | requestIOSNotificationPermission timing | mitigate | Permission is requested only via the controlled contextual path (7-day usage anchor or departure-reminder enable), never on cold start; it can never gate tracking Start (D-06/D-07). |
| T-15-06 | Information Disclosure | Android notification body | mitigate | Body renders only pre-formatted aggregate stats (elapsed/distance/moving/stuck) via formatStuck/formatElapsed — never raw lat/lng (PII guard T-02-07 preserved). |
| T-15-07 | Denial of Service | foreground-service binding | accept | Enrichment is body-content only; channel id, notification id, and foregroundServiceNotificationId are unchanged, so the single-shade-entry foreground-service contract cannot regress (Pitfall 3). |
</threat_model>

<verification>
- `flutter test test/unit/notifications/notification_service_test.dart test/unit/features/tracking/tracking_notification_service_test.dart --no-pub` GREEN.
- `flutter test --no-pub` full suite GREEN (no Android foreground-service regression).
- `flutter analyze` clean on the three touched files.
</verification>

<success_criteria>
- IOS-10: contextual notification request; scheduled notifications fire after grant; never gates Start.
- IOS-11: no phantom notification on iOS (service + controller gates).
- IOS-14: Android enriched two-line stats body with D-14 invariants intact.
</success_criteria>

<output>
After completion, create `.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-03-SUMMARY.md`.
</output>
