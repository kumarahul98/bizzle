---
phase: 15-notifications-permissions-onboarding-ux-on-ios
plan: 02
type: execute
wave: 1
depends_on: ["15-01"]
files_modified:
  - lib/shared/utils/formatters.dart
  - lib/config/constants.dart
  - lib/config/routes.dart
  - lib/features/tracking/services/tracking_permission_service.dart
  - lib/features/tracking/widgets/permission_banner.dart
  - lib/features/onboarding/screens/onboarding_location_priming_screen.dart
  - lib/features/dashboard/screens/dashboard_screen.dart
autonomous: true
requirements: [IOS-09, IOS-13, IOS-14]
must_haves:
  truths:
    - "On iOS, preflight()/currentStatus() resolve on location alone and never return notificationDenied"
    - "When only When-In-Use is granted on iOS, the user lands in foregroundOnly degraded state with an iOS-specific dismissible 'Open Settings' banner"
    - "An iOS-only location priming screen renders before the system When-In-Use prompt — reachable for both fresh and already-signed-in users via the dashboard Start gate"
    - "formatElapsed and shared formatStuck exist in formatters.dart and produce parity output for both surfaces"
  artifacts:
    - path: "lib/shared/utils/formatters.dart"
      provides: "formatElapsed + formatStuck top-level functions"
      contains: "String formatElapsed"
    - path: "lib/features/onboarding/screens/onboarding_location_priming_screen.dart"
      provides: "iOS location priming screen (Surface A)"
      min_lines: 60
    - path: "lib/features/tracking/services/tracking_permission_service.dart"
      provides: "iOS branch in preflight() and currentStatus()"
      contains: "defaultTargetPlatform == TargetPlatform.iOS"
  key_links:
    - from: "lib/features/tracking/services/tracking_permission_service.dart"
      to: "TrackingPermissionStatus.foregroundOnly"
      via: "iOS branch returns foregroundOnly when locationAlways not granted"
      pattern: "foregroundOnly"
    - from: "lib/config/routes.dart"
      to: "OnboardingLocationPrimingScreen"
      via: "kRouteLocationPriming registration"
      pattern: "kRouteLocationPriming"
    - from: "lib/features/dashboard/screens/dashboard_screen.dart"
      to: "kRouteLocationPriming"
      via: "iOS Start gate navigates to priming screen before first preflight() when location undetermined"
      pattern: "kRouteLocationPriming"
---

<objective>
Deliver the iOS permission correctness thread plus the shared formatters both active-commute surfaces depend on (IOS-09), and the formatter foundation for IOS-13/IOS-14 parity. No native code — pure Dart/Flutter.

Three concerns:
1. Platform-branch `TrackingPermissionService.preflight()` and `currentStatus()` so iOS resolves on location alone and never touches `Permission.notification` (D-06). Android's four-step dance stays byte-for-byte unchanged.
2. The iOS location priming screen (Surface A, D-01) + route registration + the dashboard Start-gate wiring that actually navigates to it before the first system When-In-Use prompt, and the iOS degraded-state banner copy (D-03/D-05) reusing the existing `foregroundOnly` path.
3. Extract `formatStuck` and add `formatElapsed` to `formatters.dart` — the single source of truth consumed by the Android notification (Plan 03) and the Live Activity bridge (Plan 05).

Purpose: Make iOS permission flows first-class without blocking Start on notifications, and establish the formatter parity contract.
Output: 1 new screen, 6 modified files, GREEN permission + formatter tests.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-PATTERNS.md
@.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-UI-SPEC.md

# Existing code to read before modifying:
@lib/features/tracking/services/tracking_permission_service.dart
@lib/features/tracking/widgets/permission_banner.dart
@lib/features/onboarding/screens/onboarding_screen.dart
@lib/features/dashboard/screens/dashboard_screen.dart
@lib/shared/utils/formatters.dart
@lib/config/routes.dart

<interfaces>
<!-- Established patterns the executor must follow (from 15-PATTERNS.md). -->
- Platform branch: `if (defaultTargetPlatform == TargetPlatform.iOS) { ... }` (NEVER dart:io Platform in this service — Pitfall 2).
- preflight() iOS branch inserted AFTER backgroundGranted resolves, BEFORE the notification probe:
  `return backgroundGranted ? TrackingPermissionStatus.fullyGranted : TrackingPermissionStatus.foregroundOnly;`
- currentStatus() iOS branch: `if (defaultTargetPlatform == TargetPlatform.iOS) return locationStatus;` before the notification probe block.
- Priming screen is a ConsumerWidget reusing onboarding_screen.dart's Scaffold→SafeArea→LayoutBuilder→SingleChildScrollView→ConstrainedBox→IntrinsicHeight sticky-footer layout, padding fromLTRB(32,60,32,32), FeatureTick rows with 16px gaps, TraevyFonts.ui(...) text, tokens.textDim/textMuted. CTA copies GoogleContinueButton shell (14px radius, bgElev, borderStr) with Icons.location_on_outlined. Guard async taps with `if (!context.mounted) return;`.
- PermissionBanner gains an optional `String? body` param defaulting to existing Android copy; call site passes kIosPermissionBannerBody on iOS.
- All copy + route name + templates are constants appended to constants.dart (see 15-PATTERNS.md lines 506-592 for the exact block).
- DASHBOARD START GATE (verified seam): dashboard_screen.dart `_handleStart` (line 56) calls `service.preflight()` at line 63 — this is the single, reliable point the system When-In-Use prompt fires for BOTH fresh and already-signed-in users (the onboarding push chain at onboarding_screen.dart is sign-in-only and is SKIPPED by repeat users routed straight to MainShell by the app.dart auth gate). `service.currentStatus()` (line 224) is probe-only (never prompts) and returns `TrackingPermissionStatus.denied` when locationWhenInUse is undetermined/denied — use it to detect the undetermined state without surfacing a prompt.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared formatters (formatElapsed, formatStuck)</name>
  <files>lib/shared/utils/formatters.dart, lib/features/tracking/services/tracking_notification_service.dart</files>
  <behavior>
    - formatElapsed(82) == '01:22'; formatElapsed(3855) == '1:04:15'; MM:SS below 1hr, H:MM:SS at/above.
    - formatStuck(240) == '4m'; formatStuck(3720) == '1h2m' (matches existing private _formatStuck).
    - tracking_notification_service.dart uses the extracted top-level formatStuck (private _formatStuck removed).
  </behavior>
  <action>
Add two top-level functions to lib/shared/utils/formatters.dart following the existing `/// doc\nString name(int seconds)` style (see 15-PATTERNS.md lines 465-497 for exact bodies):
- `formatElapsed(int seconds)` → 'MM:SS' below 3600s, 'H:MM:SS' at/above. Document that it is distinct from formatDuration (Pitfall 7).
- `formatStuck(int seconds)` → 'Xm' below 1hr, 'XhYm' (or 'Xh' on the hour) above. This is the extraction target of the private `_formatStuck` currently at lines 269-275 of tracking_notification_service.dart.
Then in tracking_notification_service.dart: delete the private `_formatStuck` method and replace its call site (line ~259) with the imported top-level `formatStuck(...)`. formatters.dart is already imported there (line 65). Do NOT change any notification body/template logic yet — that is Plan 03; this task only swaps the formatter source.
  </action>
  <verify>
    <automated>flutter test test/unit/shared/utils/formatters_test.dart --no-pub</automated>
  </verify>
  <done>formatters_test.dart GREEN; _formatStuck no longer private to the notification service; existing notification tests still pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: iOS branch in TrackingPermissionService (preflight + currentStatus)</name>
  <files>lib/features/tracking/services/tracking_permission_service.dart</files>
  <behavior>
    - On iOS (defaultTargetPlatform override), preflight() returns fullyGranted when locationAlways granted and foregroundOnly otherwise — never probes Permission.notification, never returns notificationDenied.
    - currentStatus() on iOS returns the location-derived status only, never notificationDenied (Pitfall 5).
    - Android path unchanged: the four-step dance and the notification probe still run on non-iOS.
  </behavior>
  <action>
Per D-06 and 15-RESEARCH.md §1: read the file, then insert `if (defaultTargetPlatform == TargetPlatform.iOS) { return backgroundGranted ? TrackingPermissionStatus.fullyGranted : TrackingPermissionStatus.foregroundOnly; }` in preflight() immediately after backgroundGranted resolves and BEFORE `final notifStatus = await _probe(Permission.notification)` (insertion point ~line 190-198). Keep the existing ordering-invariant assert (fineGranted before Always) intact — it applies to both platforms. Add the matching guard in currentStatus() (`if (defaultTargetPlatform == TargetPlatform.iOS) return locationStatus;`) before its notification-probe block (~line 234-237). Add the D-06 explanatory comment at each branch. `flutter/foundation.dart` is already imported. Do NOT add dart:io Platform here (Pitfall 2) — tests rely on debugDefaultTargetPlatformOverride.
  </action>
  <verify>
    <automated>flutter test test/unit/features/tracking/tracking_permission_service_test.dart --no-pub</automated>
  </verify>
  <done>iOS-branch permission tests GREEN; Android dance tests unchanged and still GREEN; no notificationDenied path reachable on iOS.</done>
</task>

<task type="auto">
  <name>Task 3: Location priming screen + route + dashboard Start gate + degraded banner copy</name>
  <files>lib/config/constants.dart, lib/config/routes.dart, lib/features/tracking/widgets/permission_banner.dart, lib/features/onboarding/screens/onboarding_location_priming_screen.dart, lib/features/dashboard/screens/dashboard_screen.dart</files>
  <action>
1. constants.dart — append the Phase 15 block exactly as specified in 15-PATTERNS.md lines 506-592: kLiveActivityAppGroupId='group.com.travey.app', kLiveActivityUrlScheme='traevy', kLiveActivityId='commute', kIosPermissionBannerBody, all kIosLocationPriming* copy constants, kTrackingNotificationBodyLine1Template='● REC  {elapsed} · {km} km', kTrackingNotificationBodyLine2Template='Moving {moving} · Stuck {stuck}', kRouteLocationPriming='/location-priming'. (The Live Activity / notification-template constants are declared here now so Plans 03/05 import them; no behavior change yet.) No hardcoded strings — CLAUDE.md rule.

2. onboarding_location_priming_screen.dart — create the ConsumerWidget per UI-SPEC Surface A and 15-PATTERNS.md lines 35-161: Icons.location_on_rounded (48px, accent), 22px/w700 heading (kIosLocationPrimingHeading), 16px/textDim body (max width 280, kIosLocationPrimingBody), three FeatureTick rows (16px gaps) from the Tick constants, Spacer, a local _LocationCTAButton (copy GoogleContinueButton shell, Icons.location_on_outlined, label kIosLocationPrimingCta) that on tap requests locationWhenInUse via TrackingPermissionService (using ref), then pops back to the caller (`Navigator.pop(context)`), guarded with `if (!context.mounted) return;`. CRITICAL ORDERING (avoids a double prompt): the CTA's job is to surface the system When-In-Use prompt ONCE here. After it pops, the dashboard gate (step 5) resumes and calls `preflight()`, which re-probes locationWhenInUse — it must now see it already granted and therefore NOT trigger a second system prompt. The priming screen must NOT call `preflight()` itself and must NOT request locationAlways (that is D-02, requested at first trip Start). Also add a centered "Skip for now" link (kIosLocationPrimingSkip, textMuted) that pops without requesting (the dashboard gate then lets preflight() run as before, which on iOS surfaces the prompt inline — acceptable fallback), and the terms blurb (kIosLocationPrimingTerms, textMuted). Add Semantics on the CTA and ExcludeSemantics on the decorative icon per UI-SPEC Accessibility. Keep the file's widgets under 100 lines each (extract _LocationCTAButton in-file).

3. routes.dart — import the new screen and register `kRouteLocationPriming: (context) => const OnboardingLocationPrimingScreen(),` in kAppRoutes (pattern at lines 53-62).

4. permission_banner.dart — add an optional `this.body` (final String? body) constructor param defaulting to the existing Android copy via `body ?? '<existing copy>'` in the MaterialBanner content (15-PATTERNS.md lines 619-660). At the tracking-screen call site that constructs PermissionBanner for foregroundOnly, pass `body: defaultTargetPlatform == TargetPlatform.iOS ? kIosPermissionBannerBody : null`. (Read permission_gate.dart / the tracking status layout to find the exact call site.)

5. dashboard_screen.dart — wire the priming screen so it actually shows before the first system When-In-Use prompt (D-01), for BOTH fresh and already-signed-in users. In `_handleStart` (line 56), BEFORE the `final status = await service.preflight();` call at line 63, add an iOS-only one-time gate:
   - Guard on platform: `if (defaultTargetPlatform == TargetPlatform.iOS)` (import `package:flutter/foundation.dart`; do NOT use dart:io Platform — match the service's Pitfall 2 convention).
   - Detect undetermined location WITHOUT prompting: `final pre = await service.currentStatus();` then `if (!context.mounted) return;`. `currentStatus()` is probe-only and returns `TrackingPermissionStatus.denied` when locationWhenInUse is not yet granted (the "first time" signal — once granted it returns foregroundOnly/fullyGranted and the gate is naturally skipped, so no extra persisted "shown" flag is needed).
   - When `pre == TrackingPermissionStatus.denied`: `await Navigator.pushNamed(context, kRouteLocationPriming);` then `if (!context.mounted) return;`. The priming screen's CTA requests When-In-Use and pops; on return, control falls through to the existing `final status = await service.preflight();` which now sees location already granted (no double prompt — see Task 2 step's CTA ordering note).
   - When `pre` is anything else, skip the priming push and fall straight through to the existing preflight() flow unchanged.
   Leave the Android path and the entire post-preflight() status-handling block (lines 64–89) byte-for-byte unchanged. Import kRouteLocationPriming from constants.dart (constants is already imported at line 5).
  </action>
  <verify>
    <automated>flutter analyze lib/features/onboarding/screens/onboarding_location_priming_screen.dart lib/config/routes.dart lib/config/constants.dart lib/features/tracking/widgets/permission_banner.dart lib/features/dashboard/screens/dashboard_screen.dart --no-pub 2>&1 | tail -5</automated>
  </verify>
  <done>Priming screen renders Surface A; route registered; dashboard_screen.dart contains 'kRouteLocationPriming' (the gate is wired and reachable for fresh + repeat users); priming CTA requests When-In-Use once and preflight() sees it granted (no double prompt); banner accepts iOS copy and defaults to Android copy; all Phase 15 constants present; flutter analyze clean on touched files.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user → permission system | User-granted location scope (When-In-Use vs Always) controls background recording capability |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-03 | Elevation of Privilege | TrackingPermissionService iOS branch | mitigate | iOS branch only ever returns location-derived statuses; it cannot grant tracking when location is denied. Start gating remains location-driven (D-06) — notification permission can never be escalated into a tracking gate. |
| T-15-04 | Spoofing | onboarding route navigation | accept | Priming screen is informational only; the actual permission grant is the OS system dialog (not spoofable by app UI). |
</threat_model>

<verification>
- `flutter test test/unit/features/tracking/tracking_permission_service_test.dart test/unit/shared/utils/formatters_test.dart --no-pub` GREEN.
- `flutter test --no-pub` full suite GREEN (no Android regression).
- `flutter analyze` clean on the seven touched files.
</verification>

<success_criteria>
- IOS-09: iOS resolves on location alone; foregroundOnly degraded path reachable with iOS banner copy; the priming screen precedes the system prompt and is reachable from the dashboard Start gate for both fresh and already-signed-in users.
- Formatter parity foundation (formatElapsed + shared formatStuck) ready for Plans 03 and 05.
</success_criteria>

<output>
After completion, create `.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-02-SUMMARY.md`.
</output>
