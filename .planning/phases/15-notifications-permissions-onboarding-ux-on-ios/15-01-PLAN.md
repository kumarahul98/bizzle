---
phase: 15-notifications-permissions-onboarding-ux-on-ios
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - test/unit/features/tracking/live_activity_service_test.dart
  - test/unit/features/tracking/tracking_notification_service_test.dart
  - test/unit/features/tracking/tracking_permission_service_test.dart
  - test/unit/notifications/notification_service_test.dart
  - test/unit/shared/utils/formatters_test.dart
autonomous: false
requirements: [IOS-09, IOS-10, IOS-11, IOS-13, IOS-14]
user_setup:
  - service: "Apple personal-team free provisioning"
    why: "Validate App Group entitlement provisions on a personal team before any Live Activity Swift code is written (RESEARCH.md Pitfall 1 / Assumption A3 — HIGH risk)"
    dashboard_config:
      - task: "Confirm a physical iPhone (iOS 17+) is connected and trusted for on-device install via Xcode automatic signing"
        location: "Xcode → Runner target → Signing & Capabilities (personal team selected)"

must_haves:
  truths:
    - "App Group provisioning on the personal-team profile is confirmed PASS or the IOS-13 approach is re-decided before any Swift is written"
    - "Failing/scaffolded unit tests exist for every Dart behavior this phase adds (permission iOS branch, notification request, Platform.isAndroid gate, enriched body, Live Activity lifecycle, formatters)"
  artifacts:
    - path: "test/unit/shared/utils/formatters_test.dart"
      provides: "RED tests for formatElapsed + formatStuck"
      contains: "formatElapsed"
    - path: "test/unit/features/tracking/live_activity_service_test.dart"
      provides: "RED tests for LiveActivityService lifecycle + iOS 17 gate"
      contains: "LiveActivityService"
  key_links:
    - from: "Wave 0 device probe result"
      to: "Plan 04 (native Swift Widget Extension)"
      via: "BLOCKING gate — Swift work only proceeds on PASS"
      pattern: "App Group"
---

<objective>
Wave 0: de-risk the phase before any implementation. Two outputs:

1. A BLOCKING on-device probe confirming the personal-team free-provisioning profile can provision an App Group entitlement shared between the `Runner` target and a new `TraevyLiveActivity` Widget Extension target. Per RESEARCH.md Pitfall 1 / Assumption A3 this is the phase's single highest-risk dependency (LOW confidence) and MUST be validated on a real iPhone before any Swift / Widget-Extension code (Plan 04) is written.
2. Test scaffolds (failing/MISSING tests) for every Dart behavior this phase introduces, so subsequent waves execute against existing test files (Nyquist compliance).

Purpose: Avoid building the entire Live Activity native target only to discover at device-install time that App Groups cannot be provisioned on a free account — at which point the fallback (custom method channel, no App Group) would require re-planning Plans 04/05.
Output: One human-verified provisioning decision + five test files (4 extended, 1 new).
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-RESEARCH.md
@.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-VALIDATION.md

<interfaces>
<!-- TripSnapshot shape (from lib/features/tracking/services/trip_accumulator.dart) — the Live Activity tests assert against this -->
```dart
class TripSnapshot {
  const TripSnapshot({
    required this.startedAt,       // DateTime (UTC)
    required this.elapsedSeconds,  // int
    required this.distanceMeters,  // double
    required this.timeMovingSeconds, // int
    required this.timeStuckSeconds,  // int
    required this.currentSpeedMs,    // double
  });
}
```
Existing test files to EXTEND (do not recreate): test/unit/features/tracking/tracking_permission_service_test.dart, test/unit/features/tracking/tracking_notification_service_test.dart, test/unit/notifications/notification_service_test.dart.
NEW test files: test/unit/features/tracking/live_activity_service_test.dart, test/unit/shared/utils/formatters_test.dart.
Use `debugDefaultTargetPlatformOverride = TargetPlatform.iOS` to exercise iOS branches (NEVER dart:io Platform in tests — see RESEARCH.md Pitfall 2).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold formatter + Live Activity unit tests (RED)</name>
  <files>test/unit/shared/utils/formatters_test.dart, test/unit/features/tracking/live_activity_service_test.dart</files>
  <action>
Create test/unit/shared/utils/formatters_test.dart covering the two formatters Plan 02 will add to lib/shared/utils/formatters.dart:
- `formatElapsed(int seconds)`: assert formatElapsed(0) == '00:00', formatElapsed(82) == '01:22', formatElapsed(3599) == '59:59', formatElapsed(3600) == '1:00:00', formatElapsed(3855) == '1:04:15'. (MM:SS below 1hr, H:MM:SS at/above — IOS-13/14 parity, distinct from formatDuration per RESEARCH.md Pitfall 7.)
- `formatStuck(int seconds)`: assert formatStuck(240) == '4m', formatStuck(2040) == '34m', formatStuck(3600) == '1h', formatStuck(3720) == '1h2m'. (Matches the existing private _formatStuck logic in tracking_notification_service.dart lines 269-275, which Plan 02 extracts.)
Import 'package:traevy/shared/utils/formatters.dart'. These functions do not exist yet — the test file MUST fail to compile/run until Plan 02 adds them. That is the intended RED state.

Create test/unit/features/tracking/live_activity_service_test.dart as a scaffold for the LiveActivityService that Plan 05 implements. Write the test bodies referencing the intended API but mark the suite so it documents the contract:
- `start()` is a no-op when `debugDefaultTargetPlatformOverride = TargetPlatform.android` (returns without calling the plugin).
- iOS 17 gate: `start()` does not create an activity when areActivitiesSupported() returns false.
- `_contentState` maps a TripSnapshot to the 7 keys: elapsedFormatted, distanceFormatted, movingFormatted, stuckFormatted, isMoving, direction, startDate (per UI-SPEC Native Extension Contract).
Because LiveActivityService does not exist yet, leave the body assertions written against the intended public surface and add a top-level comment `// Wave 0 scaffold — implemented in Plan 05`. The file is expected RED until Plan 05.
  </action>
  <verify>
    <automated>flutter test test/unit/shared/utils/formatters_test.dart --no-pub 2>&1 | tail -5; echo "EXPECTED RED — formatters not yet added (Plan 02)"</automated>
  </verify>
  <done>Both test files exist; formatters_test.dart fails because formatElapsed/formatStuck are undefined; live_activity_service_test.dart documents the LiveActivityService contract with a Wave 0 scaffold marker.</done>
</task>

<task type="auto">
  <name>Task 2: Extend permission / notification / tracking-notification test stubs (RED)</name>
  <files>test/unit/features/tracking/tracking_permission_service_test.dart, test/unit/notifications/notification_service_test.dart, test/unit/features/tracking/tracking_notification_service_test.dart</files>
  <action>
Read each existing test file first, then APPEND new test groups (do not rewrite existing tests):

tracking_permission_service_test.dart — add an `group('iOS branch (IOS-09/IOS-10)')` with:
- preflight() under `debugDefaultTargetPlatformOverride = TargetPlatform.iOS` returns `TrackingPermissionStatus.fullyGranted` when locationAlways granted, and never probes Permission.notification (assert via the existing mock/fake seam — TrackingPermissionService.forTesting at lines 123-129).
- preflight() returns `TrackingPermissionStatus.foregroundOnly` when only When-In-Use granted on iOS (D-03).
- currentStatus() on iOS never returns `notificationDenied` (RESEARCH.md Pitfall 5).
These will be RED until Plan 02 adds the iOS branch.

notification_service_test.dart — add a test asserting `requestIOSNotificationPermission()` resolves the IOSFlutterLocalNotificationsPlugin implementation and calls requestPermissions(alert/badge/sound). RED until Plan 03.

tracking_notification_service_test.dart — add two groups:
- IOS-11 gate: `showRecording()` returns early / posts nothing when `Platform.isAndroid` is false (note: this guard uses dart:io Platform per PATTERNS.md, so test the controller-level gate via TrackingServiceController instead, OR assert the service returns without invoking the injected plugin). RED until Plan 03.
- IOS-14 enriched body: `_renderBody`-equivalent output is two lines using kTrackingNotificationBodyLine1Template / Line2Template; assert channel id (kTrackingNotificationChannelId) and notification id (kTrackingNotificationId) are unchanged (D-14 contract, RESEARCH.md Pitfall 3). RED until Plan 03.

Where a referenced constant does not yet exist, write the test referencing it so the file is RED for the right reason.
  </action>
  <verify>
    <automated>flutter test test/unit/features/tracking/tracking_permission_service_test.dart test/unit/notifications/notification_service_test.dart --no-pub 2>&1 | tail -8; echo "EXPECTED partial RED — iOS branch + requestIOSNotificationPermission not yet implemented (Plans 02/03)"</automated>
  </verify>
  <done>The three existing test files contain appended RED test groups covering IOS-09 iOS branch, IOS-10 notification request, IOS-11 gate, and IOS-14 enriched-body + D-14 invariants. Existing passing tests still pass.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: BLOCKING App-Group device-provisioning probe</name>
  <action>Human runs the on-device App-Group provisioning probe described below and reports PASS or FAIL. This gate blocks Plan 04 (native Swift) until resolved.</action>
  <what-built>
Test scaffolds for all Dart behaviors are in place (Tasks 1-2). This checkpoint validates the single highest-risk native dependency on a real device BEFORE Plan 04 writes any Swift.
  </what-built>
  <how-to-verify>
On a physical iPhone running iOS 17+ (free / personal-team provisioning):
1. Open `ios/Runner.xcworkspace` in Xcode.
2. File → New → Target → Widget Extension. Product name `TraevyLiveActivity`, "Embed in Application: Runner", Activate.
3. For BOTH the `Runner` and `TraevyLiveActivity` targets: Signing & Capabilities → + Capability → App Groups → add `group.com.travey.app`. Ensure "Automatically manage signing" is on and your personal team is selected.
4. Build and install on the connected iPhone (Cmd+R / `flutter run` to the device).
5. Observe whether code signing succeeds for BOTH targets with the App Group entitlement.

Report ONE of:
- "PASS — App Group provisioned, app installed on device, no signing error" → Plan 04 proceeds with the live_activities plugin + App Group bridge.
- "FAIL — signing error mentioning com.apple.security.application-groups (or the entitlement is rejected)" → describe the exact error. Plans 04/05 switch to the no-App-Group fallback (custom method channel + ActivityKit in AppDelegate, ContentState baked at Activity.request() time) per RESEARCH.md Pitfall 1 mitigation, and this phase returns to replanning those two plans before continuing.

NOTE: the throwaway Xcode target created here for the probe may be deleted after the test; Plan 04 creates the real target with its Swift sources. (If it provisions cleanly you may keep it.)
  </how-to-verify>
  <resume-signal>Type "PASS" (App Groups provision on device) or "FAIL: <error>" (fallback path needed)</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| device toolchain → signed app | Provisioning profile entitlements determine what capabilities the installed binary may use |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-01 | Tampering | App Group shared container (UserDefaults suite) | accept | Container is sandboxed to the app's signed team; only Runner + its embedded extension can read/write. No untrusted process shares the group on a personal-team device. |
| T-15-02 | Information Disclosure | data placed in the App Group container for the Live Activity | mitigate | Only pre-formatted display strings (elapsed/distance) are ever written to the shared container — never raw lat/lng (enforced by the _contentState contract validated in Task 1). |
</threat_model>

<verification>
- Test files exist and run (RED where the implementation is pending in later waves).
- The device-provisioning probe returns an explicit PASS/FAIL decision recorded in the SUMMARY.
</verification>

<success_criteria>
- All five test files present; `flutter test test/unit/ --no-pub` collects them (failures expected only in the new RED groups).
- A documented App-Group provisioning decision (PASS → proceed with plugin; FAIL → fallback re-plan) before Plan 04 begins.
</success_criteria>

<output>
After completion, create `.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-01-SUMMARY.md` recording: the test scaffolds added, and the device-provisioning probe result (PASS/FAIL + exact error if any).
</output>
