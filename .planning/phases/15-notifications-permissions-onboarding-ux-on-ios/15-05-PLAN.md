---
phase: 15-notifications-permissions-onboarding-ux-on-ios
plan: 05
type: execute
wave: 3
depends_on: ["15-02", "15-04"]
files_modified:
  - pubspec.yaml
  - lib/features/tracking/services/live_activity_service.dart
  - lib/features/tracking/providers/tracking_providers.dart
requirements: [IOS-13]
autonomous: true
must_haves:
  truths:
    - "On iOS 17+, starting a trip creates a Live Activity; on iOS < 17 it is a no-op (blue indicator only)"
    - "The Live Activity updates from the TripAccumulator snapshot stream on the same 5s cadence as the Android notification, and is dismissed when the trip stops"
    - "Tapping the Live Activity Stop button (traevy://stop) routes to TrackingServiceController.stop()"
    - "Orphaned Live Activities are ended when tracking is idle (post app-kill cleanup)"
  artifacts:
    - path: "lib/features/tracking/services/live_activity_service.dart"
      provides: "Dart bridge: init/start/update/end/endAll + iOS-17 gate + url-scheme stop routing"
      contains: "LiveActivityService"
      min_lines: 60
    - path: "lib/features/tracking/providers/tracking_providers.dart"
      provides: "liveActivityServiceProvider + lifecycle wiring in TrackingNotifier"
      contains: "liveActivityServiceProvider"
  key_links:
    - from: "lib/features/tracking/providers/tracking_providers.dart"
      to: "liveActivityServiceProvider.update / .end"
      via: "TrackingNotifier._maybeRefreshNotification + stop()"
      pattern: "liveActivityServiceProvider"
    - from: "lib/features/tracking/services/live_activity_service.dart"
      to: "TrackingServiceController.stop()"
      via: "urlSchemeStream host=='stop' listener"
      pattern: "data.host == 'stop'"
---

<objective>
Wire the Dart side of the iOS Live Activity (IOS-13, D-08/D-09/D-10). This connects the native extension (Plan 04) to the live trip data via the live_activities plugin, completing the iOS active-commute surface. Depends on Plan 02 (formatters + Phase 15 constants) and Plan 04 (native Widget Extension + Info.plist).

Concerns:
1. Add the two pub packages (live_activities ^2.4.9, device_info_plus ^13.1.0) verified in RESEARCH.
2. Create LiveActivityService: init (with URL-scheme stop routing), iOS-17 gate (device_info_plus + areActivitiesSupported/Enabled), start/update/end/endAll, and the TripSnapshot → ContentState mapping using the Plan 02 formatters. All operations swallow errors (Deviation Rule 4 — Live Activity is additive; tracking must continue).
3. Wire it into tracking_providers.dart: a keep-alive Provider, an update call in TrackingNotifier._maybeRefreshNotification (same 5s cadence as the Android notification), an end call in stop(), and idle/orphan cleanup (endAll when TrackingIdle).

Purpose: The iOS 17+ Live Activity reflects live trip stats and its Stop button ends the trip; iOS < 17 degrades cleanly to blue-indicator-only.
Output: pubspec + 1 new service + provider wiring, GREEN live_activity_service tests.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-PATTERNS.md
@.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-RESEARCH.md

# Existing code to read before modifying:
@lib/features/tracking/providers/tracking_providers.dart
@lib/features/tracking/services/tracking_service_controller.dart
@lib/features/tracking/services/trip_accumulator.dart

<interfaces>
<!-- Packages (RESEARCH lines 96-121, VERIFIED on pub.dev): live_activities: ^2.4.9, device_info_plus: ^13.1.0 -->
<!-- LiveActivityService full pattern: 15-PATTERNS.md lines 248-348 (authoritative). Key points: -->
- Constructor injection: `LiveActivityService({LiveActivities? plugin}) : _plugin = plugin ?? LiveActivities();`
- init(controller): no-op on non-iOS; `_plugin.init(appGroupId: kLiveActivityAppGroupId, urlScheme: kLiveActivityUrlScheme, requestAndroidNotificationPermission: false)`; then listen urlSchemeStream and `if (data.host == 'stop') unawaited(controller.stop());`
- _isLiveActivitySupported(): non-iOS→false; areActivitiesSupported() && areActivitiesEnabled() && DeviceInfoPlugin().iosInfo major version >= 17 (D-09 floor — areActivitiesSupported alone returns true on 16.1+, insufficient).
- _contentState(TripSnapshot s, String direction): the 7 keys matching Plan 04's struct — elapsedFormatted: formatElapsed(s.elapsedSeconds), distanceFormatted: formatDistance(s.distanceMeters), movingFormatted: formatStuck(s.timeMovingSeconds), stuckFormatted: formatStuck(s.timeStuckSeconds), isMoving: s.currentSpeedMs >= kStuckSpeedThresholdMs, direction, startDate: s.startedAt.millisecondsSinceEpoch.toDouble() (send as Double so it matches Plan 04's `ContentState.startDate: Double` — the bridge can't decode an int as a Swift Date).
- start/update use kLiveActivityId='commute'; end calls endActivity; endAll calls endAllActivities (orphan cleanup, Pitfall 4). All wrapped try/on Object {} (Deviation Rule 4).
<!-- Provider wiring: 15-PATTERNS.md lines 352-410. liveActivityServiceProvider is a manual Provider (no @riverpod — Phase 1 D-12 constraint), keep-alive (no autoDispose). Update call mirrors the existing notification refresh in _maybeRefreshNotification with unawaited(...).catchError((_){}) on the same 5s throttle. end() called in stop(). endAll() when TrackingIdle is confirmed. -->
<!-- TripSnapshot fields: startedAt(DateTime), elapsedSeconds(int), distanceMeters(double), timeMovingSeconds(int), timeStuckSeconds(int), currentSpeedMs(double). -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add packages + LiveActivityService bridge</name>
  <files>pubspec.yaml, lib/features/tracking/services/live_activity_service.dart</files>
  <action>
1. `flutter pub add live_activities device_info_plus` (pins ^2.4.9 / ^13.1.0 per RESEARCH; run pub get).

2. Create lib/features/tracking/services/live_activity_service.dart per 15-PATTERNS.md lines 248-348 (authoritative). Implement: constructor injection of LiveActivities; `init(TrackingServiceController controller)` that no-ops off iOS, calls `_plugin.init(appGroupId: kLiveActivityAppGroupId, urlScheme: kLiveActivityUrlScheme, requestAndroidNotificationPermission: false)`, and subscribes to urlSchemeStream routing `data.host == 'stop'` to `unawaited(controller.stop())`; `_isLiveActivitySupported()` (areActivitiesSupported && areActivitiesEnabled && iOS major >= 17 via device_info_plus); `start(TripSnapshot, String direction)` (gate, then createActivity(kLiveActivityId, _contentState(...)), store _activityId); `update(TripSnapshot, String direction)` (no-op if no _activityId, else updateActivity); `end()` (endActivity + clear _activityId); `endAll()` (endAllActivities). The `_contentState` map uses formatElapsed/formatDistance/formatStuck and the 7 keys exactly matching Plan 04's ContentState struct. Wrap every plugin call in try/on Object {} — Live Activity is additive and must never break tracking (Deviation Rule 4). Use defaultTargetPlatform (not dart:io Platform) for the iOS gate.
  </action>
  <verify>
    <automated>flutter test test/unit/features/tracking/live_activity_service_test.dart --no-pub</automated>
  </verify>
  <done>Packages added; LiveActivityService implements init/start/update/end/endAll with the iOS-17 gate, url-scheme stop routing, and the 7-key _contentState; live_activity_service_test.dart GREEN.</done>
</task>

<task type="auto">
  <name>Task 2: Wire LiveActivityService into tracking providers lifecycle</name>
  <files>lib/features/tracking/providers/tracking_providers.dart</files>
  <action>
Per 15-PATTERNS.md lines 352-410: read tracking_providers.dart, then:
1. Declare `liveActivityServiceProvider` as a manual keep-alive `Provider<LiveActivityService>((ref) => LiveActivityService(), name: 'liveActivityServiceProvider')` alongside trackingNotificationServiceProvider (no @riverpod — Phase 1 D-12 constraint; no autoDispose).
2. Ensure init() is invoked once with the TrackingServiceController (wire from the existing service-init seam — find where trackingNotificationServiceProvider/controller are initialized at startup and add the parallel liveActivityService init).
3. On trip start (where TrackingActive begins), call `liveActivityService.start(snapshot, direction)` for iOS-17+ (the service self-gates; just call it).
4. In TrackingNotifier._maybeRefreshNotification(TrackingActive active): add a parallel `unawaited(ref.read(liveActivityServiceProvider).update(TripSnapshot(...from active...), direction).catchError((Object _) {}))` on the SAME existing 5s throttle as the notification refresh (do not add a new throttle constant — Assumption A2).
5. In stop(): add `unawaited(ref.read(liveActivityServiceProvider).end().catchError((Object _) {}))` after the controller stop.
6. Orphan cleanup: when TrackingIdle is confirmed (the finalize listener), call endAll() so a Live Activity orphaned by an app-kill is cleared on next idle (Pitfall 4).
Construct the TripSnapshot from the TrackingActive fields (startedAt, elapsedSeconds, distanceMeters, timeMovingSeconds, timeStuckSeconds, currentSpeedMs). Use the existing unawaited(...).catchError pattern (lines 302-316). Do not await Live Activity calls in the synchronous state path.
  </action>
  <verify>
    <automated>flutter test test/unit/features/tracking/ --no-pub 2>&1 | tail -6</automated>
  </verify>
  <done>liveActivityServiceProvider declared and initialized; start/update/end/endAll wired at the trip lifecycle points on the existing 5s cadence; tracking notifier tests GREEN; no Android regression.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Live Activity URL scheme → tracking controller | The Stop button's traevy://stop callback crosses into the app and triggers a state-changing stop() |
| TripAccumulator stream → App Group container | Snapshot data is bridged through the plugin into the shared container the widget reads |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-15-11 | Tampering / unauthorized action | urlSchemeStream listener | mitigate | Exact-match `data.host == 'stop'` guard; any other URL/host is ignored. No arbitrary host routing (ASVS V5 input validation). |
| T-15-12 | Information Disclosure | _contentState payload | mitigate | Only pre-formatted aggregate strings are bridged (formatElapsed/formatDistance/formatStuck); raw lat/lng never enters the ContentState map (PII guard T-02-07 preserved). |
| T-15-13 | Denial of Service | Live Activity plugin failures | accept | All plugin calls are wrapped try/on Object {} (Deviation Rule 4); a Live Activity failure can never crash or stall tracking — the trip continues with the blue indicator. |
</threat_model>

<verification>
- `flutter test test/unit/features/tracking/ --no-pub` GREEN (live_activity_service + tracking_notifier).
- `flutter test --no-pub` full suite GREEN.
- `flutter analyze` clean on touched Dart files.
- (On-device) live updates during a backgrounded commute + Stop-button dismissal is human-gated UAT per 15-VALIDATION.md (Simulator cannot reproduce ActivityKit background cadence) — recorded as human_needed.
</verification>

<success_criteria>
- IOS-13 (Dart half): iOS 17+ start creates a Live Activity, it updates from TripAccumulator on the 5s cadence and dismisses on stop; the Stop button routes to TrackingServiceController.stop(); iOS < 17 is a clean no-op; orphans cleared on idle.
</success_criteria>

<output>
After completion, create `.planning/phases/15-notifications-permissions-onboarding-ux-on-ios/15-05-SUMMARY.md`. Note that live ActivityKit behavior is device-UAT-gated (human_needed), folding into the Phase 16 combined device pass.
</output>
