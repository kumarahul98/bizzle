---
phase: 09-authentication
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - lib/features/auth/models/auth_state.dart
  - lib/database/daos/trips_dao.dart
  - lib/database/daos/user_preferences_dao.dart
autonomous: true
requirements: [AUTH-01, AUTH-03]

must_haves:
  truths:
    - "AuthState is a sealed class with exactly three variants: loading, guest, signedIn(uid, name, email)"
    - "Calling backfillUserId rewrites every 'local_user' row to the Firebase uid in trips and user_preferences"
    - "tripsDao.backfillUserId returns the count of rewritten rows so the caller can detect first sign-in"
    - "The backfill test (Plan 01) turns GREEN"
  artifacts:
    - path: "lib/features/auth/models/auth_state.dart"
      provides: "sealed AuthState { AuthLoading, AuthGuest, AuthSignedIn(uid,name,email) }"
      contains: "sealed class AuthState"
      min_lines: 30
    - path: "lib/database/daos/trips_dao.dart"
      provides: "backfillUserId(String newUserId) -> Future<int>"
      contains: "backfillUserId"
    - path: "lib/database/daos/user_preferences_dao.dart"
      provides: "backfillUserId(String newUserId) -> Future<int>"
      contains: "backfillUserId"
  key_links:
    - from: "lib/database/daos/trips_dao.dart"
      to: "trips.userId == kDefaultUserId"
      via: "explicit-WHERE update (never .replace())"
      pattern: "userId\\.equals\\(kDefaultUserId\\)"
---

<objective>
Build the two foundational, fully-unit-testable pieces of Phase 9 auth: the sealed `AuthState` model and the `backfillUserId` DAO methods. Both have defined inputs/outputs and no platform-channel dependency, so this plan turns the Plan 01 backfill RED test GREEN.

Purpose: Downstream waves consume `AuthState` (app gate, settings, providers) and `backfillUserId` (AuthService). Defining the contracts here lets Wave 3 build against fixed types.
Output: lib/features/auth/models/auth_state.dart; backfillUserId added to both DAOs.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-authentication/09-RESEARCH.md
@.planning/phases/09-authentication/09-PATTERNS.md

<interfaces>
<!-- Sealed-state analog: lib/features/tracking/state/tracking_state.dart -->
@immutable sealed class TrackingState { const TrackingState(); }
final class TrackingIdle extends TrackingState { const TrackingIdle(); }      // const singleton
final class TrackingActive extends TrackingState { const TrackingActive({required this.startedAt, ...}); final DateTime startedAt; }  // payload subtype

<!-- Explicit-WHERE update analog: lib/database/daos/trips_dao.dart updateTrip lines 115-119 -->
Future<void> updateTrip(TripsCompanion companion) {
  return (update(trips)..where((t) => t.id.equals(companion.id.value))).write(companion);
}
// .write(...) returns Future<int> (rows affected) — capture it for backfill.

<!-- constants (from Plan 01 / existing) -->
const String kDefaultUserId = 'local_user';

<!-- user_preferences is a single row at id = 1, userId defaults to kDefaultUserId -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create the sealed AuthState model</name>
  <files>lib/features/auth/models/auth_state.dart</files>
  <read_first>
    - lib/features/tracking/state/tracking_state.dart (the exact sealed-class shape to copy: @immutable base, final-class subtypes, const singletons, payload subtype with final fields, doc-comment "exhaustive switch, no default")
    - lib/features/trips/providers/trip_management_providers.dart (lines 13-42: alternate sealed-state example + the "Never add a default branch" doc convention)
    - .planning/phases/09-authentication/09-PATTERNS.md (section "auth_state.dart" — analog and apply-to notes)
    - lib/config/constants.dart (kPlaceholderUserName for the AuthSignedIn name fallback contract reference)
  </read_first>
  <behavior>
    - AuthLoading is a const singleton (identical at every call site, like TrackingIdle).
    - AuthGuest is a const singleton (a valid permanent state per D-06, not an error).
    - AuthSignedIn carries final uid, name, email (all required, like TrackingActive's payload).
    - An exhaustive `switch (authState)` over the three variants compiles with no default branch; adding a fourth variant would be a compile error at call sites.
  </behavior>
  <action>
    Create lib/features/auth/models/auth_state.dart. Define `@immutable sealed class AuthState { const AuthState(); }` with three `final class` subtypes per D-01/D-06: `AuthLoading` (const constructor, singleton), `AuthGuest` (const constructor, singleton), `AuthSignedIn` (`const AuthSignedIn({required this.uid, required this.name, required this.email})` with three final String fields). Match tracking_state.dart's dartdoc style: class-level doc explaining "Use exhaustive switch... Never add a default branch" and the CLAUDE.md sealed-state rationale. Do NOT add a `mapNameFallback` helper here — the displayName→kPlaceholderUserName fallback lives in the notifier (Plan 03), not the model. No equals/hashCode override needed (const singletons use identical(); AuthSignedIn is compared by reference per the tracking analog).
  </action>
  <verify>
    <automated>flutter analyze lib/features/auth/models/auth_state.dart</automated>
  </verify>
  <acceptance_criteria>
    - File contains `sealed class AuthState` and three `final class` subtypes named exactly AuthLoading, AuthGuest, AuthSignedIn.
    - AuthSignedIn has `final String uid`, `final String name`, `final String email`, all required-named in a const constructor.
    - AuthLoading and AuthGuest have `const` constructors.
    - `flutter analyze` on the file reports zero issues.
  </acceptance_criteria>
  <done>Sealed AuthState with loading/guest/signedIn variants exists, matches the tracking_state.dart shape, analyzes clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add backfillUserId to TripsDao and UserPreferencesDao (turns Plan 01 backfill_test GREEN)</name>
  <files>lib/database/daos/trips_dao.dart, lib/database/daos/user_preferences_dao.dart</files>
  <read_first>
    - lib/database/daos/trips_dao.dart (updateTrip lines 106-119: the explicit-WHERE `(update(trips)..where(...)).write(...)` pattern + the "Never use .replace()" doc; DAO-scope comment lines 64-69)
    - lib/database/daos/user_preferences_dao.dart (the _kUserPreferencesId scoping discipline line 11; upsert lines 142-157)
    - test/unit/features/auth/backfill_test.dart (the Plan 01 RED contract this task satisfies — assert what it expects)
    - .planning/phases/09-authentication/09-PATTERNS.md (trips_dao + user_preferences_dao MODIFY sections)
    - lib/config/constants.dart (kDefaultUserId)
  </read_first>
  <behavior>
    - After inserting trips with userId == kDefaultUserId, `tripsDao.backfillUserId('uid-123')` sets every such row's userId to 'uid-123' and returns the rewritten-row count.
    - A second `backfillUserId` call returns 0 (no 'local_user' rows remain).
    - `userPreferencesDao.backfillUserId('uid-123')` rewrites the single id=1 row's userId from kDefaultUserId to 'uid-123'.
    - Rows whose userId is already a real uid are untouched.
  </behavior>
  <action>
    In lib/database/daos/trips_dao.dart add `Future<int> backfillUserId(String newUserId)` using the same explicit-WHERE form as updateTrip: `(update(trips)..where((t) => t.userId.equals(kDefaultUserId))).write(TripsCompanion(userId: Value(newUserId)))`. `.write(...)` returns `Future<int>` (rows affected) — return it directly so AuthService can detect first sign-in (RESEARCH Pattern 4). Add a dartdoc matching updateTrip's style: reference D-11, "Never use update(trips).replace() for partial updates" (CLAUDE.md / Phase 3 STATE), and that the return value is the changed-row count. Import kDefaultUserId from constants (add the import if absent).
    In lib/database/daos/user_preferences_dao.dart add the same `Future<int> backfillUserId(String newUserId)` filtering on `userId.equals(kDefaultUserId)` (constants already imported, line 2). Doc-comment notes it is at most one row (single-row table at id=1) and returns Future<int> for symmetry, but the caller keys off the trips count for the first-sign-in signal.
    Do NOT change the table-level userId defaults — new guest-mode rows must keep defaulting to kDefaultUserId (RESEARCH Runtime State Inventory). Do NOT run codegen for new methods — backfillUserId is hand-written DAO logic, no .g.dart change.
  </action>
  <verify>
    <automated>flutter test test/unit/features/auth/backfill_test.dart && flutter analyze lib/database/daos/trips_dao.dart lib/database/daos/user_preferences_dao.dart</automated>
  </verify>
  <acceptance_criteria>
    - Both DAOs expose `Future<int> backfillUserId(String newUserId)`.
    - Both use `(update(table)..where((t) => t.userId.equals(kDefaultUserId))).write(...)` — `grep "\.replace(" lib/database/daos/trips_dao.dart lib/database/daos/user_preferences_dao.dart` finds no .replace() in the new code.
    - trips backfillUserId returns the changed-row count (Future<int>, the `.write` result returned directly).
    - `flutter test test/unit/features/auth/backfill_test.dart` is GREEN.
    - `flutter analyze` on both DAOs reports zero issues.
  </acceptance_criteria>
  <done>backfillUserId exists on both DAOs using explicit-WHERE updates, returns changed-row count, and the Plan 01 backfill_test passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth uid → local Drift rows | The Firebase uid is written across local trip/preference rows; source is trusted (Firebase profile) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | Tampering | backfill UPDATE | mitigate | Explicit `WHERE userId = kDefaultUserId` scopes the rewrite to local-only rows; never `.replace()` (could zero other columns) — Phase 3 / CLAUDE.md mandate. |
| T-09-02-02 | Repudiation | partial backfill | mitigate | Atomicity is enforced by AuthService's transaction wrapper (Plan 03); the DAO methods are standalone but documented to run inside that transaction. |
</threat_model>

<verification>
- `flutter test test/unit/features/auth/backfill_test.dart` GREEN.
- `flutter analyze lib/features/auth/models/ lib/database/daos/` zero issues.
- No `.replace(` in the new DAO code.
</verification>

<success_criteria>
- Sealed AuthState (loading/guest/signedIn) exists and matches codebase convention.
- backfillUserId on both DAOs uses explicit-WHERE, returns changed count, no .replace().
- Plan 01 backfill_test is GREEN.
</success_criteria>

<output>
After completion, create `.planning/phases/09-authentication/09-02-SUMMARY.md`
</output>
