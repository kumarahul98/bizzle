---
phase: 09-authentication
plan: 04
type: execute
wave: 4
depends_on: [09-03]
files_modified:
  - lib/app.dart
  - lib/features/auth/screens/splash_screen.dart
  - lib/features/auth/screens/sign_in_success_screen.dart
autonomous: true
requirements: [AUTH-02, AUTH-03]

must_haves:
  truths:
    - "On app boot, the loading state shows the static splash; guest and signedIn both show MainShell"
    - "The splash is a full-screen bg-token Container with a centered TraevyLogoMark, no spinner or text"
    - "The one-time confirmation screen shows the user's initial, 'You're signed in.' headline, body, and a 'Let's go' CTA that lands on MainShell"
  artifacts:
    - path: "lib/app.dart"
      provides: "auth gate switching on sealed AuthState (loading/guest/signedIn) via Dart switch"
      contains: "switch (auth)"
    - path: "lib/features/auth/screens/splash_screen.dart"
      provides: "static SplashScreen (bg Container + centered TraevyLogoMark)"
      contains: "TraevyLogoMark"
    - path: "lib/features/auth/screens/sign_in_success_screen.dart"
      provides: "one-time SignInSuccessScreen (avatar + headline + body + Let's go CTA)"
      contains: "kCopyConfirmHeadline"
  key_links:
    - from: "lib/app.dart"
      to: "authStateProvider"
      via: "ref.watch + sealed switch (NOT .when)"
      pattern: "switch \\(auth\\)"
---

<objective>
Wire the app-level auth gate and build the two new auth screens that only `app.dart` and onboarding navigation reference: the static splash (loading state) and the one-time post-sign-in confirmation screen. This plan owns `app.dart` exclusively so it runs in parallel with Plan 05.

Purpose: AUTH-02 session-restore routing (loading→splash, restored session→MainShell) and the AUTH-03 first-sign-in confirmation (D-04, D-12).
Output: modified app.dart auth gate; splash_screen.dart; sign_in_success_screen.dart.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-authentication/09-UI-SPEC.md
@.planning/phases/09-authentication/09-PATTERNS.md
@.planning/phases/09-authentication/09-CONTEXT.md

<interfaces>
<!-- app.dart current home wiring (lib/app.dart): ConsumerWidget; ref.watch(directionBackfillProvider) line 28; themeMode via userPreferenceProvider.when lines 33-39; home: const MainShell() line 47 -->

<!-- From Plan 02/03: sealed AuthState { AuthLoading, AuthGuest, AuthSignedIn(uid,name,email) }; authStateProvider (NotifierProvider<AuthStateNotifier, AuthState>) -->

<!-- CRITICAL: app.dart auth gate uses a Dart `switch` over the sealed AuthState, NOT .when (that is the AsyncValue API used by userPreferenceProvider). RESEARCH Pitfall 6. -->
final auth = ref.watch(authStateProvider);
final Widget home = switch (auth) {
  AuthLoading()  => const SplashScreen(),
  AuthGuest()    => const MainShell(),
  AuthSignedIn() => const MainShell(),
};

<!-- TraevyLogoMark (lib/shared/widgets/traevy_logo_mark.dart): const TraevyLogoMark({this.size = 56}); inverts via onSurface/scaffoldBackgroundColor -->

<!-- Token access: Theme.of(context).extension<TraevyTokensExt>()! → bg, accentBg, accent, textDim, borderStr, bgElev. TraevyFonts.ui(size:, weight:, letterSpacing:, height:, color:) -->

<!-- Onboarding-screen layout rhythm to mirror for the confirmation screen: lib/features/onboarding/screens/onboarding_screen.dart (Scaffold > SafeArea > Padding(horizontal:28,vertical:24) > Column(crossAxisStart); 36px/-1.2/1.05 headline; 16px textDim body; Spacer(); bottom CTA) -->

<!-- AccountRow avatar treatment to reuse: lib/features/settings/widgets/account_row.dart lines 43-60 (44dp accentBg circle + accent initial) -->

<!-- GoogleContinueButton neutral shell to mirror (minus glyph) for 'Let's go': lib/features/onboarding/widgets/google_continue_button.dart (bgElev fill, borderStr border, 14px radius, horizontal:18 vertical:16) -->

<!-- From Plan 01: kCopyConfirmHeadline, kCopyConfirmBody, kCopyConfirmCta -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the static splash screen (D-04, UI-SPEC §A)</name>
  <files>lib/features/auth/screens/splash_screen.dart</files>
  <read_first>
    - lib/shared/widgets/traevy_logo_mark.dart (the widget it hosts; default size 56; light/dark inversion)
    - lib/features/onboarding/screens/onboarding_screen.dart (token read pattern line 21: `Theme.of(context).extension<TraevyTokensExt>()!`)
    - .planning/phases/09-authentication/09-UI-SPEC.md (§A Static Splash — full-screen bg Container, centered logo, no spinner/text, respects light/dark)
    - .planning/phases/09-authentication/09-PATTERNS.md (splash_screen.dart section)
  </read_first>
  <action>
    Create lib/features/auth/screens/splash_screen.dart with `class SplashScreen extends StatelessWidget` (const constructor). Build a full-screen `Container` (`width/height: double.infinity` or a `ColoredBox`) filled with `Theme.of(context).extension<TraevyTokensExt>()!.bg`, child a `Center(child: TraevyLogoMark())` (default size 56). NO AppBar, NO Scaffold chrome required, NO CircularProgressIndicator, NO text — it is a sub-second flash-frame during Firebase session restore. Keep the widget under 100 lines (trivially is). Dartdoc: reference D-04 and that it is rendered inline from app.dart's auth gate, not a named route.
  </action>
  <verify>
    <automated>flutter analyze lib/features/auth/screens/splash_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `class SplashScreen extends StatelessWidget` with a const constructor.
    - Fills with `TraevyTokensExt.bg` and centers a `TraevyLogoMark`.
    - `grep -E "CircularProgressIndicator|AppBar|Text\(" lib/features/auth/screens/splash_screen.dart` finds none.
    - `flutter analyze` on the file reports zero issues.
  </acceptance_criteria>
  <done>SplashScreen is a static bg-token Container with a centered TraevyLogoMark, no spinner/text/appbar, analyzes clean.</done>
</task>

<task type="auto">
  <name>Task 2: Build the one-time confirmation screen (D-12, UI-SPEC §C)</name>
  <files>lib/features/auth/screens/sign_in_success_screen.dart</files>
  <read_first>
    - lib/features/onboarding/screens/onboarding_screen.dart (entire file — copy the scaffold rhythm: Scaffold > SafeArea > Padding(horizontal:28) > Column; 36px/w700/-1.2/1.05 headline; 16px textDim body; Spacer(); bottom CTA)
    - lib/features/settings/widgets/account_row.dart (lines 43-60: 44dp accentBg avatar circle + accent initial — reuse the treatment)
    - lib/features/onboarding/widgets/google_continue_button.dart (the neutral button shell to mirror for 'Let's go', minus the Google glyph)
    - .planning/phases/09-authentication/09-UI-SPEC.md (§C Post-Sign-In Confirmation Screen — avatar 44-56dp, 48px gap, headline, 12px gap, body, Spacer, 'Let's go' neutral CTA → MainShell)
    - .planning/phases/09-authentication/09-PATTERNS.md (sign_in_success_screen.dart section)
    - lib/config/constants.dart (kCopyConfirmHeadline, kCopyConfirmBody, kCopyConfirmCta)
  </read_first>
  <action>
    Create lib/features/auth/screens/sign_in_success_screen.dart with `class SignInSuccessScreen extends StatelessWidget`. Constructor takes the signed-in user's display `initial` (String) — passed by the onboarding handler (Plan 05) which has the AuthSignedIn payload — and optionally `name`. Layout (UI-SPEC §C): `Scaffold` with `bg` background, `SafeArea`, `Padding(horizontal: 28)`, `Column(crossAxisAlignment: start)` containing: a 44dp (or 56 hero) `accentBg` circle with the `accent`-colored `initial` letter (reuse AccountRow's avatar treatment), 48px gap, `kCopyConfirmHeadline` in `TraevyFonts.ui(size: 36, weight: w700, letterSpacing: -1.2, height: 1.05, color: onSurface)`, 12px gap, `kCopyConfirmBody` in `TraevyFonts.ui(size: 16, color: tokens.textDim, height: 1.5)`, `Spacer()`, then a neutral "Let's go" CTA mirroring GoogleContinueButton's shell (bgElev fill, borderStr border, 14px radius, horizontal:18 vertical:16, label `kCopyConfirmCta` Inter 14px w600) WITHOUT the Google glyph. The CTA's onTap navigates to MainShell — use `Navigator.of(context).pushReplacement(MaterialPageRoute(builder: (_) => const MainShell()))` (replacement so back-button does not return to the one-time screen). Extract the avatar circle or CTA into a private sub-widget if the build method approaches 100 lines (CLAUDE.md). Use named copy constants, never inline string literals.
  </action>
  <verify>
    <automated>flutter analyze lib/features/auth/screens/sign_in_success_screen.dart</automated>
  </verify>
  <acceptance_criteria>
    - `class SignInSuccessScreen extends StatelessWidget` with a constructor accepting the user `initial`.
    - Uses `kCopyConfirmHeadline`, `kCopyConfirmBody`, `kCopyConfirmCta` (no inline literals — `grep "You're signed in" lib/features/auth/screens/sign_in_success_screen.dart` finds nothing).
    - Avatar uses `accentBg` fill + `accent` initial (AccountRow treatment).
    - 'Let's go' CTA navigates via `pushReplacement` to MainShell.
    - Build method (or any sub-widget) under ~100 lines; `flutter analyze` zero issues.
  </acceptance_criteria>
  <done>SignInSuccessScreen renders the avatar + confirmation copy + neutral 'Let's go' CTA that replaces the route with MainShell; uses copy constants; analyzes clean.</done>
</task>

<task type="auto">
  <name>Task 3: Wire the app.dart auth gate (sealed switch, NOT .when)</name>
  <files>lib/app.dart</files>
  <read_first>
    - lib/app.dart (entire file — the existing ConsumerWidget, directionBackfillProvider watch line 28, userPreferenceProvider.when themeMode block lines 33-39, home: const MainShell() line 47)
    - lib/features/auth/providers/auth_providers.dart (authStateProvider from Plan 03)
    - lib/features/auth/models/auth_state.dart (the sealed AuthState variants)
    - lib/features/auth/screens/splash_screen.dart (SplashScreen from Task 1)
    - .planning/phases/09-authentication/09-PATTERNS.md (app.dart MODIFY section — sealed switch, Pitfall 6/A5: do NOT use .when)
    - .planning/phases/09-authentication/09-UI-SPEC.md (Interaction & State Contract table)
  </read_first>
  <action>
    In lib/app.dart, add `final auth = ref.watch(authStateProvider);` inside `build`. Compute `final Widget home = switch (auth) { AuthLoading() => const SplashScreen(), AuthGuest() => const MainShell(), AuthSignedIn() => const MainShell(), };` — a Dart sealed `switch` with NO default branch (RESEARCH Pitfall 6 / A5: AuthState is a sealed value from a NotifierProvider, NOT an AsyncValue — `.when` would not apply). Replace `home: const MainShell()` at line 47 with `home: home`. KEEP the existing `ref.watch(directionBackfillProvider)` line and the `userPreferenceProvider.when(...)` themeMode block unchanged. Add imports for auth_providers, auth_state, and splash_screen. Update the class dartdoc to note the Phase 9 auth gate.
  </action>
  <verify>
    <automated>flutter analyze lib/app.dart && flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `grep "switch (auth)" lib/app.dart` matches; the switch has three arms (AuthLoading/AuthGuest/AuthSignedIn) and no `default`.
    - `home: home` replaces `home: const MainShell()`; `directionBackfillProvider` watch and themeMode block remain.
    - `grep "authStateProvider" lib/app.dart` matches; no `.when(` applied to authStateProvider.
    - `flutter analyze lib/app.dart` zero issues; `flutter test` full suite GREEN.
  </acceptance_criteria>
  <done>app.dart watches authStateProvider and routes loading→SplashScreen, guest/signedIn→MainShell via an exhaustive sealed switch; existing backfill and theme wiring intact; full suite passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth state → UI routing | AuthState drives MaterialApp.home; state flows down (UDF) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-04-01 | Information Disclosure | confirmation screen | mitigate | Renders only displayName initial + fixed copy; no token/email-in-logs; uses trusted Firebase profile data (V5 null-coalesce). |
| T-09-04-02 | Denial of Service (self) | unhandled auth variant | mitigate | Exhaustive sealed switch with no default — a new AuthState variant is a compile error, never a silent crash/blank screen. |
</threat_model>

<verification>
- `flutter analyze lib/app.dart lib/features/auth/screens/` zero issues.
- `flutter test` full suite GREEN.
- app.dart uses a sealed switch, not .when, on authStateProvider.
</verification>

<success_criteria>
- Auth gate routes loading→splash, guest/signedIn→MainShell.
- Splash is a static bg-token Container + centered logo.
- Confirmation screen shows avatar + copy + 'Let's go' → MainShell.
</success_criteria>

<output>
After completion, create `.planning/phases/09-authentication/09-04-SUMMARY.md`
</output>
