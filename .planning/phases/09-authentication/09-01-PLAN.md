---
phase: 09-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/config/constants.dart
  - lib/config/routes.dart
  - test/unit/features/auth/google_sign_in_api_probe_test.dart
  - test/unit/features/auth/auth_state_notifier_test.dart
  - test/unit/features/auth/auth_service_test.dart
  - test/unit/features/auth/backfill_test.dart
autonomous: true
requirements: [AUTH-01, AUTH-02, AUTH-03, BACK-01]
user_setup:
  - service: firebase
    why: "Firebase Auth (Google provider) backs AUTH-01/BACK-01; flutterfire configure generates lib/firebase_options.dart and google-services.json"
    env_vars: []
    dashboard_config:
      - task: "Create a Firebase project and enable the Google sign-in provider (Authentication → Sign-in method → Google)"
        location: "Firebase Console"
      - task: "Register the debug keystore SHA-1 AND SHA-256 fingerprints on the Android app, then re-download google-services.json into android/app/ (release currently signs with the debug keystore — see RESEARCH Pitfall 3)"
        location: "Firebase Console → Project settings → Your apps → Android"
      - task: "Run `flutterfire configure` to generate lib/firebase_options.dart and copy the Web client ID (APIs & Services → Credentials → 'Web client (auto created by Google Service)') into kGoogleServerClientId"
        location: "Local machine + GCP Credentials"

must_haves:
  truths:
    - "FlutterFire trio + flutter_secure_storage resolve to a mutually-compatible installed set"
    - "All Phase 9 string/key/route constants exist as named constants (no hardcoded literals downstream)"
    - "Wave 0 RED test files exist and define the AUTH-01/02/03 behaviour contracts the implementation waves must satisfy"
    - "The installed google_sign_in 7.x authenticate()/idToken API surface is pinned by a probe test before any AuthService code is written"
  artifacts:
    - path: "pubspec.yaml"
      provides: "firebase_core, firebase_auth, google_sign_in, flutter_secure_storage dependencies"
      contains: "firebase_auth"
    - path: "lib/config/constants.dart"
      provides: "kGoogleServerClientId, kFirebaseIdTokenKey, kDisabledSignInOpacity, Phase 9 copy constants"
      contains: "kFirebaseIdTokenKey"
    - path: "lib/config/routes.dart"
      provides: "kRouteSignInSuccess route name + kAppRoutes entry"
      contains: "kRouteSignInSuccess"
    - path: "test/unit/features/auth/auth_service_test.dart"
      provides: "RED contract for sign-in sequence + token cache + backfill signal"
    - path: "test/unit/features/auth/auth_state_notifier_test.dart"
      provides: "RED contract for authStateChanges mapping + degrade-to-guest"
  key_links:
    - from: "lib/config/constants.dart"
      to: "GoogleSignIn.instance.initialize"
      via: "kGoogleServerClientId passed as serverClientId in main.dart (Plan 03)"
      pattern: "kGoogleServerClientId"
---

<objective>
Lay the dependency, constant, route, and test-contract foundation for Phase 9 authentication. This is the Wave 0 plan: it pins the FlutterFire package set, adds every Phase 9 named constant and route, writes the RED test files that define the AUTH-01/02/03 behaviour contracts, and pins the google_sign_in 7.x API surface with a compile-probe test BEFORE any implementation code is written.

Purpose: Implementation waves (02-05) must build against fixed contracts (constants, routes, test expectations, verified v7 API) rather than discovering them. RESEARCH Open Question A2 / Pitfall 1 make the google_sign_in v7 API the single biggest technical risk — this plan resolves it first.
Output: Updated pubspec.yaml, constants.dart, routes.dart; four new test files under test/unit/features/auth/.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-authentication/09-CONTEXT.md
@.planning/phases/09-authentication/09-RESEARCH.md
@.planning/phases/09-authentication/09-PATTERNS.md
@.planning/phases/09-authentication/09-VALIDATION.md

<interfaces>
<!-- Existing constants block (constants.dart) the new constants live beside. Extracted — do not re-explore. -->
const String kDefaultUserId = 'local_user';        // line 24
const String kPlaceholderUserName = 'Traveller';   // line 593
const String kPlaceholderUserInitial = 'T';        // line 599
const String kBrandShortName = 'tv';               // line 607

<!-- Existing routes (routes.dart) — kRouteSignInSuccess is added the same way. -->
const String kRouteOnboarding = '/onboarding';     // line 29
final Map<String, WidgetBuilder> kAppRoutes = { ... kRouteOnboarding: (ctx) => const OnboardingScreen(), };  // line 36-45

<!-- Existing FlutterFire/Riverpod versions (pubspec.yaml). -->
flutter_riverpod: ^3.3.1   // line 43

<!-- Wave 0 test analogs -->
<!-- In-memory Drift: test/unit/database/trips_dao_test.dart lines 13-20 -->
<!-- Hand-rolled fake DAO/service via implements+noSuchMethod: test/widget/features/settings/settings_screen_test.dart lines 30-84 -->
<!-- Sealed-state map test: test/unit/features/tracking/tracking_state_map_test.dart -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add FlutterFire dependency set + Phase 9 constants + sign-in-success route</name>
  <files>pubspec.yaml, lib/config/constants.dart, lib/config/routes.dart</files>
  <read_first>
    - pubspec.yaml (the dependency block to extend; flutter_riverpod ^3.3.1 at line 43)
    - lib/config/constants.dart (analog block: kDefaultUserId line 24, kPlaceholderUserName line 593, kPlaceholderUserInitial line 599, kBrandShortName line 607)
    - lib/config/routes.dart (analog: kRouteOnboarding declaration line 29 + kAppRoutes entry line 40)
    - .planning/phases/09-authentication/09-RESEARCH.md (Standard Stack table + Installation block — pinned versions)
    - .planning/phases/09-authentication/09-UI-SPEC.md (Copywriting Contract table — exact copy strings)
  </read_first>
  <action>
    In pubspec.yaml add to dependencies: `firebase_core: ^4.9.0`, `firebase_auth: ^6.5.1`, `google_sign_in: ^7.2.0`, `flutter_secure_storage: ^9.2.4`. Run `flutter pub add firebase_core firebase_auth google_sign_in flutter_secure_storage` to let the resolver pick a mutually-compatible set, then verify with `flutter pub outdated`; pin whatever resolves and commit pubspec.lock. NEVER bump one FlutterFire plugin in isolation (RESEARCH Pitfall 4). Do NOT add cloud_firestore (CLAUDE.md / RESEARCH anti-pattern — forbidden in client).
    In lib/config/constants.dart add, beside the existing kDefaultUserId/kPlaceholderUserName block: `kGoogleServerClientId` (String — the Web OAuth client ID from the Firebase project; public client identifier, safe in source; leave a documented placeholder value to be filled after `flutterfire configure`), `kFirebaseIdTokenKey` (String, secure-storage key e.g. 'firebase_id_token'), `kDisabledSignInOpacity` (double = 0.38, per UI-SPEC D-15 note), and the UI-SPEC copy constants as String consts: `kCopySignInSheetHeadline = 'Back up your commutes'`, `kCopySignInSheetSubtext = 'Your trips sync automatically when you sign in.'`, `kCopySettingsGuestSignIn = 'Sign in to back up'`, `kCopyConfirmHeadline = "You're signed in."`, `kCopyConfirmBody = 'Your commutes will back up automatically.'`, `kCopyConfirmCta = "Let's go"`, `kCopySignInDisabledTooltip = 'Sign-in not configured'`, `kCopySignInFailedHeadline = "Couldn't sign in."`, `kCopySignInFailedBody = 'Check your connection and try again.'`. Match the existing dartdoc-comment-per-constant style.
    In lib/config/routes.dart add `const String kRouteSignInSuccess = '/sign-in-success';` next to kRouteOnboarding, with a dartdoc explaining it is the one-time confirmation screen (D-12). Do NOT add it to kAppRoutes if it will be pushed via MaterialPageRoute by the onboarding handler; add the comment documenting the decision. (Plan 04 owns the screen; this plan only reserves the name.)
  </action>
  <verify>
    <automated>flutter pub get && flutter analyze lib/config/constants.dart lib/config/routes.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "firebase_auth" pubspec.yaml` returns >= 1 and `flutter pub get` exits 0.
    - `grep -E "kGoogleServerClientId|kFirebaseIdTokenKey|kDisabledSignInOpacity|kCopySignInSheetHeadline|kCopyConfirmHeadline" lib/config/constants.dart` matches all five.
    - `grep "kRouteSignInSuccess" lib/config/routes.dart` matches.
    - `grep -c "cloud_firestore" pubspec.yaml` returns 0.
    - `flutter analyze lib/config/` reports zero issues.
  </acceptance_criteria>
  <done>FlutterFire trio + flutter_secure_storage installed and resolved; all Phase 9 constants and the kRouteSignInSuccess route name exist; analyze is clean on config files.</done>
</task>

<task type="auto">
  <name>Task 2: Probe-test the installed google_sign_in 7.x API surface (RED, Open Question A2)</name>
  <files>test/unit/features/auth/google_sign_in_api_probe_test.dart</files>
  <read_first>
    - test/unit/features/tracking/tracking_state_map_test.dart (test file shape: imports, group/test, expect)
    - .planning/phases/09-authentication/09-RESEARCH.md (Code Examples §1 + Assumptions A2 + Open Question 1 — the exact uncertainty)
    - The installed package API: read `~/.pub-cache/.../google_sign_in*/lib/google_sign_in.dart` and the package `example/` after `flutter pub get` to confirm the real symbol names.
  </read_first>
  <action>
    Create test/unit/features/auth/google_sign_in_api_probe_test.dart. This is a COMPILE-PROBE test that pins the v7 API the implementation will use, resolving RESEARCH A2 before AuthService is written. Reference (do not call at runtime — these hit platform channels) the v7 symbols so a wrong symbol name is a compile error: `GoogleSignIn.instance`, `GoogleSignIn.instance.initialize`, `GoogleSignIn.instance.authenticate`, `GoogleSignIn.instance.supportsAuthenticate`, and the authentication-result type exposing `idToken`. Write a test that asserts on TYPES / function tear-offs rather than invoking them: e.g. assign the method tear-offs to typed local variables and `expect(tearOff, isNotNull)`. Also assert `GoogleAuthProvider.credential` accepts a named `idToken:` parameter (reference the tear-off / construct a credential with `idToken: 'x'` — credential construction is pure, no channel). Document at the top of the file exactly which property path yields the idToken (`.authentication.idToken` vs `.idToken`) as discovered from the installed package, so Plan 03 uses the verified path. The test is expected to PASS once symbols are correct — it is the contract pin, not a behaviour RED.
  </action>
  <verify>
    <automated>flutter test test/unit/features/auth/google_sign_in_api_probe_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - File references `GoogleSignIn.instance` and `authenticate` and `idToken` and compiles.
    - Top-of-file comment records the verified idToken property path against the installed package version.
    - `flutter test test/unit/features/auth/google_sign_in_api_probe_test.dart` exits 0 (symbols resolve).
    - No call to `GoogleSignIn().signIn()` or `.authentication.accessToken` anywhere (v6 API — RESEARCH Pitfall 1).
  </acceptance_criteria>
  <done>The installed google_sign_in 7.x API surface used by AUTH-01 is pinned by a compiling probe test; the idToken access path is documented for Plan 03.</done>
</task>

<task type="auto">
  <name>Task 3: Write RED behaviour test contracts for AuthService, AuthStateNotifier, and backfill</name>
  <files>test/unit/features/auth/auth_state_notifier_test.dart, test/unit/features/auth/auth_service_test.dart, test/unit/features/auth/backfill_test.dart, .planning/phases/09-authentication/09-VALIDATION.md</files>
  <read_first>
    - test/widget/features/settings/settings_screen_test.dart (lines 30-84: hand-rolled fake via `implements ... noSuchMethod`, capture-calls list)
    - test/unit/database/trips_dao_test.dart (lines 8-55: in-memory `AppDatabase(DatabaseConnection(NativeDatabase.memory(), closeStreamsSynchronously: true))`, insert→assert)
    - test/unit/features/tracking/tracking_state_map_test.dart (sealed-state assertion style)
    - .planning/phases/09-authentication/09-RESEARCH.md (Validation Architecture → Phase Requirements → Test Map; Code Examples §2/§3)
    - .planning/phases/09-authentication/09-PATTERNS.md (Test Pattern Assignments table)
  </read_first>
  <action>
    Create three RED test files declaring the contracts the implementation waves satisfy. These reference symbols that do not exist yet (AuthService, AuthStateNotifier, AuthState subtypes, backfillUserId) so they are expected to FAIL TO COMPILE until Waves 1-2 land — that is the intended RED state; document this in each file header and in the SUMMARY. Mark them so the Wave 0 gate records them as pending.
    1. test/unit/features/auth/auth_state_notifier_test.dart — contract: a fake `Stream<User?>`-style source maps null → `AuthGuest`, a non-null user → `AuthSignedIn(uid,name,email)` with name falling back to kPlaceholderUserName when displayName is null; and when `firebaseReady == false` the notifier emits `AuthGuest` without subscribing (D-15 degrade). Use a ProviderContainer with overrides. (AUTH-01, AUTH-02.)
    2. test/unit/features/auth/auth_service_test.dart — contract: `AuthService.signIn()` writes the Firebase ID token to a fake FlutterSecureStorage under `kFirebaseIdTokenKey`, calls `backfillUserId(uid)` on both DAOs, and returns `true` when the trips backfill changed > 0 rows / `false` when 0 (D-10, D-11, D-12, RESEARCH Pattern 4). Use hand-rolled fakes (implements + noSuchMethod) for FirebaseAuth, GoogleSignIn, secure storage, and the DAOs — mirror `_FakeUserPreferencesDao`. Assert the token is NEVER passed to any logging sink (no print/log capture contains it). (AUTH-01, AUTH-02.)
    3. test/unit/features/auth/backfill_test.dart — contract: against an in-memory AppDatabase, insert trips + a user_preferences row with userId = kDefaultUserId, call `tripsDao.backfillUserId('uid-123')` and `userPreferencesDao.backfillUserId('uid-123')`, assert all rows now carry 'uid-123' and that trips.backfillUserId returns the changed-row count; a second call returns 0 (no remaining local_user rows). (AUTH-03.)
    Populate the Per-Task Verification Map row(s) in 09-VALIDATION.md for these test IDs as pending.
  </action>
  <verify>
    <automated>flutter test test/unit/features/auth/ 2>&1 | grep -E "Compilation|Error|failed|backfillUserId|AuthService" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - Three files exist: auth_state_notifier_test.dart, auth_service_test.dart, backfill_test.dart.
    - Each references the not-yet-existing symbol it pins (AuthStateNotifier, AuthService, backfillUserId) — confirmed by grep.
    - backfill_test.dart uses `NativeDatabase.memory()` (in-memory Drift, no platform channels).
    - auth_service_test.dart uses hand-rolled `implements ... noSuchMethod` fakes (no real FirebaseAuth/GoogleSignIn instantiation).
    - The run is RED (compile failure on missing symbols) — documented as the intended Wave 0 state in each file header.
  </acceptance_criteria>
  <done>RED contracts for AUTH-01/02/03 exist and define exact expected behaviours; backfill test uses in-memory Drift; auth tests use injectable fakes; 09-VALIDATION.md verification map updated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → app | kGoogleServerClientId / firebase_options identify the Firebase project; public client identifiers, not secrets |
| external package set → app | FlutterFire/google_sign_in versions control the native auth surface |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | Information Disclosure | constants.dart secrets | accept | kGoogleServerClientId + Firebase API key are public client identifiers (RESEARCH Runtime State Inventory); no real secret enters source. No `.env` needed. |
| T-09-01-02 | Tampering | dependency set | mitigate | Pin the verified FlutterFire trio + commit pubspec.lock; never bump one plugin in isolation (Pitfall 4). |
| T-09-01-03 | Tampering | cloud_firestore client leak | mitigate | Assert `grep -c cloud_firestore pubspec.yaml == 0` in Task 1 acceptance (CLAUDE.md / PDF mandate). |
</threat_model>

<verification>
- `flutter pub get` resolves the FlutterFire set with no version-solving failure.
- `flutter analyze lib/config/` zero issues.
- `flutter test test/unit/features/auth/google_sign_in_api_probe_test.dart` passes (API pinned).
- The three behaviour test files are RED (compile-failing on missing symbols) — the documented Wave 0 state.
</verification>

<success_criteria>
- FlutterFire trio + flutter_secure_storage installed and mutually compatible; no cloud_firestore.
- All Phase 9 constants and kRouteSignInSuccess exist.
- google_sign_in 7.x API surface pinned by a passing probe test with the idToken path documented.
- RED contracts for AUTH-01/02/03 written and recorded in 09-VALIDATION.md.
</success_criteria>

<output>
After completion, create `.planning/phases/09-authentication/09-01-SUMMARY.md`
</output>
