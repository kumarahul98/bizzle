---
phase: 07-polish-notifications
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - test/widget/features/settings/settings_screen_test.dart
autonomous: true
requirements:
  - UX-02

must_haves:
  truths:
    - "Wave 0 test scaffold exists and compiles (even before SettingsScreen is created)"
    - "Test file declares all groups required by VALIDATION.md"
    - "Automated verify command runs without MissingPluginException"
  artifacts:
    - path: "test/widget/features/settings/settings_screen_test.dart"
      provides: "Widget test scaffold covering UX-02 acceptance criteria"
      contains: "settings_screen_test"
  key_links:
    - from: "test/widget/features/settings/settings_screen_test.dart"
      to: "lib/features/settings/screens/settings_screen.dart (plan 04)"
      via: "import and SettingsScreen instantiation in _pumpSettingsScreen helper"
      pattern: "SettingsScreen"
---

<objective>
Create the Wave 0 test scaffold for the Settings screen as required by VALIDATION.md.

Purpose: The test file must be written before SettingsScreen is implemented so RED tests guide
implementation in plan 04. Tests compile with stub data and fail until the real widget ships.

Output: test/widget/features/settings/settings_screen_test.dart with all test groups declared.
Note: This file imports SettingsScreen which does not exist until plan 04. The file will fail
to compile until plan 04 runs — that is correct Wave 0 behavior (RED phase).
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/.planning/ROADMAP.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/07-polish-notifications/07-CONTEXT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/07-polish-notifications/07-UI-SPEC.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/07-polish-notifications/07-VALIDATION.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/07-polish-notifications/07-PATTERNS.md

<interfaces>
<!-- Analog to use as the template for this test file. -->

From test/widget/features/dashboard/dashboard_screen_test.dart (exact pattern to follow):
```dart
// Pump helper pattern:
Future<void> _pumpDashboardScreen(WidgetTester tester, {...}) async {
  await tester.pumpWidget(
    ProviderScope(
      overrides: [...],
      child: MaterialApp(home: const DashboardScreen(), routes: kAppRoutes),
    ),
  );
  await tester.pump();
}

// Test group pattern:
void main() {
  group('DashboardScreen', () {
    testWidgets('renders ...', (tester) async { ... });
  });
}
```

UserPreferencesValue will have these fields after plan 01 runs (plan 02 can reference this):
```dart
class UserPreferencesValue {
  const UserPreferencesValue({
    required this.userId,
    required this.darkMode,
    required this.morningCutoffHour,
    required this.eveningCutoffHour,
    required this.reminderEnabled,
    required this.reminderTime,
    required this.weekendReminder,
    required this.weeklyNotificationEnabled,  // added by plan 01 task 2
  });
  const UserPreferencesValue.defaults(); // weeklyNotificationEnabled = false
}
```

userPreferenceProvider (declared in plan 03 in settings_providers.dart):
```dart
final StreamProvider<UserPreferencesValue> userPreferenceProvider = ...;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write settings_screen_test.dart Wave 0 scaffold</name>
  <files>test/widget/features/settings/settings_screen_test.dart</files>
  <read_first>
    - test/widget/features/dashboard/dashboard_screen_test.dart (read entire file — copy pump helper pattern, group structure, ProviderScope override pattern)
    - test/widget/features/stats/stats_screen_test.dart (skim for any additional patterns)
    - lib/features/settings/providers/settings_providers.dart (will not exist yet — that is expected; use the import path that plan 03 will create)
  </read_first>
  <action>
Create the directory `test/widget/features/settings/` if it does not exist.

Write `test/widget/features/settings/settings_screen_test.dart` with the following structure:

```dart
import 'package:flutter/material.dart';
import 'package:flutter_riverpod/flutter_riverpod.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:traevy/config/constants.dart';
import 'package:traevy/config/routes.dart';
import 'package:traevy/database/daos/user_preferences_dao.dart';
import 'package:traevy/features/dashboard/screens/dashboard_screen.dart';
import 'package:traevy/features/settings/providers/settings_providers.dart';
import 'package:traevy/features/settings/screens/settings_screen.dart';

// ---------------------------------------------------------------------------
// Helpers
// ---------------------------------------------------------------------------

/// Pump a [SettingsScreen] with [prefs] as the Riverpod override.
///
/// Mirrors the dashboard_screen_test pump helper pattern (PATTERNS.md).
Future<void> _pumpSettingsScreen(
  WidgetTester tester, {
  UserPreferencesValue prefs = const UserPreferencesValue.defaults(),
}) async {
  await tester.pumpWidget(
    ProviderScope(
      overrides: <Override>[
        userPreferenceProvider.overrideWith(
          (ref) => Stream<UserPreferencesValue>.value(prefs),
        ),
      ],
      child: const MaterialApp(
        home: SettingsScreen(),
      ),
    ),
  );
  await tester.pump();
}

/// Pump a [DashboardScreen] with routes wired so gear icon navigation works.
Future<void> _pumpDashboardWithRoutes(WidgetTester tester) async {
  await tester.pumpWidget(
    ProviderScope(
      overrides: <Override>[
        userPreferenceProvider.overrideWith(
          (ref) => Stream<UserPreferencesValue>.value(
            const UserPreferencesValue.defaults(),
          ),
        ),
      ],
      child: MaterialApp(
        home: const DashboardScreen(),
        routes: kAppRoutes,
      ),
    ),
  );
  await tester.pump();
}

// ---------------------------------------------------------------------------
// Tests
// ---------------------------------------------------------------------------

void main() {
  group('SettingsScreen', () {
    testWidgets('renders without error', (WidgetTester tester) async {
      await _pumpSettingsScreen(tester);
      expect(find.byType(SettingsScreen), findsOneWidget);
    });

    testWidgets(
      'renders exactly 3 RadioListTile rows in Appearance section',
      (WidgetTester tester) async {
        await _pumpSettingsScreen(tester);
        expect(find.byType(RadioListTile<String>), findsNWidgets(3));
      },
    );

    testWidgets(
      'renders System default, Light, and Dark radio labels',
      (WidgetTester tester) async {
        await _pumpSettingsScreen(tester);
        expect(find.text(kSettingsDarkModeSystemLabel), findsOneWidget);
        expect(find.text(kSettingsDarkModeLightLabel), findsOneWidget);
        expect(find.text(kSettingsDarkModeDarkLabel), findsOneWidget);
      },
    );

    testWidgets(
      'renders Weekly summary SwitchListTile in Notifications section',
      (WidgetTester tester) async {
        await _pumpSettingsScreen(tester);
        expect(find.text(kSettingsWeeklySummaryLabel), findsOneWidget);
      },
    );

    testWidgets(
      'renders Daily reminder SwitchListTile in Notifications section',
      (WidgetTester tester) async {
        await _pumpSettingsScreen(tester);
        expect(find.text(kSettingsReminderLabel), findsOneWidget);
      },
    );

    testWidgets(
      'hides reminder time row when reminderEnabled is false',
      (WidgetTester tester) async {
        await _pumpSettingsScreen(
          tester,
          prefs: const UserPreferencesValue(
            userId: 'test',
            darkMode: kDarkModeSystem,
            morningCutoffHour: 12,
            eveningCutoffHour: 12,
            reminderEnabled: false,
            reminderTime: null,
            weekendReminder: false,
            weeklyNotificationEnabled: false,
          ),
        );
        // Reminder time row should be absent or opacity 0.38 when disabled.
        // Verify the Reminder time label exists but the tile is disabled.
        // (AnimatedOpacity keeps it in the tree — we check enabled state.)
        final reminderTimeFinder = find.text(kSettingsReminderTimeLabel);
        if (reminderTimeFinder.evaluate().isNotEmpty) {
          // If present, the enclosing ListTile must be disabled
          final listTile = tester.widget<ListTile>(
            find.ancestor(
              of: reminderTimeFinder,
              matching: find.byType(ListTile),
            ),
          );
          expect(listTile.enabled, isFalse);
        }
      },
    );

    testWidgets(
      'shows reminder time row as enabled when reminderEnabled is true',
      (WidgetTester tester) async {
        await _pumpSettingsScreen(
          tester,
          prefs: const UserPreferencesValue(
            userId: 'test',
            darkMode: kDarkModeSystem,
            morningCutoffHour: 12,
            eveningCutoffHour: 12,
            reminderEnabled: true,
            reminderTime: '08:00',
            weekendReminder: false,
            weeklyNotificationEnabled: false,
          ),
        );
        expect(find.text(kSettingsReminderTimeLabel), findsOneWidget);
      },
    );

    testWidgets(
      'renders AppBar with Settings title',
      (WidgetTester tester) async {
        await _pumpSettingsScreen(tester);
        expect(find.text(kSettingsAppBarTitle), findsOneWidget);
      },
    );
  });

  group('DashboardScreen gear icon navigation', () {
    testWidgets(
      'gear icon appears as 4th trailing AppBar action',
      (WidgetTester tester) async {
        await _pumpDashboardWithRoutes(tester);
        // Gear icon is identified by tooltip kSettingsTooltip.
        expect(
          find.byTooltip(kSettingsTooltip),
          findsOneWidget,
        );
      },
    );

    testWidgets(
      'tapping gear icon navigates to SettingsScreen',
      (WidgetTester tester) async {
        await _pumpDashboardWithRoutes(tester);
        await tester.tap(find.byTooltip(kSettingsTooltip));
        await tester.pumpAndSettle();
        expect(find.byType(SettingsScreen), findsOneWidget);
      },
    );
  });
}
```

IMPORTANT NOTES for the executor:
- This file WILL fail to compile until plan 01 (constants), plan 03 (settings_providers.dart),
  and plan 04 (settings_screen.dart) complete. That is expected Wave 0 RED state.
- The DashboardScreen navigation test requires the gear icon to exist in DashboardScreen (plan 04
  adds it). The test will fail until then.
- Run `flutter test test/widget/features/settings/` only after plan 04 completes.
- The `UserPreferencesValue` constructor in tests includes `weeklyNotificationEnabled` which
  is added by plan 01 task 2. If plan 01 has not run yet, the tests fail to compile — that is correct.
  </action>
  <verify>
    <automated>ls /Users/coolman/Documents/Projects/bizzle/test/widget/features/settings/settings_screen_test.dart</automated>
  </verify>
  <done>
    test/widget/features/settings/settings_screen_test.dart exists.
    File contains group('SettingsScreen') and group('DashboardScreen gear icon navigation').
    File contains _pumpSettingsScreen and _pumpDashboardWithRoutes helpers.
    File references kSettingsDarkModeSystemLabel, kSettingsDarkModeLightLabel, kSettingsDarkModeDarkLabel.
    File references kSettingsTooltip for gear icon detection.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test overrides → provider | ProviderScope.overrides inject fake stream; ensures tests are isolated from real DB |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | Tampering | test ProviderScope overrides | accept | Tests use Stream.value() as a controlled fake; no real DB writes occur in widget tests; isolation is the point |
| T-07-02-02 | Denial of Service | test file compile failure (Wave 0) | accept | Intentional — Wave 0 tests are RED until plan 04 ships SettingsScreen; CI will fail in the interim; plan 04 must run immediately after plan 03 |
</threat_model>

<verification>
After this plan:
1. `ls test/widget/features/settings/settings_screen_test.dart` — file exists
2. File has `group('SettingsScreen', ...)` with 7+ test cases
3. File has `group('DashboardScreen gear icon navigation', ...)` with 2 test cases
4. After plan 04 runs: `flutter test test/widget/features/settings/` passes green
</verification>

<success_criteria>
- test/widget/features/settings/settings_screen_test.dart exists
- Contains pump helpers _pumpSettingsScreen and _pumpDashboardWithRoutes
- All VALIDATION.md Wave 0 gaps covered: RadioListTile rows, gear icon navigation, reminder visibility
- File will pass GREEN after plans 01, 03, 04 complete
</success_criteria>

<output>
After completion, create `.planning/phases/07-polish-notifications/07-02-SUMMARY.md`
</output>
