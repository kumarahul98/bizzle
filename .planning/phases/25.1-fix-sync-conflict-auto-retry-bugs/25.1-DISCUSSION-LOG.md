# Phase 25.1: Fix Sync Conflict & Auto-Retry Bugs - Discussion Log

> **Audit trail only.** Do not use as input to planning, research, or execution agents.
> Decisions are captured in CONTEXT.md — this log preserves the alternatives considered.

**Date:** 2026-07-11
**Phase:** 25.1-fix-sync-conflict-auto-retry-bugs
**Areas discussed:** Phase scope & reframing, Auto-retry gate semantics, Merge UX default, Regression test design

---

## Pre-discussion discovery

Codebase scout found the roadmap's premise stale: both Phase 24 defects were already
fixed in the v0.3 merge to main (PR #2, commit `54f70eb`, 2026-07-06 — after the
2026-06-16 verification). `git blame` confirms `_lastAutoRetry = _now()` at
`sync_engine.dart:309` landed in that commit, and the conflict sheet gained a real
field-by-field merge UI. The genuinely missing piece is the auto-retry gate regression
test; the existing merge test is weak (passes if Merge ≡ keep-local).

---

## Phase scope & reframing

| Option | Description | Selected |
|--------|-------------|----------|
| Verify + close test gaps | Accept existing fixes; add missing/strengthened tests, small quirk fixes, re-verify | ✓ |
| Tests + deeper hardening | Also audit both files beyond the named defects | |
| Treat fixes as unverified — reimplement | Re-derive both fixes from the Phase 24 gaps | |

**User's choice:** Verify + close test gaps (Recommended)

| Option | Description | Selected |
|--------|-------------|----------|
| Leave history, note in 25.1 | Phase 24 records stay historical; 25.1 docs carry the discovery | ✓ |
| Amend Phase 24 records too | Update 24-VERIFICATION.md and roadmap/state notes | |

**User's choice:** Leave history, note in 25.1 (Recommended)

---

## Auto-retry gate semantics

| Option | Description | Selected |
|--------|-------------|----------|
| Keep — manual retry stamps too | Any retry protects server 4h; manual never blocked | ✓ |
| Only auto triggers stamp | Manual taps don't consume the auto window | |

**User's choice:** Keep — manual retry stamps too (Recommended)

| Option | Description | Selected |
|--------|-------------|----------|
| Consolidate + rename | Single well-named predicate for both triggers + banner | ✓ |
| Leave as-is | Don't touch working code beyond named defects | |

**User's choice:** Consolidate + rename (Recommended)

---

## Merge UX default

| Option | Description | Selected |
|--------|-------------|----------|
| Default to local | Untouched merge keeps local data; opt fields INTO cloud | ✓ |
| Keep cloud default | Merge starts from cloud version | |
| Highlight differing fields only | Segmented control only where values differ | |

**User's choice:** Default to local (Recommended)

| Option | Description | Selected |
|--------|-------------|----------|
| Remove Merge All | Merge is per-trip; keep two bulk buttons | |
| Keep Merge All | D-04 symmetry; accept Merge All ≡ Keep All Local with new default | ✓ |

**User's choice:** Keep Merge All (went against recommendation — user accepted the
equivalence consciously)

---

## Regression test design

| Option | Description | Selected |
|--------|-------------|----------|
| Full gate contract | (1) no re-fire within window, (2) re-fires after window, (3) manual stamp suppresses following auto trigger | ✓ |
| Minimal per SC3 only | Only assertion (1) | |

**User's choice:** Full gate contract (Recommended)

| Option | Description | Selected |
|--------|-------------|----------|
| Strengthen widget test | Two differing fields, mixed selection, assert ≠ use-cloud AND ≠ keep-local | ✓ |
| Extract merge logic + unit test | Pure-function seam (noted as Phase 26 candidate) | |
| Keep existing test as-is | Accept the keep-local blind spot | |

**User's choice:** Strengthen widget test (Recommended)

---

## Claude's Discretion

- Exact name of the consolidated gate predicate
- Test placement/organization within existing test files
- Fixture helpers for the two-differing-fields merge conflict
- Banner-gate predicate split if semantics diverge (behavior must stay identical)

## Deferred Ideas

- Merge-logic extraction to a pure function → Phase 26 refactor candidate
- Overlap-conflict UUID semantics (cloud copy stays in Firestore under different id)
- Differing-fields-only merge UI polish

## Session context

- Earlier this session (plan-phase gate questions), the user chose: real field-by-field
  merge (not remove/relabel), research-first planning, and UI-SPEC generation before
  planning. Discussion for this phase was triggered from that pause.
