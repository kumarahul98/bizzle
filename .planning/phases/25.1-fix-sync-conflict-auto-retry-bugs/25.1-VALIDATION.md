---
phase: 25.1
slug: fix-sync-conflict-auto-retry-bugs
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-07-11
---

# Phase 25.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | flutter_test (bundled with Flutter 3.41.6 SDK) |
| **Config file** | none — standard `flutter test` discovery over `test/` |
| **Quick run command** | `flutter test test/unit/sync/sync_engine_test.dart test/widget/features/settings/conflict_resolution_sheet_test.dart` |
| **Full suite command** | `flutter test` |
| **Estimated runtime** | ~30 seconds (quick) / ~2-3 minutes (full) |

---

## Sampling Rate

- **After every task commit:** Run `flutter test test/unit/sync/sync_engine_test.dart test/widget/features/settings/conflict_resolution_sheet_test.dart`
- **After every plan wave:** Run `flutter test` (full suite — confirm no regression in other consumers of `SyncEngine`/`ConflictResolutionSheet`)
- **Before `/gsd:verify-work`:** Full suite green PLUS `flutter analyze` clean (confirms the D-04 rename left no stale references)
- **Max feedback latency:** ~30 seconds

---

## Per-Task Verification Map

No formal REQ-IDs are mapped to this phase (roadmap: "Requirements: TBD" — internal bug-fix phase). Mapping the roadmap's Success Criteria and CONTEXT.md decisions instead. Task IDs to be filled by the planner.

| Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|------------|-----------------|-----------|-------------------|-------------|--------|
| TBD | TBD | TBD | SC1 / D-07 (gate contract: no re-fire within window, re-fire after, manual stamp suppresses) | — | N/A | unit | `flutter test test/unit/sync/sync_engine_test.dart --plain-name "auto-retry"` | ✅ group exists, ❌ 3 new assertions (W0) | ⬜ pending |
| TBD | TBD | TBD | SC2+SC3 / D-08 (two-differing-field merge ≠ use-cloud AND ≠ keep-local) | — | N/A | widget | `flutter test test/widget/features/settings/conflict_resolution_sheet_test.dart` | ✅ file exists, ❌ strengthened test (W0) | ⬜ pending |
| TBD | TBD | TBD | D-04 (predicate consolidation/rename, banner behavior unchanged) | — | N/A | unit + static | `flutter analyze` + SC1 test command | N/A — existing tests pass post-rename | ⬜ pending |
| TBD | TBD | TBD | D-05 (merge default → 'local'; Merge All with no picks ≡ Keep All Local; TWO locations: display default + `_applyAll` ternary fallback) | — | N/A | widget | New D-08 test asserts explicitly | ❌ W0 (same file) | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Three new tests in `test/unit/sync/sync_engine_test.dart` "SyncEngine auto-retry time gate" group — D-07 / SC3 (unit half)
- [ ] One new test + `distanceMeters` fixture parameter in `test/widget/features/settings/conflict_resolution_sheet_test.dart` — D-08 / SC3 (widget half)
- [ ] No new test files, no framework install — both target files exist and the baseline is green (28/28 pass)

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| Merge sheet segmented buttons visually default to "Local" after D-05 | D-05 | Visual default state; widget test asserts logic, not appearance | Trigger a restore conflict on device, select Merge, confirm all segments start on Local |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 60s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
