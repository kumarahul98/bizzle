---
phase: 25.1-fix-sync-conflict-auto-retry-bugs
verified: 2026-07-11T17:47:35Z
status: human_needed
score: 3/3 must-haves verified
overrides_applied: 0
human_verification:
  - test: "Trigger a real cloud-restore conflict on a device/emulator, select 'Merge' on a conflicted trip, expand the per-field panel"
    expected: "All five per-field SegmentedButtons (startTime, endTime, durationSeconds, distanceMeters, direction) render with 'Local' visually pre-selected (accent-filled) on first expand — not 'Cloud'"
    why_human: "Visual rendering/appearance of the default selection state cannot be fully confirmed by widget-test assertions on selection state alone; this is explicitly flagged as a Manual-Only Verification in 25.1-VALIDATION.md (not yet checked off) and is the kind of visual-default check that requires eyes on a running app."
---

# Phase 25.1: Fix Sync Conflict & Auto-Retry Bugs Verification Report

**Phase Goal:** The two correctness defects found by Phase 24's verification — a broken auto-retry throttle and a fake Merge conflict resolution — are fixed before Phase 26 extends the same files with breaks/edit-metadata sync.
**Verified:** 2026-07-11T17:47:35Z
**Status:** human_needed
**Re-verification:** No — initial verification

## Requirement Traceability Note

The phase's plan frontmatter (`25.1-01-PLAN.md`, `25.1-02-PLAN.md`) declares `requirements: [D-03, D-04, D-07]` and `[D-05, D-06, D-08]` respectively. These are **phase-local decision IDs** (from `25.1-CONTEXT.md` / `25.1-UI-SPEC.md`), not formal `REQUIREMENTS.md` requirement IDs — `.planning/REQUIREMENTS.md` contains no `D-03..D-08` entries and does not map any requirement ID to Phase 25.1 (confirmed via grep: zero matches for `D-0[3-8]` and zero matches for `25.1` in the requirements traceability table). ROADMAP.md itself lists `**Requirements**: TBD` for this phase, consistent with this being an internal correctness-fix phase inserted between Phase 25 and Phase 26. **Per instructions, this is not treated as a failure** — it is expected and documented (also called out explicitly in `25.1-VALIDATION.md` line 39: "No formal REQ-IDs are mapped to this phase").

## Goal Achievement

### Observable Truths (Roadmap Success Criteria)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | `SyncEngine._lastAutoRetry` is assigned when a retry occurs, so `kFailedAutoRetryWindow` actually throttles auto-retry triggers | ✓ VERIFIED | `lib/sync/sync_engine.dart:312` — `_lastAutoRetry = _now();` inside `retryFailed()`. The gate condition is consolidated into a single getter `autoRetryWindowElapsed` (`:105-107`) consumed identically by the connectivity-restore listener (`:354`), `handleResume()` (`:371`), and the dashboard `_StuckBannerGate` (`dashboard_screen.dart:194`). Zero remaining references to the old, un-throttled `isAutoRetryExhausted` name (`grep -rn "isAutoRetryExhausted" lib/ test/` → 0 matches). |
| 2 | Selecting "Merge" no longer silently runs the same code path as "Use Cloud" — it performs a real field-by-field merge | ✓ VERIFIED | `conflict_resolution_sheet.dart:47-73` — the `kConflictMerge` branch is a distinct code path from `kConflictUseCloud` (`:44-46`), building a `merged` row via 5 independent per-field ternaries (`startTime`, `endTime`, `durationSeconds`, `distanceMeters`, `direction`) that read `_mergeSelections`. Both leak points (displayed `SegmentedButton` default at `:174-175` and the `_applyAll` fallback ternaries at `:54-68`) now default to `'local'` (was `'cloud'`) — confirmed via source read, zero `?? 'cloud'` occurrences remain. |
| 3 | A regression test exists for the auto-retry time gate (second trigger within window does not re-fire) and for the Merge path (merged output differs from pure Use Cloud) | ✓ VERIFIED | `test/unit/sync/sync_engine_test.dart:646-671` — `'second trigger within window does not re-fire retryFailed'` asserts the failed row stays `kSyncStatusFailed` and `api.syncCalls.length` is unchanged. `test/widget/features/settings/conflict_resolution_sheet_test.dart:296-374` — `'Merge with two differing fields produces output distinct from both pure Use Cloud and pure Keep Local'` asserts `durationSeconds==100` (local, `isNot(999)`) and `distanceMeters==20000.0` (cloud opt-in, `isNot(12000.0)`) against the actual Drift row. Both tests pass (see Behavioral Spot-Checks below). |

**Score:** 3/3 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| `lib/sync/sync_engine.dart` | `autoRetryWindowElapsed` getter (renamed from `isAutoRetryExhausted`), consumed by connectivity listener + `handleResume()` | ✓ VERIFIED | Getter defined `:105-107`, called at `:354` and `:371` (3 total occurrences: 1 def + 2 call sites, as required by plan acceptance criteria). |
| `lib/features/dashboard/screens/dashboard_screen.dart` | `_StuckBannerGate` consumes renamed getter, unchanged polarity | ✓ VERIFIED | `:194` — `if (!syncEngine.autoRetryWindowElapsed) return const SizedBox.shrink();` — `!` negation preserved exactly. |
| `test/unit/sync/sync_engine_test.dart` | 3 new D-07 regression tests in "SyncEngine auto-retry time gate" group | ✓ VERIFIED | 3 tests present with exact required names (`:646`, `:673`, `:697-699`); all pass. |
| `lib/features/settings/widgets/conflict_resolution_sheet.dart` | Local-defaulted merge at both leak points | ✓ VERIFIED | Display default `:174-175` (`?? 'local'`), 5 `_applyAll` ternaries `:54-68` all `(selections[...] ?? 'local') == 'local'`. Bulk buttons ('Keep All Local'/'Use All Cloud'/'Merge All') unmodified. |
| `test/widget/features/settings/conflict_resolution_sheet_test.dart` | `distanceMeters` fixture parameter + new two-differing-field merge test | ✓ VERIFIED | `distanceMeters` parameter present in both `_tripJson` and `_companion` (`:18`, `:40`); new test at `:296-374` named exactly as required, contains "distinct from both". |

### Key Link Verification

| From | To | Via | Status | Details |
|------|-----|-----|--------|---------|
| `sync_engine.dart` connectivity-restore listener (`:354`) | `autoRetryWindowElapsed` getter | direct getter call, no inline duplicate | ✓ WIRED | `if (autoRetryWindowElapsed) { unawaited(retryFailed()); } else { unawaited(processPending()); }` |
| `sync_engine.dart` `handleResume()` (`:371`) | `autoRetryWindowElapsed` getter | direct getter call, no inline duplicate | ✓ WIRED | Same pattern as above; both call sites confirmed identical structure. |
| `dashboard_screen.dart` `_StuckBannerGate.build` (`:194`) | `sync_engine.dart` `autoRetryWindowElapsed` | `syncEngine.autoRetryWindowElapsed` read | ✓ WIRED | Confirmed via source read. |
| `conflict_resolution_sheet.dart` SegmentedButton `selected:` (`:173-176`) | `conflict_resolution_sheet.dart` `_applyAll` merge ternaries (`:52-70`) | matching `?? 'local'` fallback in both locations | ✓ WIRED | Display and apply defaults verified identical — no divergence between what's shown selected and what's written. |
| `conflict_resolution_sheet_test.dart` D-08 test | `db.tripsDao.findById('m2')` | asserting the merged Drift row, not widget internals | ✓ WIRED | Confirmed at `:369-373` — assertions read the actual Drift row post-merge. |

### Behavioral Spot-Checks

| Behavior | Command | Result | Status |
|----------|---------|--------|--------|
| Auto-retry gate contract (3 new + 1 pre-existing test) | `flutter test test/unit/sync/sync_engine_test.dart --plain-name "auto-retry"` | `+4: All tests passed!` | ✓ PASS |
| Merge default flip + D-08 strengthened test (4 tests) | `flutter test test/widget/features/settings/conflict_resolution_sheet_test.dart` | `+4: All tests passed!` | ✓ PASS |
| Full project test suite (regression check) | `flutter test` | `+603 ~10: All tests passed!` (603 passed, 10 skipped, 0 failed) | ✓ PASS |
| Static analysis (no new issues) | `flutter analyze` | 263 pre-existing info-level issues (baseline was 270 before this phase per SUMMARY/deferred-items.md; net reduction from `dart format` reflow), 0 errors, 0 warnings | ✓ PASS |
| Zero stale references to renamed getter | `grep -rn "isAutoRetryExhausted" lib/ test/` | no matches | ✓ PASS |

### Requirements Coverage

No formal `REQUIREMENTS.md` IDs are mapped to this phase (see "Requirement Traceability Note" above). Plan-declared phase-local decision IDs (D-03 through D-08) are tracked instead, and each is satisfied:

| Decision ID | Description | Status | Evidence |
|-------------|-------------|--------|----------|
| D-03 | Manual `retryFailed()` never blocked; its stamp suppresses a following auto trigger | ✓ SATISFIED | Test `'manual retryFailed stamps the window...'` passes. |
| D-04 | Gate predicate consolidated into a single, correctly-named getter | ✓ SATISFIED | `autoRetryWindowElapsed`, 1 definition + 2 call sites, 0 stale references. |
| D-05 | Merge default flipped to 'local' at both leak points | ✓ SATISFIED | Verified in code at both locations. |
| D-06 | Bulk buttons unchanged; 'Merge All' with no picks now equals 'Keep All Local' (accepted consequence) | ✓ SATISFIED | Bulk button code untouched; no UI signal added (per plan, intentional). |
| D-07 | Auto-retry gate regression coverage (window suppression, elapse re-fire, manual stamp) | ✓ SATISFIED | 3 new tests, all passing. |
| D-08 | Merge regression test proving output differs from both pure outcomes | ✓ SATISFIED | New widget test passing, asserts against Drift row. |

### Anti-Patterns Found

A prior code review (`25.1-REVIEW.md`, standard depth, 5 files) found **0 critical issues**. The following pre-existing/adjacent items were flagged but do not block phase goal achievement (none are regressions introduced by this phase, and none touch the three roadmap success criteria):

| File | Line | Pattern | Severity | Impact |
|------|------|---------|----------|--------|
| `conflict_resolution_sheet.dart` | 45, 71 | Merge/Use-Cloud writes don't call `SyncQueueDao.enqueueUpdate` (pre-existing, not introduced this phase) | ⚠️ Warning | A merged/cloud-resolved trip isn't pushed back to Firestore — out of scope for this phase's goal (throttle + merge-authenticity), noted in REVIEW.md WR-01. |
| `conflict_resolution_sheet.dart` | 76-78 | `ref` used after `await` without a `mounted` guard | ⚠️ Warning | Pre-existing lifecycle risk, not introduced this phase (REVIEW.md WR-02). |
| `conflict_resolution_sheet.dart` | 52-70 | Non-selectable fields (`timeMovingSeconds`, etc.) always take the cloud value regardless of the local-default flip | ⚠️ Warning | Documented consequence of the 5-field-only merge scope (REVIEW.md WR-03) — not a regression of this phase's stated goal, which only required Merge to be behaviorally distinct from Use Cloud, which it now is. |
| `test/unit/sync/sync_engine_test.dart` | 661-664, 684-686 | Flaky-race pattern: rising-edge trigger from `enqueueCreate` could theoretically race `markFailed` | ⚠️ Warning | REVIEW.md WR-04 — currently passes deterministically (confirmed: ran the target test group, all 4 green) but should be hardened before Phase 26 touches the same file. Not a functional gap in the phase's goal — the tests pass and correctly pin the intended contract today. |
| `test/widget/features/settings/conflict_resolution_sheet_test.dart` | 144,146,150,153,156 | `print()` debug statements | ℹ️ Info | Pre-existing (REVIEW.md IN-01), cosmetic. |

None of these rise to 🛑 Blocker — they were already known/logged (REVIEW.md, deferred-items.md) and are explicitly out of scope for this bug-fix phase's stated goal.

### Human Verification Required

### 1. Merge sheet visual default state on a real device/emulator

**Test:** Trigger a real cloud-restore conflict (or use an existing dev/staging conflict scenario), select "Merge" for a conflicted trip, and expand the per-field panel.
**Expected:** All five per-field `SegmentedButton`s (startTime, endTime, durationSeconds, distanceMeters, direction) visually render with "Local" pre-selected/accent-filled — not "Cloud" — on first expand, matching the D-05 code change.
**Why human:** This is explicitly listed as a "Manual-Only Verification" in `25.1-VALIDATION.md` ("Merge sheet segmented buttons visually default to 'Local' after D-05... Visual default state; widget test asserts logic, not appearance") and its checkbox is unchecked. The widget test (`conflict_resolution_sheet_test.dart`) programmatically confirms the applied/written values are correct, but does not assert on-screen visual pre-selection styling. Both `25.1-01-SUMMARY.md` and `25.1-02-SUMMARY.md` explicitly defer this to phase verification and it has not yet been performed.

### Gaps Summary

No functional gaps found. All three roadmap Success Criteria are met with passing automated evidence (unit tests, widget tests, full suite regression check, static analysis, source-level verification of both fix sites and both key-link wiring paths). The one outstanding item is a human/visual confirmation explicitly scoped as "deferred to phase verification" by the plan authors themselves, and it has not yet been performed — this routes the phase to `human_needed` rather than `passed` per the verification decision tree (Step 9), even though the score is 3/3.

The requirement-ID traceability gap (D-03..D-08 not present in `REQUIREMENTS.md`) is expected and documented, not a defect — see "Requirement Traceability Note" above.

---

*Verified: 2026-07-11T17:47:35Z*
*Verifier: Claude (gsd-verifier)*
