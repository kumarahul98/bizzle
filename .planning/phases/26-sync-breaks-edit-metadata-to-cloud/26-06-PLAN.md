---
phase: 26-sync-breaks-edit-metadata-to-cloud
plan: 06
type: execute
wave: 4
depends_on: ["26-05"]
files_modified:
  - lib/sync/merge_resolution.dart
  - test/unit/sync/merge_resolution_test.dart
  - lib/features/settings/widgets/conflict_resolution_sheet.dart
  - test/widget/features/settings/conflict_resolution_sheet_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "The field-by-field merge logic that lived inside _applyAll's inline branch is now a pure, independently-unit-tested function pinning the CURRENT 5-field behavior before any Phase 26 change is added"
    - "In a merge, breaks and totalPausedSeconds follow whichever side won startTime/endTime; directionSource follows the direction selection; isEdited is forced true in merge output"
    - "Choosing Use Cloud (bulk or per-trip) for a conflict also replaces the local trip's breaks with the cloud trip's breaks, not just the 5 scalar fields"
    - "The conflict sheet shows a read-only 'Local: N breaks · Cloud: M breaks' line only when the two sides' break counts differ, with no per-break controls"
  artifacts:
    - path: "lib/sync/merge_resolution.dart"
      provides: "resolveMerge({local, cloud, localBreaks, cloudBreaks, selections}) -> ({trip, breaks}) pure function"
      exports: ["resolveMerge"]
    - path: "test/unit/sync/merge_resolution_test.dart"
      provides: "Pinned 5-field merge behavior + D-04 ride-along cases"
  key_links:
    - from: "lib/features/settings/widgets/conflict_resolution_sheet.dart"
      to: "lib/sync/merge_resolution.dart"
      via: "_applyAll's Merge branch calls resolveMerge(...) instead of the inline copyWith chain"
      pattern: "resolveMerge\\("
    - from: "lib/features/settings/widgets/conflict_resolution_sheet.dart"
      to: "lib/database/daos/trip_breaks_dao.dart"
      via: "Use Cloud AND Merge branches both call deleteBreaksForTrip + insertBreaks inside a transaction for the winning side's breaks"
      pattern: "deleteBreaksForTrip"
---

<objective>
Execute CONTEXT.md D-06 in its locked order: FIRST extract `conflict_resolution_sheet.dart`'s inline
merge logic into a pure `lib/sync/merge_resolution.dart` function and pin the CURRENT 5-field
behavior with unit tests, THEN add the D-04 breaks/metadata ride-along rules on top. Also close the
gap SC5 exposes beyond Merge alone: the "Use Cloud" resolution must carry breaks too (today it only
touches the 5 scalar `TripsCompanion` fields). Finish with the D-05 read-only breaks-differ indicator.

Purpose: this is the LAST plan in the phase — it makes conflict resolution (all three actions: Keep
Local / Use Cloud / Merge) behave consistently with "breaks ride along with whichever side wins, no
per-break merge UI" (roadmap SC5), completing the phase's cloud-fidelity guarantee end-to-end.

Output: a tested pure merge function, both Use-Cloud and Merge conflict-resolution paths correctly
carrying breaks, and a visible (but non-interactive) breaks-differ indicator in the conflict sheet.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-CONTEXT.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-RESEARCH.md
@CLAUDE.md
@.planning/phases/25.1-fix-sync-conflict-auto-retry-bugs/25.1-CONTEXT.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-05-SUMMARY.md

<interfaces>
<!-- Full current conflict_resolution_sheet.dart read this session — use directly. -->

From lib/features/settings/widgets/conflict_resolution_sheet.dart (lines 28-82, `_applyAll` — the
EXACT logic to extract, byte-for-byte, before changing anything):
```dart
Future<void> _applyAll(String defaultAction) async {
  final tripsDao = ref.read(tripsDaoProvider);
  int resolvedCount = 0;
  for (final conflict in widget.conflicts) {
    final action = _resolutions[conflict.localTrip.id] ?? defaultAction;
    if (action == kConflictKeepLocal) { continue; }
    final companion = conflict.cloudTrip.copyWith(
      id: drift.Value(conflict.localTrip.id),
      updatedAt: drift.Value(DateTime.now().toUtc()),
    );
    if (action == kConflictUseCloud) {
      await tripsDao.updateTrip(companion);
      resolvedCount++;
    } else if (action == kConflictMerge) {
      final localTrip = conflict.localTrip;
      final cloudTrip = conflict.cloudTrip;
      final selections = _mergeSelections[localTrip.id] ?? {};
      final merged = cloudTrip.copyWith(
        id: drift.Value(localTrip.id),
        startTime: (selections['startTime'] ?? 'local') == 'local' ? drift.Value(localTrip.startTime) : cloudTrip.startTime,
        endTime: (selections['endTime'] ?? 'local') == 'local' ? drift.Value(localTrip.endTime) : cloudTrip.endTime,
        durationSeconds: (selections['durationSeconds'] ?? 'local') == 'local' ? drift.Value(localTrip.durationSeconds) : cloudTrip.durationSeconds,
        distanceMeters: (selections['distanceMeters'] ?? 'local') == 'local' ? drift.Value(localTrip.distanceMeters) : cloudTrip.distanceMeters,
        direction: (selections['direction'] ?? 'local') == 'local' ? drift.Value(localTrip.direction) : cloudTrip.direction,
        updatedAt: drift.Value(DateTime.now().toUtc()),
      );
      await tripsDao.updateTrip(merged);
      resolvedCount++;
    }
  }
  ref.read(restoreControllerProvider.notifier).resolveConflicts(resolvedCount);
  if (mounted) { Navigator.of(context).pop(); }
}
```
Note the `kConflictKeepLocal` default in EVERY `selections[field] ?? 'local'` ternary — this is the
25.1-02 fix already live; preserve it verbatim in the extracted function.

Per-field `SegmentedButton` UI loop (lines 150-188, the D-05 indicator's insertion point — add the
new read-only line as a sibling, not inside, this `for` loop):
```dart
for (final field in ['startTime', 'endTime', 'durationSeconds', 'distanceMeters', 'direction'])
  Row(/* SegmentedButton local/cloud toggle */),
```

Per CONTEXT.md D-04 (verbatim): "breaks + totalPausedSeconds follow whichever side won the time
fields (startTime/endTime) so a break can never fall outside the merged trip's window.
directionSource follows the direction toggle's chosen side. isEdited resolves automatically
(Claude's discretion; merged output is user-touched, so true is the natural result)."

Per CONTEXT.md D-05 (verbatim): "The conflict sheet gains a read-only breaks indicator shown only
when the two sides differ in breaks (e.g. 'Local: 2 breaks · Cloud: none'). No per-break controls,
no new toggleable rows — the five existing mergeable fields stay as-is."

From lib/config/constants.dart (Plan 02 output): `kConflictBreaksDifferTemplate` — a
`'Local: {local} breaks · Cloud: {cloud} breaks'`-shaped template string, mirroring
`kAutoRestoreResultTemplate`'s `{n}`-placeholder `.replaceAll` convention.

From lib/database/daos/trip_breaks_dao.dart: `deleteBreaksForTrip(String tripId)`,
`insertBreaks(List<TripBreaksCompanion>)` — the EXACT delete-all-then-insert pattern already used
by `TripManagementNotifier.editTrip`'s full-edit path (`lib/features/trips/providers/trip_management_providers.dart:133-148`)
for wholesale break replacement; reuse this shape for both the Use-Cloud and Merge branches' breaks write.

From lib/sync/restore_conflict.dart (Plan 05 output): `RestoreConflict.cloudBreaks` /
`.localBreaks` — both already populated by `RestoreController.restore()`, so this plan needs NO
further DB reads to know either side's break list; it only needs `TripBreaksDao` for the WRITE
(delete+insert), not for reading.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract resolveMerge as a pure function, pin the CURRENT 5-field behavior (D-06 step 1)</name>
  <read_first>
    lib/features/settings/widgets/conflict_resolution_sheet.dart
    lib/sync/restore_conflict.dart
    .planning/phases/25.1-fix-sync-conflict-auto-retry-bugs/25.1-CONTEXT.md
  </read_first>
  <files>lib/sync/merge_resolution.dart, test/unit/sync/merge_resolution_test.dart</files>
  <behavior>
    - `resolveMerge` with an EMPTY `selections` map (no user taps) resolves EVERY field to 'local' — matches the 25.1-02 default (`?? 'local'` on all 5 fields) exactly.
    - `resolveMerge` with `selections: {'durationSeconds': 'cloud'}` and all other fields untouched resolves ONLY `durationSeconds` to cloud's value; the other 4 stay local — pins the existing per-field independence.
    - `resolveMerge`'s output `trip.id` always equals `local.id` (never `cloud.id`) and `trip.updatedAt` is set to a fresh timestamp — matches the existing `id: drift.Value(localTrip.id)` remap.
    - This task's tests reproduce the file's CURRENT 5-field-only behavior — no breaks/metadata ride-along yet (that is Task 2).
  </behavior>
  <action>
1. Create `lib/sync/merge_resolution.dart`. Define `typedef MergeResult = ({TripsCompanion trip, List<TripBreaksCompanion> breaks});`. Define `MergeResult resolveMerge({required TripRow local, required TripsCompanion cloud, required Map<String, String> selections})` (breaks/localBreaks/cloudBreaks params come in Task 2 — keep this first extraction scoped to EXACTLY the current 5-field logic, per D-06's explicit "extract first, pin, THEN add ride-along" sequencing). Move `_applyAll`'s Merge-branch `copyWith(...)` chain into this function VERBATIM (same field list, same `(selections[field] ?? 'local') == 'local'` ternaries, same `id: Value(local.id)`, same `updatedAt: Value(DateTime.now().toUtc())`). Return `(trip: merged, breaks: const <TripBreaksCompanion>[])` (empty breaks placeholder for this task — Task 2 fills it in).
2. Create `test/unit/sync/merge_resolution_test.dart` covering every case in `<behavior>` above, using plain `TripRow`/`TripsCompanion` fixtures (no widget harness, no DB — this is why D-06 mandates the extraction: pure-function unit tests, no `WidgetTester`).
3. In `lib/features/settings/widgets/conflict_resolution_sheet.dart`: replace `_applyAll`'s inline Merge-branch `copyWith` chain with a call to `resolveMerge(local: localTrip, cloud: cloudTrip, selections: selections).trip`, then `await tripsDao.updateTrip(merged);` exactly as before. Import `package:traevy/sync/merge_resolution.dart`. Do NOT touch the `kConflictUseCloud` branch or the UI build method yet (Task 2 handles those).
  </action>
  <verify>
    <automated>flutter test test/unit/sync/merge_resolution_test.dart test/widget/features/settings/conflict_resolution_sheet_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/unit/sync/merge_resolution_test.dart` green, covering all cases in `<behavior>`.
    - `flutter test test/widget/features/settings/conflict_resolution_sheet_test.dart` STILL passes unchanged — proves the extraction preserved externally-observable behavior byte-for-byte (this is the "pin the contract" proof, mirroring the 25.1-01 "pin before rename" convention).
    - `flutter analyze lib/sync/merge_resolution.dart lib/features/settings/widgets/conflict_resolution_sheet.dart` clean.
  </acceptance_criteria>
  <done>The merge logic is a pure, independently-tested function; the widget calls it with no behavior change yet — a safe extraction proven by the still-green pre-existing widget suite.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: D-04 ride-along rules + Use-Cloud breaks carry-along + D-05 indicator</name>
  <read_first>
    lib/sync/merge_resolution.dart
    lib/features/settings/widgets/conflict_resolution_sheet.dart
    lib/database/daos/trip_breaks_dao.dart
    lib/config/constants.dart
  </read_first>
  <files>lib/sync/merge_resolution.dart, test/unit/sync/merge_resolution_test.dart, lib/features/settings/widgets/conflict_resolution_sheet.dart, test/widget/features/settings/conflict_resolution_sheet_test.dart</files>
  <behavior>
    - `resolveMerge` with `localBreaks`/`cloudBreaks` supplied: when `startTime` resolves to 'local' (default or explicit), the output `breaks` are LOCAL's breaks (rebuilt with fresh UUIDs, `tripId: local.id`); when `startTime` resolves to 'cloud', the output `breaks` are CLOUD's breaks (same `tripId: local.id` remap — never the cloud trip's original id, since the merged row is always written under `local.id`).
    - `resolveMerge` output's `totalPausedSeconds` follows the SAME side as `startTime`'s winner (local's value if local won, cloud's if cloud won) — not an independent selection.
    - `resolveMerge` output's `directionSource` follows the `direction` field's selection (local's `directionSource` if `direction` resolved local, cloud's if cloud).
    - `resolveMerge` output's `isEdited` is ALWAYS `true`, regardless of any selection (D-04 discretion).
    - In the widget: choosing "Use All Cloud" (or a per-trip explicit Use Cloud) for a conflict whose `cloudBreaks` differs from `localBreaks` results in the local trip's `trip_breaks` rows being replaced with cloud's (verifiable via `db.tripBreaksDao.breaksForTrip`).
    - The conflict sheet shows the text `kConflictBreaksDifferTemplate` (with `{local}`/`{cloud}` replaced by each side's break COUNT) for a conflict whose `localBreaks.length != cloudBreaks.length`, and shows NOTHING extra when the counts are equal (including both-zero).
  </behavior>
  <action>
1. In `lib/sync/merge_resolution.dart`: add `required List<TripBreakRow> localBreaks` and `required List<TripBreaksCompanion> cloudBreaks` params to `resolveMerge`. After computing `useLocalTime = (selections['startTime'] ?? 'local') == 'local'` (reuse the SAME resolved boolean the existing `startTime` ternary already computes — do not introduce a new/independent selection key, per D-04's "reuse the SAME selections['startTime']/['endTime'] choice"), build the winning breaks list: if `useLocalTime`, map `localBreaks` to fresh `TripBreaksCompanion.insert(id: const Uuid().v4(), tripId: local.id, startTime: b.startTime, endTime: Value(b.endTime!))`; else map `cloudBreaks` to `TripBreaksCompanion.insert(id: const Uuid().v4(), tripId: local.id, startTime: b.startTime.value, endTime: b.endTime)` (cloud breaks are already `Value`-wrapped companions from `TripSerializer.fromJson`/`RestoreConflict.cloudBreaks` — read the actual field types before writing the extraction, they may already carry plain `DateTime` for `startTime` since `TripBreaksCompanion.insert`'s `startTime` param is non-nullable; adjust accordingly). Import `package:uuid/uuid.dart`. Set `totalPausedSeconds: useLocalTime ? Value(local.totalPausedSeconds) : (cloud.totalPausedSeconds.present ? cloud.totalPausedSeconds : Value(local.totalPausedSeconds))` on the merged companion. Compute `useLocalDirection = (selections['direction'] ?? 'local') == 'local'` (already computed for the existing `direction` field ternary) and set `directionSource: useLocalDirection ? Value(local.directionSource) : (cloud.directionSource.present ? cloud.directionSource : Value(local.directionSource))`. Set `isEdited: const Value(true)` unconditionally. Return `(trip: merged, breaks: winningBreaks)`.
2. Extend `test/unit/sync/merge_resolution_test.dart` with cases for every bullet in `<behavior>` above (ride-along on local-wins, ride-along on cloud-wins, directionSource-follows-direction, isEdited-always-true).
3. In `lib/features/settings/widgets/conflict_resolution_sheet.dart`: inject `ref.read(tripBreaksDaoProvider)` and `ref.read(appDatabaseProvider)` into `_applyAll`. Change the Merge branch to call `resolveMerge(local: localTrip, cloud: cloudTrip, selections: selections, localBreaks: conflict.localBreaks, cloudBreaks: conflict.cloudBreaks)`, then wrap the write in `await database.transaction(() async { await tripsDao.updateTrip(result.trip); await breaksDao.deleteBreaksForTrip(localTrip.id); if (result.breaks.isNotEmpty) await breaksDao.insertBreaks(result.breaks); });` (mirrors `TripManagementNotifier.editTrip`'s wholesale-replace shape at `trip_management_providers.dart:133-148`). Change the `kConflictUseCloud` branch similarly: after `await tripsDao.updateTrip(companion);`, ALSO wrap a `deleteBreaksForTrip(conflict.localTrip.id)` + (if `conflict.cloudBreaks.isNotEmpty`) `insertBreaks(...)` (remap each `cloudBreaks` entry's `tripId` to `conflict.localTrip.id` — the SAME "always write under the local id" rule as Task 1's `resolveMerge`, since an `OverlapConflict`'s `cloudBreaks` originally carry the DIFFERENT cloud trip's id) inside the SAME transaction as the `updateTrip` call (currently `updateTrip` is NOT wrapped in a transaction at all — wrap both calls together so they are atomic).
4. Add the D-05 indicator: in the `ExpansionTile`'s `children` list, as a sibling to (not inside) the existing per-field `SegmentedButton` loop, add a conditional `Text` row: `if (conflict.localBreaks.length != conflict.cloudBreaks.length) Padding(..., child: Text(kConflictBreaksDifferTemplate.replaceAll('{local}', conflict.localBreaks.length.toString()).replaceAll('{cloud}', conflict.cloudBreaks.length.toString())))`. This row is ALWAYS visible when counts differ (not gated behind `selectedAction == kConflictMerge`) since it is informational for any resolution choice, per D-05's "shown only when the two sides differ" (no action-gating mentioned).
5. Extend `test/widget/features/settings/conflict_resolution_sheet_test.dart`: add a case seeding a `SameUuidConflict` with `localBreaks`/`cloudBreaks` of differing length, asserting the indicator text is visible after expanding the tile; add a case with EQUAL (including both-empty) break counts asserting the indicator text is ABSENT; add a case choosing "Use All Cloud" on a conflict with differing breaks and asserting `db.tripBreaksDao.breaksForTrip(tripId)` afterward matches cloud's breaks (count and, where feasible, timestamps).
  </action>
  <verify>
    <automated>flutter test test/unit/sync/merge_resolution_test.dart test/widget/features/settings/conflict_resolution_sheet_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/unit/sync/merge_resolution_test.dart` green, covering all D-04 ride-along cases.
    - `flutter test test/widget/features/settings/conflict_resolution_sheet_test.dart` green, including the NEW D-05 indicator visible/absent cases and the Use-Cloud-carries-breaks case, with ALL 5 pre-existing tests in this file (from before Plan 06) still passing unchanged.
    - `grep -n "deleteBreaksForTrip" lib/features/settings/widgets/conflict_resolution_sheet.dart` shows it called from BOTH the `kConflictUseCloud` and Merge branches.
    - `flutter analyze lib/sync/merge_resolution.dart lib/features/settings/widgets/conflict_resolution_sheet.dart` clean; `flutter analyze` (whole project) reports zero new warnings versus Plan 05's end-state.
  </acceptance_criteria>
  <done>Merge output correctly rides breaks/metadata along with the winning side; Use Cloud also carries breaks (closing the SC5 gap beyond Merge); the conflict sheet visibly signals a breaks difference with no per-break controls, exactly per D-05.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User-selected merge choice -> local Drift write | The merge/use-cloud selections are trusted local UI state, but the WRITE they produce must not corrupt trip/break referential integrity (a break row pointing at the wrong trip id) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-16 | Tampering / Integrity | Breaks written under the wrong `tripId` after an OverlapConflict's "Use Cloud"/Merge resolution | mitigate | Every winning-side break write explicitly remaps `tripId` to `conflict.localTrip.id` (Task 2, both branches) — never reuses the parsed cloud trip's original id, which for an `OverlapConflict` is a DIFFERENT UUID than the row actually being written |
| T-26-17 | Tampering / Integrity | Non-atomic trip-update + breaks-replace (partial write on crash) | mitigate | Both the Use-Cloud and Merge branches wrap `updateTrip` + `deleteBreaksForTrip` + `insertBreaks` in ONE `database.transaction()` (Task 2) — the pre-existing `kConflictUseCloud` branch was NOT transactional before this plan; this closes that gap too |
</threat_model>

<verification>
- `flutter test test/unit/sync/merge_resolution_test.dart test/widget/features/settings/conflict_resolution_sheet_test.dart` green after each task.
- `flutter test` (full project suite) green with zero regressions — this is the LAST plan in the phase; run it as the final gate.
- `flutter analyze` (whole project) reports zero new warnings versus baseline.
</verification>

<success_criteria>
- [ ] `resolveMerge` is a pure, independently-unit-tested function; its extraction preserved existing widget-test behavior exactly (D-06 sequencing honored)
- [ ] D-04 ride-along: breaks/totalPausedSeconds follow the time-field winner; directionSource follows direction; isEdited always true in merge output
- [ ] Use Cloud also carries breaks, not just the 5 scalar fields (closes the SC5 gap)
- [ ] Both Use-Cloud and Merge breaks writes are transactional and correctly remap tripId
- [ ] D-05: read-only breaks-differ indicator shown only when counts differ, no per-break controls
- [ ] Full project test suite green; zero new analyzer warnings — phase-closing gate
</success_criteria>

<output>
After completion, create `.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-06-SUMMARY.md`
</output>
