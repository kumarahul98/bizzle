---
phase: 26-sync-breaks-edit-metadata-to-cloud
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/database/tables/user_preferences_table.dart
  - lib/database/database.dart
  - drift_schemas/drift_schema_v7.json
  - test/generated_migrations/schema.dart
  - test/generated_migrations/schema_v7.dart
  - test/unit/database/migration_v7_test.dart
  - lib/database/daos/trip_breaks_dao.dart
  - lib/database/daos/user_preferences_dao.dart
  - lib/config/constants.dart
  - test/unit/database/trip_breaks_dao_test.dart
  - test/unit/database/user_preferences_dao_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "A fresh install and an upgrading install both open the Drift database at schema v7 with a backfillMarkerVersion column on user_preferences, defaulting to 0"
    - "TripBreaksDao can fetch break rows for many trip ids in ONE query, ordered by startTime ascending per trip"
    - "UserPreferencesDao can read and write the backfill marker as a single-column upsert, mirroring the existing setHasSeenOnboarding shape"
    - "All Phase 26 client-side constants (breaks cap, backfill marker target version, conflict breaks-differ copy) live in constants.dart, not hardcoded at call sites"
  artifacts:
    - path: "lib/database/database.dart"
      provides: "schemaVersion=7; onUpgrade branch `from < 7 && to >= 7` adds userPreferences.backfillMarkerVersion"
      contains: "from < 7 && to >= 7"
    - path: "lib/database/daos/trip_breaks_dao.dart"
      provides: "breaksForTripIds(List<String>) -> Map<String, List<TripBreakRow>>, single WHERE tripId IN (...) query"
      exports: ["TripBreaksDao"]
    - path: "lib/database/daos/user_preferences_dao.dart"
      provides: "getBackfillMarkerVersion()/setBackfillMarkerVersion(int)"
    - path: "test/unit/database/migration_v7_test.dart"
      provides: "SchemaVerifier-based v6->v7 migration proof (additive, existing rows survive, new column defaults to 0)"
  key_links:
    - from: "lib/database/daos/trip_breaks_dao.dart"
      to: "lib/database/tables/trip_breaks_table.dart"
      via: "select(tripBreaks)..where((b) => b.tripId.isIn(tripIds))"
      pattern: "tripId\\.isIn"
    - from: "lib/database/daos/user_preferences_dao.dart"
      to: "lib/config/constants.dart"
      via: "kBackfillMarkerVersion compared against the stored marker"
      pattern: "kBackfillMarkerVersion"
---

<objective>
Lay the LOCAL (client-side, no network) foundation the rest of Phase 26 builds on: bump the Drift
schema to v7 with a version-keyed backfill marker column (CONTEXT.md D-03), add a batch break-lookup
DAO method (avoids N+1 during sync), add the marker DAO methods, and register every new Phase 26
constant (breaks cap, marker target version, D-05 conflict-sheet copy) in `constants.dart`.

Purpose: every later plan in this phase (wire contract, backfill trigger, restore, merge) needs one
or more of these primitives. Doing them together, up front, in ONE plan avoids `constants.dart` being
touched by multiple parallel-wave plans (a file-ownership conflict) and lets every later plan simply
import already-defined constants/DAO methods instead of re-deriving them.

Output: schema v7 (migration + generated schema snapshot + migration test), `TripBreaksDao.breaksForTripIds`,
`UserPreferencesDao` marker get/set, and the full Phase 26 constants block.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-CONTEXT.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-RESEARCH.md
@CLAUDE.md

<interfaces>
<!-- Exact patterns to copy, read in full this session. -->

From lib/database/database.dart (lines 39-40, 110-123 — the v5->v6 branch is the direct template):
```dart
int get schemaVersion => 6;   // -> bump to 7
// ...
if (from < 6 && to >= 6) {
  await m.addColumn(userPreferences, userPreferences.homeLat);
  // ...3 more addColumn calls...
  await m.addColumn(trips, trips.directionSource);
}
```
New branch goes AFTER this one: `if (from < 7 && to >= 7) { await m.addColumn(userPreferences, userPreferences.backfillMarkerVersion); }`.

From lib/database/tables/user_preferences_table.dart (lines 79-88 — nullable-coord pattern) and
lines 54-64 (`weeklyNotificationEnabled`/`autoPauseEnabled` — the closer analog: a
`.withDefault(const Constant(...))` boolean/int column, NOT nullable, since the marker needs a
comparable "never ran" value):
```dart
BoolColumn get autoPauseEnabled => boolean().withDefault(const Constant(false))();
```
New column: `IntColumn get backfillMarkerVersion => integer().withDefault(const Constant(0))();`
— 0 means "backfill has never run on this install".

From lib/database/daos/trip_breaks_dao.dart (lines 34-39, `breaksForTrip` — the direct template
for the new batch method):
```dart
Future<List<TripBreakRow>> breaksForTrip(String tripId) {
  return (select(tripBreaks)
        ..where((b) => b.tripId.equals(tripId))
        ..orderBy([(b) => OrderingTerm.asc(b.startTime)]))
      .get();
}
```

From lib/database/daos/user_preferences_dao.dart (lines 244-251, `setHasSeenOnboarding` — the
direct template for a single-column upsert) and lines 124-147 (`getOrDefault`'s row->value mapping
pattern for the getter):
```dart
Future<void> setHasSeenOnboarding(bool value) {
  return into(userPreferences).insertOnConflictUpdate(
    UserPreferencesCompanion.insert(
      id: const Value<int>(_kUserPreferencesId),
      hasSeenOnboarding: Value<bool>(value),
    ),
  );
}
```
`UserPreferencesValue` (lines 21-104) is a plain Dart value class with a `.defaults()` const
constructor — it must gain a `backfillMarkerVersion` field (default `0`) alongside the other 13,
and `getOrDefault()`/`watch()`/`upsert()` (lines 124-230) must read/write it in lockstep with every
other field (all 4 already-existing call sites construct `UserPreferencesValue` explicitly with
every named field — grep confirms no positional construction anywhere).

From lib/config/constants.dart (lines 1171-1195, the Phase 24 block — placement + doc-comment
template) and lines 1109-1121 (existing `kDirectionSourceManual`/`Geofence`/`Time` — do NOT
redefine, only reference numerically in comments) and line 871 (`kMaxSyncBatchTrips = 1000` —
sibling DoS-cap constant style) and line 1183 (`kAutoRestoreResultTemplate` — the `{n}`-placeholder
template-string convention to mirror for the new D-05 indicator string):
```dart
const String kAutoRestoreResultTemplate = 'Restored {n} trips';
// usage: kAutoRestoreResultTemplate.replaceAll('{n}', count.toString())
```

From test/unit/database/migration_v6_test.dart (full file read this session — the EXACT structure
to mirror for migration_v7_test.dart): a `group` with (a) "vN schema opens cleanly at version N",
(b) "vN -> v(N+1) migration applies, preserves existing rows, defaults new column" (seed via the
`vN.DatabaseAtN` generated helper class using `RawValuesInsertable`, then
`verifier.migrateAndValidate(migratedDb, N+1)`, then assert survival + new-column default via
`customSelect`), (c) "fresh install defaults" control case. Imports `'../../generated_migrations/schema.dart'`
and `'../../generated_migrations/schema_v5.dart' as v5` — the new test needs `schema_v6.dart as v6`.

From test/unit/database/trip_breaks_dao_test.dart (full file read this session) — `insertParentTrip()`
helper + `const uuid = Uuid();` pattern for building FK-satisfying fixtures; extend with a new
`group('breaksForTripIds')` alongside the existing groups, NOT a new file.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Schema v6 -&gt; v7 migration — backfillMarkerVersion column</name>
  <read_first>
    lib/database/database.dart
    lib/database/tables/user_preferences_table.dart
    test/unit/database/migration_v6_test.dart
    .planning/phases/21-home-office-locations-geofence/21-01-PLAN.md
  </read_first>
  <files>lib/database/tables/user_preferences_table.dart, lib/database/database.dart, test/unit/database/migration_v7_test.dart</files>
  <behavior>
    - A v6 install migrated to v7 preserves an existing user_preferences row and an existing trip row unchanged, and the migrated prefs row reads `backfillMarkerVersion == 0`.
    - A fresh install (onCreate, no row) reads `backfillMarkerVersion == 0` via `getOrDefault()`-equivalent default (this task only proves the DB-level default; the DAO getter itself is Task 2).
    - `verifier.migrateAndValidate(migratedDb, 7)` succeeds with no DDL drift against the generated v7 snapshot.
  </behavior>
  <action>
1. In `lib/database/tables/user_preferences_table.dart`: add `IntColumn get backfillMarkerVersion => integer().withDefault(const Constant(0))();` after `officeLng`, with a doc comment following the file's existing style: version-keyed marker (CONTEXT.md D-03) tracking "backfill done for payload schema v{N}"; `0` = never run; added by schema migration v6 -> v7 (additive).
2. In `lib/database/database.dart`: bump `int get schemaVersion => 6;` to `=> 7;`. Add a new `if (from < 7 && to >= 7) { await m.addColumn(userPreferences, userPreferences.backfillMarkerVersion); }` branch AFTER the existing `from < 6` branch, with a doc comment matching the file's established per-branch comment style (Phase 26, D-03, additive-only, no UPDATE/DROP).
3. Run the migration ceremony EXACTLY as documented in `.planning/phases/21-home-office-locations-geofence/21-01-PLAN.md:167-168`, from the repo root: `dart run build_runner build --delete-conflicting-outputs`, then `dart run drift_dev schema dump lib/database/database.dart drift_schemas/drift_schema_v7.json`, then `dart run drift_dev schema generate drift_schemas/ test/generated_migrations/`. Commit the generated `drift_schemas/drift_schema_v7.json` and the regenerated `test/generated_migrations/` files (including the new `schema_v6.dart` snapshot and the updated `schema.dart` helper) — these are checked-in generated artifacts per project convention (CLAUDE.md: "Generated files are `.g.dart` -- commit them to version control").
4. Create `test/unit/database/migration_v7_test.dart` mirroring `test/unit/database/migration_v6_test.dart`'s exact structure (imports `'../../generated_migrations/schema.dart'` and `'../../generated_migrations/schema_v6.dart' as v6`): (a) "v6 schema opens cleanly at version 6", (b) "v6 -> v7 migration applies, preserves existing rows, defaults backfillMarkerVersion to 0 (additive)" — seed one `user_preferences` row and one `trips` row through the generated v6 schema classes via `RawValuesInsertable`, run `verifier.migrateAndValidate(migratedDb, 7)`, then assert the trip survives unchanged AND the migrated prefs row's `backfillMarkerVersion == 0` via `db.userPreferencesDao`-style read or `customSelect('SELECT backfill_marker_version FROM user_preferences ...')`, (c) "fresh install: backfillMarkerVersion defaults to 0 (control)".
  </action>
  <verify>
    <automated>flutter test test/unit/database/migration_v7_test.dart test/unit/database/migration_v6_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `drift_schemas/drift_schema_v7.json` exists and `test/generated_migrations/schema_v6.dart` exists (the new "previous" snapshot).
    - `grep -n "from < 7 && to >= 7" lib/database/database.dart` matches.
    - `flutter test test/unit/database/migration_v7_test.dart` passes with 3 tests green.
    - `flutter test test/unit/database/migration_v6_test.dart` still passes unchanged (no regression from the schema bump).
    - `flutter analyze` reports no new warnings in the touched files.
  </acceptance_criteria>
  <done>Schema is at v7; the migration is additive-only and proven by a SchemaVerifier test; existing v6 tests are unaffected.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TripBreaksDao batch lookup + UserPreferencesDao marker + Phase 26 constants</name>
  <read_first>
    lib/database/daos/trip_breaks_dao.dart
    lib/database/daos/user_preferences_dao.dart
    lib/config/constants.dart
    test/unit/database/trip_breaks_dao_test.dart
    test/unit/database/user_preferences_dao_test.dart
  </read_first>
  <files>lib/database/daos/trip_breaks_dao.dart, lib/database/daos/user_preferences_dao.dart, lib/config/constants.dart, test/unit/database/trip_breaks_dao_test.dart, test/unit/database/user_preferences_dao_test.dart</files>
  <behavior>
    - `breaksForTripIds([])` returns `{}` with no query executed.
    - `breaksForTripIds([tripA.id, tripB.id])` returns a map keyed by tripId, each value ordered by `startTime` ascending, and correctly excludes a third trip's breaks not in the requested id list.
    - `getBackfillMarkerVersion()` returns `0` on a fresh DB (no row); after `setBackfillMarkerVersion(2)`, `getBackfillMarkerVersion()` returns `2` and every OTHER preference field (e.g. `darkMode`) is untouched (single-column upsert, same guarantee as `setHasSeenOnboarding`).
  </behavior>
  <action>
1. In `lib/database/daos/trip_breaks_dao.dart`: add `Future<Map<String, List<TripBreakRow>>> breaksForTripIds(List<String> tripIds)` — empty-list short-circuit returns `{}`; otherwise one `select(tripBreaks)..where((b) => b.tripId.isIn(tripIds))..orderBy([(b) => OrderingTerm.asc(b.startTime)])` query, grouped into the map via `putIfAbsent`. Doc comment: Phase 26, avoids N+1 during sync (RESEARCH.md Don't-Hand-Roll).
2. In `lib/database/daos/user_preferences_dao.dart`: add `backfillMarkerVersion` as a required `int` field to `UserPreferencesValue`'s main constructor AND its `.defaults()` const constructor (default `0`), and thread it through `getOrDefault()` and `watch()`'s row-mapping (both currently construct `UserPreferencesValue` with all 13 existing named fields — add the 14th in both places) and through `upsert()`'s companion build. Add `Future<int> getBackfillMarkerVersion()` (read via `getOrDefault()`, project just that field) and `Future<void> setBackfillMarkerVersion(int version)` (single-column `insertOnConflictUpdate` targeting `id: _kUserPreferencesId`, mirroring `setHasSeenOnboarding`'s exact shape).
3. In `lib/config/constants.dart`: append a new `// Phase 26 — Sync Breaks & Edit Metadata to Cloud` section (after the Phase 25 block) with: `const int kMaxBreaksPerTrip = 50;` (doc comment: must numerically match backend `kMaxBreaksPerTrip` in `validation.ts`), `const int kBackfillMarkerVersion = 2;` (doc comment: "backfill done for payload schema v2"; compared against `UserPreferencesDao.getBackfillMarkerVersion()`), `const String kConflictBreaksDifferTemplate = 'Local: {local} breaks · Cloud: {cloud} breaks';` (doc comment: D-05 read-only conflict-sheet indicator, shown only when local/cloud break counts differ; `{local}`/`{cloud}` replaced via `.replaceAll`, mirroring `kAutoRestoreResultTemplate`'s `{n}` convention).
4. In `test/unit/database/trip_breaks_dao_test.dart`: add a new `group('breaksForTripIds')` (do not touch the existing groups) covering: empty-list returns `{}`, two trips each with breaks returns a correctly-keyed+ordered map, a third trip's breaks are excluded when its id is not requested.
5. In `test/unit/database/user_preferences_dao_test.dart`: add cases mirroring the existing `setHasSeenOnboarding` test pair (creates-row-on-fresh-DB, writes-only-the-target-column) for `setBackfillMarkerVersion`/`getBackfillMarkerVersion`.
  </action>
  <verify>
    <automated>flutter test test/unit/database/trip_breaks_dao_test.dart test/unit/database/user_preferences_dao_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "breaksForTripIds" lib/database/daos/trip_breaks_dao.dart` matches.
    - `grep -n "kMaxBreaksPerTrip\|kBackfillMarkerVersion\|kConflictBreaksDifferTemplate" lib/config/constants.dart` shows all three.
    - `flutter test test/unit/database/trip_breaks_dao_test.dart test/unit/database/user_preferences_dao_test.dart` green.
    - `flutter analyze` clean on all touched files.
  </acceptance_criteria>
  <done>Batch break lookup and the backfill marker DAO methods exist and are tested; every Phase 26 constant is centralized in constants.dart for later plans to import.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| None new | This plan is purely local Drift schema/DAO/constants work — no network, no untrusted input crosses a boundary here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-07 | Tampering | Drift migration (schema v6->v7) | mitigate | Additive-only migration (`addColumn`, no UPDATE/DROP) proven by `SchemaVerifier.migrateAndValidate`, matching the established project convention — prevents accidental data loss on upgrade |
| T-26-08 | Information Disclosure | `backfillMarkerVersion` value | accept | A plain integer counter, not PII-adjacent (unlike `homeLat`/`homeLng`); no logging restriction needed |
</threat_model>

<verification>
- `flutter test test/unit/database/` green after both tasks, including the new migration_v7_test.dart.
- `flutter analyze` reports zero new warnings.
- `dart format --output=none --set-exit-if-changed lib/database/ lib/config/constants.dart` passes (or run `dart format .` before finishing).
</verification>

<success_criteria>
- [ ] Schema is v7; migration is additive-only and SchemaVerifier-proven
- [ ] `TripBreaksDao.breaksForTripIds` exists, batches via a single `IN` query, tested
- [ ] `UserPreferencesDao` marker get/set exist, single-column upsert semantics, tested
- [ ] All Phase 26 client constants centralized in `constants.dart` (no plan after this one needs to touch constants.dart)
</success_criteria>

<output>
After completion, create `.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-02-SUMMARY.md`
</output>
