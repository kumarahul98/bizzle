---
phase: 26-sync-breaks-edit-metadata-to-cloud
plan: 03
type: execute
wave: 2
depends_on: ["26-01", "26-02"]
files_modified:
  - lib/sync/trip_serializer.dart
  - test/unit/sync/trip_serializer_test.dart
  - lib/sync/api_client.dart
  - lib/sync/sync_engine.dart
  - test/unit/sync/sync_engine_test.dart
  - test/unit/sync/restore_controller_test.dart
  - test/sync/restore_controller_test.dart
  - test/widget/features/settings/conflict_resolution_sheet_test.dart
autonomous: true
requirements: []

must_haves:
  truths:
    - "TripSerializer.toJson emits totalPausedSeconds/isEdited/directionSource/breaks for a trip, with breaks sourced from a separately-supplied break list (not a TripRow field)"
    - "TripSerializer.toJson NEVER emits more than kMaxBreaksPerTrip (50) break entries — a >50-break trip is truncated client-side (oldest-first retention) instead of becoming a 400 poison pill in the sync queue"
    - "TripSerializer.fromJson parses the same 4 fields, defaulting cleanly when a server response omits them, and returns the trip companion AND its break companions as one unit"
    - "SyncEngine batch-fetches breaks for a chunk of trips via ONE TripBreaksDao query before serializing, never N per-trip queries"
    - "The full flutter test suite compiles and passes after this plan (no orphaned fixtures left broken by the signature change)"
  artifacts:
    - path: "lib/sync/trip_serializer.dart"
      provides: "toJson(TripRow, List<TripBreakRow>) with kMaxBreaksPerTrip truncation; fromJson(Map) -> ParsedTrip ({trip, breaks} record)"
      exports: ["TripSerializer", "ParsedTrip"]
    - path: "test/unit/sync/trip_serializer_test.dart"
      provides: "Round-trip tests for the 4 new fields + breaks, including empty-breaks, at-cap (kMaxBreaksPerTrip), and >50 truncation cases"
  key_links:
    - from: "lib/sync/sync_engine.dart"
      to: "lib/database/daos/trip_breaks_dao.dart"
      via: "breaksForTripIds(liveTrips.map((t) => t.id).toList()) called ONCE before the chunk loop"
      pattern: "breaksForTripIds"
    - from: "lib/sync/api_client.dart"
      to: "lib/sync/trip_serializer.dart"
      via: "trips.map((t) => TripSerializer.toJson(t, breaksByTripId[t.id] ?? const []))"
      pattern: "TripSerializer\\.toJson\\(t,"
    - from: "lib/sync/trip_serializer.dart"
      to: "lib/config/constants.dart"
      via: "breaks.take(kMaxBreaksPerTrip) truncation at serialization time (client-side mirror of the backend zod .max cap)"
      pattern: "take\\(kMaxBreaksPerTrip\\)"
---

<objective>
Extend the client wire codec (`TripSerializer`), its two call sites (`ApiClient.syncTrips`/`restoreTrips`),
and `SyncEngine._drain`'s batch-fetch-then-serialize path so a trip's breaks + 3 metadata fields flow
to and from the NOW-DEPLOYED backend (Plan 01) exactly as the zod schema expects. This is the plan
where the client starts EMITTING the new fields — it depends on Plan 01's live deploy per roadmap SC2.

Purpose: closes the "denormalize breaks back into the wire payload" gap RESEARCH.md's Pitfall 3
identifies — breaks were deliberately normalized (Phase 18) for per-segment editing, and now need to
ride along in the sync/restore JSON without becoming a `TripRow` field.

Output: `TripSerializer` handles 2 directions x (11 existing fields + 4 new fields + breaks array,
capped client-side at `kMaxBreaksPerTrip`); `ApiClient`/`SyncEngine` are wired to the new signatures;
every existing test file that constructs fixtures against the OLD signatures is fixed so the full
suite compiles and passes green.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-CONTEXT.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-RESEARCH.md
@CLAUDE.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-01-SUMMARY.md
@.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-02-SUMMARY.md

<interfaces>
<!-- Full current files read this session — use directly, no exploration needed. -->

From lib/sync/trip_serializer.dart (full 62-line file):
```dart
static Map<String, dynamic> toJson(TripRow t) => <String, dynamic>{
  'id': t.id, 'startTime': t.startTime.toUtc().toIso8601String(), /* ...9 more... */
};
static TripsCompanion fromJson(Map<String, dynamic> json) => TripsCompanion.insert(
  id: json['id'] as String, /* ...10 more, each Value<T>(...)-wrapped where optional... */
);
```

From lib/sync/api_client.dart (lines 128-193, the two call sites to change):
```dart
Future<void> syncTrips(List<TripRow> trips) async {
  final body = jsonEncode({'trips': trips.map(TripSerializer.toJson).toList()});
  await _send((token) => _client.post(Uri.parse('$_baseUrl$kSyncTripsPath'), headers: _headers(token), body: body));
}
Future<List<TripsCompanion>> restoreTrips() async {
  // ...decode/envelope-unwrap...
  return trips.map((e) => TripSerializer.fromJson(e as Map<String, dynamic>)).toList();
}
```

From lib/sync/sync_engine.dart (lines 199-231, `_drain`'s upsert section — the insertion point for
the batch break-fetch) and lines 43-68 (constructor — needs a `TripBreaksDao` seam) and lines 414-431
(`syncEngineProvider` — production wiring, needs `tripBreaksDao: ref.watch(tripBreaksDaoProvider)`):
```dart
final liveTrips = <TripRow>[];
// ...builds liveTrips from upserts.entries via _tripsDao.findById...
for (var i = 0; i < liveTrips.length; i += kMaxSyncBatchTrips) {
  final chunk = liveTrips.sublist(i, end);
  try {
    await _api.syncTrips(chunk);
    // ...
```

From lib/database/daos/trip_breaks_dao.dart (Plan 02 output — read the actual file, do not assume):
`Future<Map<String, List<TripBreakRow>>> breaksForTripIds(List<String> tripIds)` — returns each
trip's breaks ordered by `startTime` ASCENDING (chronological), which is what makes a simple
`.take(kMaxBreaksPerTrip)` in `toJson` equal "keep the first 50 chronologically".

From lib/config/constants.dart (Plan 02 output): `kMaxBreaksPerTrip = 50` — numerically matches the
backend `kMaxBreaksPerTrip` zod `.max()` cap in `backend/functions/src/utils/validation.ts`.

From test/unit/sync/sync_engine_test.dart (lines 1-100 read this session): `FakeApiClient implements
ApiClient` with `Future<void> syncTrips(List<TripRow> trips) async { syncCalls.add(...); }` and
`Future<List<TripsCompanion>> restoreTrips() async => <TripsCompanion>[];` — BOTH signatures change.
`buildEngine()` constructs `SyncEngine(apiClient: api, syncQueueDao: queueDao, tripsDao: tripsDao,
status: status, isSignedIn: ..., isOnline: ..., now: ...)` — needs a `tripBreaksDao:` argument added.

From test/unit/sync/restore_controller_test.dart (full 280-line file read this session):
`_companion(id, ...) => TripSerializer.fromJson(_tripJson(id, ...));` — this now returns a record,
not a `TripsCompanion` directly. `_FakeApiClient.restoreTrips()` returns `Future<List<TripsCompanion>>`
— must become `Future<List<ParsedTrip>>` (or whatever this plan names the record typedef).

From test/sync/restore_controller_test.dart (the STALE/duplicate copy, lines 1-40 read this session):
`FakeApiClient implements ApiClient` with explicit (non-noSuchMethod-covered) `syncTrips`/`restoreTrips`
signatures — same two methods, same compile-break risk.

From test/widget/features/settings/conflict_resolution_sheet_test.dart (lines 14-52 read this
session): its OWN `_companion(id, ...) => TripSerializer.fromJson(_tripJson(id, ...));` helper —
same compile-break risk as restore_controller_test.dart's.

DO NOT add feature-specific test coverage (D-07/D-10/D-11/merge/breaks-indicator) to the three test
files above in THIS plan — that belongs to Plan 05 (restore_controller) and Plan 06 (merge/UI), which
depend on this plan and will extend these same files further. This plan's job for those 3 files is
ONLY to make them compile and pass again after the signature change (minimal, mechanical fixes).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TripSerializer — 2-arg toJson (with 50-break cap), record-returning fromJson, new round-trip tests</name>
  <read_first>
    lib/sync/trip_serializer.dart
    lib/database/tables/trip_breaks_table.dart
    lib/config/constants.dart
  </read_first>
  <files>lib/sync/trip_serializer.dart, test/unit/sync/trip_serializer_test.dart</files>
  <behavior>
    - `toJson(trip, [])` includes `totalPausedSeconds: trip.totalPausedSeconds, isEdited: trip.isEdited, directionSource: trip.directionSource, breaks: []` alongside the 11 existing keys, byte-for-byte camelCase.
    - `toJson(trip, [break1, break2])` includes a `breaks` array of `{startTime, endTime}` ISO-8601-UTC-string maps, ordered as given.
    - `toJson(trip, [51 breaks ordered by startTime ascending])` emits EXACTLY `kMaxBreaksPerTrip` (50) entries — the FIRST 50 of the supplied list (oldest-first retention, i.e. the first 50 chronologically given the DAO's startTime-ascending ordering); the 51st entry is dropped, no throw (revision issue 6 — client-side mirror of the backend cap).
    - `toJson(trip, [exactly 50 breaks])` emits all 50 unchanged (at-cap boundary, no off-by-one).
    - `fromJson(json)` on a payload WITH all 4 new fields + 2 breaks returns a record whose `.trip` is a `TripsCompanion` with `totalPausedSeconds`/`isEdited`/`directionSource` set via `Value(...)`, and whose `.breaks` is a `List<TripBreaksCompanion>` of length 2 with fresh (non-empty, non-colliding) UUIDs and `tripId` equal to the parsed trip's id.
    - `fromJson(json)` on a payload OMITTING all 4 new keys returns `.trip` with `totalPausedSeconds=0, isEdited=false, directionSource=kDirectionSourceTime` and `.breaks` = `[]` (server-omission default parity with the backend's own defaults).
    - `fromJson` on a payload with exactly `kMaxBreaksPerTrip` (50) break entries parses all 50 without loss.
  </behavior>
  <action>
1. In `lib/sync/trip_serializer.dart`: add `typedef ParsedTrip = ({TripsCompanion trip, List<TripBreaksCompanion> breaks});` at the top level. Change `toJson`'s signature to `static Map<String, dynamic> toJson(TripRow t, List<TripBreakRow> breaks)`. Add 4 keys to the returned map: `'totalPausedSeconds': t.totalPausedSeconds`, `'isEdited': t.isEdited`, `'directionSource': t.directionSource`, `'breaks': breaks.take(kMaxBreaksPerTrip).map((b) => {'startTime': b.startTime.toUtc().toIso8601String(), 'endTime': b.endTime!.toUtc().toIso8601String()}).toList()` — the `!` on `endTime` is safe: `trip_breaks_table.dart`'s own doc guarantees a finalized/persisted trip never carries an open (null-endTime) break. The `.take(kMaxBreaksPerTrip)` truncation (import `kMaxBreaksPerTrip` from `lib/config/constants.dart`, added in Plan 02) is a DEFENSIVE client-side cap mirroring the backend zod `.max(50)`: without it, a >50-break trip would get a 400 from `/trips/sync` — a NON-retryable rejection that permanently poison-pills the trip in the sync queue. Callers supply breaks in startTime-ascending order (both `breaksForTripIds` and `breaksForTrip` order chronologically), so `.take(50)` keeps the FIRST 50 chronologically — oldest-first retention per revision issue 6. Dropping excess break detail beyond 50 is an accepted trade-off (sync stays alive; 50+ breaks in one commute is far outside real usage). Add a doc comment on `toJson` stating exactly this.
2. Change `fromJson`'s return type from `TripsCompanion` to `ParsedTrip`. Keep the existing `TripsCompanion.insert(...)` body but add 3 more fields: `totalPausedSeconds: Value<int>((json['totalPausedSeconds'] as num?)?.toInt() ?? 0)`, `isEdited: Value<bool>(json['isEdited'] as bool? ?? false)`, `directionSource: Value<String>(json['directionSource'] as String? ?? kDirectionSourceTime)` (import `kDirectionSourceTime` from `lib/config/constants.dart`). Parse `final breaksJson = json['breaks'] as List<dynamic>? ?? const [];` then map each entry to `TripBreaksCompanion.insert(id: const Uuid().v4(), tripId: tripId, startTime: DateTime.parse(entry['startTime'] as String).toUtc(), endTime: Value<DateTime>(DateTime.parse(entry['endTime'] as String).toUtc()))` (import `package:uuid/uuid.dart`, mirroring the exact `const Uuid()`/`.v4()` pattern used in `tracking_service_controller.dart:293,297` and `trip_management_providers.dart:141`). Break ids are ALWAYS freshly generated on parse — the wire format carries no break id (roadmap SC1 locks the embedded shape to `{startTime, endTime}` only), so restore/merge can never round-trip an original local break UUID; this is intentional, not a gap. Return `(trip: tripsCompanion, breaks: breakCompanions)`.
3. Create `test/unit/sync/trip_serializer_test.dart` covering every case in `<behavior>` above — including the >50 truncation case (51 in, first-50-chronological out) and the exactly-50 at-cap case. Follow the existing project convention for building UTC `DateTime`/ISO fixtures (see `test/unit/database/trip_breaks_dao_test.dart` for the `DateTime.utc(...)` + `const uuid = Uuid();` idiom).
  </action>
  <verify>
    <automated>flutter test test/unit/sync/trip_serializer_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "typedef ParsedTrip" lib/sync/trip_serializer.dart` matches.
    - `grep -n "take(kMaxBreaksPerTrip)" lib/sync/trip_serializer.dart` matches (client-side truncation present).
    - `flutter test test/unit/sync/trip_serializer_test.dart` green, covering all 7 behaviors listed above as distinct test cases (including >50 truncation and at-cap boundary).
    - `flutter analyze lib/sync/trip_serializer.dart` clean.
  </acceptance_criteria>
  <done>TripSerializer is the single, tested source of truth for the 2-directional wire codec including breaks, with the 50-break cap enforced client-side so sync can never emit a payload the backend would 400; no other file yet depends on the new shape (that's Task 2).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire ApiClient + SyncEngine to the new TripSerializer shape</name>
  <read_first>
    lib/sync/api_client.dart
    lib/sync/sync_engine.dart
    lib/database/providers.dart
    test/unit/sync/sync_engine_test.dart
  </read_first>
  <files>lib/sync/api_client.dart, lib/sync/sync_engine.dart, test/unit/sync/sync_engine_test.dart</files>
  <behavior>
    - `ApiClient.syncTrips(trips, breaksByTripId)` serializes each trip via `TripSerializer.toJson(t, breaksByTripId[t.id] ?? const [])` — a trip with no entry in the map serializes with an empty breaks array (never throws).
    - `ApiClient.restoreTrips()` returns `List<ParsedTrip>` (envelope-unwrap logic unchanged; only the final `.map` call target changes).
    - `SyncEngine._drain` calls `TripBreaksDao.breaksForTripIds` exactly ONCE per drain (not once per chunk, not once per trip) before entering the chunk loop.
    - `syncEngineProvider`'s production wiring passes the real `tripBreaksDaoProvider`-sourced DAO.
  </behavior>
  <action>
1. In `lib/sync/api_client.dart`: change `syncTrips`'s signature to `Future<void> syncTrips(List<TripRow> trips, Map<String, List<TripBreakRow>> breaksByTripId)`. Change the body's tearoff `trips.map(TripSerializer.toJson).toList()` to `trips.map((t) => TripSerializer.toJson(t, breaksByTripId[t.id] ?? const [])).toList()`. Change `restoreTrips`'s return type from `Future<List<TripsCompanion>>` to `Future<List<ParsedTrip>>`; change the final `.map((e) => TripSerializer.fromJson(...))` call's target type accordingly (the call itself is unchanged — only the inferred return type flows through). Keep the malformed-envelope try/catch exactly as-is (a malformed break entry inside one trip's JSON now falls through to the SAME catch-all `SyncException.transport()` — do not add a separate try/catch, per the existing "never a raw exception" contract).
2. In `lib/sync/sync_engine.dart`: add `required TripBreaksDao tripBreaksDao` to the constructor and a `final TripBreaksDao _tripBreaksDao;` field (mirror the existing injected-seam style exactly — every dependency is a named constructor param, never a global read). Import `package:traevy/database/daos/trip_breaks_dao.dart`. In `_drain`, immediately AFTER `liveTrips` is fully built (before the `for (var i = 0; ...)` chunk loop begins), add `final breaksByTripId = await _tripBreaksDao.breaksForTripIds(liveTrips.map((t) => t.id).toList());`. Change the chunk loop's `await _api.syncTrips(chunk)` call to `await _api.syncTrips(chunk, breaksByTripId)`. In the `syncEngineProvider` at the bottom of the file, add `tripBreaksDao: ref.watch(tripBreaksDaoProvider)` to the `SyncEngine(...)` construction (import `package:traevy/database/providers.dart` if not already imported — it is, for `tripsDaoProvider`).
3. In `test/unit/sync/sync_engine_test.dart`: update `FakeApiClient.syncTrips` to the new 2-arg signature (record the `breaksByTripId` argument too, e.g. `final List<Map<String, List<String>>> syncBreaksCalls` or simplest: just accept and ignore the param if no existing test asserts on it — but DO add at least one new test asserting the engine actually passes a non-empty `breaksByTripId` map through when a queued trip has break rows in the test DB). Update `FakeApiClient.restoreTrips` return type to `Future<List<ParsedTrip>> async => <ParsedTrip>[];`. Update `buildEngine()` to pass `tripBreaksDao: db.tripBreaksDao` (the test already has a real in-memory `AppDatabase db` per the existing `setUp`). Run the full file after the edit and fix any other construction site referencing the old signatures.
  </action>
  <verify>
    <automated>flutter test test/unit/sync/sync_engine_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "breaksForTripIds" lib/sync/sync_engine.dart` shows exactly one call site inside `_drain`, positioned before the chunk loop.
    - `flutter test test/unit/sync/sync_engine_test.dart` green, including at least one new test proving breaks are batch-fetched and passed to `syncTrips`.
    - `flutter analyze lib/sync/` clean.
  </acceptance_criteria>
  <done>SyncEngine emits breaks + metadata to the (now-live, per Plan 01) backend via one batch DB read per drain; ApiClient's two methods match TripSerializer's new shape.</done>
</task>

<task type="auto">
  <name>Task 3: Compile-fix ripple in dependent test fixtures (no new feature coverage)</name>
  <read_first>
    test/unit/sync/restore_controller_test.dart
    test/sync/restore_controller_test.dart
    test/widget/features/settings/conflict_resolution_sheet_test.dart
  </read_first>
  <files>test/unit/sync/restore_controller_test.dart, test/sync/restore_controller_test.dart, test/widget/features/settings/conflict_resolution_sheet_test.dart</files>
  <action>
1. In `test/unit/sync/restore_controller_test.dart`: change the `_companion(...)` helper to `TripSerializer.fromJson(_tripJson(...)).trip` (extract just the trip companion — this file's existing tests only assert on trip-level fields, not breaks; Plan 05 adds breaks-specific cases later). Change `_FakeApiClient.restoreTrips()`'s return type to `Future<List<ParsedTrip>>` and its body to wrap each companion: `return _companions.map((c) => (trip: c, breaks: const <TripBreaksCompanion>[])).toList();` — OR simplify by changing the stored field itself from `List<TripsCompanion>` to `List<ParsedTrip>` and adjusting the constructor call sites minimally; pick whichever keeps the diff smallest while compiling. Do NOT add new test cases in this task.
2. In `test/sync/restore_controller_test.dart`: first run `flutter test test/sync/restore_controller_test.dart` AS-IS (before editing) to confirm it currently compiles/passes against pre-Phase-26 code — if it does NOT even compile/pass today, note that in the summary and skip further fixture work here (true dead code, flag for Plan 05's Pitfall-5 cleanup task instead of touching it now). If it DOES currently pass, apply the SAME two fixes as step 1 (`FakeApiClient.restoreTrips`/`syncTrips` signatures) so it keeps compiling and passing — do not delete it in this task; deletion-or-fix is Plan 05's explicit job (RESEARCH.md Pitfall 5).
3. In `test/widget/features/settings/conflict_resolution_sheet_test.dart`: change its own `_companion(...)` helper the same way as step 1 (`TripSerializer.fromJson(...).trip`). Do not touch anything else in this file — its `SameUuidConflict(localTrip: ..., cloudTrip: ...)` call sites keep working unchanged as long as any new `RestoreConflict` fields Plan 05 introduces later default safely (that is Plan 05's concern, not this task's).
  </action>
  <verify>
    <automated>flutter test test/unit/sync/ test/widget/features/settings/conflict_resolution_sheet_test.dart test/sync/restore_controller_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/unit/sync/ test/widget/features/settings/conflict_resolution_sheet_test.dart` passes with ZERO new failures compared to before this plan.
    - `test/sync/restore_controller_test.dart` either still compiles+passes (fixed) or is explicitly documented in the SUMMARY as pre-existing dead code deferred to Plan 05 — it must not be silently left in a BROKEN (non-compiling) state either way.
    - `flutter test` (full suite) compiles with zero analyzer errors across the whole `test/` tree.
  </acceptance_criteria>
  <done>The TripSerializer/ApiClient signature change from Tasks 1-2 does not leave any test file in a non-compiling state; the full suite is green going into Wave 3.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> `POST /trips/sync` (outbound) | This plan is where the client STARTS emitting the 4 new fields against the now-live (Plan 01) backend |
| server -> client (`GET /trips/restore` response, inbound) | `fromJson`'s parsing of a server-supplied `breaks` array is untrusted-in-principle (a compromised/buggy server response) even though same-origin |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-26-09 | Tampering / DoS (poison payload) | `TripSerializer.fromJson` parsing a malformed `breaks` entry | mitigate | A cast/parse failure inside the `breaks` map (e.g. bad ISO string) propagates up through `ApiClient.restoreTrips`'s existing catch-all (`on Object { throw const SyncException.transport(); }`) — no raw exception escapes to the UI, matching the pre-existing contract for the other 11 fields |
| T-26-10 | Denial of Service (self, availability) | `SyncEngine._drain` batch break fetch | accept | `breaksForTripIds` is a single local SQLite `IN` query bounded by `kMaxSyncBatchTrips` (1000) trip ids per drain — not a network call, no external DoS surface; already covered under Don't-Hand-Roll (Plan 02) |
| T-26-18 | Denial of Service (self, sync-queue poison pill) | `TripSerializer.toJson` emitting a >50-entry `breaks` array | mitigate | `.take(kMaxBreaksPerTrip)` truncation at serialization time (Task 1) guarantees the client can never emit a payload the backend zod `.max(50)` would 400 — a 400 is non-retryable and would otherwise leave the trip permanently stuck in the sync queue; oldest-first retention drops only excess break detail, never the trip itself (revision issue 6) |
</threat_model>

<verification>
- `flutter test test/unit/sync/trip_serializer_test.dart` green after Task 1, including the >50 truncation and at-cap boundary cases.
- `flutter test test/unit/sync/sync_engine_test.dart` green after Task 2, including a new breaks-batch-fetch assertion.
- `flutter test` (full suite) compiles with zero analyzer errors and zero new failures after Task 3.
- `flutter analyze` clean on every touched file.
</verification>

<success_criteria>
- [ ] `TripSerializer.toJson`/`fromJson` handle 4 new fields + breaks in both directions, tested in isolation
- [ ] `toJson` truncates to `kMaxBreaksPerTrip` (oldest-first) so a >50-break trip can never become a 400 poison pill; proven by a unit test
- [ ] `ApiClient.syncTrips`/`restoreTrips` match the new shapes
- [ ] `SyncEngine._drain` batch-fetches breaks ONCE per drain via `TripBreaksDao.breaksForTripIds`
- [ ] Full `flutter test` suite compiles and passes with zero regressions after the signature-ripple fixes
</success_criteria>

<output>
After completion, create `.planning/phases/26-sync-breaks-edit-metadata-to-cloud/26-03-SUMMARY.md`
</output>
