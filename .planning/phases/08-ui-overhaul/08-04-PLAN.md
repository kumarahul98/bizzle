---
phase: 08-ui-overhaul
plan: 04
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - lib/features/shell/screens/main_shell.dart
  - lib/features/shell/providers/main_shell_provider.dart
  - lib/app.dart
  - lib/config/routes.dart
  - lib/features/dashboard/screens/dashboard_screen.dart
  - lib/features/dashboard/widgets/home_header.dart
  - lib/features/dashboard/widgets/hero_record_card.dart
  - lib/features/dashboard/widgets/today_section.dart
  - lib/features/dashboard/widgets/week_loss_card.dart
  - lib/features/dashboard/widgets/empty_slot_row.dart
  - lib/features/dashboard/widgets/in_progress_card.dart
  - lib/features/dashboard/widgets/today_trips_section.dart
  - lib/features/dashboard/widgets/weekly_summary_card.dart
  - lib/features/dashboard/providers/dashboard_providers.dart
  - lib/features/trips/providers/history_providers.dart
  - lib/features/stats/providers/stats_providers.dart
  - lib/features/settings/providers/settings_providers.dart
  - .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md
  - test/widget/features/dashboard/dashboard_screen_test.dart
  - test/widget/features/shell/main_shell_test.dart
  - test/widget/app_test.dart
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "A provider disposal audit document (08-04-PROVIDER-AUDIT.md) is committed listing every provider consumed by the four tab screens, its disposal mode, and the decision (remove .autoDispose / keep + keepAlive / keep as-is) — Review HIGH #1"
    - "No provider consumed by DashboardScreen, HistoryScreen, StatsScreen, or SettingsScreen retains `.autoDispose` if it owns a stream-from-Drift, side-effect, or expensive computation that must persist across tab switches"
    - "Bottom NavigationBar with exactly four destinations (Today, Trips, Stats, Settings) is present at the app root"
    - "Selecting a destination switches the IndexedStack child without unmounting the previous tab (tab state survives switch)"
    - "Dashboard renders the home header (date+greeting+avatar), hero record card (124dp circular START button), today section (TODAY label + trip count + bgElev card with TripRowCards and empty slot row), and week loss card (You lost X to traffic this week)"
    - "Tapping the hero record card START button still runs the existing permission-check flow and pushes the tracking route"
    - "The 'See stats →' link on the week loss card switches the shell's tab index to 2 (Stats) instead of pushing a route"
    - "System back-button from a tab (e.g. Stats reached via tab switch) does NOT pop to Dashboard — it exits the app (correct Android bottom-nav behaviour). This is asserted by a widget test. — Review MEDIUM #4"
    - "dashboard_screen_test.dart updates remove the obsolete AppBar icon assertions and add NavigationBar assertions"
    - "app_test.dart smoke test mounts MainShell as home and verifies the four destinations are visible"
  artifacts:
    - path: .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md
      provides: written audit of every tab-screen provider's disposal mode + decision (Review HIGH #1)
      contains: "autoDispose"
    - path: lib/features/shell/screens/main_shell.dart
      provides: MainShell ConsumerWidget with NavigationBar + IndexedStack
      contains: "class MainShell extends ConsumerWidget"
    - path: lib/features/shell/providers/main_shell_provider.dart
      provides: mainShellIndexProvider (Riverpod Notifier<int>)
      contains: "mainShellIndexProvider"
    - path: lib/app.dart
      provides: MaterialApp with home: MainShell
      contains: "home: const MainShell"
    - path: lib/features/dashboard/widgets/hero_record_card.dart
      provides: HeroRecordCard (replaces the FAB)
      contains: "class HeroRecordCard"
    - path: lib/features/dashboard/widgets/home_header.dart
      provides: HomeHeader (date + greeting + avatar)
      contains: "class HomeHeader"
    - path: lib/features/dashboard/widgets/week_loss_card.dart
      provides: WeekLossCard (You lost ... to traffic this week)
      contains: "class WeekLossCard"
    - path: lib/features/dashboard/widgets/today_section.dart
      provides: TodaySection (TODAY label + trips + empty slot row)
      contains: "class TodaySection"
    - path: lib/features/dashboard/widgets/empty_slot_row.dart
      provides: EmptySlotRow (dashed circle placeholder)
      contains: "class EmptySlotRow"
  key_links:
    - from: lib/features/shell/screens/main_shell.dart NavigationBar
      to: lib/features/shell/providers/main_shell_provider.dart mainShellIndexProvider
      via: ref.watch + ref.read(mainShellIndexProvider.notifier).setIndex
      pattern: "mainShellIndexProvider"
    - from: lib/features/dashboard/widgets/hero_record_card.dart onStart callback
      to: lib/features/dashboard/screens/dashboard_screen.dart _handleStart
      via: HeroRecordCard(onStart: () => _handleStart(context, ref))
      pattern: "_handleStart"
    - from: lib/features/dashboard/widgets/week_loss_card.dart "See stats →" link
      to: lib/features/shell/providers/main_shell_provider.dart mainShellIndexProvider
      via: ref.read(mainShellIndexProvider.notifier).setIndex(2)
      pattern: "setIndex(2)"
---

<objective>
Introduce `MainShell` — a Riverpod-driven 4-tab `NavigationBar` shell — as the new app home, restyle the Dashboard to the Traevy spec (HomeHeader + HeroRecordCard + TodaySection + WeekLossCard), wire `app.dart` to mount MainShell, and update the existing `dashboard_screen_test.dart` to drop the obsolete AppBar icon assertions and add NavigationBar assertions (Pitfall 7). The hero record card replaces the legacy FAB but preserves the existing `_handleStart` permission-handler logic via a callback (per RESEARCH.md Pattern 4 and Open Question 3). The week-loss card's "See stats →" link switches the shell tab index to 2 rather than pushing a route (per Assumption A5/A6).

Purpose: Bring the app's outermost layout to the Traevy design. After this plan, the running app shows the new navigation pattern even before the other screens are restyled — they simply render with the new theme (Plan 02) inside the IndexedStack.

Output: New `lib/features/shell/` directory with MainShell + provider; restyled Dashboard with 5 sub-widgets; legacy widgets (in_progress_card, today_trips_section, weekly_summary_card) restyled/deleted as appropriate; tests updated; smoke test renders the new shell.

Review-incorporated changes (vs original Plan 04):
- **HIGH #1 — Provider disposal audit (Review HIGH #1):** Before any UI changes, this plan now produces a written audit at `.planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md` covering every provider consumed by DashboardScreen, HistoryScreen, StatsScreen, and SettingsScreen. Any `.autoDispose` providers whose state must persist across tab switches (because IndexedStack keeps all four tabs simultaneously mounted) are either (a) un-disposed by removing `.autoDispose`, (b) explicitly opted-in to `ref.keepAlive()` inside their build body, or (c) documented as safe-to-redispose with rationale.
- **MEDIUM #4 — Back-button behaviour (Review MEDIUM #4):** Acceptance criteria now require a widget test that the system back button pressed on a non-dashboard tab does NOT pop the navigator (because the tab switch was a state change, not a route push). This is the correct Android bottom-nav behaviour — back from Stats reached via tab switch exits the app or goes home; it does not go back to Dashboard.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/CLAUDE.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-CONTEXT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-UI-SPEC.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-RESEARCH.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-REVIEWS.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-02-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-03-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/lib/app.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/routes.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/constants.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme_extension.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/dashboard/screens/dashboard_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/dashboard/widgets/in_progress_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/dashboard/widgets/today_trips_section.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/dashboard/widgets/weekly_summary_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/dashboard/providers/dashboard_providers.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/trips/providers/history_providers.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/providers/stats_providers.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/settings/providers/settings_providers.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/trips/screens/history_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/screens/stats_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/settings/screens/settings_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/trip_row_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/section_label.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/stuck_bar.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/features/dashboard/dashboard_screen_test.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/features/shell/main_shell_test.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/app_test.dart

<interfaces>
<!-- Existing providers in lib/features/dashboard/providers/dashboard_providers.dart that this plan consumes: -->
//   - todaysTripSummariesProvider :: StreamProvider<List<TripSummary>>
//   - weeklySummaryProvider       :: StreamProvider<WeeklySummary>  (provides movingMinutes + stuckMinutes)
//   - inProgressTripProvider      :: Provider<TrackingActiveState?>  (already used by InProgressCard)
//   - userPreferenceProvider      :: StreamProvider<UserPreferences>

// Existing routes (lib/config/routes.dart):
//   kRouteTracking, kRouteHistory, kRouteStats, kRouteSettings, kRouteTripDetail
// This plan ADDS:
//   const String kRouteOnboarding = '/onboarding';   (registered in kAppRoutes; screen is added in Plan 07)
//   const String kRouteMainShell  = '/';             (new logical name — but app.dart still uses `home:` not the routes map)

// New shell provider:
//   final mainShellIndexProvider = NotifierProvider<MainShellIndexNotifier, int>(MainShellIndexNotifier.new);
//   class MainShellIndexNotifier extends Notifier<int> {
//     @override int build() => 0;
//     void setIndex(int i) => state = i;
//   }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 0: Provider disposal audit (Review HIGH #1) — MUST run before Tasks 1–3</name>
  <files>.planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md, lib/features/dashboard/providers/dashboard_providers.dart, lib/features/trips/providers/history_providers.dart, lib/features/stats/providers/stats_providers.dart, lib/features/settings/providers/settings_providers.dart</files>
  <read_first>
    - lib/features/dashboard/screens/dashboard_screen.dart (full file — list every `ref.watch(...)` / `ref.read(...)` call)
    - lib/features/dashboard/providers/dashboard_providers.dart (full file — capture every provider declaration and its modifier)
    - lib/features/trips/screens/history_screen.dart (full file — list every `ref.watch(...)` / `ref.read(...)` call)
    - lib/features/trips/providers/history_providers.dart if it exists (otherwise grep `lib/features/trips/` for `riverpod` provider declarations)
    - lib/features/stats/screens/stats_screen.dart (full file — list every `ref.watch(...)` / `ref.read(...)` call)
    - lib/features/stats/providers/stats_providers.dart (full file)
    - lib/features/settings/screens/settings_screen.dart (full file — list every `ref.watch(...)` / `ref.read(...)` call)
    - lib/features/settings/providers/settings_providers.dart if it exists (otherwise grep for the relevant provider declarations)
    - lib/features/auth/providers/*.dart, lib/features/tracking/providers/*.dart (any provider transitively consumed by the four tab screens)
    - .planning/phases/08-ui-overhaul/08-REVIEWS.md (Review HIGH #1 — full statement of the IndexedStack disposal problem and the four enumerated provider examples: todaysTripSummariesProvider, statsSummaryProvider, historyGroupsProvider, userPreferenceProvider)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Pattern 3 + Example 4 (IndexedStack lifecycle implications)
  </read_first>
  <behavior>
    - A new audit document is committed at `.planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md` containing one row per provider consumed (directly or transitively) by the four tab screens, with columns:
        | Provider | File | Type (Provider/StreamProvider/NotifierProvider/etc.) | autoDispose? (yes/no) | Used by tabs | Stream/side-effect? | Decision | Rationale |
    - For every provider where the audit identifies a defect (autoDispose + must-persist-across-tabs), the corresponding provider declaration is fixed:
        - **Decision A — Remove `.autoDispose`:** Strip the `.autoDispose` modifier from the provider declaration. Add a `///` doc comment line citing "Phase 8 IndexedStack — must persist across tab switches (Review HIGH #1)".
        - **Decision B — Keep `.autoDispose` + `ref.keepAlive()` inside build:** Inside the provider's build body, call `final link = ref.keepAlive();` so the provider survives even when watchers detach (the IndexedStack switch doesn't actually unsubscribe — both tabs remain mounted — but defensive-keepAlive guards against future refactors). Add a `///` doc comment citing the same.
        - **Decision C — Keep `.autoDispose` as-is (safe-to-redispose):** Provider produces a pure derived computation cheap to rebuild (e.g. selector over already-cached data). Audit row must explain why redisposal is harmless.
    - The audit document explicitly lists the four providers called out by the reviewer — `todaysTripSummariesProvider`, `statsSummaryProvider`, `historyGroupsProvider` (or its actual name in this codebase), `userPreferenceProvider` — and states the decision applied to each.
    - If a reviewer-named provider does NOT exist in the codebase under the listed name, the audit document records the actual name found in the codebase and notes "renamed from {reviewer's name}".
    - No silent skips: every provider consumed by the four tabs is listed in the audit table.
    - After fixes, all existing widget/unit tests (`flutter test`) still pass — provider behaviour is preserved end-to-end.
  </behavior>
  <action>
    1. Enumerate every Riverpod provider consumed by the four tab screens. Use:
       ```bash
       grep -nE "ref\.(watch|read|listen)\(" lib/features/dashboard/screens/dashboard_screen.dart lib/features/trips/screens/history_screen.dart lib/features/stats/screens/stats_screen.dart lib/features/settings/screens/settings_screen.dart
       ```
       Then for each provider call, trace it back to its declaration file. Also grep for transitive providers used inside the widgets in each tab's `widgets/` directory.

    2. For every provider found, capture in a working list:
       - Symbol name (e.g. `todaysTripSummariesProvider`)
       - Declaration file path
       - Provider kind (`Provider`, `StreamProvider`, `FutureProvider`, `NotifierProvider`, `AsyncNotifierProvider`, etc.)
       - Whether `.autoDispose` is present
       - Whether the build body opens a stream/listener (Drift watch, controller subscription, timer)

    3. Apply the decision matrix:
       - **Has `.autoDispose` AND opens a stream/listener AND consumed by ≥ 1 tab:** → Decision A (remove `.autoDispose`). Rationale: IndexedStack keeps every tab mounted, so the stream would never be unsubscribed in practice; explicit removal documents intent and prevents accidental future restoration of autoDispose-with-stream.
       - **Has `.autoDispose` AND opens a stream/listener AND consumed by only ONE tab (no cross-tab visibility):** → Decision A still preferred; tabs are simultaneously mounted, so semantic "only one tab uses it" doesn't reduce IndexedStack overhead.
       - **Has `.autoDispose` AND is a pure derived/cheap computation:** → Decision C (keep as-is). Rationale: redisposal is harmless and cheap; the `.autoDispose` annotation is documentation of intent.
       - **NO `.autoDispose` (default kept-alive):** → No code change. Document the provider in the audit table.

    4. Create `.planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md` with the following sections:
       - Header: `# Provider Disposal Audit — Phase 8 MainShell (Review HIGH #1)` plus a 2-line summary.
       - Section "Audit Methodology": brief description of the grep traversal + decision matrix.
       - Section "Audit Results": the full table (one row per provider) with the eight columns listed above.
       - Section "Reviewer-Called-Out Providers": four bullet entries, one for each provider in the review (`todaysTripSummariesProvider`, `statsSummaryProvider`, `historyGroupsProvider`, `userPreferenceProvider`). Each entry: actual symbol name found, decision applied, link to row in the table.
       - Section "Code Changes": list of files edited and what was changed (e.g. "`lib/features/stats/providers/stats_providers.dart` — removed `.autoDispose` from `statsSummaryProvider` (Decision A)").

    5. Apply Decision A / B fixes to the provider declaration files. Each provider that is modified gets:
       - The `.autoDispose` modifier removed (or `ref.keepAlive()` inserted for Decision B).
       - A `///` doc comment line referencing "Phase 8 IndexedStack — Review HIGH #1".

    6. Run `dart format` on every modified provider file. Run `flutter analyze` on the four provider files (and any code-generated `.g.dart` siblings if Riverpod's annotation generator is in use; if so run `dart run build_runner build --delete-conflicting-outputs` first).

    7. Run the full widget test suite (`flutter test`) — every existing test must still pass. If any test breaks because it asserted on the autoDispose-disposed state, update that test (and document the change in the audit's "Code Changes" section).

    8. Do NOT yet create `MainShell` or refactor the dashboard — those are Tasks 1–3 of this plan and depend on the audit being complete.
  </action>
  <verify>
    <automated>test -f .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md &amp;&amp; grep -q "todaysTripSummariesProvider" .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md &amp;&amp; grep -q "statsSummaryProvider" .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md &amp;&amp; grep -qE "(historyGroupsProvider|tripsByDayProvider|historyProvider)" .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md &amp;&amp; grep -q "userPreferenceProvider" .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md &amp;&amp; grep -qE "Decision (A|B|C)" .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md &amp;&amp; flutter analyze lib/features/dashboard/providers/ lib/features/trips/providers/ lib/features/stats/providers/ lib/features/settings/providers/ 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md` exists.
    - The audit document contains a table row (with all 8 columns populated) for EVERY provider consumed by the four tab screens — grep-verifiable count: `grep -c '^|' 08-04-PROVIDER-AUDIT.md` returns at least the number of providers found by the enumeration in step 1 of <action>.
    - The audit document explicitly references each of the four reviewer-called-out providers: `todaysTripSummariesProvider`, `statsSummaryProvider`, the history-tab equivalent of `historyGroupsProvider` (record the actual name if different), and `userPreferenceProvider`.
    - Each row in the audit table has a non-empty "Decision" cell containing "A", "B", or "C".
    - Every provider receiving Decision A no longer contains `.autoDispose` in its declaration (verified by `grep -L "\.autoDispose" {file}` for each Decision-A row).
    - Every provider receiving Decision B has a `ref.keepAlive()` call in its build body (verified by grep).
    - Each modified provider declaration carries a `///` doc comment line containing the string "Phase 8 IndexedStack" or "Review HIGH #1".
    - `flutter analyze` over `lib/features/{dashboard,trips,stats,settings}/providers/` exits with 0 issues.
    - `flutter test` (full suite) exits 0 — no existing tests broken by the audit fixes.
    - Audit document includes a "Code Changes" section enumerating each file edited with a one-line summary.
  </acceptance_criteria>
  <done>
    Every provider consumed by the four tab screens has been audited. Defects (autoDispose providers that must persist across IndexedStack tab switches) are fixed in code. The audit document is committed for future reference. Full test suite still passes. Tasks 1–3 of this plan can now proceed knowing the provider layer is IndexedStack-safe. Review HIGH #1 satisfied.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 1: MainShell + provider + app.dart wire + routes.dart additions</name>
  <files>lib/features/shell/screens/main_shell.dart, lib/features/shell/providers/main_shell_provider.dart, lib/app.dart, lib/config/routes.dart, test/widget/features/shell/main_shell_test.dart, test/widget/app_test.dart</files>
  <read_first>
    - lib/app.dart (full file)
    - lib/config/routes.dart (full file)
    - lib/features/dashboard/screens/dashboard_screen.dart (existing top-level dashboard)
    - lib/features/trips/screens/history_screen.dart, lib/features/stats/screens/stats_screen.dart, lib/features/settings/screens/settings_screen.dart (the IndexedStack children — read constructors only)
    - .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md (the audit from Task 0 — confirm no Decision-A fixes were missed before mounting the IndexedStack)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Example 4 MainShell + Pattern 3
    - .planning/phases/08-ui-overhaul/08-REVIEWS.md (Review MEDIUM #4 — back-button on tab switch must NOT pop)
    - test/widget/features/shell/main_shell_test.dart (Plan 01 RED test — your MainShell must satisfy)
    - test/widget/app_test.dart (existing smoke test — must be updated to use MainShell)
  </read_first>
  <behavior>
    - `lib/features/shell/providers/main_shell_provider.dart`:
      ```dart
      final mainShellIndexProvider = NotifierProvider<MainShellIndexNotifier, int>(MainShellIndexNotifier.new);
      class MainShellIndexNotifier extends Notifier<int> {
        @override int build() =&gt; 0;
        void setIndex(int i) { if (i &gt;= 0 &amp;&amp; i &lt; 4) state = i; }
      }
      ```
    - `lib/features/shell/screens/main_shell.dart`:
      ```dart
      class MainShell extends ConsumerWidget {
        const MainShell({super.key});
        @override
        Widget build(BuildContext context, WidgetRef ref) {
          final index = ref.watch(mainShellIndexProvider);
          return Scaffold(
            body: IndexedStack(
              index: index,
              children: const <Widget>[
                DashboardScreen(),
                HistoryScreen(),
                StatsScreen(),
                SettingsScreen(),
              ],
            ),
            bottomNavigationBar: NavigationBar(
              selectedIndex: index,
              onDestinationSelected: ref.read(mainShellIndexProvider.notifier).setIndex,
              destinations: const <NavigationDestination>[
                NavigationDestination(icon: Icon(Icons.home_outlined),       selectedIcon: Icon(Icons.home),       label: 'Today'),
                NavigationDestination(icon: Icon(Icons.list_alt_outlined),   selectedIcon: Icon(Icons.list_alt),   label: 'Trips'),
                NavigationDestination(icon: Icon(Icons.bar_chart_outlined),  selectedIcon: Icon(Icons.bar_chart),  label: 'Stats'),
                NavigationDestination(icon: Icon(Icons.settings_outlined),   selectedIcon: Icon(Icons.settings),   label: 'Settings'),
              ],
            ),
          );
        }
      }
      ```
    - **Back-button behaviour (Review MEDIUM #4):** Because `MainShell` uses a single `Scaffold` + `IndexedStack` (tab switches are state updates, not route pushes), the navigator stack contains only the MainShell route. Pressing the system back button from any tab pops the navigator — and since MainShell is the root, the OS treats that as "exit the app" (or `moveTaskToBack`, depending on Android version). This is the correct bottom-nav behaviour: back from Stats does NOT navigate back to Dashboard. No additional `WillPopScope`/`PopScope` wrapper is needed; the default behaviour is correct. A widget test in Task 3 explicitly asserts this.
    - `lib/app.dart`:
        - import `package:traevy/features/shell/screens/main_shell.dart`.
        - delete the `import 'package:traevy/features/dashboard/screens/dashboard_screen.dart';` line (DashboardScreen is now referenced only inside MainShell).
        - change `home: const DashboardScreen()` → `home: const MainShell()`.
        - keep all other behaviour (themeMode wiring, directionBackfillProvider watch).
    - `lib/config/routes.dart`:
        - add `const String kRouteOnboarding = '/onboarding';` near the other route constants. Do NOT register it in `kAppRoutes` yet (Plan 07 will register the screen builder when OnboardingScreen exists). Reserve the name only.
        - do NOT change any existing routes.
    - The Plan-01 RED test `test/widget/features/shell/main_shell_test.dart` passes GREEN.
    - `test/widget/app_test.dart` smoke test updated to expect MainShell instead of DashboardScreen.
  </behavior>
  <action>
    1. Create the `lib/features/shell/screens/` and `lib/features/shell/providers/` directories.
    2. Create `lib/features/shell/providers/main_shell_provider.dart` with the Notifier exactly as shown in <behavior>. Add `///` doc comments referencing UI-SPEC.md §2 and CLAUDE.md "Riverpod for all state".
    3. Create `lib/features/shell/screens/main_shell.dart` with the ConsumerWidget exactly as shown in <behavior>. Import the four screens. Add a `///` doc comment block above the class explaining:
        - Why IndexedStack is chosen (preserves state of all four tabs simultaneously — Pattern 3).
        - Why no `PopScope`/`WillPopScope` is wrapped (Review MEDIUM #4 — back-button from a tab must exit the app, not pop to dashboard, which is the default behaviour of a single-route MaterialApp).
        - Reference to `.planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md` for the IndexedStack-safety guarantee on consumed providers.
    4. Update `lib/app.dart`:
        - Replace the `DashboardScreen` import with `package:traevy/features/shell/screens/main_shell.dart`.
        - Replace `home: const DashboardScreen()` with `home: const MainShell()`.
        - No other changes — preserve `directionBackfillProvider` watch, theme wiring, route map.
    5. Update `lib/config/routes.dart`:
        - Add `/// Onboarding screen route (D-08; screen registered in Plan 07).` then `const String kRouteOnboarding = '/onboarding';` after the other route constants.
        - Leave `kAppRoutes` unchanged (registration happens in Plan 07).
    6. Update `test/widget/app_test.dart`:
        - Replace any `find.byType(DashboardScreen)` smoke check with `find.byType(MainShell)`.
        - Add `expect(find.byType(NavigationBar), findsOneWidget);` and `expect(find.text('Today'), findsOneWidget);`.
        - Ensure all four destination labels resolve.
    7. Run `dart format` and `flutter analyze` over all six modified files. Expect zero warnings.
    8. Run `flutter test test/widget/features/shell/main_shell_test.dart test/widget/app_test.dart` — both must pass.
    9. Run `flutter test test/widget/features/dashboard/dashboard_screen_test.dart` — it will fail because the existing icon-locator assertions reference AppBar action icons that no longer exist on the DashboardScreen now that those icons live on the bottom NavigationBar. The fix is in Task 3 of this plan. For this task, simply verify the failure is exactly the "find.byIcon(Icons.history)" / "find.byIcon(Icons.bar_chart)" / "find.byTooltip(kSettingsTooltip)" line numbers from Pitfall 7 — no other tests should break.
  </action>
  <verify>
    <automated>flutter analyze lib/features/shell/ lib/app.dart lib/config/routes.dart 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/features/shell/main_shell_test.dart test/widget/app_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/shell/screens/main_shell.dart` declares `class MainShell extends ConsumerWidget`.
    - `lib/features/shell/providers/main_shell_provider.dart` declares `final mainShellIndexProvider = NotifierProvider&lt;MainShellIndexNotifier, int&gt;(...)` and `class MainShellIndexNotifier extends Notifier&lt;int&gt;` with `setIndex(int i)`.
    - `lib/app.dart` line matches: `home: const MainShell(),` and does NOT import DashboardScreen.
    - `lib/config/routes.dart` declares `const String kRouteOnboarding = '/onboarding';`.
    - `flutter test test/widget/features/shell/main_shell_test.dart` exits 0 — the Plan 01 RED main_shell test turns GREEN.
    - `flutter test test/widget/app_test.dart` exits 0.
    - `flutter analyze` exits 0 over the modified files.
    - `wc -l lib/features/shell/screens/main_shell.dart` &lt; 80.
    - `lib/features/shell/screens/main_shell.dart` does NOT wrap MainShell in `PopScope` or `WillPopScope` (verified by `! grep -E "PopScope|WillPopScope" lib/features/shell/screens/main_shell.dart`) — the default back-button behaviour is correct for a bottom-nav app (Review MEDIUM #4).
    - Task 0 audit document exists and was committed before this task ran (`test -f .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md`).
  </acceptance_criteria>
  <done>
    MainShell is the app home; tab index is owned by a Riverpod Notifier; routes.dart reserves kRouteOnboarding; the Plan-01 main_shell RED test turns GREEN; app_test smoke passes. Back-button behaviour is the OS default (exit-app from any tab) — no wrapper added (Review MEDIUM #4).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Dashboard restyle — HomeHeader + HeroRecordCard + TodaySection + EmptySlotRow + WeekLossCard</name>
  <files>lib/features/dashboard/widgets/home_header.dart, lib/features/dashboard/widgets/hero_record_card.dart, lib/features/dashboard/widgets/today_section.dart, lib/features/dashboard/widgets/empty_slot_row.dart, lib/features/dashboard/widgets/week_loss_card.dart, lib/features/dashboard/widgets/in_progress_card.dart, lib/features/dashboard/widgets/today_trips_section.dart, lib/features/dashboard/widgets/weekly_summary_card.dart, lib/features/dashboard/screens/dashboard_screen.dart</files>
  <read_first>
    - lib/features/dashboard/screens/dashboard_screen.dart (full file — _handleStart and _showSettingsDialog logic MUST be preserved)
    - lib/features/dashboard/widgets/weekly_summary_card.dart (current shape — to be REPLACED by WeekLossCard)
    - lib/features/dashboard/widgets/today_trips_section.dart (current shape — to be REPLACED by TodaySection)
    - lib/features/dashboard/widgets/in_progress_card.dart (Pitfall 4 — radius 12 → must update to 16; also Pitfall 9 — surfaceContainerLow → tokens.bgElev)
    - lib/features/dashboard/providers/dashboard_providers.dart (todaysTripSummariesProvider, weeklySummaryProvider, inProgressTripProvider — confirm the Task-0 audit has applied any required `.autoDispose` removal)
    - .planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md (Task 0 audit — re-check decisions before consuming providers in widgets)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §3 Home / Dashboard Screen (ASCII layout + token-by-token spec)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Pattern 4 Hero Record Card + Open Question 3 (preserve _handleStart on DashboardScreen)
    - lib/shared/widgets/trip_row_card.dart, section_label.dart, stuck_bar.dart (the primitives Plan 03 provided)
  </read_first>
  <behavior>
    - `HomeHeader` (≤ 80 lines):
        - Renders: `Row(Column(Text('Mon · 28 Apr' — formatted now() via DateFormat.MMMd() + weekday abbrev, style: TraevyFonts.ui(size: 11, weight: w600, letterSpacing: 1.0, color: tokens.textMuted), uppercase), 4dp gap, Text('Hi, $kPlaceholderUserName', style: textTheme.titleLarge)), Spacer, Container(36×36 circle, surface bg, child: Text(kPlaceholderUserInitial, TraevyFonts.ui(size: 14, w700, color: text))))`.
        - Padding EdgeInsets.fromLTRB(20, 16, 20, 12).
    - `HeroRecordCard` (≤ 100 lines):
        - Props: `({required bool isTracking, required VoidCallback onStart, String? autoLabelDirection, String? autoLabelTime})`.
        - Renders a `Card` (the global CardTheme provides bgElev + radius 16 + border; here override shape to BorderRadius.circular(24) to match the 24dp hero spec).
        - Inside: Column(crossAxisAlignment: center, padding: 28dp horizontal, 24dp vertical) of:
            1. SectionLabel('Ready to record')
            2. 16dp gap
            3. 124dp circular GestureDetector(onTap: isTracking ? null : onStart) — Container(width: 124, height: 124, decoration: BoxDecoration(color: tokens.record, shape: BoxShape.circle, boxShadow: [BoxShadow(color: Color(0x40B43C28) for light theme / Color(0x66000000) for dark, blurRadius: 32, offset: Offset(0, 12))]), child: Column(center: Icon(Icons.play_arrow_rounded, size: 36, color: Colors.white), 4dp gap, Text('START', TraevyFonts.ui(size: 13, w700, letterSpacing: 2, color: Colors.white)))).
            4. 16dp gap
            5. RichText: "Auto-labelled " (TraevyFonts.ui(size: 12.5, w400, color: tokens.textDim)) + "$autoLabelDirection" (w700, color: onSurface) + " · $autoLabelTime" (TraevyFonts.mono(size: 12.5, w500, color: tokens.textDim)). Hide auto-label row when isTracking == true (show "● RECORDING" pill instead via record-color background).
        - `_resolveShadowColor(context)`: return Color.fromRGBO(180, 60, 40, 0.25) when brightness == light, else Color.fromRGBO(0, 0, 0, 0.4).
    - `TodaySection` (≤ 100 lines):
        - ConsumerWidget. Watches `todaysTripSummariesProvider`.
        - Renders: SectionLabel('Today') on left of header Row, Spacer, Text('{n} of {n+empty}' — empty count derived: if n &lt; 2 expected, show empty placeholder count).
        - Below: Container(bgElev card with radius 16, border tokens.border) wrapping a Column of N `TripRowCard(...)` separated by 1dp dividers, optionally followed by `EmptySlotRow` when only one trip is recorded today (Pitfall: also handle 0-trip case — show two `EmptySlotRow`s with subtitle 'Tap START or add manually').
        - Tap on TripRowCard navigates to kRouteTripDetail (preserve existing behaviour).
    - `EmptySlotRow` (≤ 60 lines):
        - Renders Row: 36dp circle Container with DottedBorder visual via CustomPaint OR simpler: a Container with dashed-style border via `BoxDecoration.border` is not available — use `Container(width: 36, height: 36, decoration: BoxDecoration(shape: BoxShape.circle, border: Border.all(color: tokens.borderStr, width: 1.5)), child: Center(child: Icon(Icons.add_rounded, size: 18, color: tokens.textMuted)))`. (Dashed-circle visual approximation; full dashed effect deferred to optional polish — UI-SPEC says "dashed circle icon", but Flutter has no built-in dashed border. The plain bordered circle with a + icon is acceptable as the spec's intent is "empty placeholder".)
        - 14dp gap, then Column: Text('Evening commute' — labelLarge, color: tokens.textDim) + Text('Tap START or add manually' — TraevyFonts.ui(size: 11.5, w400, color: tokens.textMuted)).
    - `WeekLossCard` (≤ 100 lines):
        - ConsumerWidget. Watches `weeklySummaryProvider`.
        - Renders a Card (override shape to radius 18 for hero card per UI-SPEC §11) containing:
            1. Row: SectionLabel('This week'), Spacer, GestureDetector(onTap: () => ref.read(mainShellIndexProvider.notifier).setIndex(2), child: Text('See stats →', TraevyFonts.ui(size: 12, w600, color: tokens.accent))).
            2. 12dp gap
            3. Text('You lost', textTheme.bodyMedium color: tokens.textDim)
            4. Text(formatStuckHm(summary.stuckMinutes) — e.g. '1h 42m', TraevyFonts.mono(size: 38, w500, color: tokens.stuck, letterSpacing: -1.5))
            5. Text('to traffic this week.', textTheme.bodyMedium color: tokens.textDim)
            6. 14dp gap
            7. StuckBar(movingMinutes: summary.movingMinutes, stuckMinutes: summary.stuckMinutes, height: 14)
            8. 8dp gap
            9. Row: Text('${formatHm(summary.movingMinutes)} moving', TraevyFonts.mono(size: 11.5, color: tokens.moving)), Spacer, Text('${formatHm(summary.movingMinutes + summary.stuckMinutes)} total', TraevyFonts.mono(size: 11.5, color: tokens.textDim)).
        - `formatHm` and `formatStuckHm` are local helpers (DRY violation acceptable for now; if a shared formatters.dart exists in lib/shared/utils/, reuse it — read first).
  </behavior>
  <action>
    1. Create the new files `home_header.dart`, `hero_record_card.dart`, `today_section.dart`, `empty_slot_row.dart`, `week_loss_card.dart` under `lib/features/dashboard/widgets/`. Implement each per <behavior> using TraevyFonts and Theme.of(context).extension<TraevyTokensExt>()! — never hardcode hex.
    2. Restyle `lib/features/dashboard/widgets/in_progress_card.dart`: replace `colorScheme.surfaceContainerLow` with `tokens.bgElev` (Pitfall 9) and `BorderRadius.circular(12)` with `BorderRadius.circular(16)` (Pitfall 4). Keep its public API and constructor unchanged.
    3. Refactor `lib/features/dashboard/screens/dashboard_screen.dart`:
        - Remove the `AppBar` entirely.
        - Remove the `FloatingActionButton`.
        - Remove the AppBar action icons (history, bar_chart, settings) — these moved to NavigationBar.
        - Build the Scaffold body as `SafeArea(child: CustomScrollView(slivers: [SliverToBoxAdapter(HomeHeader()), SliverToBoxAdapter(SizedBox(height: 8)), SliverToBoxAdapter(HeroRecordCard(...)), SliverToBoxAdapter(SizedBox(height: 24)), SliverToBoxAdapter(TodaySection()), SliverToBoxAdapter(SizedBox(height: 24)), SliverToBoxAdapter(WeekLossCard()), SliverToBoxAdapter(SizedBox(height: 24))]))`.
        - Preserve `_handleStart`, `_showSettingsDialog`, `_showPermissionDeniedSnackBar`, and every other private permission-handler. Wire them via `HeroRecordCard(onStart: () => _handleStart(context, ref))`.
        - Preserve `inProgressTripProvider` watch — pass to HeroRecordCard so it can render the RECORDING variant.
    4. Delete `lib/features/dashboard/widgets/today_trips_section.dart` and `lib/features/dashboard/widgets/weekly_summary_card.dart` (CLAUDE.md "No dead code").
    5. `dart format lib/features/dashboard/`, then `flutter analyze lib/features/dashboard/` — expect 0 warnings.
    6. Task 3 of this plan will repair any remaining test failures from the dashboard_screen_test (specifically the Pitfall 7 icon-locator lines).
    - The legacy widgets `today_trips_section.dart`, `weekly_summary_card.dart` are DELETED (CLAUDE.md "No dead code"). `in_progress_card.dart` is restyled in place: radius 12 → 16 (Pitfall 4) and color from `colorScheme.surfaceContainerLow` → `Theme.of(context).extension<TraevyTokensExt>()!.bgElev` (Pitfall 9). Header/icon colors switch to tokens.
    - `dashboard_screen.dart` is restructured (≤ 200 lines — internal widget can exceed 100 when it is purely a layout coordinator):
        - Remove the `AppBar` entirely.
        - Remove the `FloatingActionButton`.
        - Remove the AppBar action icons (history, bar_chart, settings) — these moved to NavigationBar.
        - The Scaffold body becomes a `SafeArea(child: CustomScrollView(slivers: [SliverToBoxAdapter(HomeHeader()), SliverToBoxAdapter(SizedBox(height: 8)), SliverToBoxAdapter(HeroRecordCard(...)), SliverToBoxAdapter(SizedBox(height: 24)), SliverToBoxAdapter(TodaySection()), SliverToBoxAdapter(SizedBox(height: 24)), SliverToBoxAdapter(WeekLossCard()), SliverToBoxAdapter(SizedBox(height: 24))]))`.
        - Keep `_handleStart`, `_showSettingsDialog`, `_showPermissionDeniedSnackBar`, and any other private permission-handler logic. Wire them via `HeroRecordCard(onStart: () => _handleStart(context, ref))`.
        - Keep `inProgressTripProvider` watch — pass to HeroRecordCard so it can render the "RECORDING" variant (or render an InProgressCard alongside).
  </action>
  <verify>
    <automated>flutter analyze lib/features/dashboard/ 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/features/dashboard/dashboard_screen_test.dart 2&gt;&amp;1 | tee /tmp/p4t2.log ; echo "Task 3 will repair any remaining test failures"</automated>
  </verify>
  <acceptance_criteria>
    - The five new widget files exist and each declares a `StatelessWidget` or `ConsumerWidget` with `const` constructor.
    - `wc -l lib/features/dashboard/widgets/home_header.dart` &lt; 80; the four other new widgets &lt; 100 lines each.
    - `lib/features/dashboard/widgets/weekly_summary_card.dart` and `lib/features/dashboard/widgets/today_trips_section.dart` no longer exist (verified by `! test -f`).
    - `lib/features/dashboard/widgets/in_progress_card.dart` no longer references `colorScheme.surfaceContainerLow` (replaced with `tokens.bgElev`); radius 12 replaced with 16 (verified by `grep -q "BorderRadius.circular(16)" lib/features/dashboard/widgets/in_progress_card.dart && ! grep -q "surfaceContainerLow" lib/features/dashboard/widgets/in_progress_card.dart`).
    - `lib/features/dashboard/screens/dashboard_screen.dart` no longer creates an `AppBar` or `FloatingActionButton` (verified by `! grep -E "AppBar\(|FloatingActionButton" lib/features/dashboard/screens/dashboard_screen.dart`).
    - `lib/features/dashboard/screens/dashboard_screen.dart` still defines `_handleStart` (verified by `grep -q "_handleStart" lib/features/dashboard/screens/dashboard_screen.dart`).
    - `flutter analyze lib/features/dashboard/` exits with 0 issues.
    - No file in `lib/features/dashboard/` references `Color(0xFF...)` directly (greps return zero matches).
  </acceptance_criteria>
  <done>
    Dashboard renders HomeHeader + HeroRecordCard + TodaySection + WeekLossCard inside a SafeArea-wrapped CustomScrollView. The legacy WeeklySummaryCard and TodayTripsSection are deleted. InProgressCard is updated to Phase 8 tokens. _handleStart logic is preserved. Analyzer is clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Update dashboard_screen_test.dart for the new shell + dashboard layout (incl. back-button assertion)</name>
  <files>test/widget/features/dashboard/dashboard_screen_test.dart, test/widget/features/shell/main_shell_test.dart</files>
  <read_first>
    - test/widget/features/dashboard/dashboard_screen_test.dart (full file — identify every assertion that hits the old AppBar action icons; specifically the lines 286–301 noted in Pitfall 7)
    - test/widget/features/shell/main_shell_test.dart (full file — back-button test will be added here, alongside the existing nav-destination assertions)
    - lib/features/dashboard/screens/dashboard_screen.dart (the file you just refactored in Task 2)
    - lib/features/shell/screens/main_shell.dart (so test setup can mount MainShell when navigation behaviour is being asserted)
    - .planning/phases/08-ui-overhaul/08-REVIEWS.md (Review MEDIUM #4 — back-button from a tab must NOT pop to dashboard)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Pitfall 7 details
  </read_first>
  <behavior>
    - Every existing test that does NOT touch AppBar icons continues to pass unchanged (trip-list rendering, in-progress card visibility, weekly summary numbers, FAB-tap permission flow — though FAB tap becomes hero-card tap).
    - The two assertions that referenced `find.byIcon(Icons.history)` and `find.byIcon(Icons.bar_chart)` are removed (those icons moved to NavigationBar; verifying them is now MainShell's responsibility).
    - The `find.byTooltip(kSettingsTooltip)` assertion is removed (the settings AppBar icon is gone; navigation to settings is now via the NavigationBar destination 'Settings').
    - The "tap FAB → permission flow" test is updated to "tap HeroRecordCard START → permission flow": locate the hero by `find.text('START')`, then `await tester.tap(find.text('START'))`. The downstream permission dialog / SnackBar assertions remain identical because the handler logic is unchanged.
    - At least one new test verifies the new layout: `find.byType(HomeHeader)` exists, `find.byType(HeroRecordCard)` exists, `find.byType(WeekLossCard)` exists, `find.byType(TodaySection)` exists.
    - **Back-button test (Review MEDIUM #4) — added to `main_shell_test.dart`:**
        - Mount `MainShell` inside `MaterialApp(home: const MainShell())`.
        - Switch to a non-default tab: `await tester.tap(find.text('Stats')); await tester.pumpAndSettle();`.
        - Verify Stats tab is visible: `expect(find.byType(StatsScreen), findsOneWidget);`.
        - Simulate the system back button: invoke `await tester.binding.handlePopRoute();` (or `await SystemChannels.navigation.invokeMethod('popRoute');`) and capture the return value.
        - Assert: `expect(returnValue, isFalse);` — `handlePopRoute()` returns `false` when no route was popped, which is the correct behaviour because the navigator stack has only the MainShell root route. The tab switch was a state update, not a route push.
        - Assert: `expect(find.byType(StatsScreen), findsOneWidget);` AFTER the pop attempt — still on Stats tab; back button did NOT switch back to Dashboard.
        - Assert: `expect(find.byType(DashboardScreen), findsNothing);` — confirms back button did NOT navigate back to Dashboard.
    - All tests under `test/widget/features/dashboard/` and `test/widget/features/shell/` pass GREEN.
  </behavior>
  <action>
    1. Open `test/widget/features/dashboard/dashboard_screen_test.dart`.
    2. Delete the two `expect(find.byIcon(Icons.history), findsOneWidget);` and `expect(find.byIcon(Icons.bar_chart), findsOneWidget);` assertions (the Pitfall 7 lines).
    3. Delete the `expect(find.byTooltip(kSettingsTooltip), findsOneWidget);` assertion if present.
    4. For any test that performs `await tester.tap(find.byType(FloatingActionButton))`, replace with:
       ```dart
       await tester.tap(find.text('START').first);
       ```
       (the START label is unique inside HeroRecordCard).
    5. Add a new `testWidgets('renders new Traevy dashboard layout', (tester) async { ... })` block that:
        - Pumps DashboardScreen wrapped in ProviderScope + MaterialApp with `theme: buildLightTheme()`.
        - Asserts `find.byType(HomeHeader)`, `find.byType(HeroRecordCard)`, `find.byType(TodaySection)`, `find.byType(WeekLossCard)` each `findsOneWidget`.
        - Asserts the "Hi, Traveller" greeting renders (`find.text('Hi, Traveller')` findsOneWidget).
        - Asserts the "You lost" caption renders (`find.text('You lost')`, `find.text('to traffic this week.')` each findsOneWidget).
    6. Add a separate test that mounts `MainShell` (not just DashboardScreen) and asserts that tapping the "See stats →" link inside WeekLossCard updates the shell index to 2 — verified by `find.byType(StatsScreen)` becoming the visible IndexedStack child after `pumpAndSettle`.
    7. Open `test/widget/features/shell/main_shell_test.dart` and APPEND a new `testWidgets('back button from non-default tab does NOT pop to dashboard (Review MEDIUM #4)', (tester) async { ... })` block per the <behavior> spec above. Use `tester.binding.handlePopRoute()` (returns `Future&lt;bool&gt;`). Imports needed: `package:flutter/services.dart` (for `SystemChannels` if using the channel-based variant) and `package:traevy/features/stats/screens/stats_screen.dart`.
    8. Update imports in both test files to add the new widget classes (HomeHeader, HeroRecordCard, TodaySection, WeekLossCard) and MainShell + StatsScreen + DashboardScreen as needed.
    9. Run `dart format test/widget/features/dashboard/dashboard_screen_test.dart test/widget/features/shell/main_shell_test.dart`.
    10. Run `flutter test test/widget/features/dashboard/ test/widget/features/shell/` and confirm full GREEN.
    11. Run `flutter test` for the full widget suite as a regression check — only theme-related visual changes may have broken downstream tests, those are Plan 05/06/07's responsibility.
  </action>
  <verify>
    <automated>flutter analyze test/widget/features/dashboard/ test/widget/features/shell/ 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/features/dashboard/ test/widget/features/shell/ test/widget/app_test.dart &amp;&amp; grep -q "handlePopRoute" test/widget/features/shell/main_shell_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "find.byIcon(Icons.history)" test/widget/features/dashboard/dashboard_screen_test.dart` returns 0.
    - `grep -c "find.byIcon(Icons.bar_chart)" test/widget/features/dashboard/dashboard_screen_test.dart` returns 0.
    - `grep -c "find.byType(FloatingActionButton)" test/widget/features/dashboard/dashboard_screen_test.dart` returns 0.
    - The dashboard test file contains assertions for `find.byType(HomeHeader)`, `find.byType(HeroRecordCard)`, `find.byType(TodaySection)`, `find.byType(WeekLossCard)`.
    - `test/widget/features/shell/main_shell_test.dart` contains the new back-button test (Review MEDIUM #4) — verified by `grep -q "handlePopRoute" test/widget/features/shell/main_shell_test.dart` AND `grep -q "Review MEDIUM #4" test/widget/features/shell/main_shell_test.dart`.
    - The back-button test asserts `expect(.*isFalse)` after `handlePopRoute()` (verified by grep).
    - The back-button test asserts `find.byType(StatsScreen), findsOneWidget` AFTER the pop attempt (verified by grep).
    - The back-button test asserts `find.byType(DashboardScreen), findsNothing` AFTER the pop attempt (verified by grep).
    - `flutter test test/widget/features/dashboard/` exits 0.
    - `flutter test test/widget/features/shell/` exits 0.
    - `flutter test test/widget/app_test.dart` exits 0.
    - `flutter analyze test/widget/features/dashboard/ test/widget/features/shell/` exits 0.
  </acceptance_criteria>
  <done>
    All dashboard widget tests pass on the new layout. The "tap to start" permission flow continues to assert correct dialog/SnackBar behaviour. The new layout is asserted by widget type. The shell-driven "See stats →" tab switch is covered. A dedicated back-button test (Review MEDIUM #4) asserts that pressing system back from a non-default tab does NOT pop to dashboard — the navigator stack has only the root MainShell route, so `handlePopRoute()` returns false and the Stats tab remains visible.
  </done>
</task>

</tasks>

<verification>
- `.planning/phases/08-ui-overhaul/08-04-PROVIDER-AUDIT.md` exists and lists all four reviewer-called-out providers (Review HIGH #1).
- `flutter analyze lib/features/shell/ lib/features/dashboard/ lib/app.dart lib/config/routes.dart lib/features/dashboard/providers/ lib/features/trips/providers/ lib/features/stats/providers/ lib/features/settings/providers/` exits 0.
- `flutter test test/widget/features/dashboard/ test/widget/features/shell/ test/widget/app_test.dart` passes.
- `flutter test` (full suite) still passes after the audit's provider edits.
- `! grep -q "WeeklySummaryCard\|TodayTripsSection" lib/features/dashboard/widgets/*.dart` (legacy widgets deleted).
- `grep -q "home: const MainShell" lib/app.dart`.
- `grep -q "ref.read(mainShellIndexProvider.notifier).setIndex(2)" lib/features/dashboard/widgets/week_loss_card.dart`.
- `grep -q "handlePopRoute" test/widget/features/shell/main_shell_test.dart` (Review MEDIUM #4).
- `! grep -E "PopScope|WillPopScope" lib/features/shell/screens/main_shell.dart` (Review MEDIUM #4 — default back behaviour is correct, no wrapper).
</verification>

<success_criteria>
- **Review HIGH #1:** Provider disposal audit document committed; every reviewer-called-out provider analyzed; required code changes applied; full test suite green.
- MainShell ships with 4 NavigationBar destinations.
- app.dart mounts MainShell as home.
- Dashboard has no AppBar / FAB; new HomeHeader, HeroRecordCard, TodaySection, EmptySlotRow, WeekLossCard render.
- _handleStart permission-handler logic is preserved on DashboardScreen.
- "See stats →" switches shell index rather than pushing a route.
- Dashboard widget tests + shell tests + app_test all pass.
- **Review MEDIUM #4:** Back-button test asserts system back from a non-default tab does NOT pop to dashboard; MainShell does NOT wrap any `PopScope`/`WillPopScope`.
- Legacy WeeklySummaryCard / TodayTripsSection are deleted.
- InProgressCard radius 16 + bgElev fix applied (Pitfalls 4 + 9).
- Analyzer clean across all modified files.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-overhaul/08-04-SUMMARY.md` documenting: MainShell architecture, the deleted-vs-replaced dashboard widget map, the test changes made for Pitfall 7, the provider disposal audit results (Review HIGH #1) with a summary table of decisions, the back-button behaviour test (Review MEDIUM #4), and a note that Plans 05/06/07 still need to restyle the other tabs.
</output>
