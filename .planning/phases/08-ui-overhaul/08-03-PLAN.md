---
phase: 08-ui-overhaul
plan: 03
type: execute
wave: 3
depends_on: [01, 02]
files_modified:
  - lib/shared/widgets/stuck_bar.dart
  - lib/shared/widgets/trip_row_card.dart
  - lib/shared/widgets/section_label.dart
  - lib/shared/widgets/traevy_toggle.dart
  - lib/shared/widgets/stat_mini_card.dart
  - lib/shared/widgets/traevy_logo_mark.dart
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-04
  - UX-05
must_haves:
  truths:
    - "StuckBar renders a proportional moving/stuck horizontal bar with rounded ends and token-driven colors"
    - "TripRowCard renders direction avatar (to_office/to_home), name, duration mono, start→end·distance line, and stuck text right-aligned"
    - "SectionLabel renders UPPERCASE text in textMuted color with letterSpacing 1.0"
    - "TraevyToggle: off uses borderStr bg + left knob; on uses moving bg + right knob; tap inverts via onChanged callback"
    - "StatMiniCard renders label (Inter 10.5sp muted caps) + value (JetBrains Mono 22sp) + tone tinting"
    - "TraevyLogoMark renders 56dp rounded square (radius 16) with text-bg / bg-text + 'tv' mono 700 28sp"
    - "All five Wave 0 widget tests from Plan 01 turn GREEN"
  artifacts:
    - path: lib/shared/widgets/stuck_bar.dart
      provides: StuckBar widget (movingMinutes, stuckMinutes, height)
      contains: "class StuckBar"
    - path: lib/shared/widgets/trip_row_card.dart
      provides: TripRowCard widget (direction, startTime, endTime, duration, distance, stuckMinutes, onTap)
      contains: "class TripRowCard"
    - path: lib/shared/widgets/section_label.dart
      provides: SectionLabel widget (text, size)
      contains: "class SectionLabel"
    - path: lib/shared/widgets/traevy_toggle.dart
      provides: TraevyToggle widget (value, onChanged)
      contains: "class TraevyToggle"
    - path: lib/shared/widgets/stat_mini_card.dart
      provides: StatMiniCard widget (label, value, unit?, tone)
      contains: "class StatMiniCard"
    - path: lib/shared/widgets/traevy_logo_mark.dart
      provides: TraevyLogoMark widget (size)
      contains: "class TraevyLogoMark"
  key_links:
    - from: lib/shared/widgets/*.dart
      to: lib/config/theme_extension.dart TraevyTokensExt
      via: Theme.of(context).extension<TraevyTokensExt>()
      pattern: "extension<TraevyTokensExt>"
    - from: lib/shared/widgets/stat_mini_card.dart
      to: lib/config/theme.dart TraevyFonts.mono
      via: TraevyFonts.mono(size: 22, weight: w600)
      pattern: "TraevyFonts.mono"
---

<objective>
Implement the six shared Traevy UI primitives that every Phase 8 screen depends on: `StuckBar`, `TripRowCard`, `SectionLabel`, `TraevyToggle`, `StatMiniCard`, and `TraevyLogoMark`. All six live under `lib/shared/widgets/`, are `StatelessWidget`s (TraevyToggle uses GestureDetector + AnimatedContainer per the locked spec), pull every colour from `Theme.of(context).extension<TraevyTokensExt>()!`, and pull mono typography via `TraevyFonts.mono(...)`. Each widget stays under 100 lines (CLAUDE.md "widgets under 100 lines"). The Wave 0 widget tests staged in Plan 01 for these five primitives (TraevyLogoMark intentionally has no Wave 0 test — it is purely visual and used only inside Onboarding) must all pass GREEN at the end of this plan.

Purpose: Plans 04, 05, 06, and 07 all consume these primitives. Centralising the contract here means downstream plans simply compose, never restyle.

Output: Six widget files; all five Wave-0 RED tests turn GREEN; analyzer clean.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/CLAUDE.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-CONTEXT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-UI-SPEC.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-RESEARCH.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-02-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme_extension.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/constants.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/shared/widgets/stuck_bar_test.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/shared/widgets/trip_row_card_test.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/shared/widgets/section_label_test.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/shared/widgets/traevy_toggle_test.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/shared/widgets/stat_mini_card_test.dart

<interfaces>
<!-- Public API surface for downstream plans (04, 05, 06, 07). -->

// lib/shared/widgets/stuck_bar.dart
class StuckBar extends StatelessWidget {
  const StuckBar({required this.movingMinutes, required this.stuckMinutes, this.height = 14, super.key});
  final int movingMinutes;
  final int stuckMinutes;
  final double height;
}

// lib/shared/widgets/trip_row_card.dart
class TripRowCard extends StatelessWidget {
  const TripRowCard({
    required this.direction,           // String — kDirectionToOffice or kDirectionToHome
    required this.startTime,           // DateTime
    required this.endTime,             // DateTime
    required this.durationMinutes,     // int
    required this.distanceMeters,      // double
    required this.stuckMinutes,        // int
    this.showDivider = true,
    this.onTap,
    super.key,
  });
}

// lib/shared/widgets/section_label.dart
class SectionLabel extends StatelessWidget {
  const SectionLabel(this.text, {this.fontSize = 12, super.key});
  final String text;
  final double fontSize;
}

// lib/shared/widgets/traevy_toggle.dart
class TraevyToggle extends StatelessWidget {
  const TraevyToggle({required this.value, required this.onChanged, super.key});
  final bool value;
  final ValueChanged<bool> onChanged;
}

// lib/shared/widgets/stat_mini_card.dart
enum StatMiniCardTone { neutral, moving, stuck }
class StatMiniCard extends StatelessWidget {
  const StatMiniCard({
    required this.label,
    required this.value,
    this.unit,
    this.tone = StatMiniCardTone.neutral,
    super.key,
  });
}

// lib/shared/widgets/traevy_logo_mark.dart
class TraevyLogoMark extends StatelessWidget {
  const TraevyLogoMark({this.size = 56, super.key});
  final double size;
}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: StuckBar + SectionLabel + TraevyLogoMark (atomic primitives)</name>
  <files>lib/shared/widgets/stuck_bar.dart, lib/shared/widgets/section_label.dart, lib/shared/widgets/traevy_logo_mark.dart</files>
  <read_first>
    - lib/config/theme.dart (TraevyFonts.mono + TraevyTokens — already present from Plan 02)
    - lib/config/theme_extension.dart (TraevyTokensExt — already present)
    - lib/config/constants.dart (kBrandShortName, kFontMono — added in Plan 01)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §3 Home Screen StuckBar, §10 StuckBar contract, §9 Onboarding 'tv' logo, §1 Section header style
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Example 2 StuckBar reference + "Don't Hand-Roll" StuckBar entry
    - test/widget/shared/widgets/stuck_bar_test.dart (the RED test from Plan 01)
    - test/widget/shared/widgets/section_label_test.dart (the RED test from Plan 01)
  </read_first>
  <behavior>
    - StuckBar(movingMinutes: M, stuckMinutes: S, height: H):
        - When M + S == 0: renders a single ColoredBox with `tokens.surface2` background, height H, full width, BorderRadius.circular(H/2).
        - When M + S > 0: renders a `ClipRRect(BorderRadius.circular(H/2))` containing a `Row` of two `Expanded` children: left flex=M color=tokens.moving, right flex=S color=tokens.stuck. The Row sits inside a Container(color: tokens.surface2, height: H) so partial bars still show track background.
        - default height = 14.
    - SectionLabel(text, fontSize):
        - Renders `Text(text.toUpperCase(), style: TraevyFonts.ui(size: fontSize, weight: FontWeight.w600, color: tokens.textMuted, letterSpacing: 1.0))`.
        - default fontSize = 12.
    - TraevyLogoMark(size):
        - Renders a `Container(width: size, height: size, decoration: BoxDecoration(color: tokens.text, borderRadius: BorderRadius.circular(16)))` containing `Center(child: Text(kBrandShortName, style: TraevyFonts.mono(size: size * 0.5, weight: FontWeight.w700, color: tokens.bg, letterSpacing: -1.0)))`.
        - "tokens.text" / "tokens.bg" must use the ColorScheme values from Theme (text via `theme.colorScheme.onSurface`, bg via `theme.scaffoldBackgroundColor`) so dark mode flips automatically.
    - All three widgets pass `flutter analyze` with zero warnings, all extend `StatelessWidget`, all use `const` constructors with `super.key`, all stay under 60 lines.
    - All Plan 01 RED tests for these three widgets turn GREEN.
  </behavior>
  <action>
    1. Create `lib/shared/widgets/stuck_bar.dart`. Use ColoredBox + ClipRRect to avoid CustomPainter (per "Don't Hand-Roll" in 08-RESEARCH.md). Resolve colors as:
       ```dart
       final tokens = Theme.of(context).extension<TraevyTokensExt>()!;
       ```
    2. Create `lib/shared/widgets/section_label.dart`. Single Text widget; no Padding (callers wrap as needed).
    3. Create `lib/shared/widgets/traevy_logo_mark.dart`. The "tv" string comes from `kBrandShortName` (constants.dart). Text color must be `Theme.of(context).scaffoldBackgroundColor` (which equals `t.bg`) and container color must be `Theme.of(context).colorScheme.onSurface` (which equals `t.text` per Plan 02 mapping).
    4. Each file:
       - `import 'package:flutter/material.dart';`
       - imports `package:traevy/config/theme.dart` (for TraevyFonts) and `package:traevy/config/theme_extension.dart` (for TraevyTokensExt) as needed.
       - Has a `///`-prefixed class doc comment citing the UI-SPEC.md section.
    5. Run `dart format lib/shared/widgets/stuck_bar.dart lib/shared/widgets/section_label.dart lib/shared/widgets/traevy_logo_mark.dart`.
    6. Run `flutter analyze lib/shared/widgets/` — expect zero warnings.
    7. Run `flutter test test/widget/shared/widgets/stuck_bar_test.dart test/widget/shared/widgets/section_label_test.dart` — both must pass GREEN. (TraevyLogoMark has no Wave 0 test by design; smoke-render it inside Onboarding plan 07.)
  </action>
  <verify>
    <automated>flutter analyze lib/shared/widgets/stuck_bar.dart lib/shared/widgets/section_label.dart lib/shared/widgets/traevy_logo_mark.dart 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/shared/widgets/stuck_bar_test.dart test/widget/shared/widgets/section_label_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/shared/widgets/stuck_bar.dart` declares `class StuckBar extends StatelessWidget` with `const StuckBar({required this.movingMinutes, required this.stuckMinutes, this.height = 14, super.key})`.
    - `wc -l lib/shared/widgets/stuck_bar.dart` &lt; 60.
    - `lib/shared/widgets/section_label.dart` declares `class SectionLabel extends StatelessWidget` and uses `text.toUpperCase()`.
    - `lib/shared/widgets/traevy_logo_mark.dart` declares `class TraevyLogoMark extends StatelessWidget` and renders `kBrandShortName` from constants.dart.
    - `flutter test test/widget/shared/widgets/stuck_bar_test.dart` exits 0.
    - `flutter test test/widget/shared/widgets/section_label_test.dart` exits 0.
    - `flutter analyze` over all three files exits with 0 issues.
    - No file imports anything from `lib/features/` (one-way dependency: shared ← config).
  </acceptance_criteria>
  <done>
    StuckBar renders 50/50 bar at the expected colours; SectionLabel uppercases its text and applies letterSpacing 1.0; TraevyLogoMark renders the "tv" mark in correct light/dark colours. Two Wave-0 RED tests turn GREEN.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: TripRowCard + TraevyToggle + StatMiniCard (interactive + composite primitives)</name>
  <files>lib/shared/widgets/trip_row_card.dart, lib/shared/widgets/traevy_toggle.dart, lib/shared/widgets/stat_mini_card.dart</files>
  <read_first>
    - lib/config/theme.dart + lib/config/theme_extension.dart (already authored)
    - lib/config/constants.dart (kDirectionToOffice, kDirectionToHome, kFontMono)
    - lib/shared/widgets/stuck_bar.dart (just authored — for style consistency)
    - lib/features/trips/widgets/trip_card.dart (existing legacy widget — study what props it currently consumes; TripRowCard's API is similar but spec is different)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §5 Trip History (TripRow spec), §8 Settings (Toggle spec), §4 Active Recording (StatMiniCard usage)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Example 3 TraevyToggle reference
    - test/widget/shared/widgets/trip_row_card_test.dart (Plan 01 RED test)
    - test/widget/shared/widgets/traevy_toggle_test.dart (Plan 01 RED test)
    - test/widget/shared/widgets/stat_mini_card_test.dart (Plan 01 RED test)
    - lib/shared/utils/ (any existing duration/distance formatters that could be reused)
  </read_first>
  <behavior>
    - TripRowCard renders, in a 14dp-vertical / 20dp-horizontal padded Row:
        1. 36dp circular avatar. direction==kDirectionToOffice → backgroundColor=tokens.accentBg + Icon(Icons.arrow_forward_rounded, color: tokens.accent, size: 18). direction==kDirectionToHome → backgroundColor=tokens.movingBg + Icon(Icons.arrow_back_rounded, color: tokens.moving, size: 18).
        2. 14dp gap, then a Column with two rows of Text:
            - Top row: direction display name (Inter 15sp w600, color=onSurface) on the left; durationMinutes formatted as e.g. "47m" or "1h 12m" (TraevyFonts.mono(size: 13, weight: w600, color: onSurface)) right-aligned.
            - Bottom row: "{HH:mm} → {HH:mm} · {distance}" using TraevyFonts.mono(size: 12, weight: w400, color: tokens.textDim) on the left; "{stuckMinutes}m stuck" using TraevyFonts.mono(size: 12, weight: w600, color: tokens.stuck) right-aligned. Hide "X stuck" segment when stuckMinutes == 0.
        3. If `showDivider == true`, paint a 1dp Divider(color: tokens.border) BELOW the row content (use a Column wrapping the InkWell + Divider so the divider sits at the bottom).
        4. Wrap the row content in `InkWell(onTap: onTap)` so tap state ripples (only when onTap != null).
    - Direction display name is derived locally: kDirectionToOffice → "To office", kDirectionToHome → "To home". Use a private `_displayName(direction)` function (do not hardcode in constants.dart — these are UI strings only, fine to keep in this widget).
    - HH:mm formatting uses `intl: ^0.20.2` `DateFormat.Hm()` (already in pubspec).
    - Distance formatting: if distanceMeters >= 1000 → "{X.X} km" (one decimal), else "{round(meters)} m".
    - Duration formatting: H × 3600 + M × 60 — but the prop is `durationMinutes` (int), so format as "{H}h {M}m" when ≥ 60, else "{M}m".
    - TraevyToggle: matches §8 of UI-SPEC verbatim — 38×22dp pill, 18dp white knob with shadow `BoxShadow(color: Color(0x33000000), blurRadius: 3, offset: Offset(0, 1))`, on=tokens.moving / off=tokens.borderStr, animated 180ms.
    - StatMiniCard: Container with `tokens.bgElev` bg, BorderRadius.circular(16), Border(color: tokens.border), padding EdgeInsets.symmetric(horizontal: 14, vertical: 12). Inside: Column(crossAxisAlignment: start) of:
        1. label uppercase via SectionLabel(label, fontSize: 10.5)
        2. 6dp gap
        3. Row alignment: BaselineRow of value (TraevyFonts.mono(size: 22, weight: w600, color: _toneColor)) + optional unit (TraevyFonts.mono(size: 11, weight: w500, color: tokens.textDim)) with 4dp spacing.
        - _toneColor: neutral=onSurface, moving=tokens.moving, stuck=tokens.stuck.
    - All three widgets stay under 100 lines; all three turn the corresponding Wave-0 RED tests GREEN.
  </behavior>
  <action>
    1. Create `lib/shared/widgets/trip_row_card.dart` (~80 lines). Use `Material(color: Colors.transparent, child: InkWell(...))` to keep ripple visible against transparent backgrounds (the row is rendered inside a parent Card in History plans). Implement helpers:
       ```dart
       String _formatDuration(int minutes) =&gt; minutes &gt;= 60 ? '${minutes ~/ 60}h ${minutes % 60}m' : '${minutes}m';
       String _formatDistance(double meters) =&gt; meters &gt;= 1000 ? '${(meters / 1000).toStringAsFixed(1)} km' : '${meters.round()} m';
       String _displayName(String direction) =&gt; direction == kDirectionToOffice ? 'To office' : 'To home';
       ```
    2. Create `lib/shared/widgets/traevy_toggle.dart` (~40 lines) following Example 3 of 08-RESEARCH.md verbatim. Use AnimatedContainer + AnimatedAlign with 180ms duration.
    3. Create `lib/shared/widgets/stat_mini_card.dart` (~70 lines). Pull tone color from a private `_toneColor(BuildContext, StatMiniCardTone)` helper that reads TraevyTokensExt.
    4. Each file:
       - Imports flutter/material.dart and the config files.
       - Has `///` class doc comment citing the UI-SPEC.md section.
       - Uses `const` constructors and `super.key`.
    5. Run `dart format` on the three new files.
    6. Run `flutter analyze lib/shared/widgets/` — expect zero warnings.
    7. Run `flutter test test/widget/shared/widgets/` — all five Wave-0 tests must pass GREEN now (the three from this task plus the two from Task 1 of this plan).
  </action>
  <verify>
    <automated>flutter analyze lib/shared/widgets/ 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/shared/widgets/</automated>
  </verify>
  <acceptance_criteria>
    - `lib/shared/widgets/trip_row_card.dart` declares `class TripRowCard extends StatelessWidget` with `direction`, `startTime`, `endTime`, `durationMinutes`, `distanceMeters`, `stuckMinutes`, `showDivider`, `onTap` fields.
    - `wc -l lib/shared/widgets/trip_row_card.dart` &lt; 100.
    - `wc -l lib/shared/widgets/traevy_toggle.dart` &lt; 60.
    - `wc -l lib/shared/widgets/stat_mini_card.dart` &lt; 100.
    - `flutter test test/widget/shared/widgets/` passes (all 5 tests).
    - `flutter analyze lib/shared/widgets/` exits with 0 issues.
    - No file in `lib/shared/widgets/` references `Color(0xFF...)` directly (greps for `Color\(0x` return zero matches).
    - TraevyToggle's `AnimatedContainer` duration is exactly `Duration(milliseconds: 180)`.
  </acceptance_criteria>
  <done>
    All six Phase 8 shared primitives live under `lib/shared/widgets/` with token-aware styling. All five Plan-01 RED tests pass GREEN. Analyzer is clean. Downstream plans 04–07 can compose these without restyling.
  </done>
</task>

</tasks>

<verification>
- `flutter analyze lib/shared/widgets/` exits 0.
- `flutter test test/widget/shared/widgets/` passes (5/5 tests).
- `grep -rE "Color\(0x" lib/shared/widgets/` returns no matches.
- `wc -l lib/shared/widgets/*.dart` reports each file under 100 lines.
</verification>

<success_criteria>
- Six shared widget files exist with the documented public API.
- All five Wave-0 RED widget tests turn GREEN.
- No widget hardcodes hex values (all colors via TraevyTokensExt).
- Each widget is under 100 lines (CLAUDE.md).
- Analyzer is clean across the new directory.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-overhaul/08-03-SUMMARY.md` documenting: line count for each widget, which UI-SPEC sections each implements, and the formatter helpers introduced.
</output>
