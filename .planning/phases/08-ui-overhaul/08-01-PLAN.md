---
phase: 08-ui-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - assets/fonts/Inter-Regular.ttf
  - assets/fonts/Inter-Medium.ttf
  - assets/fonts/Inter-SemiBold.ttf
  - assets/fonts/Inter-Bold.ttf
  - assets/fonts/JetBrainsMono-Regular.ttf
  - assets/fonts/JetBrainsMono-Medium.ttf
  - assets/fonts/JetBrainsMono-SemiBold.ttf
  - lib/config/constants.dart
  - test/flutter_test_config.dart
  - test/widget/shared/widgets/stuck_bar_test.dart
  - test/widget/shared/widgets/trip_row_card_test.dart
  - test/widget/shared/widgets/section_label_test.dart
  - test/widget/shared/widgets/traevy_toggle_test.dart
  - test/widget/shared/widgets/stat_mini_card_test.dart
  - test/widget/features/shell/main_shell_test.dart
  - test/unit/config/theme_test.dart
  - test/unit/config/theme_extension_test.dart
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-04
  - UX-05
must_haves:
  truths:
    - "google_fonts ^8.1.0 is declared in pubspec dependencies"
    - "Eight font TTF files (Inter 400/500/600/700 + JetBrainsMono 400/500/600) exist under assets/fonts/"
    - "pubspec.yaml flutter.assets section lists assets/fonts/"
    - "Phase 8 constants block in constants.dart declares kFontUI='Inter' and kFontMono='JetBrainsMono' plus placeholder name/initial"
    - "flutter_test_config.dart disables google_fonts runtime fetching before every test"
    - "Eight RED Wave-0 test files exist with failing expectations for the shared widgets, theme, theme extension, and MainShell"
  artifacts:
    - path: pubspec.yaml
      provides: google_fonts dependency + assets/fonts/ asset declaration
      contains: "google_fonts:"
    - path: assets/fonts/Inter-Regular.ttf
      provides: Inter weight 400 TTF
      min_bytes: 50000
    - path: assets/fonts/JetBrainsMono-Regular.ttf
      provides: JetBrains Mono weight 400 TTF
      min_bytes: 50000
    - path: lib/config/constants.dart
      provides: Phase 8 font + placeholder constants
      contains: "kFontUI = 'Inter'"
    - path: test/flutter_test_config.dart
      provides: pre-test hook disabling google_fonts network fetch
      contains: "allowRuntimeFetching = false"
    - path: test/widget/shared/widgets/stuck_bar_test.dart
      provides: RED widget test for StuckBar
      contains: "StuckBar"
    - path: test/unit/config/theme_test.dart
      provides: RED unit test asserting buildLightTheme returns ThemeData with Traevy primary
      contains: "buildLightTheme"
  key_links:
    - from: pubspec.yaml flutter.assets
      to: assets/fonts/
      via: asset bundle declaration
      pattern: "- assets/fonts/"
    - from: test/flutter_test_config.dart
      to: package:google_fonts/google_fonts.dart
      via: import + GoogleFonts.config.allowRuntimeFetching=false
      pattern: "GoogleFonts.config.allowRuntimeFetching = false"
---

<objective>
Lay the foundation for the Phase 8 Traevy redesign: declare the `google_fonts ^8.1.0` dependency, bundle the Inter (400/500/600/700) and JetBrains Mono (400/500/600) TTF assets, append Phase 8 string/font constants to `lib/config/constants.dart`, and stage every Wave-0 RED test file required by later plans (StuckBar, TripRowCard, SectionLabel, TraevyToggle, StatMiniCard, MainShell, buildLightTheme, TraevyTokensExt.lerp). Also install the project-wide test config hook that disables google_fonts runtime fetching so widget tests stay deterministic on CI without network.

Purpose: Establish the package + asset + test scaffolding all subsequent Phase 8 plans depend on. Without this plan, downstream plans either cannot compile (no `google_fonts` import) or write tests that silently fall back to Roboto.

Output: pubspec.yaml updates, 7 TTF asset files committed, constants block appended, 8 RED test files plus `flutter_test_config.dart`.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/CLAUDE.md
@/Users/coolman/Documents/Projects/bizzle/.planning/PROJECT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/ROADMAP.md
@/Users/coolman/Documents/Projects/bizzle/.planning/STATE.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-CONTEXT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-UI-SPEC.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-RESEARCH.md
@/Users/coolman/Documents/Projects/bizzle/pubspec.yaml
@/Users/coolman/Documents/Projects/bizzle/lib/config/constants.dart

<interfaces>
<!-- Existing constants.dart already exposes Phase 1–7 constants. Append a new -->
<!-- "Phase 8" section after the Phase 7 block. Do NOT modify or relocate any -->
<!-- existing constant. Below are the new constants this plan adds: -->

// Phase 8 — UI Overhaul (Traevy design system)
const String kFontUI = 'Inter';
const String kFontMono = 'JetBrainsMono';
const String kPlaceholderUserName = 'Traveller';
const String kPlaceholderUserInitial = 'T';
const String kBrandShortName = 'tv';      // "tv" mark for TraevyLogoMark
const String kBrandFullName = 'Traevy';
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: pubspec + font assets + constants block</name>
  <files>pubspec.yaml, assets/fonts/Inter-Regular.ttf, assets/fonts/Inter-Medium.ttf, assets/fonts/Inter-SemiBold.ttf, assets/fonts/Inter-Bold.ttf, assets/fonts/JetBrainsMono-Regular.ttf, assets/fonts/JetBrainsMono-Medium.ttf, assets/fonts/JetBrainsMono-SemiBold.ttf, lib/config/constants.dart</files>
  <read_first>
    - pubspec.yaml (full file — to insert google_fonts in correct alphabetical position and add fonts to flutter.assets)
    - lib/config/constants.dart (full file — append at end after Phase 7 block; do not modify existing constants)
    - .planning/phases/08-ui-overhaul/08-CONTEXT.md (Typography decision block + Specifics block for brand name)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md (Pitfall 2 google_fonts runtime fetch, Assumption A10 asset bundling rules, Environment Availability section)
  </read_first>
  <behavior>
    - pubspec.yaml: a new dependency line `  google_fonts: ^8.1.0` exists under `dependencies:` between `geolocator: ^14.0.2` and `intl: ^0.20.2` (alphabetical insert).
    - pubspec.yaml: under `flutter:`, an `assets:` section exists (or has been extended) listing `    - assets/fonts/` so the entire directory is bundled at build time (per A10 in 08-RESEARCH.md).
    - Seven TTF files exist at the listed paths under assets/fonts/ — each non-empty, ≥ 50 KB (Google Fonts TTFs are 90–250 KB). The four Inter weights are 400/500/600/700; the three JetBrains Mono weights are 400/500/600.
    - lib/config/constants.dart: a clearly-delimited "Phase 8 — UI Overhaul (Traevy design system)" comment block is appended at the end of the file, declaring exactly: `kFontUI = 'Inter'`, `kFontMono = 'JetBrainsMono'`, `kPlaceholderUserName = 'Traveller'`, `kPlaceholderUserInitial = 'T'`, `kBrandShortName = 'tv'`, `kBrandFullName = 'Traevy'`. Each constant has a `///` doc comment referencing Phase 8.
    - Running `flutter pub get` succeeds with no version conflict messages.
  </behavior>
  <action>
    1. Add `google_fonts: ^8.1.0` to pubspec.yaml dependencies (alphabetical position: after `geolocator: ^14.0.2`, before `intl: ^0.20.2`).
    2. In pubspec.yaml under the `flutter:` section (after `uses-material-design: true`), add an `assets:` list containing one entry `- assets/fonts/` (preserve any existing entries; do NOT touch the existing `assets/icons/` reference if present).
    3. Download the seven Google Fonts TTFs from `https://fonts.gstatic.com/` or the Google Fonts ZIP bundles (run `curl -L -o assets/fonts/Inter-Regular.ttf https://github.com/google/fonts/raw/main/ofl/inter/Inter%5Bopsz,wght%5D.ttf` is variable-axis; instead download the static TTFs from `https://github.com/google/fonts/tree/main/ofl/inter/static` — Inter-Regular.ttf, Inter-Medium.ttf, Inter-SemiBold.ttf, Inter-Bold.ttf — and from `https://github.com/google/fonts/tree/main/ofl/jetbrainsmono/static` — JetBrainsMono-Regular.ttf, JetBrainsMono-Medium.ttf, JetBrainsMono-SemiBold.ttf). Place under assets/fonts/. Verify each file is &gt;50 KB after download.
    4. Append the Phase 8 constants block to lib/config/constants.dart with the six exact values listed in <behavior>. Use `///` doc comments referencing "Phase 8 — UI Overhaul" + design-token source.
    5. Run `flutter pub get` and `dart format lib/config/constants.dart pubspec.yaml`.
    6. Do NOT remove or change any existing constants. Do NOT introduce hardcoded hex values in constants.dart (tokens belong in Plan 02's theme.dart per CLAUDE.md "no hardcoded values" — constants.dart owns names and label strings only).
  </action>
  <verify>
    <automated>flutter pub get 2&gt;&amp;1 | grep -E "(Got dependencies|No dependencies have changed)" &amp;&amp; ls -la assets/fonts/*.ttf | wc -l | grep -q 7 &amp;&amp; grep -q "kFontUI = 'Inter'" lib/config/constants.dart &amp;&amp; grep -q "kFontMono = 'JetBrainsMono'" lib/config/constants.dart &amp;&amp; grep -q "google_fonts: \^8.1.0" pubspec.yaml &amp;&amp; flutter analyze lib/config/constants.dart</automated>
  </verify>
  <acceptance_criteria>
    - `pubspec.yaml` line matches regex `^  google_fonts: \^8\.1\.0$`.
    - `pubspec.yaml` under `flutter:` contains `- assets/fonts/`.
    - `ls assets/fonts/*.ttf | wc -l` outputs `7`.
    - Each `assets/fonts/*.ttf` has `stat -f%z` (or `wc -c`) ≥ 50000.
    - `lib/config/constants.dart` contains, verbatim and as separate `const` declarations:
      `const String kFontUI = 'Inter';`
      `const String kFontMono = 'JetBrainsMono';`
      `const String kPlaceholderUserName = 'Traveller';`
      `const String kPlaceholderUserInitial = 'T';`
      `const String kBrandShortName = 'tv';`
      `const String kBrandFullName = 'Traevy';`
    - `flutter pub get` exits 0 with no resolver conflicts.
    - `flutter analyze lib/config/constants.dart` exits 0 with no warnings.
  </acceptance_criteria>
  <done>
    pubspec declares google_fonts ^8.1.0 and bundles assets/fonts/; seven TTF files (>50 KB each) are committed under assets/fonts/; constants.dart owns the six new Phase 8 identifier constants; flutter pub get succeeds; flutter analyze exits clean.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: test config + Wave 0 RED test scaffolds</name>
  <files>test/flutter_test_config.dart, test/widget/shared/widgets/stuck_bar_test.dart, test/widget/shared/widgets/trip_row_card_test.dart, test/widget/shared/widgets/section_label_test.dart, test/widget/shared/widgets/traevy_toggle_test.dart, test/widget/shared/widgets/stat_mini_card_test.dart, test/widget/features/shell/main_shell_test.dart, test/unit/config/theme_test.dart, test/unit/config/theme_extension_test.dart</files>
  <read_first>
    - test/widget/features/dashboard/dashboard_screen_test.dart (study existing test helpers, ProviderScope wrapping, MaterialApp setup pattern)
    - test/widget/features/settings/settings_screen_test.dart (same — confirm how widget tests mount the theme)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md (Validation Architecture section + Test Isolation Concern section — google_fonts and widget tests)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §10 Shared Component Contracts (props for StuckBar, TripRowCard, SectionLabel, TraevyToggle, StatMiniCard)
  </read_first>
  <behavior>
    - test/flutter_test_config.dart: defines `Future&lt;void&gt; testExecutable(FutureOr&lt;void&gt; Function() testMain) async { GoogleFonts.config.allowRuntimeFetching = false; await testMain(); }`.
    - Each of the eight RED test files imports its target file path (which does NOT exist yet) so the test fails at the import level — this is the deliberate RED state for Wave 0. After Plans 02/03/04 land, these tests must turn GREEN with no further changes (other than the obvious code under test being added).
    - The eight test files cover:
        1. stuck_bar_test.dart — `StuckBar(movingMinutes: 0, stuckMinutes: 0)` renders without crashing AND `StuckBar(movingMinutes: 30, stuckMinutes: 10)` produces a Row with two children whose flex values match the inputs.
        2. trip_row_card_test.dart — Renders for direction 'to_office' (accentBg avatar) and 'to_home' (movingBg avatar); duration text uses kFontMono family; tap callback fires.
        3. section_label_test.dart — Renders the given text in UPPERCASE with letterSpacing &gt;= 1.0 and the textMuted token color from TraevyTokensExt.
        4. traevy_toggle_test.dart — Off state renders borderStr bg + knob aligned-left; on state renders moving bg + knob aligned-right; tap invokes onChanged with the inverted value.
        5. stat_mini_card_test.dart — Renders label (Inter) + value (JetBrainsMono) + optional unit; tone='stuck' tints the value with the stuck token color.
        6. main_shell_test.dart — MainShell mounts under a ProviderScope and shows a NavigationBar with exactly four NavigationDestinations whose labels are 'Today', 'Trips', 'Stats', 'Settings'; tapping the second destination switches the IndexedStack child to HistoryScreen.
        7. theme_test.dart — `buildLightTheme()` returns a ThemeData whose `colorScheme.primary` equals `Color(0xFF3A5F8F)` (light accent), `colorScheme.error` equals `Color(0xFFC0392B)` (light danger), `scaffoldBackgroundColor` equals `Color(0xFFFAFAF7)` (light bg); same shape for `buildDarkTheme()` with the dark hex values.
        8. theme_extension_test.dart — `TraevyTokensExt.fromTokens(TraevyTokens.light).lerp(TraevyTokensExt.fromTokens(TraevyTokens.dark), 0.5)` produces a non-null instance whose `bgElev` is the Color.lerp midpoint of `0xFFFFFFFF` and `0xFF22242E`.
  </behavior>
  <action>
    1. Create `test/flutter_test_config.dart` with the standard Flutter pre-test hook. Exact body:

       ```dart
       import 'dart:async';
       import 'package:flutter_test/flutter_test.dart';
       import 'package:google_fonts/google_fonts.dart';

       /// Flutter test bootstrapping hook. Runs once before all tests in any
       /// `test/` subtree. Disables google_fonts runtime network fetching so
       /// widget tests never attempt to download fonts (Pitfall 2 in
       /// .planning/phases/08-ui-overhaul/08-RESEARCH.md).
       Future&lt;void&gt; testExecutable(FutureOr&lt;void&gt; Function() testMain) async {
         GoogleFonts.config.allowRuntimeFetching = false;
         await testMain();
       }
       ```

    2. Create the seven Wave-0 test files. Each one imports `package:traevy/...` targets that do not yet exist (Plans 02/03/04 will create them). Each test uses `testWidgets` or `test` (depending on widget vs unit) and asserts the behaviour listed in <behavior>. Wrap widget tests in:

       ```dart
       await tester.pumpWidget(
         ProviderScope(
           child: MaterialApp(
             theme: buildLightTheme(),
             home: Scaffold(body: ...widget under test...),
           ),
         ),
       );
       ```

       Use concrete expectations from CONTEXT.md / UI-SPEC.md hex values — do NOT use vague "looks correct".

    3. For `main_shell_test.dart`, use `find.byType(NavigationBar)`, `find.byType(NavigationDestination)` (matcher: hasLength(4)), `find.text('Today')`, `find.text('Trips')`, `find.text('Stats')`, `find.text('Settings')`. Drive the tap with `await tester.tap(find.text('Trips'))` then `await tester.pumpAndSettle()` and assert `find.byType(HistoryScreen)` appears under the IndexedStack.

    4. For `theme_test.dart`, expect exact `Color` instances:
       - Light: primary=Color(0xFF3A5F8F), error=Color(0xFFC0392B), scaffoldBackgroundColor=Color(0xFFFAFAF7), brightness=Brightness.light, useMaterial3=true.
       - Dark: primary=Color(0xFF8AABCF), error=Color(0xFFE05A4A), scaffoldBackgroundColor=Color(0xFF1A1B22), brightness=Brightness.dark, useMaterial3=true.

    5. For `theme_extension_test.dart`, assert:
       - `Theme.of(context).extension&lt;TraevyTokensExt&gt;()` returns non-null when the theme is `buildLightTheme()`.
       - The light extension's `moving` color equals Color(0xFF2E8B57) and the dark's equals Color(0xFF5BC88A).
       - `TraevyTokensExt.fromTokens(TraevyTokens.light).lerp(TraevyTokensExt.fromTokens(TraevyTokens.dark), 0.5)?.bgElev` equals `Color.lerp(const Color(0xFFFFFFFF), const Color(0xFF22242E), 0.5)`.

    6. Add `// ignore_for_file: uri_does_not_exist` at the top of each Wave-0 test file ONLY if necessary to silence analyzer noise until the targets exist; otherwise leave the analyzer warning visible (the tests are deliberately RED).

    7. Run `dart format` on every new file. Confirm `flutter test test/flutter_test_config.dart` is reachable (Flutter auto-discovers it). Run `flutter test test/unit/config/theme_test.dart` — it MUST fail with an import error referencing `lib/config/theme.dart` not exporting `buildLightTheme` (the RED state).

    8. Do NOT create the production targets in this task — they are owned by Plans 02 / 03 / 04. This task ONLY scaffolds the RED state.
  </action>
  <verify>
    <automated>test -f test/flutter_test_config.dart &amp;&amp; grep -q "allowRuntimeFetching = false" test/flutter_test_config.dart &amp;&amp; ls test/widget/shared/widgets/*.dart | wc -l | grep -q 5 &amp;&amp; test -f test/widget/features/shell/main_shell_test.dart &amp;&amp; test -f test/unit/config/theme_test.dart &amp;&amp; test -f test/unit/config/theme_extension_test.dart &amp;&amp; (flutter test test/unit/config/theme_test.dart 2&gt;&amp;1 | grep -qE "(Error|Failed|undefined|not found|uri_does_not_exist|buildLightTheme)" &amp;&amp; echo "RED confirmed" || (echo "Test passed prematurely — Wave 0 must be RED" &amp;&amp; exit 1))</automated>
  </verify>
  <acceptance_criteria>
    - `test/flutter_test_config.dart` exists and contains `GoogleFonts.config.allowRuntimeFetching = false;`.
    - The five files under `test/widget/shared/widgets/` exist: `stuck_bar_test.dart`, `trip_row_card_test.dart`, `section_label_test.dart`, `traevy_toggle_test.dart`, `stat_mini_card_test.dart`.
    - `test/widget/features/shell/main_shell_test.dart` exists.
    - `test/unit/config/theme_test.dart` and `test/unit/config/theme_extension_test.dart` exist.
    - Each Wave-0 test file references the production import path it expects (e.g. `package:traevy/shared/widgets/stuck_bar.dart`).
    - `flutter test test/unit/config/theme_test.dart` exits non-zero (RED state — `buildLightTheme` does not yet exist).
    - All eight new files pass `dart format --output=none --set-exit-if-changed`.
  </acceptance_criteria>
  <done>
    `flutter_test_config.dart` is in place and disables runtime fetching. Eight RED test files compile-fail (or assertion-fail) deterministically against the not-yet-existing targets, ready for Plans 02/03/04 to turn GREEN.
  </done>
</task>

</tasks>

<verification>
- `flutter pub get` succeeds.
- `ls assets/fonts/*.ttf | wc -l` reports 7.
- `grep -q "kFontUI = 'Inter'" lib/config/constants.dart` exits 0.
- `grep -q "allowRuntimeFetching = false" test/flutter_test_config.dart` exits 0.
- `flutter test test/unit/config/theme_test.dart` exits non-zero (deliberate RED).
- `flutter analyze lib/config/constants.dart pubspec.yaml` exits 0.
</verification>

<success_criteria>
- google_fonts ^8.1.0 declared and resolved.
- Seven TTF font files bundled and listed in pubspec assets.
- Phase 8 constants (kFontUI, kFontMono, kPlaceholderUserName, kPlaceholderUserInitial, kBrandShortName, kBrandFullName) added without disturbing prior constants.
- Test bootstrap disables google_fonts network fetching globally.
- All Wave-0 RED tests exist and fail deterministically.
- No existing tests broken by this plan (only new files added; constants.dart additions are append-only).
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-overhaul/08-01-SUMMARY.md` documenting: TTF source URLs, pubspec diff, constants added, test files staged.
</output>
