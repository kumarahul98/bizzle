---
phase: 08-ui-overhaul
plan: 05
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - lib/features/tracking/screens/tracking_screen.dart
  - lib/features/tracking/widgets/recording_header.dart
  - lib/features/tracking/widgets/elapsed_display.dart
  - lib/features/tracking/widgets/stop_button.dart
  - lib/features/tracking/widgets/faux_map_card.dart
  - lib/features/tracking/widgets/tracking_active_layout.dart
  - lib/features/tracking/widgets/tracking_idle_layout.dart
  - lib/features/tracking/widgets/tracking_status_layout.dart
  - lib/features/tracking/widgets/tracking_error_layout.dart
  - lib/features/tracking/widgets/tracking_tiles_row.dart
  - lib/features/tracking/widgets/duration_tile.dart
  - lib/features/tracking/widgets/distance_tile.dart
  - lib/features/tracking/widgets/current_speed_tile.dart
  - lib/features/trips/screens/history_screen.dart
  - lib/features/trips/widgets/history_view_toggle.dart
  - lib/features/trips/widgets/trip_section_card.dart
  - lib/features/trips/widgets/trip_card.dart
  - test/widget/features/tracking/tracking_screen_test.dart
  - test/widget/features/trips/history_screen_test.dart
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "Tracking screen Variant A renders: ● RECORDING pill + direction label header, ELAPSED label + 76sp mono timer, 3-card stat row (Distance, Speed, Stuck), 180dp faux map, full-width text-bg Stop and save trip button"
    - "History screen renders 'Trips' title with calendar + plus icon buttons, List/Calendar pill segmented control, date-grouped trip sections with TripRowCards in bgElev cards"
    - "Tracking widget tests and history widget tests pass GREEN"
    - "trip_card.dart either re-exports TripRowCard or is deleted in favour of TripRowCard imports across history"
  artifacts:
    - path: lib/features/tracking/widgets/recording_header.dart
      provides: pulsing ● RECORDING pill + direction label header
      contains: "class RecordingHeader"
    - path: lib/features/tracking/widgets/elapsed_display.dart
      provides: ELAPSED label + huge mono timer
      contains: "class ElapsedDisplay"
    - path: lib/features/tracking/widgets/stop_button.dart
      provides: full-width text-bg Stop and save trip button
      contains: "class StopButton"
    - path: lib/features/tracking/widgets/faux_map_card.dart
      provides: 180dp mapBg faux map placeholder
      contains: "class FauxMapCard"
    - path: lib/features/trips/widgets/history_view_toggle.dart
      provides: List/Calendar pill segmented control
      contains: "class HistoryViewToggle"
    - path: lib/features/trips/widgets/trip_section_card.dart
      provides: date header + grouped trip rows in bgElev card
      contains: "class TripSectionCard"
  key_links:
    - from: lib/features/tracking/widgets/tracking_active_layout.dart
      to: lib/features/tracking/widgets/{recording_header,elapsed_display,stop_button,faux_map_card}.dart + lib/shared/widgets/stat_mini_card.dart
      via: composition under SafeArea + Padding(20)
      pattern: "RecordingHeader"
    - from: lib/features/trips/screens/history_screen.dart
      to: lib/features/trips/widgets/{history_view_toggle,trip_section_card}.dart + lib/shared/widgets/trip_row_card.dart
      via: direct child widgets of the screen
      pattern: "TripSectionCard"
---

<objective>
Restyle the Active Recording screen to Variant A spec and the Trip History screen to the new "Trips" layout (title row + List/Calendar pill + date-grouped sections). Both screens compose the shared primitives from Plan 03 (StatMiniCard, TripRowCard, SectionLabel, StuckBar) and the Traevy theme from Plan 02 (no hex literals in feature code). The legacy `trip_card.dart` is collapsed into `TripRowCard` imports (delete or re-export). Existing tracking and history widget tests are updated for the new layout and pass GREEN.

Purpose: Bring the two highest-traffic non-dashboard screens to the design. After this plan, four of seven user-visible screens (Dashboard, Tracking, History, plus the still-restyled-by-Plan-06/07 Trip Detail/Stats/Settings/Onboarding) reflect the new design.

Output: 11 modified tracking files (some restyle-in-place, some new sub-widget files), 4 modified history files (restyle screen + 2 new widgets + collapse trip_card), 2 updated test files.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/CLAUDE.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-CONTEXT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-UI-SPEC.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-RESEARCH.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-02-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-03-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/screens/tracking_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/widgets/tracking_active_layout.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/widgets/tracking_idle_layout.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/widgets/tracking_status_layout.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/widgets/tracking_error_layout.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/widgets/tracking_tiles_row.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/widgets/duration_tile.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/widgets/distance_tile.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/tracking/widgets/current_speed_tile.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/trips/screens/history_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/trips/widgets/trip_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/trip_row_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/section_label.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/stat_mini_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/stuck_bar.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme_extension.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/features/tracking/tracking_screen_test.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/features/trips/history_screen_test.dart

<interfaces>
<!-- Existing providers consumed (do NOT modify): -->
//   trackingStateProvider :: Provider<TrackingState>  (sealed: Idle | Starting | Active | Stopping | Error)
//   allTripSummariesProvider :: StreamProvider<List<TripSummary>>  (history feed)
//   trackingControllerProvider :: handles start/stop

// Existing tracking layouts to restyle (NOT delete):
//   TrackingActiveLayout, TrackingIdleLayout, TrackingStatusLayout, TrackingErrorLayout

// Existing constants kept:
//   kRouteTripDetail
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tracking screen Variant A restyle (active layout + sub-widgets + tile updates)</name>
  <files>lib/features/tracking/screens/tracking_screen.dart, lib/features/tracking/widgets/recording_header.dart, lib/features/tracking/widgets/elapsed_display.dart, lib/features/tracking/widgets/stop_button.dart, lib/features/tracking/widgets/faux_map_card.dart, lib/features/tracking/widgets/tracking_active_layout.dart, lib/features/tracking/widgets/tracking_idle_layout.dart, lib/features/tracking/widgets/tracking_status_layout.dart, lib/features/tracking/widgets/tracking_error_layout.dart, lib/features/tracking/widgets/tracking_tiles_row.dart, lib/features/tracking/widgets/duration_tile.dart, lib/features/tracking/widgets/distance_tile.dart, lib/features/tracking/widgets/current_speed_tile.dart, test/widget/features/tracking/tracking_screen_test.dart</files>
  <read_first>
    - lib/features/tracking/screens/tracking_screen.dart (full file; identify how it dispatches on sealed TrackingState)
    - lib/features/tracking/widgets/tracking_active_layout.dart (current Variant pre-redesign)
    - lib/features/tracking/widgets/duration_tile.dart, distance_tile.dart, current_speed_tile.dart (current tile shapes; these are replaced by StatMiniCard wrappers but keep their public APIs so tests pass)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §4 Active Recording Screen Variant A
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Open Question 5 (transient states retained) + "Don't Hand-Roll → Pulsing recording dot"
    - test/widget/features/tracking/tracking_screen_test.dart (full file; identify which assertions rely on text labels like 'Duration', 'Distance', 'Speed' — those labels remain valid; assertions on specific font sizes/colours do NOT exist per RESEARCH §Wave 0)
  </read_first>
  <behavior>
    - `RecordingHeader` (≤ 80 lines, StatefulWidget for pulse animation OR ConsumerWidget consuming an AnimationController via a TickerProviderStateMixin — use AnimatedBuilder + AnimationController(vsync: this, duration: const Duration(milliseconds: 1500))..repeat(reverse: true) per "Don't Hand-Roll"):
        - Renders Row(crossAxisAlignment: center): pulsing 8dp circle (Container with BoxDecoration shape circle, color: tokens.record, AnimatedBuilder modulates opacity 0.5↔1.0), 6dp gap, Text('RECORDING', TraevyFonts.ui(size: 12, w700, letterSpacing: 1.5, color: tokens.record)), Spacer, Text(directionLabel — e.g. 'To office', TraevyFonts.ui(size: 12, w600, color: tokens.textMuted)).
        - Padding EdgeInsets.fromLTRB(20, 16, 20, 12).
        - Props: `({required String directionLabel, super.key})`.
    - `ElapsedDisplay` (≤ 60 lines):
        - Renders Column(crossAxisAlignment: center): SectionLabel('Elapsed', fontSize: 11), 8dp gap, Text(formatElapsed(durationSeconds), TraevyFonts.mono(size: 76, weight: w500, letterSpacing: -3, color: onSurface)).
        - `formatElapsed(int s)` returns "HH:MM:SS" (zero-padded).
        - Props: `({required int durationSeconds, super.key})`.
    - `StopButton` (≤ 60 lines):
        - Renders InkWell wrapping Container(width: double.infinity, padding: EdgeInsets.symmetric(horizontal: 18, vertical: 20), decoration: BoxDecoration(color: onSurface (=tokens.text), borderRadius: BorderRadius.circular(16)), child: Row(center: Icon(Icons.stop_rounded, size: 22, color: tokens.bg), 12dp gap, Text('Stop and save trip', TraevyFonts.ui(size: 15, w600, color: tokens.bg)))).
        - Props: `({required VoidCallback onPressed, super.key})`.
    - `FauxMapCard` (≤ 60 lines):
        - Renders Container(height: 180, decoration: BoxDecoration(color: tokens.mapBg, borderRadius: BorderRadius.circular(16), border: Border.all(color: tokens.border))). Optionally overlay a `CustomPaint` painter that draws a faint grid of horizontal+vertical lines at 24dp spacing in tokens.surface2 — keep painter ≤ 20 lines.
    - `TrackingActiveLayout` (restyled, ≤ 100 lines):
        - Removes the old AppBar entirely (RecordingHeader replaces it).
        - Body: SafeArea(child: Padding(20dp horizontal, 16dp vertical, child: Column(children: [RecordingHeader(directionLabel), Spacer, ElapsedDisplay(durationSeconds), 32dp gap, TrackingTilesRow (3 StatMiniCards), 24dp gap, FauxMapCard, Spacer, StopButton])))).
    - `TrackingTilesRow` (restyled, ≤ 60 lines):
        - Renders Row(children: [Expanded(DistanceTile(distanceMeters)), 12dp gap, Expanded(CurrentSpeedTile(speedKmh)), 12dp gap, Expanded(DurationTile(stuckSeconds, label: 'Stuck'))]).
        - Replace the three tile widgets with `StatMiniCard(label, value, unit, tone)` wrappers — each tile widget becomes a thin adapter that formats the underlying provider value and renders a StatMiniCard. The existing public API of each tile widget (e.g. DistanceTile constructor) stays the same so tracking_screen_test.dart's `find.byType(DistanceTile)` assertions still resolve.
    - DistanceTile: tone=neutral, label='DISTANCE', value=formatted distance e.g. "4.1", unit="km" (or "m" when &lt;1km).
    - CurrentSpeedTile: tone=moving when speed≥10 km/h else stuck. label='SPEED', value=integer km/h, unit='km/h'.
    - DurationTile when used for STUCK (existing widget reused with `label` override): tone=stuck, label='STUCK', value formatted as e.g. '4m' or '12s'.
    - `TrackingIdleLayout`, `TrackingStatusLayout`, `TrackingErrorLayout`: NO structural changes, but every hardcoded `colorScheme.surfaceContainerLow` reference is replaced with `Theme.of(context).extension<TraevyTokensExt>()!.bgElev` (Pitfall 9), and every direct `colorScheme.primary` button styling is left intact (M3 ButtonTheme picks up Phase 8 colours automatically). Card radius 12 → 16 where applicable (Pitfall 4).
    - `tracking_screen.dart`: dispatches on `TrackingState` exactly as before. Variant A is the active layout only; idle/status/error remain their existing layouts (per Open Question 5).
    - `tracking_screen_test.dart` (Wave 0 gap from RESEARCH.md): update assertions that rely on `find.text('Duration')` to instead use `find.text('STUCK')` for the third tile (DurationTile's label changed for the active layout). `find.text('Distance')` becomes `find.text('DISTANCE')`. `find.text('Speed')` becomes `find.text('SPEED')`. These match the uppercased SectionLabel inside StatMiniCard.
  </behavior>
  <action>
    1. Create the four new sub-widget files (RecordingHeader, ElapsedDisplay, StopButton, FauxMapCard). Wire animations as specified.
    2. Update TrackingActiveLayout to compose the new sub-widgets.
    3. Update each of the three tile files (duration_tile, distance_tile, current_speed_tile) to be thin wrappers over `StatMiniCard`. Preserve their constructor APIs so tests resolving them by type continue to work.
    4. Replace `colorScheme.surfaceContainerLow` usages with `tokens.bgElev` in tracking_active_layout.dart and the three other layouts.
    5. Replace `BorderRadius.circular(12)` with `BorderRadius.circular(16)` anywhere in `lib/features/tracking/widgets/` that creates a card.
    6. Update `test/widget/features/tracking/tracking_screen_test.dart`:
        - Change `find.text('Duration')` → `find.text('STUCK')` (or whichever label maps; align with new SectionLabel-driven uppercase labels — verify by reading the test).
        - Change `find.text('Distance')` → `find.text('DISTANCE')`.
        - Change `find.text('Speed')` → `find.text('SPEED')`.
        - Where the existing test asserts the elapsed timer renders, update to `find.byType(ElapsedDisplay)` and `find.textContaining(':')` to match the HH:MM:SS string.
        - Add at least one new assertion: `find.byType(RecordingHeader)` findsOneWidget when TrackingState is Active.
        - Add `find.byType(StopButton)` findsOneWidget when active; tapping it should fire the existing stop handler — assert by stubbing the controller.
    7. `dart format` then `flutter analyze lib/features/tracking/` — expect 0 warnings.
    8. `flutter test test/widget/features/tracking/` must pass GREEN.
  </action>
  <verify>
    <automated>flutter analyze lib/features/tracking/ 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/features/tracking/</automated>
  </verify>
  <acceptance_criteria>
    - The four new files (recording_header.dart, elapsed_display.dart, stop_button.dart, faux_map_card.dart) exist, each &lt; 80 lines, each declares a `StatelessWidget` or `StatefulWidget` with `const` constructor (RecordingHeader may be Stateful for animation; the constructor itself is `const RecordingHeader({...})`).
    - `lib/features/tracking/widgets/tracking_active_layout.dart` references `RecordingHeader`, `ElapsedDisplay`, `TrackingTilesRow`, `FauxMapCard`, `StopButton` (grep all five).
    - No file in `lib/features/tracking/` references `colorScheme.surfaceContainerLow` (grep returns 0 matches).
    - No file in `lib/features/tracking/widgets/` uses `BorderRadius.circular(12)` (grep returns 0 matches).
    - `flutter test test/widget/features/tracking/` exits 0.
    - `flutter analyze lib/features/tracking/` exits 0.
    - `RecordingHeader` uses an `AnimationController` with `Duration(milliseconds: 1500)` and `..repeat(reverse: true)` (no `Timer.periodic`).
  </acceptance_criteria>
  <done>
    Variant A active recording layout renders correctly with the four new sub-widgets. Idle/Status/Error layouts adopt the new tokens without structural changes. All tracking widget tests pass with the updated label assertions.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: History screen restyle — title row, pill toggle, date-grouped sections; collapse trip_card</name>
  <files>lib/features/trips/screens/history_screen.dart, lib/features/trips/widgets/history_view_toggle.dart, lib/features/trips/widgets/trip_section_card.dart, lib/features/trips/widgets/trip_card.dart, test/widget/features/trips/history_screen_test.dart</files>
  <read_first>
    - lib/features/trips/screens/history_screen.dart (full file; identify TabBar / list/calendar branching, `table_calendar` configuration including markerDecoration colours per Pitfall 10)
    - lib/features/trips/widgets/trip_card.dart (full file; Pitfall 8 — `_DirectionChip` has hardcoded TextStyle(fontSize:12) to fix)
    - lib/shared/widgets/trip_row_card.dart (Plan 03 output — direct replacement for trip_card.dart's external API)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §5 Trip History Screen + §10 TripRowCard contract
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Pitfall 8 (trip_card hardcoded TextStyle) + Pitfall 10 (table_calendar colours)
    - test/widget/features/trips/history_screen_test.dart (full file)
    - lib/features/trips/widgets/edit_trip_sheet.dart, manual_entry_sheet.dart (do NOT modify — Plan 7 audits theme compatibility only)
  </read_first>
  <behavior>
    - `HistoryViewToggle` (≤ 70 lines):
        - StatefulWidget OR ConsumerWidget. Props: `({required HistoryView selectedView, required ValueChanged<HistoryView> onChanged, super.key})`.
        - Renders a single Container(decoration: BoxDecoration(color: Colors.transparent, borderRadius: BorderRadius.circular(14), border: Border.all(color: tokens.borderStr))) containing a Row of two Expanded children: each a GestureDetector (selected: background tokens.text + text color tokens.bg + radius 14 / unselected: transparent + text color tokens.textDim). Labels: 'List' and 'Calendar'. Padding inside each cell: 10dp vertical.
        - `HistoryView` is a sealed class or enum (use existing if `lib/features/trips/` already defines it; else add `enum HistoryView { list, calendar }` in this file).
    - `TripSectionCard` (≤ 90 lines):
        - Props: `({required String dateLabel, required String? subtitleLabel, required String totalLabel, required List<TripSummary> trips, required ValueChanged<TripSummary> onTripTap, super.key})`.
        - Renders Column: a header Row(dateLabel via TraevyFonts.ui(size: 13, w600, color: onSurface) on left, 4dp gap, subtitleLabel via TraevyFonts.ui(size: 11, w500, color: tokens.textMuted), Spacer, totalLabel via TraevyFonts.mono(size: 12, w600, color: tokens.textDim)).
        - Then a Container spanning full width (no horizontal padding so border lines extend to edge) with decoration BoxDecoration(color: tokens.bgElev, border: Border(top: BorderSide(color: tokens.border), bottom: BorderSide(color: tokens.border))) wrapping a Column of TripRowCard widgets. Each TripRowCard has `showDivider: index &lt; trips.length - 1`.
        - When `trips.isEmpty`: render a single text 'No trips' in TraevyFonts.ui(size: 13, w500, color: tokens.textMuted) inside the card.
    - `history_screen.dart` restyle:
        - Replace the existing AppBar with a Sliver header Row: Text('Trips', textTheme.titleLarge) on left, Spacer, two icon buttons:
            1. 36dp surface-bg circle with Icon(Icons.calendar_today_rounded, size: 18, color: onSurface) — toggles between calendar/list views.
            2. 36dp text-bg circle with Icon(Icons.add_rounded, size: 18, color: tokens.bg) — pushes the manual-entry sheet (preserve existing call site).
        - Below: 12dp gap, then HistoryViewToggle. 16dp gap, then either:
            * List view: a `ListView.builder` rendering one TripSectionCard per date group.
            * Calendar view: the existing `TableCalendar` BUT with `CalendarStyle` colours updated per Pitfall 10 — markerDecoration uses `tokens.accent`, selectedDecoration uses `tokens.text` with `BoxDecoration(shape: BoxShape.circle)`, todayDecoration uses `tokens.accentBg`, weekdayStyle/headerStyle text colours pulled from textTheme.
        - The screen's Scaffold should NOT have an AppBar (because MainShell owns navigation). It IS a child of IndexedStack and just renders content with a top padding for the title row.
    - `trip_card.dart`: this file's external use sites are inside `lib/features/dashboard/widgets/today_trips_section.dart` (deleted in Plan 04) and `lib/features/trips/screens/history_screen.dart` (replaced by TripRowCard in this plan). So `trip_card.dart` can be DELETED entirely OR replaced with a one-line re-export of TripRowCard for backwards compatibility. **Decision**: Delete `trip_card.dart` — no remaining call sites (Plan 04 already removed TodayTripsSection; the manual-entry sheet uses Material widgets not trip_card). Verify via grep that no remaining file imports `package:traevy/features/trips/widgets/trip_card.dart` after deletion. If a grep finds any import, update those files to `import 'package:traevy/shared/widgets/trip_row_card.dart';` and switch the constructor call to TripRowCard.
    - `history_screen_test.dart`:
        - Update locators relying on old `TripCard` to `find.byType(TripRowCard)`.
        - Update any reference to `AppBar` widget assertions (history's AppBar is gone) — these should already be calendar/list-specific.
        - Update the calendar-toggle test to drive HistoryViewToggle instead of the prior IconButton.
        - Add an assertion: `find.byType(HistoryViewToggle)` findsOneWidget.
        - Add an assertion that a TripSectionCard renders for at least one date when test data is seeded.
  </behavior>
  <action>
    1. Create `lib/features/trips/widgets/history_view_toggle.dart` per <behavior>.
    2. Create `lib/features/trips/widgets/trip_section_card.dart` per <behavior>.
    3. Rewrite `lib/features/trips/screens/history_screen.dart`:
        - Remove the AppBar. The screen renders a CustomScrollView or Column-with-Padding(20dp horizontal, 16dp top).
        - Use the new title row and HistoryViewToggle.
        - List view: iterate `allTripSummariesProvider.when(data: (summaries) { final grouped = _groupByDate(summaries); return ListView.builder(...); })`.
        - Calendar view: keep `TableCalendar` but update `CalendarStyle` colours per Pitfall 10.
        - Preserve the `+` icon button's call to `ManualEntrySheet` (read existing call to find the exact invocation).
        - Preserve tap-to-detail navigation: `Navigator.pushNamed(context, kRouteTripDetail, arguments: trip.id)`.
    4. Grep for `import 'package:traevy/features/trips/widgets/trip_card.dart'` across the codebase. If zero matches remain after this plan, DELETE the file. If matches remain (e.g., in test fixtures), update them to use TripRowCard.
    5. Update `test/widget/features/trips/history_screen_test.dart` per <behavior>.
    6. `dart format lib/features/trips/`, then `flutter analyze lib/features/trips/`.
    7. `flutter test test/widget/features/trips/history_screen_test.dart` must pass GREEN. (edit_trip_sheet_test, manual_entry_sheet_test, trip_detail_screen_test continue to run; trip_detail_screen_test failures are Plan 06's responsibility.)
  </action>
  <verify>
    <automated>flutter analyze lib/features/trips/ 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/features/trips/history_screen_test.dart test/widget/features/trips/edit_trip_sheet_test.dart test/widget/features/trips/manual_entry_sheet_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/trips/widgets/history_view_toggle.dart` and `trip_section_card.dart` exist, each &lt; 100 lines.
    - `lib/features/trips/widgets/trip_card.dart` is deleted (`! test -f`) AND `grep -r "features/trips/widgets/trip_card" lib/ test/` returns 0 matches.
    - `lib/features/trips/screens/history_screen.dart` no longer creates an `AppBar` (verified by `! grep -E "AppBar\(" lib/features/trips/screens/history_screen.dart`).
    - `lib/features/trips/screens/history_screen.dart` references `HistoryViewToggle` and `TripSectionCard` (greps for both succeed).
    - `lib/features/trips/screens/history_screen.dart` no longer uses `colorScheme.primary` or `colorScheme.primaryContainer` for `markerDecoration`/`selectedDecoration` — instead uses `tokens.accent` / `tokens.text` / `tokens.accentBg` (verified by grep of markerDecoration block).
    - `flutter test test/widget/features/trips/history_screen_test.dart` exits 0.
    - `flutter test test/widget/features/trips/edit_trip_sheet_test.dart test/widget/features/trips/manual_entry_sheet_test.dart` exits 0 (sheet tests should already pass — sheet styling is auto-themed; this is a regression check, not new test work).
    - `flutter analyze lib/features/trips/` exits 0.
  </acceptance_criteria>
  <done>
    History screen uses the new title row, HistoryViewToggle, and TripSectionCard-grouped TripRowCards. table_calendar colours are token-driven. trip_card.dart is gone. history_screen_test plus the edit/manual sheet tests all pass.
  </done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/tracking/ lib/features/trips/` exits 0.
- `flutter test test/widget/features/tracking/ test/widget/features/trips/history_screen_test.dart test/widget/features/trips/edit_trip_sheet_test.dart test/widget/features/trips/manual_entry_sheet_test.dart` passes.
- `! test -f lib/features/trips/widgets/trip_card.dart` (deleted).
- `! grep -r "surfaceContainerLow" lib/features/tracking/` (Pitfall 9 fully cleared in tracking).
- `! grep -r "BorderRadius.circular(12)" lib/features/tracking/widgets/ lib/features/trips/widgets/` (Pitfall 4 cleared).
</verification>

<success_criteria>
- Tracking Variant A is on-screen with pulsing dot, big mono timer, 3 stat cards, faux map, full-width stop button.
- History uses title row + HistoryViewToggle + TripSectionCard groups with TripRowCards.
- Legacy trip_card.dart is gone.
- table_calendar colours pull from tokens (Pitfall 10).
- All tracking + history widget tests pass.
- Analyzer clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-overhaul/08-05-SUMMARY.md` documenting: tracking sub-widget breakdown, table_calendar colour map, the trip_card → TripRowCard migration audit, label-string changes that drove test updates.
</output>
