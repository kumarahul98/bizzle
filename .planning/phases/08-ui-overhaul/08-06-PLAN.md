---
phase: 08-ui-overhaul
plan: 06
type: execute
wave: 4
depends_on: [02, 03]
files_modified:
  - lib/features/trips/screens/trip_detail_screen.dart
  - lib/features/trips/widgets/traffic_insight_card.dart
  - lib/features/trips/widgets/trip_timeline.dart
  - lib/features/stats/screens/stats_screen.dart
  - lib/features/stats/widgets/stats_card.dart
  - lib/features/stats/widgets/traffic_loss_hero.dart
  - lib/features/stats/widgets/donut_card.dart
  - lib/features/stats/widgets/trend_bars_card.dart
  - lib/features/stats/widgets/weekday_chart_card.dart
  - lib/features/stats/widgets/best_worst_day_card.dart
  - lib/features/stats/widgets/direction_averages_card.dart
  - lib/features/stats/widgets/traffic_waste_card.dart
  - lib/features/stats/widgets/trend_chart_card.dart
  - lib/features/stats/widgets/week_month_totals_card.dart
  - test/widget/features/trips/trip_detail_screen_test.dart
  - test/widget/features/stats/stats_screen_test.dart
autonomous: true
requirements:
  - UX-01
must_haves:
  truths:
    - "Trip Detail screen renders back arrow + center date/time + more-options, faux map 210dp, EVENING COMMUTE / direction title, Duration+Distance stat tile pair, StuckBar with moving/stuck legend, stuckBg traffic insight callout, and timeline rows"
    - "Stats screen renders Stats title + 'Last 28 days · N trips' subtitle, hero traffic-loss card (mono 56sp), donut card (110dp), 28-day TrendBars card, and WeekdayChart card"
    - "Trip detail and stats widget tests pass GREEN"
    - "Existing flutter_map TileLayer for trip detail's real route map continues to render correctly (Pitfall 1 — NOT google_maps_flutter)"
  artifacts:
    - path: lib/features/trips/widgets/traffic_insight_card.dart
      provides: stuckBg callout "You lost X minutes stuck in traffic"
      contains: "class TrafficInsightCard"
    - path: lib/features/trips/widgets/trip_timeline.dart
      provides: clock-icon timeline rows for Started/Stuck/Arrived
      contains: "class TripTimeline"
    - path: lib/features/stats/widgets/traffic_loss_hero.dart
      provides: hero "You lost X to traffic this week" 56sp mono card
      contains: "class TrafficLossHero"
    - path: lib/features/stats/widgets/donut_card.dart
      provides: 110dp donut chart with moving/stuck arcs and center stuck% label
      contains: "class DonutCard"
    - path: lib/features/stats/widgets/trend_bars_card.dart
      provides: 28-day bar chart of daily commute totals
      contains: "class TrendBarsCard"
    - path: lib/features/stats/widgets/weekday_chart_card.dart
      provides: Mon–Fri bar chart with worst/best highlight
      contains: "class WeekdayChartCard"
  key_links:
    - from: lib/features/trips/screens/trip_detail_screen.dart
      to: lib/features/trips/widgets/{traffic_insight_card,trip_timeline}.dart + lib/shared/widgets/{stuck_bar,section_label}.dart
      via: composition under SafeArea Padding(20)
      pattern: "TrafficInsightCard"
    - from: lib/features/stats/screens/stats_screen.dart
      to: lib/features/stats/widgets/{traffic_loss_hero,donut_card,trend_bars_card,weekday_chart_card}.dart
      via: SliverList of bgElev cards
      pattern: "TrafficLossHero"
---

<objective>
Restyle the Trip Detail and Stats screens. Trip Detail gains a custom header (no AppBar), a 210dp real `flutter_map` TileLayer (kept — NOT replaced with google_maps_flutter per Pitfall 1), an EVENING/MORNING commute label + direction title, a Duration + Distance stat tile pair, StuckBar + legend, a `stuckBg` traffic-insight callout card, a timeline section (clock-icon rows), and Edit / Delete border buttons. Stats screen is rewritten to four bgElev cards: TrafficLossHero (56sp mono "You lost X to traffic this week"), DonutCard (110dp), TrendBarsCard (28-day BarChart — not the legacy LineChart), WeekdayChartCard (Mon–Fri BarChart). Per Open Question 2, the legacy stats widgets `BestWorstDayCard`, `DirectionAveragesCard`, `TrafficWasteCard`, `TrendChartCard`, `WeekMonthTotalsCard` are deleted (their info folds into the new hero + donut). `StatsCard` wrapper is retained but updated to use `tokens.bgElev`.

Purpose: Bring the two remaining "content" tabs (Trips → Trip Detail flow + Stats) to the Traevy design before Plan 07 closes out Settings + Onboarding.

Output: Trip Detail screen + 2 new trip widgets; Stats screen + 4 new stats widgets + StatsCard restyle; 5 legacy stats widgets deleted; 2 test files updated.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/CLAUDE.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-CONTEXT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-UI-SPEC.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-RESEARCH.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-02-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-03-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/lib/features/trips/screens/trip_detail_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/screens/stats_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/widgets/stats_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/widgets/best_worst_day_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/widgets/direction_averages_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/widgets/traffic_waste_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/widgets/trend_chart_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/widgets/week_month_totals_card.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/stats/providers/stats_providers.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/stuck_bar.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/section_label.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme_extension.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/constants.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/features/trips/trip_detail_screen_test.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/features/stats/stats_screen_test.dart

<interfaces>
<!-- Existing providers consumed (do NOT modify): -->
//   tripByIdProvider(tripId) :: StreamProvider<Trip?>
//   statsSummaryProvider     :: StreamProvider<StatsSummary> with fields:
//     - movingMinutes (int), stuckMinutes (int)
//     - totalMinutes (int)
//     - tripCount (int)
//     - weeklyTotalsMinutes (List<int>)  // last 28 days as 4 weeks OR raw per-day
//     - perWeekdayAverageMinutes (Map<int, int>) // 1..7
//     - bestDay / worstDay (int weekday)
//     - previousWeekStuckMinutes (int) — for "vs last week" comparison
//   (If a field doesn't exist, fall back to computing from movingMinutes + stuckMinutes ONLY; do NOT add fields to the provider in this plan.)

// Existing constants kept:
//   kRouteTripDetail
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Trip Detail screen restyle + TrafficInsightCard + TripTimeline</name>
  <files>lib/features/trips/screens/trip_detail_screen.dart, lib/features/trips/widgets/traffic_insight_card.dart, lib/features/trips/widgets/trip_timeline.dart, test/widget/features/trips/trip_detail_screen_test.dart</files>
  <read_first>
    - lib/features/trips/screens/trip_detail_screen.dart (full file — keep the flutter_map TileLayer block intact; identify how it currently lays out duration/distance/StuckBar)
    - lib/shared/widgets/stuck_bar.dart, section_label.dart (Plan 03 outputs)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §6 Trip Detail Screen (ASCII layout + spec)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Pitfall 1 (flutter_map NOT google_maps) — Pitfall 9 (surfaceContainerLow → tokens.bgElev)
    - test/widget/features/trips/trip_detail_screen_test.dart (full file)
  </read_first>
  <behavior>
    - `TrafficInsightCard` (≤ 80 lines, StatelessWidget):
        - Props: `({required int stuckMinutes, required int totalMinutes, super.key})`.
        - Renders Container(padding: EdgeInsets.symmetric(horizontal: 16, vertical: 14), decoration: BoxDecoration(color: tokens.stuckBg, borderRadius: BorderRadius.circular(16))) wrapping Row(crossAxisAlignment: start, children: [Container(36×36 circle, color: tokens.stuck.withOpacity(0.18), child: Icon(Icons.access_time_rounded, size: 18, color: tokens.stuck)), 12dp gap, Expanded(child: RichText: "You lost " (textTheme.bodyMedium color: onSurface) + "$stuckMinutes minutes" (TraevyFonts.ui(size: 13.5, w700, color: tokens.stuck)) + " stuck in traffic. " + bold "That's $pct%" + " of this trip." where pct = (stuckMinutes / totalMinutes * 100).round() (handle totalMinutes==0 → render 0%).
    - `TripTimeline` (≤ 90 lines, StatelessWidget):
        - Props: `({required DateTime startTime, required DateTime endTime, required int stuckMinutes, super.key})`.
        - Renders Column with SectionLabel('Timeline') then a Column of `_TimelineRow` children. Each row: Row(children: [SizedBox(width: 56, child: Text(formatHm(time), TraevyFonts.mono(size: 12, color: tokens.textDim))), Container(28×28 circle, color: backgroundColor, child: Icon(...) ), 12dp gap, Expanded(Text(label, textTheme.bodyMedium)), if duration != null: Text('$duration', TraevyFonts.mono(size: 12, w600, color: tokens.stuck))]).
        - Three rows for v0.1 (no per-stuck-event data yet): "Started recording" (Icons.location_on_rounded, accentBg/accent), if stuckMinutes>0 "Stuck in traffic" at midpoint time (Icons.access_time_rounded, stuckBg/stuck, duration formatted), "Arrived home"/"Arrived at office" depending on direction (Icons.flag_rounded, movingBg/moving).
    - `trip_detail_screen.dart` restyle (≤ 250 lines):
        - Remove the AppBar. Top of screen is a SafeArea-wrapped Row in 20dp horizontal padding: Container(36×36 surface circle, child: IconButton(Icons.arrow_back_rounded, onPressed: Navigator.pop)), Spacer, Column(Text(formatDate(trip.startTime) — e.g. 'Mon, 28 Apr', textTheme.bodyMedium color: onSurface), Text(formatHm(trip.startTime), TraevyFonts.mono(size: 12, color: tokens.textDim))), Spacer, Container(36×36 surface circle, child: IconButton(Icons.more_horiz_rounded, onPressed: _showOptionsMenu)).
        - The flutter_map TileLayer block REMAINS in place (Pitfall 1). Height adjusted to 210dp. Wrap in ClipRRect(BorderRadius.circular(16)) so it picks up the new card-style.
        - Below map: 20dp horizontal padding column with:
            1. SectionLabel(commutePartOfDayLabel — 'Morning commute' or 'Evening commute' based on startTime hour vs kDefaultDirectionCutoffHour).
            2. 4dp gap.
            3. Text(directionDisplayName — 'To home' or 'To office', TraevyFonts.ui(size: 24, w700, letterSpacing: -0.6, color: onSurface)).
            4. 16dp gap.
            5. Card(bgElev radius 16) containing Row of two columns (Duration / Distance), each: SectionLabel(label, fontSize: 11) + 4dp gap + Text(value, TraevyFonts.mono(size: 28, w600, color: onSurface)).
            6. 12dp gap. StuckBar(movingMinutes, stuckMinutes, height: 14).
            7. 8dp gap. Row: Row(children: [Container(8 circle moving), 6dp gap, Text('$movingHm moving', TraevyFonts.mono(size: 12, color: tokens.moving))]), Spacer, Row(children: [Container(8 circle stuck), 6dp gap, Text('$stuckHm stuck', TraevyFonts.mono(size: 12, color: tokens.stuck))]).
            8. 20dp gap. TrafficInsightCard(stuckMinutes, totalMinutes).
            9. 24dp gap. TripTimeline.
            10. 24dp gap. Row: OutlinedButton.icon(Icons.edit_rounded, label 'Edit', onPressed: _showEditSheet, style: pulls onSurface border from theme), 12dp gap, OutlinedButton.icon(Icons.delete_outline_rounded, label 'Delete', onPressed: _confirmDelete, style: OutlinedButton.styleFrom(foregroundColor: tokens.danger, side: BorderSide(color: tokens.danger))).
        - Preserve all existing controller calls (_showEditSheet → existing EditTripSheet, _confirmDelete → existing trip-management delete flow).
        - Update any `colorScheme.surfaceContainerLow` references in this file → `tokens.bgElev`.
    - `trip_detail_screen_test.dart`:
        - Update assertions that hit `AppBar` widget — replace with `find.byIcon(Icons.arrow_back_rounded)` for the back button.
        - Add `find.byType(TrafficInsightCard)`, `find.byType(TripTimeline)`, `find.byType(StuckBar)` each findsOneWidget.
        - Preserve assertions on the rendered duration / distance numbers; they remain text matches but the styling moves to TraevyFonts.mono.
        - Preserve the existing flutter_map TileLayer assertions if any.
  </behavior>
  <action>
    1. Create `lib/features/trips/widgets/traffic_insight_card.dart` per <behavior>.
    2. Create `lib/features/trips/widgets/trip_timeline.dart` per <behavior>. The midpoint-stuck row may compute its time as `startTime.add(Duration(seconds: (endTime.difference(startTime).inSeconds * 0.4).round()))` — an approximation only used when no per-event stuck data exists.
    3. Restyle `lib/features/trips/screens/trip_detail_screen.dart` per <behavior>. Preserve the flutter_map TileLayer code; only wrap it in ClipRRect and adjust the height to 210dp.
    4. Update `test/widget/features/trips/trip_detail_screen_test.dart` per <behavior>.
    5. `dart format`, then `flutter analyze lib/features/trips/screens/trip_detail_screen.dart lib/features/trips/widgets/traffic_insight_card.dart lib/features/trips/widgets/trip_timeline.dart` — expect 0 warnings.
    6. `flutter test test/widget/features/trips/trip_detail_screen_test.dart` must pass GREEN.
  </action>
  <verify>
    <automated>flutter analyze lib/features/trips/screens/trip_detail_screen.dart lib/features/trips/widgets/traffic_insight_card.dart lib/features/trips/widgets/trip_timeline.dart 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/features/trips/trip_detail_screen_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/trips/widgets/traffic_insight_card.dart` and `trip_timeline.dart` exist, each &lt; 100 lines, declare `StatelessWidget` with `const` constructor.
    - `lib/features/trips/screens/trip_detail_screen.dart` no longer constructs an `AppBar` (verified by `! grep -E "AppBar\(" lib/features/trips/screens/trip_detail_screen.dart`).
    - `lib/features/trips/screens/trip_detail_screen.dart` still imports `package:flutter_map/flutter_map.dart` AND `package:latlong2/latlong.dart` (verified by grep — Pitfall 1 NOT broken).
    - `lib/features/trips/screens/trip_detail_screen.dart` does NOT import `package:google_maps_flutter/google_maps_flutter.dart` (grep returns 0 matches).
    - `lib/features/trips/screens/trip_detail_screen.dart` references `TrafficInsightCard`, `TripTimeline`, `StuckBar`.
    - `flutter test test/widget/features/trips/trip_detail_screen_test.dart` exits 0.
    - `flutter analyze` over the three files exits with 0 issues.
  </acceptance_criteria>
  <done>
    Trip Detail screen uses the new layout with custom header, real flutter_map TileLayer, mono Duration/Distance, StuckBar + legend, TrafficInsightCard callout, and TripTimeline. Edit/Delete buttons preserve existing handlers. Tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Stats screen restyle — TrafficLossHero + DonutCard + TrendBarsCard + WeekdayChartCard; delete legacy cards</name>
  <files>lib/features/stats/screens/stats_screen.dart, lib/features/stats/widgets/stats_card.dart, lib/features/stats/widgets/traffic_loss_hero.dart, lib/features/stats/widgets/donut_card.dart, lib/features/stats/widgets/trend_bars_card.dart, lib/features/stats/widgets/weekday_chart_card.dart, lib/features/stats/widgets/best_worst_day_card.dart, lib/features/stats/widgets/direction_averages_card.dart, lib/features/stats/widgets/traffic_waste_card.dart, lib/features/stats/widgets/trend_chart_card.dart, lib/features/stats/widgets/week_month_totals_card.dart, test/widget/features/stats/stats_screen_test.dart</files>
  <read_first>
    - lib/features/stats/screens/stats_screen.dart (full file — identify provider call sites and how it composes the five legacy cards)
    - lib/features/stats/providers/stats_providers.dart (full file — confirm which StatsSummary fields are available; do NOT add new fields in this plan)
    - lib/features/stats/widgets/*.dart (all six existing widgets — note their public APIs and pre-Phase-8 structure)
    - lib/shared/widgets/stuck_bar.dart, section_label.dart (Plan 03 outputs)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §7 Stats Screen (ASCII layout + token spec)
    - .planning/phases/08-ui-overhaul/08-RESEARCH.md Example 5 Donut chart with fl_chart + Open Question 2 (delete vs restyle stats cards)
    - test/widget/features/stats/stats_screen_test.dart (full file — note which assertions reference legacy widget names)
  </read_first>
  <behavior>
    - `StatsCard` (restyled wrapper, ≤ 60 lines):
        - Becomes a thin Container(bgElev, radius 16, border tokens.border, padding 20dp). Removes `colorScheme.surfaceContainerLow` references.
        - Props: `({String? title, required Widget child, EdgeInsets? padding, super.key})`. When title is non-null, renders SectionLabel(title) + 12dp gap + child; else just child.
    - `TrafficLossHero` (≤ 80 lines):
        - ConsumerWidget. Watches `statsSummaryProvider`.
        - Renders StatsCard wrapping a Column(crossAxisAlignment: start):
            1. Text('You lost', textTheme.bodyMedium color: tokens.textDim)
            2. Text(formatStuckHm(summary.stuckMinutes), TraevyFonts.mono(size: 56, weight: w500, color: tokens.stuck, letterSpacing: -2.5)) — e.g. '1h 42m'
            3. Text('to traffic this week.', textTheme.bodyMedium color: tokens.textDim)
            4. If previousWeekStuckMinutes is available on StatsSummary: 8dp gap + Text('${diffSign}${formatHm(diffAbs)} vs last week', TraevyFonts.ui(size: 12, w500, color: tokens.textDim)) where diffSign is '+' or '−' and diffAbs is absolute minutes.
            5. If the provider does NOT expose previousWeekStuckMinutes, skip the comparison row (do not invent data).
    - `DonutCard` (≤ 100 lines):
        - ConsumerWidget. Watches `statsSummaryProvider`.
        - Renders StatsCard(title: 'This week') wrapping Row(crossAxisAlignment: center, children: [SizedBox(110×110, child: Donut chart per Example 5 of RESEARCH.md — fl_chart PieChart with two sections), 16dp gap, Expanded(Column: Text('${formatHm(total)} total', TraevyFonts.mono(size: 22, w600, color: onSurface)), 12dp gap, _LegendRow('moving', summary.movingMinutes, tokens.moving), _LegendRow('stuck', summary.stuckMinutes, tokens.stuck))]).
        - Use `import 'package:fl_chart/fl_chart.dart';`. Use existing fl_chart ^1.2.0 from pubspec — do NOT add a new dependency.
    - `TrendBarsCard` (≤ 120 lines):
        - ConsumerWidget. Watches `statsSummaryProvider`.
        - Renders StatsCard(title: '28-day trend') wrapping a SizedBox(height: 100, child: BarChart(BarChartData(...) — see fl_chart docs)). Each bar is a daily total in minutes for the last 28 days. Bar color resolution:
            * Default: tokens.borderStr
            * Worst day (max value): tokens.stuck
            * Today: tokens.accent
        - X-axis labels: 'Apr 1', mid-month, today's date — sparse labels (3 ticks). Y-axis hidden.
        - If the provider doesn't expose per-day data, gracefully render a flat 28-bar approximation derived from weeklyTotalsMinutes (divide each week's total across 7 days). NEVER throw. NEVER show empty space.
    - `WeekdayChartCard` (≤ 100 lines):
        - ConsumerWidget. Watches `statsSummaryProvider`.
        - Renders StatsCard(title: 'Weekday averages') wrapping a SizedBox(height: 140, child: BarChart with 5 bars Mon–Fri). Bar color: tokens.borderStr default, tokens.stuck for worstDay, tokens.moving for bestDay. Below each bar: weekday label (Mon, Tue, …) Inter w600 11sp. Above each bar: TraevyFonts.mono(size: 11, color: tokens.textDim) showing the average duration. Footer Row in bgElev: Text('Worst ${weekdayName(worstDay)} · ${avgWorst}', size 12 w600, color: tokens.stuck), Spacer, Text('Best ${weekdayName(bestDay)} · ${avgBest}', size 12 w600, color: tokens.moving).
    - `stats_screen.dart` restyle (≤ 150 lines):
        - Remove the AppBar. Top of screen (under MainShell): Padding(20dp horizontal, 16dp top) Column: Text('Stats', textTheme.titleLarge), 4dp gap, Text('Last 28 days · ${summary.tripCount} trips', textTheme.bodyMedium color: tokens.textDim).
        - Then a ListView (or SingleChildScrollView + Column) of: TrafficLossHero(), 16dp gap, DonutCard(), 16dp gap, TrendBarsCard(), 16dp gap, WeekdayChartCard().
        - Remove all references to the five legacy widgets: WeekMonthTotalsCard, DirectionAveragesCard, BestWorstDayCard, TrafficWasteCard, TrendChartCard.
    - The five legacy widget files are DELETED. Their public classes were only consumed by `stats_screen.dart`, so deletion is safe (`grep -r "WeekMonthTotalsCard\|DirectionAveragesCard\|BestWorstDayCard\|TrafficWasteCard\|TrendChartCard"` should return 0 matches outside of git history after this plan).
    - `stats_screen_test.dart`:
        - Remove assertions on the five deleted widget types.
        - Add: `find.byType(TrafficLossHero)`, `find.byType(DonutCard)`, `find.byType(TrendBarsCard)`, `find.byType(WeekdayChartCard)` each findsOneWidget.
        - Preserve any assertion on rendered numeric values (e.g. `find.textContaining('1h')`).
        - Add an assertion that 'Stats' title renders (`find.text('Stats')` findsOneWidget) and 'Last 28 days' subtitle pattern matches.
  </behavior>
  <action>
    1. Update `lib/features/stats/widgets/stats_card.dart`: replace `colorScheme.surfaceContainerLow` with `Theme.of(context).extension<TraevyTokensExt>()!.bgElev`. Update radius to 16. Remove any tinted shadow.
    2. Create `lib/features/stats/widgets/traffic_loss_hero.dart` per <behavior>.
    3. Create `lib/features/stats/widgets/donut_card.dart` per Example 5 of RESEARCH.md.
    4. Create `lib/features/stats/widgets/trend_bars_card.dart` with fl_chart BarChart.
    5. Create `lib/features/stats/widgets/weekday_chart_card.dart` with fl_chart BarChart.
    6. Update `lib/features/stats/screens/stats_screen.dart` to compose the four new widgets and drop the AppBar.
    7. DELETE the five legacy widget files: `best_worst_day_card.dart`, `direction_averages_card.dart`, `traffic_waste_card.dart`, `trend_chart_card.dart`, `week_month_totals_card.dart`.
    8. `grep -r "WeekMonthTotalsCard\|DirectionAveragesCard\|BestWorstDayCard\|TrafficWasteCard\|TrendChartCard" lib/ test/` — confirm 0 matches; if any, fix.
    9. Update `test/widget/features/stats/stats_screen_test.dart` per <behavior>.
    10. `dart format lib/features/stats/`, then `flutter analyze lib/features/stats/` — expect 0 warnings.
    11. `flutter test test/widget/features/stats/` — must pass GREEN.
  </action>
  <verify>
    <automated>flutter analyze lib/features/stats/ 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/features/stats/ &amp;&amp; ! grep -rEq "WeekMonthTotalsCard|DirectionAveragesCard|BestWorstDayCard|TrafficWasteCard|TrendChartCard" lib/ test/</automated>
  </verify>
  <acceptance_criteria>
    - The five legacy stats widget files do NOT exist (`! test -f`).
    - `lib/features/stats/widgets/traffic_loss_hero.dart`, `donut_card.dart`, `trend_bars_card.dart`, `weekday_chart_card.dart` all exist with `class TrafficLossHero`, `class DonutCard`, `class TrendBarsCard`, `class WeekdayChartCard` declarations.
    - `lib/features/stats/widgets/stats_card.dart` no longer references `surfaceContainerLow` (grep returns 0).
    - `lib/features/stats/screens/stats_screen.dart` no longer constructs an `AppBar` (`! grep -E "AppBar\(" lib/features/stats/screens/stats_screen.dart`).
    - `lib/features/stats/screens/stats_screen.dart` imports the four new widgets (grep all four imports).
    - No reference to the five deleted widget class names remains in `lib/` or `test/`.
    - `flutter test test/widget/features/stats/` exits 0.
    - `flutter analyze lib/features/stats/` exits 0 (no warnings).
    - `wc -l` each new widget &lt; 130 (TrendBarsCard may be the largest due to fl_chart configuration).
  </acceptance_criteria>
  <done>
    Stats screen renders the four-card Traevy layout. Five legacy cards are deleted. StatsCard wrapper uses tokens.bgElev. fl_chart drives both the donut and the two bar charts. Tests pass.
  </done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/trips/screens/trip_detail_screen.dart lib/features/trips/widgets/traffic_insight_card.dart lib/features/trips/widgets/trip_timeline.dart lib/features/stats/` exits 0.
- `flutter test test/widget/features/trips/trip_detail_screen_test.dart test/widget/features/stats/` passes.
- `! grep -r "google_maps_flutter" lib/` (Pitfall 1 — never introduced).
- `! grep -rE "WeekMonthTotalsCard|DirectionAveragesCard|BestWorstDayCard|TrafficWasteCard|TrendChartCard" lib/ test/` (legacy stats widgets removed).
</verification>

<success_criteria>
- Trip Detail screen has new custom header, ClipRRect-wrapped flutter_map (still using real OSM tiles), Duration+Distance mono pair, StuckBar+legend, TrafficInsightCard, TripTimeline, Edit/Delete border buttons.
- Stats screen has TrafficLossHero, DonutCard, TrendBarsCard, WeekdayChartCard — and only those (legacy cards deleted).
- StatsCard wrapper uses tokens.bgElev.
- All trip_detail and stats widget tests pass.
- Analyzer clean.
- No google_maps_flutter ever added.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-overhaul/08-06-SUMMARY.md` documenting: the trip-detail layout structure (header + map + stats + insight + timeline + actions), the five stats widget deletions with rationale, the four new stats widgets' fl_chart configuration, and confirmation that flutter_map remains the trip-detail map renderer.
</output>
