---
phase: 08-ui-overhaul
plan: 07
type: execute
wave: 5
depends_on: [02, 03, 04]
files_modified:
  - lib/features/settings/screens/settings_screen.dart
  - lib/features/settings/widgets/settings_section.dart
  - lib/features/settings/widgets/settings_row.dart
  - lib/features/settings/widgets/account_row.dart
  - lib/features/onboarding/screens/onboarding_screen.dart
  - lib/config/routes.dart
  - test/widget/features/settings/settings_screen_test.dart
autonomous: true
requirements:
  - UX-02
  - UX-04
  - UX-05
must_haves:
  truths:
    - "Settings screen renders four grouped sections (Account, Recording, Notifications, Appearance) with uppercase section labels and bgElev cards"
    - "Account section shows 44dp accentBg avatar, placeholder name + email, Cloud sync toggle (read-only placeholder for Phase 8), Restore-from-cloud chevron row, Sign-out row in danger color"
    - "Notifications section uses TraevyToggle for daily reminder, weekend, and weekly summary rows — preserving the existing scheduleWeeklySummary / scheduleReminder calls (UX-04, UX-05)"
    - "Appearance section uses a chevron row that opens a theme picker (System / Light / Dark) — preserving the existing userPreferencesDao.updateDarkMode write (UX-02)"
    - "Onboarding screen is a static scaffold accessible at kRouteOnboarding — TraevyLogoMark + headline + feature ticks + Google button + skip link"
    - "settings_screen_test passes GREEN with TraevyToggle assertions replacing the old SwitchListTile assertions"
  artifacts:
    - path: lib/features/settings/widgets/settings_section.dart
      provides: SettingsSection (uppercase label + bgElev card with border)
      contains: "class SettingsSection"
    - path: lib/features/settings/widgets/settings_row.dart
      provides: SettingsRow (label + optional mono subtitle + trailing control)
      contains: "class SettingsRow"
    - path: lib/features/settings/widgets/account_row.dart
      provides: AccountRow (44dp avatar + name + email)
      contains: "class AccountRow"
    - path: lib/features/onboarding/screens/onboarding_screen.dart
      provides: OnboardingScreen static layout (logo + headline + ticks + Google button)
      contains: "class OnboardingScreen"
    - path: lib/features/settings/screens/settings_screen.dart
      provides: restyled SettingsScreen composing the four sections
      contains: "class SettingsScreen"
  key_links:
    - from: lib/features/settings/widgets/settings_row.dart trailing control slot
      to: lib/shared/widgets/traevy_toggle.dart TraevyToggle
      via: trailing Widget property passed in by SettingsScreen
      pattern: "TraevyToggle"
    - from: lib/features/settings/screens/settings_screen.dart Appearance section
      to: lib/features/settings/providers/settings_providers.dart userPreferencesDao
      via: existing updateDarkMode call (unchanged behaviour)
      pattern: "updateDarkMode"
    - from: lib/config/routes.dart kAppRoutes
      to: lib/features/onboarding/screens/onboarding_screen.dart
      via: kAppRoutes[kRouteOnboarding] = (context) => const OnboardingScreen()
      pattern: "OnboardingScreen"
---

<objective>
Restyle the Settings screen to the Traevy grouped-section layout (Account / Recording / Notifications / Appearance) with `TraevyToggle` replacing `SwitchListTile`, and scaffold the static Onboarding screen (logo + headline + feature ticks + Google button + skip link) accessible via `kRouteOnboarding`. The existing Notification scheduling logic (`scheduleWeeklySummary`, `scheduleReminder`) and theme preference write (`updateDarkMode`) remain unchanged — this plan only swaps presentation. The `settings_screen_test.dart` is updated to drive the new TraevyToggle widgets while preserving the original behavioural assertions for UX-02, UX-04, UX-05.

Purpose: Close out the remaining two visible screens in Phase 8 (Settings is the last MainShell tab; Onboarding is scaffolded for Phase 9 auth to wire). After this plan, every screen on the roadmap is in the Traevy design.

Output: 3 new settings widgets, restyled SettingsScreen, new OnboardingScreen, routes.dart now registers Onboarding, settings test rewritten to use TraevyToggle locators.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/coolman/Documents/Projects/bizzle/CLAUDE.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-CONTEXT.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-UI-SPEC.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-RESEARCH.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-02-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/.planning/phases/08-ui-overhaul/08-03-SUMMARY.md
@/Users/coolman/Documents/Projects/bizzle/lib/features/settings/screens/settings_screen.dart
@/Users/coolman/Documents/Projects/bizzle/lib/features/settings/providers/settings_providers.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/traevy_toggle.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/section_label.dart
@/Users/coolman/Documents/Projects/bizzle/lib/shared/widgets/traevy_logo_mark.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/routes.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/constants.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme.dart
@/Users/coolman/Documents/Projects/bizzle/lib/config/theme_extension.dart
@/Users/coolman/Documents/Projects/bizzle/test/widget/features/settings/settings_screen_test.dart

<interfaces>
<!-- Existing providers/services consumed (do NOT modify): -->
//   userPreferenceProvider :: StreamProvider<UserPreferences>
//   settingsControllerProvider (or similar) :: notifier exposing updateDarkMode, updateReminderEnabled, updateReminderTime, updateWeekendReminder, updateWeeklyNotificationEnabled
//   NotificationService.scheduleWeeklySummary / scheduleReminder are called from existing handlers — must keep working.

// Reserved route from Plan 04:
//   const String kRouteOnboarding = '/onboarding';   (already declared; this plan registers the builder in kAppRoutes)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Settings building blocks (SettingsSection + SettingsRow + AccountRow) + restyled SettingsScreen</name>
  <files>lib/features/settings/widgets/settings_section.dart, lib/features/settings/widgets/settings_row.dart, lib/features/settings/widgets/account_row.dart, lib/features/settings/screens/settings_screen.dart, test/widget/features/settings/settings_screen_test.dart</files>
  <read_first>
    - lib/features/settings/screens/settings_screen.dart (full file — identify every `SwitchListTile`, `ListTile`, `RadioListTile` and the controller calls they trigger)
    - lib/features/settings/providers/settings_providers.dart (full file — read the notifier methods, do NOT alter their signatures)
    - lib/shared/widgets/traevy_toggle.dart (Plan 03 output — Settings consumes this widget)
    - lib/shared/widgets/section_label.dart (Plan 03 output — used for section labels)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §8 Settings Screen (ASCII layout + spec)
    - .planning/phases/08-ui-overhaul/08-CONTEXT.md Settings Screen block
    - test/widget/features/settings/settings_screen_test.dart (full file — note which assertions use `find.byType(SwitchListTile)` or specific Switch keys — these must be updated to `find.byType(TraevyToggle)`)
  </read_first>
  <behavior>
    - `SettingsSection` (≤ 80 lines, StatelessWidget):
        - Props: `({required String title, required List<Widget> children, super.key})`.
        - Renders Padding(EdgeInsets.symmetric(horizontal: 20, vertical: 8)) Column(crossAxisAlignment: start, children: [SectionLabel(title, fontSize: 11), 8dp gap, Container(decoration: BoxDecoration(color: tokens.bgElev, border: Border(top: BorderSide(color: tokens.border), bottom: BorderSide(color: tokens.border))), child: Column(children: _interleavedWithDividers(children, divider: Divider(color: tokens.border, height: 1, thickness: 1))))]).
    - `SettingsRow` (≤ 90 lines, StatelessWidget):
        - Props: `({required String label, String? subtitle, Widget? trailing, VoidCallback? onTap, bool dangerous = false, super.key})`.
        - Renders InkWell(onTap: onTap, child: Padding(EdgeInsets.symmetric(horizontal: 20, vertical: 14), child: Row(crossAxisAlignment: center, children: [Expanded(Column(crossAxisAlignment: start, children: [Text(label, TraevyFonts.ui(size: 14, weight: w500, color: dangerous ? tokens.danger : onSurface)), if subtitle != null: 2dp gap + Text(subtitle, TraevyFonts.mono(size: 12, color: tokens.textDim))])), if trailing != null: 12dp gap + trailing])).
        - When trailing is null and onTap is non-null, render a default trailing Icon(Icons.chevron_right_rounded, size: 20, color: tokens.textMuted).
    - `AccountRow` (≤ 70 lines, StatelessWidget):
        - Props: `({required String name, required String email, required String initial, super.key})`.
        - Renders Padding(EdgeInsets.symmetric(horizontal: 20, vertical: 16)) Row(crossAxisAlignment: center, children: [Container(44×44, decoration: BoxDecoration(shape: BoxShape.circle, color: tokens.accentBg), child: Center(child: Text(initial, TraevyFonts.ui(size: 16, weight: w700, color: tokens.accent)))), 14dp gap, Expanded(Column(crossAxisAlignment: start, children: [Text(name, TraevyFonts.ui(size: 15, weight: w600, color: onSurface)), 2dp gap, Text(email, TraevyFonts.mono(size: 12, color: tokens.textDim))]))]).
    - `settings_screen.dart` restyle (≤ 250 lines):
        - Remove the AppBar. The screen renders inside MainShell so it just paints content with a leading 'Settings' title row.
        - Top of screen (under SafeArea, 20dp horizontal padding, 16dp top): Text('Settings', textTheme.titleLarge).
        - Body is a ListView (allow content overflow):
            * SettingsSection(title: 'Account', children: [AccountRow(name: kPlaceholderUserName, email: 'you@traevy.app', initial: kPlaceholderUserInitial), SettingsRow(label: 'Cloud sync', subtitle: 'OFF — sign in to enable', trailing: TraevyToggle(value: false, onChanged: (v) {}), onTap: null), SettingsRow(label: 'Restore from cloud', onTap: () { /* placeholder snackbar — actual restore is Phase 11 */ }), SettingsRow(label: 'Sign out', dangerous: true, onTap: null)]). Cloud sync toggle is intentionally non-functional for Phase 8 (auth lands in Phase 9). Mark with a clear `///` comment in code: "Wired in Phase 9 when authentication ships".
            * SettingsSection(title: 'Recording', children: [SettingsRow(label: 'Cutoff "to office"', subtitle: 'Before ${kDefaultDirectionCutoffHour}:00', onTap: () => _openCutoffPicker(context, ref)), SettingsRow(label: 'Auto-pause on stop', trailing: TraevyToggle(value: prefs.autoPauseOnStop ?? true, onChanged: (v) { /* if a notifier exists for this, call it; otherwise no-op + comment */}))]). If the existing settings notifier does not yet expose autoPauseOnStop, render the toggle bound to a placeholder local state (Phase 9 backlog) — guarded by a code comment. Do NOT add new fields to UserPreferences in this plan.
            * SettingsSection(title: 'Notifications', children: [SettingsRow(label: 'Daily reminder', subtitle: prefs.reminderEnabled ? '${prefs.reminderTime} · weekdays' : 'OFF', trailing: TraevyToggle(value: prefs.reminderEnabled, onChanged: (v) => settingsCtrl.updateReminderEnabled(v))), SettingsRow(label: 'Include weekends', trailing: TraevyToggle(value: prefs.weekendReminder, onChanged: (v) => settingsCtrl.updateWeekendReminder(v))), SettingsRow(label: 'Weekly summary', subtitle: 'Sunday evening', trailing: TraevyToggle(value: prefs.weeklyNotificationEnabled, onChanged: (v) => settingsCtrl.updateWeeklyNotificationEnabled(v)))]). The exact controller method names MUST match what already exists in settings_providers.dart — read the file first to confirm.
            * SettingsSection(title: 'Appearance', children: [SettingsRow(label: 'Theme', subtitle: prefs.darkMode.toUpperCase()[0] + prefs.darkMode.substring(1), onTap: () async { final picked = await _openThemePicker(context); if (picked != null) await settingsCtrl.updateDarkMode(picked); })]).
        - `_openThemePicker(context)` shows a Material `showModalBottomSheet` with three rows (System / Light / Dark) — each a SettingsRow with onTap that returns the corresponding kDarkModeSystem / kDarkModeLight / kDarkModeDark literal. The bottom sheet content is built inline (≤ 40 lines).
        - `_openCutoffPicker(context, ref)` opens a `showTimePicker`-like dialog — if a Phase 7 implementation exists, reuse it; otherwise stub with TODO **NO**, per CLAUDE.md "No TODO" — if the controller does not expose cutoff updates, this row is rendered without onTap. Verify before committing.
    - `settings_screen_test.dart`:
        - Replace `find.byType(SwitchListTile)` with `find.byType(TraevyToggle)`.
        - Replace `find.byType(RadioListTile)` (used in the prior Phase 7 theme picker test) with assertions on the new theme picker bottom sheet — locate by `find.text('System')`, `find.text('Light')`, `find.text('Dark')` inside a `find.byType(SettingsRow)` matcher.
        - Preserve all behavioural assertions: tapping the reminder toggle invokes `updateReminderEnabled(true)` on the notifier, tapping weekly summary invokes `updateWeeklyNotificationEnabled(true)`, selecting 'Dark' invokes `updateDarkMode('dark')`.
        - Add new assertions: `find.byType(SettingsSection)` findsNWidgets(4), `find.byType(AccountRow)` findsOneWidget, `find.text('Account')`, `find.text('Recording')`, `find.text('Notifications')`, `find.text('Appearance')` each findsOneWidget (case-sensitive — these are uppercased by SectionLabel; assert via `find.text('ACCOUNT')` etc. or use `find.textContaining('Account')`).
  </behavior>
  <action>
    1. Create the three settings widget files under `lib/features/settings/widgets/` per <behavior>.
    2. Rewrite `lib/features/settings/screens/settings_screen.dart` to compose the four sections.
    3. Read the existing settings_providers.dart to confirm exact controller method names. Use those exact names — do NOT add new methods.
    4. Update `test/widget/features/settings/settings_screen_test.dart` per <behavior>. Run `dart format` and `flutter analyze`.
    5. `flutter test test/widget/features/settings/` must pass GREEN. The Phase 7 UX-02 / UX-04 / UX-05 behavioural assertions MUST survive — the toggle-flip → notifier-call wiring is identical, only the widget type changed.
  </action>
  <verify>
    <automated>flutter analyze lib/features/settings/ 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/features/settings/</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/settings/widgets/settings_section.dart`, `settings_row.dart`, `account_row.dart` exist, each &lt; 100 lines.
    - `lib/features/settings/screens/settings_screen.dart` no longer constructs an `AppBar` or uses `SwitchListTile` / `RadioListTile` (greps for those widget names return 0 in the screen file).
    - `lib/features/settings/screens/settings_screen.dart` references `SettingsSection` (at least 4 occurrences via the section list).
    - `lib/features/settings/screens/settings_screen.dart` references `TraevyToggle` at least 3 times (daily reminder, include weekends, weekly summary).
    - `test/widget/features/settings/settings_screen_test.dart` uses `find.byType(TraevyToggle)` in place of the legacy `find.byType(SwitchListTile)`.
    - `flutter test test/widget/features/settings/` exits 0.
    - `flutter analyze lib/features/settings/` exits 0.
    - All behavioural assertions for UX-02 (theme update), UX-04 (weekly summary toggle), UX-05 (reminder toggle) are still present in the test file (grep for `updateDarkMode`, `updateWeeklyNotificationEnabled`, `updateReminderEnabled` in the test file — each appears at least once).
  </acceptance_criteria>
  <done>
    Settings screen uses Traevy grouped sections, accent-bg avatar, TraevyToggles for notifications, theme picker via bottom sheet. UX-02/UX-04/UX-05 behavioural wiring is preserved. Tests pass.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Onboarding screen scaffold + routes.dart registration</name>
  <files>lib/features/onboarding/screens/onboarding_screen.dart, lib/config/routes.dart</files>
  <read_first>
    - lib/config/routes.dart (Plan 04 added `kRouteOnboarding` but did NOT register a builder yet)
    - lib/shared/widgets/traevy_logo_mark.dart (Plan 03 output — used as the hero logo)
    - .planning/phases/08-ui-overhaul/08-UI-SPEC.md §9 Onboarding Screen (ASCII layout + spec)
    - .planning/phases/08-ui-overhaul/08-CONTEXT.md Specifics block (app name Traevy, logo "tv" 28sp inside 56×56 radius 16) + "Claude's Discretion" block (Onboarding scaffold only — auth wires in Phase 9)
  </read_first>
  <behavior>
    - `OnboardingScreen` (≤ 120 lines, StatelessWidget):
        - Renders Scaffold(body: SafeArea(child: Padding(EdgeInsets.symmetric(horizontal: 28, vertical: 24), child: Column(crossAxisAlignment: start, children: [...])))).
        - Children:
            1. TraevyLogoMark(size: 56).
            2. 32dp gap.
            3. Text('Track every\ncommute.', TraevyFonts.ui(size: 36, w700, letterSpacing: -1.2, height: 1.05, color: onSurface)). The newline is literal — two-line headline.
            4. 12dp gap.
            5. Text('One tap to start, automatic traffic insight, works offline. Sign in later to back up.', TraevyFonts.ui(size: 16, w400, color: tokens.textDim, height: 1.5)).
            6. 24dp gap.
            7. Column of three `_FeatureTick` rows (private widget defined in same file, ≤ 30 lines each): Row(crossAxisAlignment: start, children: [Container(28×28 circle, color: tokens.movingBg, child: Center(Icon(Icons.check_rounded, size: 16, color: tokens.moving))), 12dp gap, Expanded(Column(crossAxisAlignment: start, children: [Text(title, TraevyFonts.ui(size: 14, w600, color: onSurface)), 2dp gap, Text(subtitle, TraevyFonts.ui(size: 12, color: tokens.textDim))]))]). Titles: 'One-tap recording', 'Auto traffic detection', 'Works offline'.
            8. Spacer.
            9. Container(width: double.infinity, decoration: BoxDecoration(color: tokens.bgElev, borderRadius: BorderRadius.circular(14), border: Border.all(color: tokens.borderStr)), padding: EdgeInsets.symmetric(horizontal: 18, vertical: 16), child: Row(center: Icon(Icons.g_translate_rounded, size: 20, color: onSurface), 12dp gap, Text('Continue with Google', TraevyFonts.ui(size: 14, w600, color: onSurface)))). This is a static visual button — `onPressed` is a placeholder no-op (auth lands in Phase 9). Add a `///` comment: "Wired in Phase 9 — currently visual scaffold only".
            10. 12dp gap.
            11. Center(GestureDetector(onTap: () => Navigator.of(context).pop(), child: Text('Skip — try without account', TraevyFonts.ui(size: 14, w500, color: tokens.textDim)))). If the screen was pushed from elsewhere, pop is sensible; otherwise this scaffold serves as a route preview.
            12. 12dp gap.
            13. Text('By continuing you agree to our terms.', TraevyFonts.ui(size: 11, w400, color: tokens.textMuted), textAlign: center).
    - `routes.dart` updated so `kAppRoutes` contains an entry for `kRouteOnboarding`. The existing const declaration of `kRouteOnboarding` from Plan 04 stays — only the routes map gains the new entry.
  </behavior>
  <action>
    1. Create `lib/features/onboarding/screens/onboarding_screen.dart` per <behavior>.
    2. Update `lib/config/routes.dart`:
        - Add import `import 'package:traevy/features/onboarding/screens/onboarding_screen.dart';`.
        - Insert into `kAppRoutes`: `kRouteOnboarding: (BuildContext context) =&gt; const OnboardingScreen(),`.
    3. `dart format` both files, `flutter analyze` both files.
    4. Run a tiny smoke test: `flutter test` over the full widget suite — the onboarding screen has no Wave-0 test (per Plan 01 scope), but the routes change must not break any other test. If a test imports `kAppRoutes` directly and counts entries, it may need updating — confirm by grepping `kAppRoutes`.
    5. The "Open onboarding from settings" entry point is OUT OF SCOPE for this plan (per CONTEXT.md "Claude's Discretion" — accessible via route, not auto-displayed). Phase 9 will decide auto-display logic. Do NOT add a navigation entry in Phase 8.
  </action>
  <verify>
    <automated>flutter analyze lib/features/onboarding/ lib/config/routes.dart 2&gt;&amp;1 | grep -qE "(No issues found|0 issues)" &amp;&amp; flutter test test/widget/</automated>
  </verify>
  <acceptance_criteria>
    - `lib/features/onboarding/screens/onboarding_screen.dart` exists and declares `class OnboardingScreen extends StatelessWidget` with `const OnboardingScreen({super.key})`.
    - `wc -l lib/features/onboarding/screens/onboarding_screen.dart` &lt; 150.
    - `lib/features/onboarding/screens/onboarding_screen.dart` uses `TraevyLogoMark` (grep).
    - `lib/features/onboarding/screens/onboarding_screen.dart` does NOT call `Navigator.pushReplacement` to a tracking/dashboard route — it is a static scaffold (grep returns 0).
    - `lib/config/routes.dart` `kAppRoutes` map includes a key `kRouteOnboarding` pointing to `OnboardingScreen` (verified by grep).
    - `flutter analyze lib/features/onboarding/ lib/config/routes.dart` exits 0.
    - `flutter test test/widget/` exits 0 (no regression).
    - No new dependency is added (TraevyLogoMark, Icon, GestureDetector — all pre-existing imports).
  </acceptance_criteria>
  <done>
    Onboarding is a static, theme-aware scaffold reachable at `/onboarding`. routes.dart registers the builder. Full widget suite remains GREEN.
  </done>
</task>

</tasks>

<verification>
- `flutter analyze lib/features/settings/ lib/features/onboarding/ lib/config/routes.dart` exits 0.
- `flutter test test/widget/features/settings/ test/widget/` exits 0.
- `! grep -q "SwitchListTile" lib/features/settings/screens/settings_screen.dart`.
- `grep -c "TraevyToggle" lib/features/settings/screens/settings_screen.dart` ≥ 3.
- `grep -q "kRouteOnboarding: (BuildContext" lib/config/routes.dart`.
</verification>

<success_criteria>
- Settings screen renders 4 sections (Account, Recording, Notifications, Appearance) with AccountRow + SettingsRow + TraevyToggle.
- UX-02 / UX-04 / UX-05 behavioural wiring preserved (updateDarkMode, updateWeeklyNotificationEnabled, updateReminderEnabled all still invoked by the new widgets).
- Onboarding screen is a static scaffold registered at /onboarding.
- Full widget suite passes.
- Analyzer clean.
</success_criteria>

<output>
After completion, create `.planning/phases/08-ui-overhaul/08-07-SUMMARY.md` documenting: the SettingsRow/SettingsSection/AccountRow contract, the Phase 7 → Phase 8 controller call diff (none — identical), the new theme picker bottom sheet flow, and confirmation that Onboarding is registered but not auto-opened.
</output>
