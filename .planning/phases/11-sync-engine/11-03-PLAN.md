---
phase: 11-sync-engine
plan: 03
type: execute
wave: 3
depends_on: [11-01, 11-02]
files_modified:
  - lib/sync/restore_controller.dart
  - lib/database/daos/trips_dao.dart
  - lib/database/daos/trips_dao.g.dart
  - lib/config/constants.dart
  - lib/features/settings/screens/settings_screen.dart
  - test/unit/sync/restore_controller_test.dart
  - test/widget/features/settings/settings_screen_test.dart
autonomous: true
requirements: [SYNC-03]
must_haves:
  truths:
    - "A signed-in user can tap 'Restore from cloud' in Settings and see their cloud trips appear in the local list"
    - "Restoring twice does not create duplicate trips (dedupe by UUID)"
    - "Restore reports how many NEW trips were inserted ('Restored N trips' / 'Already up to date')"
    - "Restore failure surfaces a non-blocking error message, never a crash or frozen UI"
    - "A signed-in user sees a live cloud-sync status row reflecting SyncStatus (All synced / Syncing / N pending / Sync failed)"
    - "Tapping the failed sync status row triggers a retry of the sync engine"
    - "A guest user sees only the sign-in CTA — no restore row, no sync-status row"
  artifacts:
    - path: "lib/sync/restore_controller.dart"
      provides: "RestoreController (Notifier<RestoreState>) + restoreControllerProvider + sealed RestoreState (idle/restoring/success(count)/error)"
      min_lines: 50
    - path: "lib/database/daos/trips_dao.dart"
      provides: "insertOrIgnoreTrip — insert-or-ignore on the id PK returning whether a new row was written"
      contains: "InsertMode.insertOrIgnore"
    - path: "lib/features/settings/screens/settings_screen.dart"
      provides: "_AccountSection signed-in branch with cloud-sync-status row + restore row; guest branch unchanged"
      contains: "restoreControllerProvider"
  key_links:
    - from: "lib/features/settings/screens/settings_screen.dart"
      to: "lib/sync/restore_controller.dart"
      via: "ref.read(restoreControllerProvider.notifier).restore()"
      pattern: "restoreControllerProvider"
    - from: "lib/sync/restore_controller.dart"
      to: "lib/sync/api_client.dart (Plan 01)"
      via: "ref.read(apiClientProvider).restoreTrips()"
      pattern: "apiClientProvider"
    - from: "lib/sync/restore_controller.dart"
      to: "lib/database/daos/trips_dao.dart"
      via: "insertOrIgnoreTrip(tripJsonToCompanion(json))"
      pattern: "insertOrIgnoreTrip"
    - from: "lib/features/settings/screens/settings_screen.dart"
      to: "lib/sync/sync_engine.dart (Plan 01/02)"
      via: "ref.watch(syncStatusProvider) + SyncQueueDao.watchPending()"
      pattern: "syncStatusProvider"
---

<objective>
Deliver the manual restore-from-cloud flow (SYNC-03) and wire the two deferred
Settings `_AccountSection` rows (cloud-sync status + Restore from cloud) into the
signed-in branch. Restore downloads all of the authenticated user's trips from
the Phase 10 backend, maps each Trip JSON to a Drift companion, and inserts it
with dedupe-by-UUID — Drift stays the single source of truth and the UI updates
through the existing reactive watchers. No network call ever blocks the UI.

Purpose: Phase 11 success criterion 3 — "User can trigger cloud restore from
settings, which downloads all trips and inserts them into Drift (skipping
duplicates)". Plus the user-visible surfacing of sync status (D-09, D-10).

Output: `lib/sync/restore_controller.dart`, a new `insertOrIgnoreTrip` DAO method
(+ regenerated drift code), new copy constants, wired Settings rows, and unit +
widget tests proving dedupe, state transitions, and per-auth-state rendering.
</objective>

<contract_binding>
## CRITICAL — Bind to Plan 01 + Plan 02 frozen public names

This plan runs in **Wave 3**, after Plan 01 (Wave 1: transport + status model)
and Plan 02 (Wave 2: engine + triggers). It CONSUMES the following frozen public
names defined by D-01..D-10 in `11-CONTEXT.md`. Do NOT invent alternatives — read
`11-01-PLAN.md` and `11-02-PLAN.md` and bind to the EXACT identifiers they
export. If a name below differs from the actual Plan 01/02 export, the Plan 01/02
name wins — update this plan's references, never fork a parallel symbol.

**From Plan 01 (`lib/sync/api_client.dart`) — D-02, D-04, D-08:**
- `apiClientProvider` — `Provider<ApiClient>` (manual Riverpod, keepAlive).
- `ApiClient.restoreTrips()` — `Future<List<...>>`: performs `GET /trips/restore`,
  attaches a fresh Firebase ID token, parses `body.data.trips`, and returns the
  per-trip JSON objects. (The exact element type — `Map<String, dynamic>` vs a
  typed DTO — is Plan 01's choice; consume whatever it returns.)
- `tripJsonToCompanion(<trip json>)` — maps one restored Trip JSON (camelCase
  keys, ISO-8601 UTC timestamps per D-04) to a `TripsCompanion`. This mapper is
  Plan 01's deliverable; Plan 03 calls it, it does NOT reimplement it. If Plan 01
  did not export a standalone mapper, add the call site against whatever Plan 01
  named it and note the binding in the SUMMARY.

**From Plan 01 (`lib/sync/sync_status.dart` or co-located) — D-10:**
- `sealed SyncStatus` with the variants Plan 01 froze (expected:
  `SyncIdle` / `SyncSyncing` / `SyncSynced` / `SyncOffline` / `SyncFailed(count)`
  — switch exhaustively, never `default`).
- `syncStatusProvider` — exposes the current `SyncStatus`.

**From Plan 02 (`lib/sync/sync_engine.dart`) — D-05, D-06, D-07:**
- `syncEngineProvider` — the engine. Plan 03's failed-status row taps call the
  engine's retry entry point (expected `processPending()` and/or a
  `resetFailed()` re-enqueue). Use whatever Plan 02 exposed for "retry now".

**From `lib/config/constants.dart` — Plan 01 ADDED these (D-09):**
- Status-row copy constants ("All synced" / "Syncing…" / "N pending" /
  "Sync failed — tap to retry"). If Plan 01 named them `kCopySyncStatus*`, reuse
  those EXACT names. Restore-row copy + result-feedback copy constants (Restore
  row label, "Restored N trips", "Already up to date", restore-error) are ADDED
  BY THIS PLAN in Task 2 only if Plan 01 did not already add them — grep first to
  avoid duplicate definitions.

**Reachability note:** If Plans 01/02 are not yet on disk when this plan
executes, STOP and surface a blocker — Plan 03 cannot compile without them. This
is a Wave 3 plan by construction.
</contract_binding>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Phase contract + decisions (READ FIRST)
@.planning/phases/11-sync-engine/11-CONTEXT.md
@.planning/phases/11-sync-engine/11-01-PLAN.md
@.planning/phases/11-sync-engine/11-02-PLAN.md

# Files this plan modifies / extends
@lib/features/settings/screens/settings_screen.dart
@lib/database/daos/trips_dao.dart
@lib/config/constants.dart
@lib/database/providers.dart
@lib/features/auth/providers/auth_providers.dart
@lib/features/auth/models/auth_state.dart
@lib/database/daos/sync_queue_dao.dart
@lib/database/tables/trips_table.dart

# Test patterns to mirror EXACTLY
@test/widget/features/settings/settings_screen_test.dart
@test/unit/database/sync_queue_dao_test.dart

<interfaces>
<!-- Verified from the codebase — executor uses these directly, no exploration. -->

From lib/database/providers.dart (manual Riverpod 3.x, keepAlive — NO codegen):
```dart
final Provider<AppDatabase> appDatabaseProvider;   // ref.onDispose(db.close)
final Provider<TripsDao> tripsDaoProvider;         // appDatabaseProvider.tripsDao
final Provider<SyncQueueDao> syncQueueDaoProvider; // appDatabaseProvider.syncQueueDao
```

From lib/database/daos/trips_dao.dart (extend this DAO):
```dart
@DriftAccessor(tables: [Trips])
class TripsDao extends DatabaseAccessor<AppDatabase> with _$TripsDaoMixin {
  Future<void> insertTrip(TripsCompanion companion);   // into(trips).insert(companion)
  Future<TripRow?> findById(String id);
  // ADD in Task 1: Future<bool> insertOrIgnoreTrip(TripsCompanion c)
}
```

From lib/database/daos/sync_queue_dao.dart:
```dart
Stream<List<SyncQueueRow>> watchPending(); // status == kSyncStatusPending
```

From lib/features/auth/models/auth_state.dart (sealed — switch exhaustively):
```dart
sealed class AuthState {}
final class AuthLoading extends AuthState {}
final class AuthGuest   extends AuthState {}
final class AuthSignedIn extends AuthState { final String uid, name, email; }
```

From lib/features/auth/providers/auth_providers.dart:
```dart
final NotifierProvider<AuthStateNotifier, AuthState> authStateProvider;
final Provider<AuthService> authServiceProvider;
```

Trips table columns (lib/database/tables/trips_table.dart) — companion shape:
  id (PK, text), userId (default kDefaultUserId), startTime, endTime,
  durationSeconds, distanceMeters, routePolyline (nullable), direction,
  timeMovingSeconds, timeStuckSeconds, isManualEntry (default false),
  createdAt/updatedAt (default currentDateAndTime). Primary key = {id}.

AppDatabase ctor (lib/database/database.dart): `AppDatabase([QueryExecutor? executor])`
schemaVersion => 2. In-memory test DB: `AppDatabase(NativeDatabase.memory())`.

Drift insert-or-ignore (from RESEARCH / drift API):
```dart
final rowid = await into(trips).insert(companion, mode: InsertMode.insertOrIgnore);
// returns rowid > 0 when a NEW row was written; 0 when the id already existed.
```
NOTE: `import 'package:drift/drift.dart';` already provides `InsertMode`.
</interfaces>

<settings_wiring_reference>
The signed-in branch lives at lib/features/settings/screens/settings_screen.dart
~lines 88-104 inside `_AccountSection.build`. Current signed-in `rows` list:
  [ AccountRow(...), SettingsRow(label: kCopySettingsSignOut, dangerous: true, onTap: signOut) ].
Insert the new rows BETWEEN AccountRow and the Sign out row:
  AccountRow → [Cloud sync status row] → [Restore from cloud row] → Sign out.
Guest branch (the `_ =>` arm) stays EXACTLY as-is.
Reuse `SettingsRow` / `SettingsSection` patterns. Extract sub-widgets so neither
the section nor any new widget exceeds 100 lines (CLAUDE.md).
</settings_wiring_reference>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: insertOrIgnoreTrip DAO method + RestoreController</name>
  <files>lib/database/daos/trips_dao.dart, lib/database/daos/trips_dao.g.dart, lib/sync/restore_controller.dart, test/unit/sync/restore_controller_test.dart</files>
  <read_first>
    - lib/database/daos/trips_dao.dart (insertTrip pattern, DAO doc style)
    - lib/database/providers.dart (manual Provider/Notifier shape, keepAlive comment)
    - 11-01-PLAN.md → apiClientProvider, ApiClient.restoreTrips, tripJsonToCompanion exact names
    - test/unit/database/sync_queue_dao_test.dart (in-memory AppDatabase(NativeDatabase.memory()) setUp/tearDown)
  </read_first>
  <behavior>
    insertOrIgnoreTrip(TripsCompanion):
    - Test A: inserting a companion with a brand-new id returns true and the row is queryable via findById.
    - Test B: inserting a companion whose id already exists returns false and does NOT overwrite the existing row (existing direction/duration unchanged).
    RestoreController.restore() (state machine RestoreState: idle → restoring → success(count) | error):
    - Test C (success/dedupe): ApiClient returns 3 trips; DB already has 1 of those ids. After restore, DB holds 3 rows total and state is RestoreSuccess(count: 2) — only NEW inserts counted.
    - Test D (all-new): ApiClient returns 2 trips, DB empty → DB holds 2, state RestoreSuccess(count: 2).
    - Test E (already-up-to-date): ApiClient returns 2 trips, both already present → DB unchanged (2 rows), state RestoreSuccess(count: 0).
    - Test F (error): ApiClient.restoreTrips throws → state RestoreError, DB unchanged, NO exception propagates to the caller (caught internally).
    - Restore does NOT enqueue any sync_queue rows (assert SyncQueueDao.watchPending().first is empty after a successful restore).
  </behavior>
  <action>
    PART A — DAO (lib/database/daos/trips_dao.dart):
    Add `Future<bool> insertOrIgnoreTrip(TripsCompanion companion)` modeled on the
    existing `insertTrip` doc style. Implement with
    `into(trips).insert(companion, mode: InsertMode.insertOrIgnore)` and return
    `rowid > 0` (true = a new row was written; false = the UUID already existed and
    was skipped — D-08 dedupe-by-UUID). Document that this is the restore path and
    that it never overwrites an existing trip (client-authoritative). `InsertMode`
    comes from the existing `package:drift/drift.dart` import. Then regenerate:
    `dart run build_runner build --delete-conflicting-outputs` (updates
    trips_dao.g.dart). Commit generated output (project commits .g.dart).

    PART B — RestoreController (lib/sync/restore_controller.dart):
    Create the `lib/sync/` file. Follow the MANUAL Riverpod 3.x Notifier pattern
    from lib/database/providers.dart / auth_providers.dart — NO `@riverpod` codegen
    (drift_dev analyzer pin, project-wide constraint).
    - Define `sealed class RestoreState` with `final class RestoreIdle`,
      `RestoreRestoring`, `RestoreSuccess(int count)`, `RestoreError` (per
      CLAUDE.md sealed-state rule; switch exhaustively, no `default`).
    - `class RestoreController extends Notifier<RestoreState>` with
      `RestoreState build() => const RestoreIdle();`.
    - `Future<void> restore() async`: set state `RestoreRestoring`; in a try/catch
      call `ref.read(apiClientProvider).restoreTrips()` (Plan 01), then for each
      returned trip JSON call `ref.read(tripsDaoProvider).insertOrIgnoreTrip(
      tripJsonToCompanion(json))` and count the `true` results; set
      `RestoreSuccess(insertedCount)`. On any throw, set `RestoreError` and DO NOT
      rethrow (never block/crash UI). Use the EXACT Plan 01 symbol names
      (apiClientProvider, restoreTrips, tripJsonToCompanion) — bind, don't fork.
      Restore MUST NOT enqueue sync rows (it is a download — D-08).
    - `final NotifierProvider<RestoreController, RestoreState> restoreControllerProvider`
      (bare NotifierProvider → keepAlive, matching the providers.dart convention;
      do NOT use .autoDispose).

    PART C — Unit test (test/unit/sync/restore_controller_test.dart):
    Mirror sync_queue_dao_test.dart setUp/tearDown for the in-memory DB
    (`AppDatabase(NativeDatabase.memory())`, `tearDown(db.close)`). Use a
    `ProviderContainer` with overrides: `tripsDaoProvider.overrideWithValue(
    db.tripsDao)`, `apiClientProvider.overrideWithValue(_FakeApiClient(...))`.
    Write a `_FakeApiClient implements ApiClient` returning a scripted trip list
    (and a throwing variant for Test F) — NO network. Cover Tests A–F above. Read
    state via `container.read(restoreControllerProvider)` after awaiting
    `container.read(restoreControllerProvider.notifier).restore()`.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && dart run build_runner build --delete-conflicting-outputs && flutter test test/unit/sync/restore_controller_test.dart</automated>
  </verify>
  <done>insertOrIgnoreTrip returns true for new ids / false for existing; RestoreController transitions idle→restoring→success(count) with correct dedupe count and idle→restoring→error on failure; restore enqueues zero sync rows; all 6 unit tests green; trips_dao.g.dart regenerated.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Settings signed-in rows (cloud-sync status + Restore) + copy constants</name>
  <files>lib/config/constants.dart, lib/features/settings/screens/settings_screen.dart</files>
  <read_first>
    - lib/features/settings/screens/settings_screen.dart (_AccountSection lines 81-118; signed-in rows list lines 88-104; helper/section patterns)
    - lib/features/settings/widgets/settings_row.dart (SettingsRow: label/subtitle/trailing/onTap/dangerous; chevron auto-render)
    - lib/config/constants.dart Phase 9 block (kCopySettings* naming convention)
    - 11-01-PLAN.md → confirm whether kCopySyncStatus* / restore copy constants were already added by Plan 01 (grep before adding to avoid duplicates)
    - Plan 01 sealed SyncStatus variant names + syncStatusProvider; Plan 02 engine retry entry point
  </read_first>
  <action>
    PART A — Constants (lib/config/constants.dart), Phase 11 block at end:
    Grep first — if Plan 01 already added a copy constant, reuse it; do NOT
    redefine. Add ONLY the ones missing (no hardcoded strings anywhere):
    - Cloud-sync status row label + per-status subtitles (if not from Plan 01):
      kCopySyncStatusLabel = 'Cloud sync'; kCopySyncAllSynced = 'All synced';
      kCopySyncSyncing = 'Syncing…'; kCopySyncFailed = 'Sync failed — tap to retry';
      and a pending template — store the suffix word ('pending') and build
      '$count pending' at the call site (mirror kDashboardTripCountPlural pattern).
    - Restore row + feedback: kCopySettingsRestoreLabel = 'Restore from cloud';
      kCopyRestoreSuccessSingular ('Restored 1 trip') / a plural template noun, or
      a single template assembled at call site ('Restored $n trips');
      kCopyRestoreUpToDate = 'Already up to date';
      kCopyRestoreError = "Couldn't restore. Check your connection and try again.";
      kCopyRestoringInProgress = 'Restoring…'.
    Each constant gets a doc comment citing D-09 / SYNC-03.

    PART B — Settings (lib/features/settings/screens/settings_screen.dart):
    In `_AccountSection.build`, signed-in (`AuthSignedIn`) arm ONLY, insert two
    rows BETWEEN AccountRow and the Sign out row (order: AccountRow → sync status
    → restore → Sign out). Guest `_ =>` arm UNCHANGED.
    - Cloud-sync status row: extract a small `_CloudSyncRow extends ConsumerWidget`
      (keeps _AccountSection < 100 lines). It `ref.watch(syncStatusProvider)` and
      `ref.watch(syncQueueDaoProvider).watchPending()` (via a tiny StreamProvider or
      `ref.watch` of an AsyncValue) to compute the subtitle. Render a `SettingsRow(
      label: kCopySyncStatusLabel, subtitle: <derived>)`. Map the sealed SyncStatus
      exhaustively (switch, no default) to subtitle copy: synced→All synced,
      syncing→Syncing…, pending(N)→'$N pending', failed→Sync failed — tap to retry.
      When status is the failed variant, set `onTap` to call the Plan 02 engine
      retry entry point (`ref.read(syncEngineProvider)...processPending()` /
      resetFailed — use Plan 02's exact name); otherwise `onTap: null`.
    - Restore row: extract `_RestoreRow extends ConsumerWidget`. `ref.watch(
      restoreControllerProvider)` for the subtitle (RestoreRestoring → kCopyRestoring
      InProgress, else the restore label only). `onTap`: guard against double-tap
      while restoring; otherwise `await ref.read(restoreControllerProvider.notifier)
      .restore()` then show a SnackBar from the resulting state:
      RestoreSuccess(0) → kCopyRestoreUpToDate; RestoreSuccess(n>0) → 'Restored n
      trips' (singular/plural copy); RestoreError → kCopyRestoreError. Use
      `ScaffoldMessenger.maybeOf(context)` and a mounted guard (the screen lives in
      MainShell; SnackBar host exists at the shell level). Never block the UI — the
      controller already catches errors, so no try/catch needed at the call site.
    Both new sub-widgets MUST be < 100 lines; `_AccountSection` MUST stay < 100
    lines after the edit. Match existing import style (package:traevy/...).
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter analyze lib/features/settings/screens/settings_screen.dart lib/config/constants.dart lib/sync/restore_controller.dart</automated>
  </verify>
  <done>Signed-in Account section renders AccountRow, a cloud-sync status row driven by SyncStatus+watchPending, a Restore-from-cloud row, and Sign out — in that order; guest section unchanged; all copy from constants (no hardcoded strings); failed-status row taps the engine retry; restore row triggers RestoreController and shows result feedback; flutter analyze clean; _AccountSection and both new sub-widgets each < 100 lines.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Widget tests — _AccountSection rows per auth + SyncStatus</name>
  <files>test/widget/features/settings/settings_screen_test.dart</files>
  <read_first>
    - test/widget/features/settings/settings_screen_test.dart (FULL — _pumpSettingsScreen helper, _FakeAuthNotifier/_FakeAuthService fakes, override list, _scrollTo, existing guest/signed-in assertions incl. the "guest hides Cloud sync/Restore" test at lines 399-410)
    - Plan 01 syncStatusProvider type + SyncStatus variant constructors (for overrides)
    - lib/config/constants.dart new Phase 11 copy constants
  </read_first>
  <behavior>
    Extend the existing settings widget test suite (do NOT rewrite it; the Phase 8/9
    assertions must keep passing). Add a Phase 11 group:
    - Signed-in renders BOTH new rows: find kCopySyncStatusLabel ('Cloud sync') and
      kCopySettingsRestoreLabel ('Restore from cloud') present when AuthSignedIn.
    - Row order: Cloud sync + Restore appear, and kCopySettingsSignOut still present.
    - SyncStatus reactivity: override syncStatusProvider to the synced variant →
      subtitle shows kCopySyncAllSynced; override to the failed variant → subtitle
      shows kCopySyncFailed and the row is tappable (has onTap / chevron).
    - Guest UNCHANGED: the EXISTING test 'guest state hides Sign out, Cloud sync,
      and Restore rows' (currently asserting findsNothing for 'Cloud sync' /
      'Restore from cloud') MUST still pass against the real constants — update it
      to assert on kCopySyncStatusLabel / kCopySettingsRestoreLabel instead of the
      literal strings so it tracks the constants.
    - Restore tap → controller invoked: override restoreControllerProvider with a
      fake/instrumented notifier (or override apiClientProvider with a _FakeApiClient
      and tripsDaoProvider with an in-memory db) so tapping Restore advances state
      and shows a SnackBar; assert the SnackBar text for success(0)/success(n)/error
      OR assert the fake controller's restore() was called. NO network.
  </behavior>
  <action>
    Reuse the existing `_pumpSettingsScreen` harness and its fakes. Extend the
    override list to allow injecting `syncStatusProvider` and (for restore) a fake
    `apiClientProvider` + in-memory `tripsDaoProvider`, OR a fake
    `restoreControllerProvider` notifier subclass (mirror `_FakeAuthNotifier`
    extending the real Notifier so the overrideWith factory type-checks). Add a fake
    SyncEngine override only if the failed-row tap path is exercised. Wrap the screen
    so a ScaffoldMessenger exists for SnackBar assertions (the existing helper uses
    `MaterialApp(home: SettingsScreen())` — MaterialApp provides a ScaffoldMessenger,
    so SnackBars surface). Use `_scrollTo` if the Account rows fall below the 600px
    test viewport (Account is top-most, so likely visible — verify). Keep all
    assertions on constants, not literals. Do not remove or weaken existing tests.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter test test/widget/features/settings/settings_screen_test.dart</automated>
  </verify>
  <done>New Phase 11 widget group passes: signed-in shows both new rows reacting to SyncStatus (synced/failed subtitles, failed tappable); restore tap drives the controller + shows result SnackBar; guest shows neither row; ALL pre-existing settings widget tests still green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| backend `GET /trips/restore` → client | Restore response is external input deserialized into Drift companions. Even though it is the user's own backend data, the JSON crosses into the local source-of-truth DB. |
| Settings UI → restore/sync controllers | User taps trigger background network + DB writes; must never block or crash the UI thread. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Tampering | tripJsonToCompanion + insertOrIgnoreTrip | mitigate | Parsing/validation is Plan 01's `tripJsonToCompanion` (D-04 contract). insertOrIgnoreTrip uses `InsertMode.insertOrIgnore` on the id PK so a malformed/duplicate id can never overwrite a good local row (client-authoritative). RestoreController catches all throws → bad payloads become RestoreError, not a corrupt DB. |
| T-11-03-02 | Denial of Service (UI freeze) | RestoreController.restore / Settings restore row | mitigate | All restore work is async + fire-and-forget from the tap; errors caught inside the controller (no rethrow); restore row guards against double-tap while RestoreRestoring. UI reads Drift only; never awaits network inline in build(). |
| T-11-03-03 | Information disclosure | restore error feedback + sync status copy | mitigate | Error SnackBar shows a fixed constant (kCopyRestoreError) — never the raw exception/`error.toString()` (may contain token/PII), matching the auth_providers PII-guard pattern. No uid/email/token logged. |
| T-11-03-04 | Elevation of privilege / cross-user data | restore writes to local Drift | accept | The backend scopes `/trips/restore` to the authenticated user's token (Phase 10, verified). Client trusts its own authenticated response; no multi-tenant data on this device. Low risk for a single-user offline-first app. |
| T-11-03-05 | Spoofing | restore request auth | transfer | Fresh Firebase ID token attachment + 401 refresh is owned by Plan 01's ApiClient (D-03). Plan 03 relies on that transport contract. |
</threat_model>

<verification>
- `dart run build_runner build --delete-conflicting-outputs` succeeds (trips_dao.g.dart regenerated for insertOrIgnoreTrip).
- `flutter analyze` is clean across the touched files (zero warnings — very_good_analysis).
- `flutter test test/unit/sync/restore_controller_test.dart` green (dedupe count, success/error states, zero sync rows enqueued).
- `flutter test test/widget/features/settings/settings_screen_test.dart` green (new Phase 11 group + all pre-existing Phase 8/9 tests).
- Manual contract check: every Plan 01/02 symbol referenced (apiClientProvider, restoreTrips, tripJsonToCompanion, syncStatusProvider, SyncStatus variants, sync engine retry entry, status copy constants) matches the actual names exported by 11-01/11-02 — no forked symbols.
</verification>

<success_criteria>
Maps to Phase 11 success criterion 3 ("User can trigger cloud restore from
settings, which downloads all trips and inserts them into Drift (skipping
duplicates)") and SYNC-03:
1. RestoreController downloads via ApiClient.restoreTrips, maps via
   tripJsonToCompanion, inserts with insert-or-ignore dedupe-by-UUID, and reports
   the count of NEW trips.
2. Restore never enqueues sync rows and never blocks/crashes the UI.
3. The signed-in Settings Account section shows a live cloud-sync status row
   (driven by SyncStatus + watchPending) and a working Restore-from-cloud row with
   result feedback; the guest section is unchanged.
4. All copy is in constants.dart; sealed state used for RestoreState; Drift remains
   the single source of truth; unit + widget tests green; analyze clean.
</success_criteria>

<output>
After completion, create
`.planning/phases/11-sync-engine/11-03-SUMMARY.md`.
Record the EXACT Plan 01/02 symbol names you bound to (apiClient.restoreTrips
return type, tripJsonToCompanion name, SyncStatus variant names, sync engine retry
method, status copy constant names) so any later replan can verify the contract
held.
</output>
