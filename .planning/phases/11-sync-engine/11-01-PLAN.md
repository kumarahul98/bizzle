---
phase: 11-sync-engine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pubspec.yaml
  - pubspec.lock
  - lib/config/constants.dart
  - lib/sync/sync_status.dart
  - lib/sync/trip_serializer.dart
  - lib/sync/api_client.dart
  - lib/database/daos/sync_queue_dao.dart
  - lib/database/daos/sync_queue_dao.g.dart
  - test/unit/sync/trip_serializer_test.dart
  - test/unit/sync/api_client_test.dart
  - test/unit/database/sync_queue_dao_test.dart
autonomous: true
requirements: [SYNC-02, SYNC-03]

must_haves:
  truths:
    - "The serializer produces a JSON map whose keys and value shapes pass the backend zod tripSchema exactly (camelCase, ISO-8601 UTC with Z, omitted userId, numeric 0 never null)."
    - "The restore parser round-trips a server Trip JSON object back into a TripsCompanion with DateTimes parsed from ISO strings."
    - "ApiClient attaches a fresh Bearer token, refreshes-and-retries once on 401, and throws a typed SyncException on non-2xx."
    - "SyncException carries the HTTP status AND a `retryable` bool: 5xx / network / timeout / 401-after-refresh-failed = retryable; 4xx (esp. 400 validation) = NON-retryable — so the engine (Plan 02) can fail-fast on a poison-pill 400 instead of burning all 3 retries."
    - "restoreTrips() unwraps the FULL response envelope `decoded['body']['data']['trips']` (the backend double-wraps as {statusCode, body:{data:{trips}}}); syncTrips/deleteTrip success parsing is aware of the same nesting."
    - "pubspec pins http ^1.6.0 and connectivity_plus ^7.1.1 (RESEARCH-verified); kApiBaseUrl is the verified stable v2 alias and is injectable via the provider for tests/emulator override."
    - "The production apiClientProvider default wires the real FirebaseAuth getIdToken/currentUser callbacks (not a stub); only tests inject fakes."
    - "SyncStatus is a finite sealed model exposed via syncStatusProvider that Plans 02 and 03 can read."
    - "SyncQueueDao.markFailed and SyncQueueDao.resetFailed mutate row status correctly."
    - "flutter analyze is clean, build_runner regenerates drift code, and the new unit tests are green."
  artifacts:
    - path: "lib/sync/sync_status.dart"
      provides: "SyncStatus sealed model + SyncStatusNotifier + syncStatusProvider"
      contains: "sealed class SyncStatus"
    - path: "lib/sync/trip_serializer.dart"
      provides: "TripRow -> Map serializer + Trip JSON -> TripsCompanion parser"
      contains: "toUtc().toIso8601String()"
    - path: "lib/sync/api_client.dart"
      provides: "ApiClient (syncTrips/deleteTrip/restoreTrips) + SyncException (statusCode + retryable) + injectable kApiBaseUrl + apiClientProvider"
      contains: "retryable"
    - path: "lib/database/daos/sync_queue_dao.dart"
      provides: "markFailed + resetFailed DAO methods"
      contains: "markFailed"
  key_links:
    - from: "lib/sync/trip_serializer.dart"
      to: "backend/functions/src/utils/validation.ts tripSchema"
      via: "exact camelCase key set + ISO-8601 Z timestamps"
      pattern: "startTime|durationSeconds|isManualEntry"
    - from: "lib/sync/api_client.dart"
      to: "kApiBaseUrl + Authorization header"
      via: "http.Client POST/DELETE/GET with Bearer token from getIdToken(); kApiBaseUrl injectable for tests/emulator"
      pattern: "Bearer|kApiBaseUrl"
    - from: "lib/sync/api_client.dart SyncException.retryable"
      to: "lib/sync/sync_engine.dart (Plan 02) error branching"
      via: "engine branches on retryable: retryable -> incrementRetry+backoff; non-retryable (400) -> markFailed immediately"
      pattern: "retryable"
---

<objective>
Build the Phase 11 sync FOUNDATION: the shared contracts, names, and persistence
hooks that Plans 02 (engine) and 03 (restore + Settings) import and bind to. This
plan owns transport, serialization, the finite sync-status model, and two new DAO
methods — nothing else. It must compile and unit-test green entirely on its own.

Purpose: A single source of truth for the client↔backend wire contract (must match
the deployed Phase 10 zod `tripSchema` byte-for-byte), the auth-token attachment +
401-refresh policy, the RETRYABLE-vs-NON-retryable error classification the engine
branches on (HIGH-2), and the `SyncStatus` finite state that the Settings row and
the engine both render — defined once so siblings cannot drift.

Output:
- `lib/sync/sync_status.dart` — `SyncStatus` sealed model + `syncStatusProvider`
- `lib/sync/trip_serializer.dart` — serializer + restore parser
- `lib/sync/api_client.dart` — `ApiClient` + `SyncException` (statusCode + retryable) + `apiClientProvider`
- `lib/config/constants.dart` — base URL, endpoint paths, backoff + Settings copy
- `lib/database/daos/sync_queue_dao.dart` — `markFailed` + `resetFailed`
- Unit tests for serializer, restore parser, api_client (MockClient), DAO methods

## Public names Plans 02 & 03 bind to (DO NOT RENAME)
Siblings depend on these exact identifiers. Any rename breaks them.
- `SyncStatus` (sealed) with variants: `SyncIdle`, `SyncSyncing`, `SyncSynced`,
  `SyncOffline`, `SyncFailed(int count)` — per D-10.
- `SyncStatusNotifier extends Notifier<SyncStatus>` and
  `final NotifierProvider<SyncStatusNotifier, SyncStatus> syncStatusProvider`.
- `ApiClient` with methods `Future<void> syncTrips(List<TripRow> trips)`,
  `Future<void> deleteTrip(String tripId)`,
  `Future<List<TripsCompanion>> restoreTrips()`.
- `SyncException` (typed error thrown by `ApiClient`) carries an int? `statusCode`,
  a `bool notSignedIn`, AND a `bool retryable` (HIGH-2 error classification): 5xx /
  network / timeout / 401-after-refresh-failed ⇒ `retryable == true`; 4xx (esp. 400
  validation) ⇒ `retryable == false`. Plan 02's engine branches on this exact flag.
- `final Provider<ApiClient> apiClientProvider` (injectable `http.Client` AND
  injectable base URL — the default is `kApiBaseUrl`; tests/emulator override it).
- `TripSerializer.toJson(TripRow)` → `Map<String, dynamic>` and
  `TripSerializer.fromJson(Map<String, dynamic>)` → `TripsCompanion`.
- `SyncQueueDao.markFailed(int id)` and `SyncQueueDao.resetFailed()`.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-sync-engine/11-CONTEXT.md

# The wire contract this plan MUST match exactly:
@backend/functions/src/utils/validation.ts
@backend/functions/src/handlers/restore-trips.ts
@backend/functions/src/handlers/sync-trips.ts

# Existing foundations to build on / extend:
@lib/config/constants.dart
@lib/database/daos/sync_queue_dao.dart
@lib/database/daos/trips_dao.dart
@lib/database/tables/trips_table.dart
@lib/database/providers.dart
@lib/features/auth/providers/auth_providers.dart
@test/unit/database/sync_queue_dao_test.dart

<interfaces>
<!-- Contracts the executor binds to. Use directly — no codebase exploration needed. -->

Backend zod tripSchema (backend/functions/src/utils/validation.ts) — the serializer
output MUST satisfy this exactly:
```ts
tripSchema = z.object({
  id: z.string().uuid(),
  userId: z.string().optional(),          // OMIT from client payload — server forces it
  startTime: z.string().datetime(),       // ISO-8601 with Z
  endTime: z.string().datetime(),         // ISO-8601 with Z
  durationSeconds: z.number().int().nonnegative(),   // number, 0 not null
  distanceMeters: z.number().nonnegative(),          // number, 0 not null
  routePolyline: z.string().max(100000).nullable(),  // null allowed
  direction: z.enum(['to_office', 'to_home']),       // stored string as-is
  timeMovingSeconds: z.number().int().nonnegative(),
  timeStuckSeconds: z.number().int().nonnegative(),
  isManualEntry: z.boolean(),
  createdAt: z.string().datetime(),
  updatedAt: z.string().datetime(),
});
// POST body: { trips: tripSchema[] } (min 1, max 1000)
```

Restore response shape (restore-trips.ts) — VERIFIED, the HTTP body is DOUBLY wrapped:
`200 { statusCode: 200, body: { data: { trips: Trip[] } } }`
⇒ the client MUST unwrap the FULL envelope `decoded['body']['data']['trips']`
(MEDIUM-1) — NOT `decoded['data']['trips']`. Each Trip has the camelCase fields above
PLUS `userId` (which the client ignores on insert — Drift userId is set by auth
backfill, not the server copy).

Sync response shape (sync-trips.ts) — same double-wrap nesting:
`200 { statusCode: 200, body: { data: { syncedIds: string[] } } }`
Error shape (all handlers): non-2xx with `{ statusCode, body: { error: string } }`.
The 400 validation error (zod) is the poison-pill case ⇒ classify NON-retryable
(HIGH-2). 5xx / timeout / socket failures ⇒ retryable.

Drift `TripRow` fields (lib/database/tables/trips_table.dart) the serializer reads:
`id:String, userId:String, startTime:DateTime(UTC), endTime:DateTime(UTC),
durationSeconds:int, distanceMeters:double, routePolyline:String?, direction:String,
timeMovingSeconds:int, timeStuckSeconds:int, isManualEntry:bool, createdAt:DateTime,
updatedAt:DateTime`. Manual entries store 0 for the numeric fields (non-nullable
columns with no GPS) — serialize as 0, never null. `distanceMeters` is a Dart
`double`; emit it as-is (JSON number) — zod `.number()` accepts it.

`TripsCompanion` (lib/database/database.dart, generated) for the restore parser:
construct via `TripsCompanion.insert(id: ..., startTime: ..., endTime: ...,
durationSeconds: ..., distanceMeters: ..., direction: ...,
timeMovingSeconds: ..., timeStuckSeconds: ...)` with optional
`routePolyline: Value(...)`, `isManualEntry: Value(...)`,
`createdAt: Value(...)`, `updatedAt: Value(...)`. `userId` is NOT set here
(column default `kDefaultUserId`; auth backfill owns it). Parse ISO strings with
`DateTime.parse(...).toUtc()`.

Token source (lib/features/auth/providers/auth_providers.dart):
`firebaseAuthProvider` exposes `FirebaseAuth`. Use
`ref.read(firebaseAuthProvider).currentUser?.getIdToken()` for a fresh token and
`...getIdToken(true)` to force-refresh on 401. `currentUser == null` ⇒ not signed
in ⇒ ApiClient must throw `SyncException(notSignedIn)` (engine treats as no-op skip,
NOT a retryable failure — per D-03).

Manual Riverpod 3.x pattern (lib/database/providers.dart): bare `Provider((ref)=>…)`
= keepAlive. NO `@riverpod` codegen (analyzer conflict with drift_dev). Use
`NotifierProvider<Notifier<T>, T>` for status. Never `StateNotifier`.

Existing constants already present (do NOT redefine): `kSyncQueueMaxRetries=3`,
`kSyncStatusPending`, `kSyncStatusSynced`, `kSyncStatusFailed`, `kSyncActionCreate/
Update/Delete`, `kFirebaseIdTokenKey`, `kDefaultUserId`, `kDirectionToOffice`,
`kDirectionToHome`.

Drift in-memory test harness (test/unit/database/sync_queue_dao_test.dart):
```dart
db = AppDatabase(DatabaseConnection(
  NativeDatabase.memory(), closeStreamsSynchronously: true));
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add deps, sync constants, and SyncQueueDao.markFailed/resetFailed</name>
  <files>pubspec.yaml, lib/config/constants.dart, lib/database/daos/sync_queue_dao.dart, lib/database/daos/sync_queue_dao.g.dart, test/unit/database/sync_queue_dao_test.dart</files>
  <action>
1. **pubspec.yaml** — add to `dependencies` (RESEARCH-verified versions):
   `http: ^1.6.0` and `connectivity_plus: ^7.1.1`. (NOTE: connectivity_plus v7 returns
   `List<ConnectivityResult>` from `onConnectivityChanged`/`checkConnectivity()` — Plan 02
   handles the List shape; v7 needs Dart ≥3.3, satisfied by SDK 3.11.4.) Keep
   alphabetical-ish ordering consistent with the existing block. Do NOT add `mockito` — the api_client tests
   use `package:http/testing.dart` (`MockClient`), which ships with `http`. Run
   `flutter pub get` and confirm it resolves clean (commit `pubspec.lock`).

2. **lib/config/constants.dart** — append a new Phase 11 section header comment and add:
   - `const String kApiBaseUrl = 'https://us-central1-travey-298a7.cloudfunctions.net/api';`
     (D-02 — the VERIFIED stable Cloud Functions v2 alias: health 200, 401 auth gate
     confirmed live. This is the canonical base URL; do NOT use the older
     `api-rdj4i7kgmq-uc.a.run.app` run.app host.) The ApiClient takes this as an
     INJECTABLE default (Task 3) so tests/emulator can override it cheaply.
   - `const String kSyncTripsPath = '/trips/sync';`
   - `const String kRestoreTripsPath = '/trips/restore';`
   - `const String kDeleteTripPathPrefix = '/trips/';` (delete id appended: `'$kDeleteTripPathPrefix$tripId'` — D-02 `/trips/{tripId}`)
   - `const Duration kSyncRetryBaseDelay = Duration(seconds: 2);` (D-06)
   - `const Duration kSyncRetryMaxDelay = Duration(seconds: 60);` (D-06 cap)
   - Settings copy constants (D-09, no hardcoded strings later):
     `kSettingsAccountSectionTitle = 'Account'` ONLY if not already present — grep first; if `_AccountSection` already renders a section title constant, reuse it, do not duplicate.
     `kSettingsCloudSyncRowLabel = 'Cloud sync'`
     `kSettingsSyncStatusAllSynced = 'All synced'`
     `kSettingsSyncStatusSyncing = 'Syncing…'`
     `kSettingsSyncStatusPendingTemplate = 'pending'` (caller builds `'$n pending'`)
     `kSettingsSyncStatusFailed = 'Sync failed — tap to retry'`
     `kSettingsSyncStatusOffline = 'Offline'`
     `kSettingsRestoreRowLabel = 'Restore from cloud'`
     `kSettingsRestoreInProgress = 'Restoring…'`
     `kSettingsRestoreResultTemplate = 'Restored'` (caller builds `'Restored $n trips'`)
     `kSettingsRestoreUpToDate = 'Already up to date'`
     `kSettingsRestoreError = "Couldn't restore. Try again."`
   Each constant gets a one-line doc comment citing the D-09/D-02/D-06 decision it implements.
   (Plan 03 consumes the Settings copy; defining it here keeps all Phase 11 strings in one commit and avoids a hardcoded-string slip in Plan 03.)

3. **lib/database/daos/sync_queue_dao.dart** — add two methods mirroring the existing
   `markSynced` style:
   - `Future<void> markFailed(int id)` → `update(syncQueue)..where(id==id)` writing
     `status: Value(kSyncStatusFailed)`. (D-06: promote to terminal failed after
     `kSyncQueueMaxRetries`, OR immediately on a NON-retryable 400 per HIGH-2.)
   - `Future<void> resetFailed()` → bulk `update(syncQueue)..where(status==kSyncStatusFailed)`
     writing `status: Value(kSyncStatusPending)` (and reset `retryCount` to 0 via
     `Value(0)` so a manual retry gets a fresh budget). This is the manual "tap to
     retry" reset used by Plan 02's `SyncEngine.retryFailed()` and surfaced by Plan 03's
     Settings failed-state row.
   Add doc comments tying each to D-06 / the Settings retry action.

4. **dart run build_runner build** — regenerate `sync_queue_dao.g.dart` (`--delete-conflicting-outputs`).
   Commit the regenerated `.g.dart`.

5. **test/unit/database/sync_queue_dao_test.dart** — append tests (same in-memory
   harness): `markFailed sets status=failed`; `resetFailed flips failed→pending and
   zeroes retryCount`; `resetFailed leaves pending/synced rows untouched`.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter pub get && dart run build_runner build --delete-conflicting-outputs && flutter test test/unit/database/sync_queue_dao_test.dart</automated>
  </verify>
  <done>http ^1.6.0 + connectivity_plus ^7.1.1 resolved in pubspec.lock; all new constants exist with doc comments (kApiBaseUrl = the us-central1-travey-298a7 alias) and no duplicate redefinitions; markFailed/resetFailed pass their in-memory tests; build_runner regenerated the DAO mixin clean.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: SyncStatus model + TripSerializer (serialize + restore parse)</name>
  <files>lib/sync/sync_status.dart, lib/sync/trip_serializer.dart, test/unit/sync/trip_serializer_test.dart</files>
  <behavior>
    Serializer (TripRow -> Map):
    - Test: produced map key set == exactly {id, startTime, endTime, durationSeconds,
      distanceMeters, routePolyline, direction, timeMovingSeconds, timeStuckSeconds,
      isManualEntry, createdAt, updatedAt} — and NO `userId` key.
    - Test: startTime/endTime/createdAt/updatedAt serialize via
      `toUtc().toIso8601String()` and END WITH 'Z' (assert `endsWith('Z')`); a
      non-UTC input DateTime still emits a 'Z'-suffixed UTC string.
    - Test: a manual-entry TripRow (distanceMeters 0.0, timeMoving/Stuck 0,
      duration 0, routePolyline null) serializes those numerics as 0 (NOT null) and
      routePolyline as null — i.e. the map passes the tripSchema's nonnegative-number
      + nullable-polyline rules (verify against the bug-manual-entry note in CONTEXT).
    - Test: direction passes through unchanged as the stored 'to_office'/'to_home'.
    Restore parser (Trip JSON Map -> TripsCompanion):
    - Test: round-trip — `fromJson(toJson(row))` yields a TripsCompanion whose
      `.value`s equal the original row's fields (DateTimes parsed back, equal to the
      original instant; routePolyline/isManualEntry preserved). `userId` is NOT set
      on the companion (Value.absent), even though the server JSON includes it.
    - Test: timestamps in the JSON are parsed with DateTime.parse(...).toUtc().
  </behavior>
  <action>
1. **lib/sync/sync_status.dart** — define the finite status model per D-10 (CLAUDE.md:
   sealed classes for finite state, no raw strings):
   ```dart
   sealed class SyncStatus { const SyncStatus(); }
   class SyncIdle    extends SyncStatus { const SyncIdle(); }
   class SyncSyncing extends SyncStatus { const SyncSyncing(); }
   class SyncSynced  extends SyncStatus { const SyncSynced(); }
   class SyncOffline extends SyncStatus { const SyncOffline(); }
   class SyncFailed  extends SyncStatus { const SyncFailed(this.count); final int count; }
   ```
   Then the shared notifier + provider (manual Riverpod 3.x, keepAlive, NOT codegen):
   ```dart
   class SyncStatusNotifier extends Notifier<SyncStatus> {
     @override SyncStatus build() => const SyncIdle();
     void set(SyncStatus s) => state = s;   // engine (Plan 02) drives this
   }
   final NotifierProvider<SyncStatusNotifier, SyncStatus> syncStatusProvider =
     NotifierProvider<SyncStatusNotifier, SyncStatus>(SyncStatusNotifier.new,
       name: 'syncStatusProvider');
   ```
   Doc-comment that Plan 02 (engine) calls `.set(...)` and Plan 03 (Settings row)
   reads via an exhaustive `switch` on `SyncStatus` (never `.when()`).

2. **lib/sync/trip_serializer.dart** — a stateless `TripSerializer` with static methods
   (or top-level functions in a class namespace):
   - `static Map<String, dynamic> toJson(TripRow t)` building the exact camelCase map
     from the interfaces block. Timestamps: `t.startTime.toUtc().toIso8601String()`
     etc. OMIT `userId`. Numeric fields straight from the row (already 0 for manual).
     `routePolyline: t.routePolyline` (nullable). `direction: t.direction`.
   - `static TripsCompanion fromJson(Map<String, dynamic> json)` mapping back to a
     `TripsCompanion.insert(...)`: parse ISO strings via `DateTime.parse(v).toUtc()`;
     `distanceMeters` read as `(json['distanceMeters'] as num).toDouble()`; int fields
     `as int`; `routePolyline: Value(json['routePolyline'] as String?)`;
     `isManualEntry: Value(json['isManualEntry'] as bool)`;
     `createdAt`/`updatedAt` as `Value(parsed)`. Do NOT set `userId`.
   Import `TripRow`/`TripsCompanion` from `package:traevy/database/database.dart`
   and `Value` from `package:drift/drift.dart`. Reference `kDirectionToOffice`/
   `kDirectionToHome` only if you add an assertion guard — otherwise pass-through.

3. **test/unit/sync/trip_serializer_test.dart** — implement the `<behavior>` tests.
   Build `TripRow` instances directly (it's a generated data class with a const-ish
   constructor — construct via `TripRow(id: ..., userId: kDefaultUserId, ...)`).
   No database needed for serializer tests (pure functions).
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter test test/unit/sync/trip_serializer_test.dart</automated>
  </verify>
  <done>SyncStatus sealed model + syncStatusProvider compile; serializer emits the exact zod key set with Z-suffixed UTC timestamps and 0-not-null numerics, omits userId; restore parser round-trips and never sets userId; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ApiClient (transport) + SyncException + apiClientProvider + MockClient tests</name>
  <files>lib/sync/api_client.dart, test/unit/sync/api_client_test.dart</files>
  <behavior>
    Using `package:http/testing.dart` MockClient (no real network, no Firebase):
    - Test: `syncTrips([row])` issues `POST {baseUrl}/trips/sync` with header
      `Authorization: Bearer <token>` and a JSON body `{ "trips": [ <serialized> ] }`;
      a 200 response completes without throwing.
    - Test: 401 on first attempt triggers a forced token refresh and ONE retry; if the
      retry returns 200 the call succeeds; assert exactly two requests were made and
      the second used the refreshed token.
    - Test: a persistent 401 (both attempts) throws `SyncException` with
      `statusCode == 401` and `retryable == true` (401-after-refresh-failed is retryable).
    - Test (HIGH-2 retryable): a 503 response throws `SyncException` with
      `statusCode == 503` and `retryable == true` (no retry here — retry/backoff is the
      engine's job in Plan 02; transport just CLASSIFIES + surfaces the error).
    - Test (HIGH-2 NON-retryable poison-pill): a 400 response throws `SyncException` with
      `statusCode == 400` and `retryable == false` (a validation 400 must NEVER consume
      the retry budget — the engine fails it fast).
    - Test (HIGH-2 transport error): a thrown socket/timeout (MockClient throws) surfaces
      as `SyncException` with `statusCode == null` and `retryable == true`.
    - Test (MEDIUM-1 FULL envelope): `restoreTrips()` on a 200 whose body is the FULL
      double-wrapped envelope `{ "statusCode": 200, "body": { "data": { "trips": [t1, t2] } } }`
      returns a `List<TripsCompanion>` of length 2 — parsed by unwrapping
      `decoded['body']['data']['trips']` (assert it reads the nested `body.data.trips`,
      NOT `data.trips`). A body missing the outer `body` wrapper must NOT silently return
      []; it throws a SyncException (envelope-shape mismatch surfaces, not swallowed).
    - Test: `deleteTrip('id')` issues `DELETE {baseUrl}/trips/id` with Bearer; 200 ok.
    - Test (injectable base URL): constructing ApiClient with a custom `baseUrl` routes
      all three calls to that host (emulator/test override) — assert the request URL host.
    - Test: not-signed-in (token source returns null) throws `SyncException` flagged
      `notSignedIn` (no HTTP request made); `notSignedIn` is NOT classified retryable
      (engine treats it as a no-op skip, not a failure).
    Inject the token via a function seam, NOT FirebaseAuth directly, so tests pass a
    fake token-getter (avoids Firebase platform channels on the test host).
  </behavior>
  <action>
1. **lib/sync/api_client.dart**:
   - `class SyncException implements Exception` carrying `final int? statusCode;`
     `final String message;`, a `final bool notSignedIn;` flag (default false), AND a
     `final bool retryable;` flag (HIGH-2 error classification). Constructors:
       * `SyncException.notSignedIn()` → `notSignedIn = true`, `statusCode = null`,
         `retryable = false` (engine no-ops; never a retry/backoff case).
       * `SyncException.http(int code)` → `statusCode = code`,
         `retryable = code >= 500 || code == 401` (5xx and a final 401-after-refresh are
         retryable; ALL other 4xx — especially 400 validation — are NON-retryable poison
         pills). Set `notSignedIn = false`.
       * `SyncException.transport([Object? cause])` → `statusCode = null`,
         `retryable = true` (network/socket/timeout failures are always retryable).
     `toString()` must NOT include any token/PII — only `statusCode`, `retryable`, and a
     short fixed message. Doc-comment the classification rule and cite HIGH-2 so Plan 02
     branches on `retryable` (NOT on raw status codes).
   - `class ApiClient`. Constructor injects:
       * `http.Client client`
       * `Future<String?> Function({bool forceRefresh}) getToken` — the token seam.
         At the provider level this is wired to
         `(({bool forceRefresh = false}) => ref.read(firebaseAuthProvider).currentUser
           ?.getIdToken(forceRefresh))`. In tests it's a fake.
       * `String baseUrl` — INJECTABLE base URL (default `kApiBaseUrl`). All request URIs
         are built from `baseUrl`, so tests/emulator pass a fake host without a global
         constant edit (addresses the LOW "no emulator override"). Provider default passes
         `kApiBaseUrl`.
   - Private `Future<http.Response> _send(Future<http.Response> Function(String token) run)`:
       1. `final token = await getToken();` if null → `throw SyncException.notSignedIn();`
       2. wrap the network in a try/catch: any thrown socket/timeout/format error (a
          non-`SyncException` `Object`) → `throw SyncException.transport(e);` (retryable).
       3. `var res = await run(token);`
       4. if `res.statusCode == 401`: `final fresh = await getToken(forceRefresh: true);`
          if fresh == null → `throw SyncException.notSignedIn();`; else `res = await run(fresh);`
       5. if `res.statusCode` not in 200..299 →
          `throw SyncException.http(res.statusCode);` (classification done in the ctor:
          400 ⇒ non-retryable, 5xx/401 ⇒ retryable).
       6. return res.
     Header builder: `{'Authorization': 'Bearer $token', 'Content-Type': 'application/json'}`.
   - `Future<void> syncTrips(List<TripRow> trips)`: POST `Uri.parse('$baseUrl$kSyncTripsPath')`
     body `jsonEncode({'trips': trips.map(TripSerializer.toJson).toList()})`.
   - `Future<void> deleteTrip(String tripId)`: DELETE
     `Uri.parse('$baseUrl$kDeleteTripPathPrefix$tripId')`.
   - `Future<List<TripsCompanion>> restoreTrips()`: GET
     `Uri.parse('$baseUrl$kRestoreTripsPath')`; on success decode the body and unwrap the
     FULL double-wrapped envelope (MEDIUM-1): read
     `decoded['body']['data']['trips']` as `List` (NOT `decoded['data']['trips']`). If the
     `body`/`data`/`trips` nesting is missing/wrong-typed, throw a `SyncException` rather
     than silently returning `[]`. Map each `(e as Map<String,dynamic>)` through
     `TripSerializer.fromJson`. (Matches restore-trips.ts `{statusCode, body:{data:{trips}}}`
     exactly — verified.)
   - `final Provider<ApiClient> apiClientProvider = Provider<ApiClient>((ref) =>
       ApiClient(client: http.Client(), baseUrl: kApiBaseUrl,
       getToken: (({bool forceRefresh = false}) =>
       ref.read(firebaseAuthProvider).currentUser?.getIdToken(forceRefresh))),
       name: 'apiClientProvider');`
     PRODUCTION TOKEN WIRING (resolves plan-check M4): the DEFAULT provider above MUST
     wire the LIVE FirebaseAuth callbacks as its token seam — the real
     `ref.read(firebaseAuthProvider).currentUser?.getIdToken(forceRefresh)` — NOT a stub,
     placeholder, or "static for now" value. This is the shipped production behavior.
     Doc-comment that tests override `apiClientProvider` (or construct `ApiClient`
     directly with a `MockClient` + fake token-getter + a test/emulator `baseUrl`); only
     tests inject fakes. Keep `http.Client()` un-disposed here is fine (app-lifetime,
     keepAlive provider) — but add `ref.onDispose(client.close)` for cleanliness in
     test-container disposal.
   - Use `dart:convert` `jsonEncode`/`jsonDecode`. NO `cloud_firestore`. NO direct
     `http.post` top-level calls — always through the injected `client`.
2. **test/unit/sync/api_client_test.dart** — implement `<behavior>` with
   `MockClient((request) async { ... })` returning crafted `http.Response`s, asserting
   method/URL/headers/body and request counts. Use a mutable counter for the
   401→refresh→retry case and a fake token-getter that returns 'tok1' then 'tok2' on
   forceRefresh. Cover the HIGH-2 retryable/non-retryable/transport cases and the
   MEDIUM-1 full-envelope case explicitly. No `ProviderContainer`/Firebase needed —
   construct `ApiClient` directly with a test `baseUrl`.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter test test/unit/sync/api_client_test.dart</automated>
  </verify>
  <done>ApiClient sends correctly-shaped authenticated requests; 401 path refreshes-and-retries once then throws on persistent 401 (retryable); non-2xx throws typed SyncException with statusCode AND a correct `retryable` classification (HIGH-2: 400→retryable=false, 503/timeout/401→retryable=true); restoreTrips unwraps the FULL envelope `decoded['body']['data']['trips']` into TripsCompanions (MEDIUM-1) and throws on a malformed envelope; base URL is injectable (default kApiBaseUrl, tests override the host); not-signed-in throws notSignedIn (retryable=false) without a request; apiClientProvider compiles; tests green. PRODUCTION wiring (M4): the apiClientProvider DEFAULT passes the LIVE FirebaseAuth callbacks as its token seam — `(({bool forceRefresh = false}) => ref.read(firebaseAuthProvider).currentUser?.getIdToken(forceRefresh))` — NOT a stub/placeholder. Tests construct ApiClient directly (or override the provider) with a fake token-getter; the shipped provider uses the real getIdToken/currentUser.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Flutter client → HTTPS Cloud Functions | Outbound sync payload + Bearer token cross the network to the deployed Phase 10 API. |
| Server JSON → Drift (restore) | Untrusted server response body parsed and inserted into the local DB. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Information disclosure | api_client.dart / SyncException | mitigate | `SyncException.toString()` and all error surfaces NEVER include the Bearer token, uid, or email — only `statusCode`, `retryable`, and a short message. Tests assert no token leakage in thrown messages. |
| T-11-02 | Spoofing | api_client.dart token attach | mitigate | Always attach a FRESH `getIdToken()`; on 401 force-refresh once (`getIdToken(true)`). `currentUser == null` ⇒ throw `notSignedIn`, never send an unauthenticated request (D-03). The production provider wires the REAL getIdToken/currentUser (M4). |
| T-11-03 | Tampering | restore parser (server JSON → Drift) | mitigate | `fromJson` constructs a typed `TripsCompanion` with explicit casts (`as num/.toDouble()`, `as int`, `as bool`, `DateTime.parse`); a malformed field throws during parse rather than corrupting a row. The full-envelope unwrap (MEDIUM-1) throws on a wrong-shaped envelope instead of silently returning []. `userId` from the server is ignored (not written) — local auth owns it. |
| T-11-04 | Denial of service | syncTrips batch size | accept | Batch chunking (≤1000) and retry/backoff live in Plan 02 (engine); the transport sends what it's given. Server enforces the 1000 cap (M1) and rejects oversized batches with 400 → surfaces as a NON-retryable `SyncException` (HIGH-2: retryable=false), so the engine fails it fast instead of hammering the server through 3 retries. |
| T-11-05 | Repudiation | n/a (one-way client-authoritative push) | accept | No server→client authority; client is sole writer. No audit requirement in v0.1 scope. |
</threat_model>

<verification>
Run from project root after all three tasks:
1. `flutter pub get` — resolves http ^1.6.0 + connectivity_plus ^7.1.1 clean.
2. `dart run build_runner build --delete-conflicting-outputs` — regenerates drift code without errors.
3. `flutter analyze` — zero issues (very_good_analysis lint set; no hardcoded strings, no `dynamic` misuse, no `cloud_firestore` import).
4. `flutter test test/unit/sync/ test/unit/database/sync_queue_dao_test.dart` — all green.
5. Manual contract spot-check: confirm the serializer key set equals the zod
   `tripSchema` keys minus `userId`, and timestamps end with `Z`.
6. Manual production-wiring check (M4): confirm the shipped `apiClientProvider` default
   wires the real `firebaseAuthProvider` `currentUser?.getIdToken(forceRefresh)` token
   seam (not a stub); only tests inject fakes.
7. Manual classification check (HIGH-2): a 400 → SyncException.retryable == false; a
   503/timeout/401 → retryable == true.
8. Manual envelope check (MEDIUM-1): restoreTrips reads `body.data.trips`, not `data.trips`.
</verification>

<success_criteria>
- pubspec.lock pins http ^1.6.0 + connectivity_plus ^7.1.1; `flutter pub get` succeeds.
- `lib/sync/sync_status.dart`, `lib/sync/trip_serializer.dart`, `lib/sync/api_client.dart` exist and compile.
- All public names listed in `<objective>` exist with the exact spellings (siblings bind to them).
- `SyncQueueDao.markFailed` / `resetFailed` exist and pass in-memory tests.
- Serializer output matches backend zod `tripSchema` exactly (key set, ISO `Z` timestamps, 0-not-null numerics, omitted `userId`); restore parser round-trips.
- ApiClient attaches fresh Bearer, 401→refresh→retry-once, throws typed `SyncException` on non-2xx with a correct `retryable` flag (HIGH-2: 400 non-retryable, 5xx/timeout/401 retryable), `notSignedIn` (non-retryable) when unauthenticated.
- restoreTrips unwraps the FULL envelope `body.data.trips` (MEDIUM-1); base URL is injectable (default `kApiBaseUrl`, the verified `us-central1-travey-298a7.cloudfunctions.net/api` alias).
- PRODUCTION token wiring (M4): the shipped `apiClientProvider` default wires the REAL FirebaseAuth `getIdToken`/`currentUser` callbacks (not a stub); only tests inject fakes.
- `flutter analyze` clean; `flutter test test/unit/` green; `build_runner` regenerates clean.
</success_criteria>

<output>
After completion, create `.planning/phases/11-sync-engine/11-01-SUMMARY.md`.
Record explicitly: the exact public names exported (SyncStatus variants,
syncStatusProvider, ApiClient method signatures, SyncException shape — INCLUDING the
`retryable` bool + its classification rule (HIGH-2), the injectable `baseUrl` param,
apiClientProvider, TripSerializer.toJson/fromJson, SyncQueueDao.markFailed/
resetFailed) so Plans 02 and 03 bind to them without re-deriving. Note that
restoreTrips unwraps the FULL envelope `body.data.trips` (MEDIUM-1) and the pinned
versions (http ^1.6.0, connectivity_plus ^7.1.1) + base URL. Confirm the
production apiClientProvider wires the real getIdToken/currentUser token seam (M4).
Note any deviation from the contract and the constants added.
</output>
</content>
</invoke>



<invoke name="Read">
<parameter name="file_path">/Users/coolman/Documents/Projects/bizzle/.planning/phases/11-sync-engine/11-02-PLAN.md