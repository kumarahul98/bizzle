---
phase: 14-background-gps-platform-branch
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/config/constants.dart
  - lib/features/tracking/services/location_settings_builder.dart
  - test/unit/features/tracking/location_settings_branch_test.dart
  - test/unit/features/tracking/reduced_accuracy_gate_test.dart
  - test/unit/features/tracking/ios_engine_stop_race_test.dart
autonomous: true
requirements: [IOS-06, IOS-07]
user_setup: []

must_haves:
  truths:
    - "The LocationSettings the GPS stream uses is selected by defaultTargetPlatform: AppleSettings on iOS, AndroidSettings on Android (SC#4)."
    - "On iOS the AppleSettings carries allowBackgroundLocationUpdates:true, pauseLocationUpdatesAutomatically:false, activityType:automotiveNavigation, accuracy:high (SC#4, IOS-06/07)."
    - "The purposeKey and iOS distanceFilter live in constants.dart — no hardcoded literals downstream."
    - "Wave 0 test files exist and define the branch / gate / stop-race contracts the later waves implement against."
  artifacts:
    - path: "lib/features/tracking/services/location_settings_builder.dart"
      provides: "buildLocationSettings() — the single defaultTargetPlatform LocationSettings branch (SC#4)"
      exports: ["buildLocationSettings"]
    - path: "lib/config/constants.dart"
      provides: "kPreciseCommutePurposeKey + kIosTrackingDistanceFilterMeters"
      contains: "kPreciseCommutePurposeKey"
    - path: "test/unit/features/tracking/location_settings_branch_test.dart"
      provides: "SC#4 branch assertion (AppleSettings 4 params on iOS, AndroidSettings on Android)"
      min_lines: 30
    - path: "test/unit/features/tracking/reduced_accuracy_gate_test.dart"
      provides: "RED scaffold for the IOS-08 gate contract (Wave 1 turns it green)"
    - path: "test/unit/features/tracking/ios_engine_stop_race_test.dart"
      provides: "RED scaffold for the iOS engine stop-race contract (Wave 1 turns it green)"
  key_links:
    - from: "lib/features/tracking/services/location_settings_builder.dart"
      to: "geolocator AppleSettings/AndroidSettings"
      via: "defaultTargetPlatform == TargetPlatform.iOS branch"
      pattern: "defaultTargetPlatform"
---

<objective>
Establish the Wave 0 validation contracts and the single source-of-truth for the platform LocationSettings branch (ROADMAP SC#4) plus the iOS-only constants that downstream waves consume. No hardcoded values, branch is unit-testable via `debugDefaultTargetPlatformOverride`.

Purpose: SC#4 demands a `defaultTargetPlatform` branch selecting the exact locked `AppleSettings(...)` on iOS. Extracting it into one helper keeps the branch in exactly one testable place (RESEARCH §3 right-sizing) and lets both the Android isolate and the iOS engine call the same builder. The other two Wave 0 tests pin the IOS-08 gate and the iOS stop-race contracts that Wave 1 implements against.
Output: `buildLocationSettings()` helper, two new constants, and three Wave 0 test files (one GREEN now, two RED scaffolds that Wave 1 satisfies).
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/14-background-gps-platform-branch/14-CONTEXT.md
@.planning/phases/14-background-gps-platform-branch/14-RESEARCH.md
@.planning/phases/14-background-gps-platform-branch/14-VALIDATION.md

<interfaces>
<!-- Extracted from the codebase — implement against these, do not re-explore. -->

geolocator (14.0.2) — AppleSettings extends LocationSettings:
```dart
AppleSettings({
  bool pauseLocationUpdatesAutomatically = false,
  ActivityType activityType = ActivityType.other,   // ActivityType.automotiveNavigation is valid
  LocationAccuracy? accuracy,
  int? distanceFilter,                                // meters
  bool showBackgroundLocationIndicator = false,
  bool allowBackgroundLocationUpdates = true,
});
AndroidSettings({ LocationAccuracy accuracy, Duration intervalDuration, ... });
enum LocationAccuracy { ..., high, ... }
```

Existing Android settings (tracking_service.dart lines 92-95) — the branch must return THIS unchanged on Android:
```dart
AndroidSettings(
  accuracy: LocationAccuracy.high,
  intervalDuration: kTrackingSampleInterval, // const Duration(seconds: 3)
)
```

Existing constants pattern (lib/config/constants.dart): top-level `const`, dartdoc with a phase/decision reference, grouped under a `// Phase N` banner comment. `kTrackingSampleInterval = Duration(seconds: 3)` already exists.

debugDefaultTargetPlatformOverride (flutter/foundation) is the unit-test seam for `defaultTargetPlatform`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 14 constants and the buildLocationSettings() platform branch</name>
  <read_first>
    - lib/config/constants.dart (append-only pattern; reuse the `// Phase N` banner + dartdoc-with-decision-ref convention; `kTrackingSampleInterval` already defined)
    - lib/features/tracking/services/tracking_service.dart (lines 87-95 — the existing AndroidSettings the branch must reproduce byte-for-byte on Android)
    - .planning/phases/14-background-gps-platform-branch/14-RESEARCH.md §1, §2, §3
  </read_first>
  <behavior>
    - On iOS: `buildLocationSettings()` returns an `AppleSettings` with `accuracy: LocationAccuracy.high`, `allowBackgroundLocationUpdates: true`, `pauseLocationUpdatesAutomatically: false`, `activityType: ActivityType.automotiveNavigation`, `showBackgroundLocationIndicator: true`, `distanceFilter: kIosTrackingDistanceFilterMeters`.
    - On Android: returns an `AndroidSettings` equal to today's (`accuracy: high`, `intervalDuration: kTrackingSampleInterval`).
    - Selection uses `defaultTargetPlatform == TargetPlatform.iOS` (NOT `dart:io Platform.isIOS`) so the branch is overridable in tests.
  </behavior>
  <action>
    Append a `// Phase 14: Background GPS platform branch` banner to constants.dart with two consts: `kPreciseCommutePurposeKey = 'PreciseCommute'` (dartdoc: must match the Info.plist NSLocationTemporaryUsageDescriptionDictionary key exactly, per D-06) and `kIosTrackingDistanceFilterMeters = 0` (dartdoc: iOS has no time-throttle; distanceFilter 0 lets pauseLocationUpdatesAutomatically:false + high accuracy drive cadence so stop-and-go traffic still emits samples — IOS-07, RESEARCH §2). Create `lib/features/tracking/services/location_settings_builder.dart` exporting a top-level `LocationSettings buildLocationSettings()` that branches on `defaultTargetPlatform`. iOS branch returns the locked `AppleSettings` named above (per D-02 / SC#4); Android branch returns the existing `AndroidSettings`. Add a file-level comment stating this is the single SC#4 branch site and the PII guard (T-02-07): this helper must never log a Position. Do NOT yet rewire tracking_service.dart to call it (Wave 1/2 owns that) — only create the helper and constants here.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter test test/unit/features/tracking/location_settings_branch_test.dart && flutter analyze lib/features/tracking/services/location_settings_builder.dart lib/config/constants.dart</automated>
  </verify>
  <acceptance_criteria>
    - `flutter test test/unit/features/tracking/location_settings_branch_test.dart` exits 0.
    - `buildLocationSettings()` under `debugDefaultTargetPlatformOverride = TargetPlatform.iOS` returns an `AppleSettings` whose `pauseLocationUpdatesAutomatically == false`, `allowBackgroundLocationUpdates == true`, `activityType == ActivityType.automotiveNavigation`, `accuracy == LocationAccuracy.high`.
    - Under `TargetPlatform.android` it returns an `AndroidSettings` with `intervalDuration == kTrackingSampleInterval`.
    - `grep -c "PreciseCommute" lib/config/constants.dart` >= 1 and `kIosTrackingDistanceFilterMeters` is defined as a const.
    - `flutter analyze` reports no new issues on the two files.
  </acceptance_criteria>
  <done>buildLocationSettings() exists, returns the locked AppleSettings on iOS and the existing AndroidSettings on Android, both constants defined with dartdoc, branch test green.</done>
</task>

<task type="auto">
  <name>Task 2: Author the SC#4 branch test (GREEN) and the IOS-08 gate + stop-race RED scaffolds</name>
  <read_first>
    - lib/features/tracking/services/location_settings_builder.dart (from Task 1 — the unit under test)
    - test/unit/features/tracking/trip_accumulator_test.dart (existing test style: group/test, debug overrides reset in tearDown)
    - test/unit/features/tracking/tracking_state_map_test.dart (existing map-shape test analog)
    - .planning/phases/14-background-gps-platform-branch/14-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <action>
    Write `location_settings_branch_test.dart` asserting the full Task 1 behavior under both `debugDefaultTargetPlatformOverride = TargetPlatform.iOS` and `= TargetPlatform.android` (set in setUp, reset to null in tearDown). Write `reduced_accuracy_gate_test.dart` and `ios_engine_stop_race_test.dart` as RED scaffolds: each declares the contract Wave 1 must satisfy using a `markTestSkipped`-free failing expectation OR an explicit `// WAVE 1` TODO-free skeleton that references the to-be-created symbols (`MainIsolateTrackingEngine`, the injectable accuracy wrapper) via commented expected-behavior bullets so the Wave 1 executor fills them in. For the gate scaffold, document the three expected outcomes (reduced→request→reduced ⇒ BLOCKED; reduced→request→precise ⇒ proceed; already precise ⇒ proceed). For the stop-race scaffold, document: drive a controllable `StreamController<Position>`; after `stop()` sets the stopping flag, a late sample pushed on the controller MUST NOT reach the accumulator. Mark the two scaffold files with a top-of-file `@Skip('Wave 1 implements: ...')` annotation so the suite stays green until Wave 1, and record both as Wave 0 ⬜ in 14-VALIDATION.md's per-task table.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter test test/unit/features/tracking/location_settings_branch_test.dart test/unit/features/tracking/reduced_accuracy_gate_test.dart test/unit/features/tracking/ios_engine_stop_race_test.dart</automated>
  </verify>
  <acceptance_criteria>
    - The three test files exist under test/unit/features/tracking/.
    - `flutter test` on the three files exits 0 (branch test passes; the two scaffolds are `@Skip`-annotated, reported as skipped not failed).
    - `location_settings_branch_test.dart` contains assertions on all four locked AppleSettings params AND on the AndroidSettings path.
    - The two scaffold files each contain a `@Skip('Wave 1 ...')` line and explicit expected-behavior documentation naming `MainIsolateTrackingEngine` (stop-race) and the reduced→request→block flow (gate).
    - 14-VALIDATION.md per-task table updated with the three Wave 0 entries.
  </acceptance_criteria>
  <done>SC#4 branch test green; gate + stop-race contracts captured as skipped scaffolds that Wave 1 un-skips and fills.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GPS sample → app code | `Position` objects carry raw lat/lng (PII). They enter via the geolocator stream the settings configure. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-07 | Information disclosure | `buildLocationSettings()` and the branch test | mitigate | The helper takes no Position and produces only a settings object; file-level comment forbids logging Position; the test asserts on settings fields only, never logs coordinates. |
| T-14-01 | Information disclosure | constants.dart purpose string | accept | `kPreciseCommutePurposeKey = 'PreciseCommute'` is an opaque key, not user data; the human-readable purpose string lives in Info.plist (Plan 02), not in code logs. |
</threat_model>

<verification>
- `flutter test test/unit/features/tracking/location_settings_branch_test.dart` green.
- The two scaffold files are skipped (not failing) so the full suite stays green.
- `flutter analyze` reports no new issues over baseline.
</verification>

<success_criteria>
- ROADMAP SC#4 branch logic exists in one helper and is proven by an automated test under both platform overrides.
- IOS-06/07 settings values (`allowBackgroundLocationUpdates`, `pauseLocationUpdatesAutomatically:false`, `automotiveNavigation`) are asserted.
- purposeKey + distanceFilter are constants (no hardcoded downstream literals).
- Wave 0 gate + stop-race contracts captured for Wave 1.
</success_criteria>

<output>
After completion, create `.planning/phases/14-background-gps-platform-branch/14-01-SUMMARY.md`
</output>
