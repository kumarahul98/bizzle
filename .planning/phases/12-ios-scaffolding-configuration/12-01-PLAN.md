---
phase: 12-ios-scaffolding-configuration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - ios/
  - ios/Podfile
autonomous: false
requirements: [IOS-01]
user_setup:
  - service: xcode
    why: "flutter build ios requires the Xcode license to be accepted and a booted iOS Simulator"
    dashboard_config:
      - task: "Accept the Xcode license (one-time, blocks all iOS builds)"
        location: "Terminal: sudo xcodebuild -license accept"
      - task: "Boot an iOS Simulator before the build-launch checkpoint"
        location: "Xcode > Open Developer Tool > Simulator, or `open -a Simulator`"

must_haves:
  truths:
    - "The ios/ project tree exists and was generated by flutter create without overwriting lib/ or android/"
    - "ios/Podfile targets iOS 15.0 with the post_install deployment-target and permission_handler macros"
    - "pod install completes successfully inside ios/"
    - "flutter build ios --simulator completes with exit 0"
    - "The app launches and renders on a booted iOS Simulator"
  artifacts:
    - path: "ios/Runner.xcodeproj/project.pbxproj"
      provides: "Generated iOS Xcode project"
      contains: "Runner"
    - path: "ios/Podfile"
      provides: "iOS 15.0 deployment target and post_install hook"
      contains: "platform :ios, '15.0'"
    - path: "ios/Runner/Info.plist"
      provides: "Default generated Info.plist (configured in plan 02)"
      contains: "CFBundleIdentifier"
  key_links:
    - from: "ios/Podfile"
      to: "all CocoaPods plugin targets"
      via: "post_install IPHONEOS_DEPLOYMENT_TARGET = 15.0"
      pattern: "IPHONEOS_DEPLOYMENT_TARGET.*15.0"
---

<objective>
Generate the iOS platform project that does not yet exist and bring it to a compile-clean, Simulator-launchable baseline. This is the foundation every subsequent Phase 12 task and every later iOS phase (13-16) builds on.

Purpose: Without an ios/ tree and a working Podfile, no iOS configuration, entitlement, or build verification can happen. This plan delivers IOS-01 (Simulator build + launch).
Output: ios/ project tree, configured Podfile (iOS 15.0 + post_install), resolved pods, a green simulator build, and a confirmed Simulator launch.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/12-ios-scaffolding-configuration/12-RESEARCH.md
@.planning/phases/12-ios-scaffolding-configuration/12-VALIDATION.md
@CLAUDE.md

<notes>
- BLOCKER (human, see STATE.md): Xcode license must be accepted before this plan runs: `sudo xcodebuild -license accept`. The build-launch checkpoint depends on it.
- ios/ folder is confirmed ABSENT. flutter create --platforms=ios . will generate it.
- Local toolchain verified: Xcode 26.5, CocoaPods 1.16.2, Flutter 3.41.6, flutterfire 1.3.2.
- Open Question (RESEARCH #1): Xcode 26 + Flutter 3.41.6 may generate ios/Package.swift (SPM) alongside the Podfile. Task 1 explicitly checks for it and runs a pre-config build to surface SPM/CocoaPods conflicts early. Do NOT hand-edit Package.swift or Podfile Firebase entries — let flutterfire manage Firebase integration if a conflict appears.
- This codebase uses flutter_map (OpenStreetMap), NOT google_maps_flutter. Do NOT add GMSServices.provideAPIKey() or any Google Maps iOS SDK setup.
</notes>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold ios/ project and check for SPM</name>
  <read_first>
    - .planning/phases/12-ios-scaffolding-configuration/12-RESEARCH.md (Recommended Execution Order; Pitfall 6; Open Question 1)
    - Confirm ios/ is absent: `ls ios/ 2>/dev/null || echo ABSENT`
  </read_first>
  <files>ios/ (generated tree)</files>
  <action>
    Run `flutter create --platforms=ios .` from the project root. This generates the ios/ project tree only; it must NOT overwrite lib/, android/, test/, or pubspec.yaml — verify with `git status` that only ios/ and possibly .metadata changed, and that no files under lib/ or android/ were modified.
    Immediately after generation, check whether an SPM manifest was generated: `ls ios/Package.swift 2>/dev/null && echo SPM_PRESENT || echo SPM_ABSENT`. Record the result in the plan SUMMARY.
    If SPM_PRESENT: run `flutter build ios --simulator` now (before any config) to surface SPM/CocoaPods conflicts early. If it fails with duplicate-symbol or missing-Firebase-pod errors, stop and let flutterfire manage Firebase integration — do NOT hand-edit ios/Package.swift or add Firebase pods to the Podfile manually; document the conflict in SUMMARY for resolution.
    Do NOT modify AppDelegate.swift — the default FlutterAppDelegate scaffold is correct (google_sign_in_ios v7 handles openURL via GeneratedPluginRegistrant; no GMSServices call because this app uses flutter_map).
  </action>
  <verify>
    <automated>test -f ios/Runner.xcodeproj/project.pbxproj && test -f ios/Runner/Info.plist && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - ios/Runner.xcodeproj/project.pbxproj exists
    - ios/Runner/Info.plist exists
    - `git status` shows no modifications under lib/ or android/
    - SPM presence recorded (SPM_PRESENT or SPM_ABSENT) in SUMMARY
  </acceptance_criteria>
  <done>ios/ tree generated; lib/ and android/ untouched; SPM presence documented</done>
</task>

<task type="auto">
  <name>Task 2: Configure Podfile for iOS 15.0 with post_install hook</name>
  <read_first>
    - .planning/phases/12-ios-scaffolding-configuration/12-RESEARCH.md (Code Examples > Podfile; Pitfall 4)
    - ios/Podfile (read the flutter create default before editing)
  </read_first>
  <files>ios/Podfile</files>
  <action>
    Edit ios/Podfile so the platform line reads exactly `platform :ios, '15.0'` (flutter create defaults to 12.0 or comments it out). Keep Flutter's generated podhelper require line, flutter_ios_podfile_setup, and the `target 'Runner'` block with use_frameworks! and use_modular_headers!.
    Replace/add the post_install block so that for every pod target's build configuration it sets `config.build_settings['IPHONEOS_DEPLOYMENT_TARGET'] = '15.0'` and sets `config.build_settings['GCC_PREPROCESSOR_DEFINITIONS']` to include `$(inherited)`, `PERMISSION_LOCATION=1`, and `PERMISSION_NOTIFICATIONS=1`. Keep the call to `flutter_additional_ios_build_settings(target)` inside the post_install loop. Use the exact Podfile content shown in 12-RESEARCH.md "Code Examples > Podfile (complete, for ios/Podfile)".
    Then run `cd ios && pod install` (run `flutter pub get` first if Generated.xcconfig is missing). Do NOT add any Firebase pod entries manually — flutter_install_all_ios_pods resolves plugin pods automatically.
  </action>
  <verify>
    <automated>grep -q "platform :ios, '15.0'" ios/Podfile && grep -q "PERMISSION_LOCATION=1" ios/Podfile && grep -q "IPHONEOS_DEPLOYMENT_TARGET.*15.0" ios/Podfile && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `grep "platform :ios" ios/Podfile` shows '15.0'
    - `grep -c PERMISSION_LOCATION=1 ios/Podfile` returns count > 0
    - `grep -c PERMISSION_NOTIFICATIONS=1 ios/Podfile` returns count > 0
    - post_install sets IPHONEOS_DEPLOYMENT_TARGET to 15.0
    - `cd ios && pod install` exits 0
  </acceptance_criteria>
  <done>Podfile targets iOS 15.0 with full post_install hook; pod install succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Build and launch on iOS Simulator</name>
  <read_first>
    - .planning/phases/12-ios-scaffolding-configuration/12-VALIDATION.md (Per-Task Verification Map: IOS-01 rows)
  </read_first>
  <what-built>
    The ios/ project is scaffolded and the Podfile is configured for iOS 15.0 with the required post_install hook and pods resolved. The agent runs `flutter build ios --simulator` to confirm a clean build, then `flutter run` against a booted Simulator.
  </what-built>
  <how-to-verify>
    1. Ensure the Xcode license is accepted (`sudo xcodebuild -license accept` if not already done) — without it the build fails.
    2. Boot an iOS Simulator: `open -a Simulator` (or Xcode > Open Developer Tool > Simulator).
    3. The agent runs `flutter build ios --simulator` — confirm it ends with "Built ... .app" and exit 0.
    4. The agent runs `flutter run` (Simulator selected) — confirm the Traevy app appears and renders its first screen on the Simulator.
    5. Report any SPM/CocoaPods linker errors if the build fails (see Pitfall 6).
  </how-to-verify>
  <resume-signal>Type "approved" if the app builds (exit 0) and launches on the Simulator, or describe the build/launch error.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build toolchain → ios/ project | flutter create + pod install generate native project files that ship in the app bundle |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01 | Tampering | ios/Podfile post_install GCC macros | mitigate | Use the exact research-supplied Podfile; permission_handler macros enable ONLY location + notifications (PERMISSION_LOCATION=1, PERMISSION_NOTIFICATIONS=1), compiling out all other permission code so the App Store binary advertises no unused entitlements |
| T-12-02 | Information disclosure | flutter create scaffold defaults | accept | Generated AppDelegate/Info.plist contain no secrets; default scaffold is Apple/Flutter standard. No PII at this layer |
| T-12-03 | Elevation of privilege | iOS deployment target floor | mitigate | Pin platform + all pods to iOS 15.0 (above firebase_core's 13.0 floor) so no pod silently lowers the security baseline |
</threat_model>

<verification>
- `flutter build ios --simulator` exits 0
- `cd ios && pod install` exits 0
- `grep "platform :ios" ios/Podfile` shows '15.0'
- `git status` confirms lib/ and android/ untouched by flutter create
- Human: app launches and renders on the Simulator
</verification>

<success_criteria>
IOS-01 satisfied: a generated ios/ project builds for the Simulator (exit 0) and the app launches on a booted Simulator. Podfile targets iOS 15.0 with the post_install hook and pods resolve cleanly.
</success_criteria>

<output>
After completion, create `.planning/phases/12-ios-scaffolding-configuration/12-01-SUMMARY.md`. Record: SPM presence (Package.swift present or absent), pod install result, build result, and any SPM/CocoaPods conflicts encountered.
</output>
