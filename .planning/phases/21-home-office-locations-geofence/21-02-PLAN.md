---
phase: 21-home-office-locations-geofence
plan: 02
type: execute
wave: 1
depends_on: ["21-01"]
files_modified:
  - lib/config/constants.dart
  - lib/database/daos/user_preferences_dao.dart
  - lib/features/settings/screens/location_picker_screen.dart
  - lib/features/settings/widgets/saved_location_tile.dart
  - lib/features/settings/screens/settings_screen.dart
  - test/widget/features/settings/location_picker_screen_test.dart
  - test/widget/features/settings/saved_location_tile_test.dart
autonomous: true
requirements: [LOC-01]
user_setup: []

must_haves:
  truths:
    - "From settings the user can open a map picker for Home and for Office, pan the map under a fixed centre crosshair, and tap a confirm button that persists the map centre's lat/lng to the single user_preferences row (SC#1, LOC-01, D-12)."
    - "The picker reads the map centre ONLY when the confirm button is tapped — never mid-pan (D-12 debounce)."
    - "The picker's initial camera is the existing saved coord if set, else the device's current location if permission is granted, else the most recent trip's end point, else a sane non-(0,0) default (D-13)."
    - "The two settings entries show the current saved location (or 'Not set') and persist across restarts (SC#1)."
    - "With no location ever set, nothing else changes — the entries simply read 'Not set' (purely additive)."
  artifacts:
    - path: "lib/features/settings/screens/location_picker_screen.dart"
      provides: "LocationPickerScreen — flutter_map with a fixed centre crosshair overlay, a Locate-me control, and a 'Set <label> here' confirm button; parameterised by which location (Home/Office) it edits"
      contains: "FlutterMap"
    - path: "lib/features/settings/widgets/saved_location_tile.dart"
      provides: "SavedLocationTile — a settings row showing label + current coord/'Not set', tapping opens the picker"
      contains: "SavedLocationTile"
    - path: "lib/database/daos/user_preferences_dao.dart"
      provides: "setHomeLocation(lat,lng) / setOfficeLocation(lat,lng) setters writing the v6 coord columns (add only if 21-01 did not already add them)"
      contains: "setHomeLocation"
  key_links:
    - from: "lib/features/settings/screens/location_picker_screen.dart"
      to: "lib/database/daos/user_preferences_dao.dart"
      via: "setHomeLocation/setOfficeLocation on confirm"
      pattern: "setHomeLocation"
    - from: "lib/features/settings/screens/settings_screen.dart"
      to: "lib/features/settings/widgets/saved_location_tile.dart"
      via: "two SavedLocationTile entries (Home, Office)"
      pattern: "SavedLocationTile"
---

<objective>
Deliver the LOC-01 user surface: a flutter_map (OpenStreetMap) location picker reachable from settings that lets the user set/change Home and Office, persisting lat/lng to the v6 `user_preferences` columns added in Plan 01. No labeling logic here (that is Plan 01's resolver + Plan 03's backfill) — this plan is purely the picker UI + persistence + settings entries.

Purpose: SC#1 — the user must be able to set and later change Home/Office via a map picker and have them persist. The picker uses the existing flutter_map/OSM stack (mirror `trip_detail_screen.dart`) with a fixed centre crosshair (pan-under-pin) so there is no tap-target ambiguity, reads the centre only on confirm (debounce), and never initialises at Null Island.
Output: `LocationPickerScreen`, `SavedLocationTile`, two settings entries, prefs setters (if needed), and widget tests.
</objective>

<tasks>

## Task 1 — Prefs setters + SavedLocationTile (TDD)
- In `user_preferences_dao.dart`, ensure `setHomeLocation(double lat, double lng)` and `setOfficeLocation(double lat, double lng)` exist (write the v6 `home_lat/home_lng/office_lat/office_lng` columns on the single row id=1, mirroring the existing `setHasSeenOnboarding` single-row update). Add them only if Plan 01 did not.
- Add `kCopyLocationNotSet`, `kSettingsHomeLocationLabel`, `kSettingsOfficeLocationLabel` to constants.dart.
- Build `SavedLocationTile` (ConsumerWidget, <100 lines): shows the label + either the formatted saved coord or `kCopyLocationNotSet`; `onTap` opens the picker. Reads the coord from `userPreferenceProvider`.
- Widget test: tile shows 'Not set' when coord null; shows a coord when set; tap invokes the open callback.

## Task 2 — LocationPickerScreen (TDD)
- Build `LocationPickerScreen({required this.isHome})` (ConsumerStatefulWidget, keep build <100 lines — extract the crosshair overlay / confirm bar if needed):
  - `FlutterMap` with a `MapController`; `TileLayer` reusing `kMapTileUrlDark/Light`, `kMapTileSubdomains`, `kMapUserAgentPackageName` (mirror trip_detail_screen).
  - Initial centre (D-13): existing saved coord for this slot ?? current device location via geolocator (only if permission already granted — do NOT prompt aggressively; reuse existing permission service if present) ?? the most recent trip's decoded end point ?? a sane default constant (NOT 0,0). Put the default in constants.dart (`kMapDefaultCenterLat/Lng`).
  - A fixed centre crosshair widget painted on top of the map (an `IgnorePointer` centred Icon) — it does NOT move; the map pans under it.
  - A "Locate me" control (small FAB/icon button) that animates the map to the current device location.
  - A prominent bottom "Set Home here" / "Set Office here" `FilledButton` that, ONLY on tap, reads `mapController.camera.center`, calls the matching prefs setter, and pops with a confirmation snackbar. Never read the centre on map move.
- Widget test: confirm button calls the correct setter with the map centre; pumping a map move then confirm uses the post-move centre (proves debounce/read-on-confirm); locate-me does not crash without a fix.

## Task 3 — Wire into settings
- Add two `SavedLocationTile` entries (Home, Office) to `settings_screen.dart` in a "Locations" / commute-preferences area, each opening `LocationPickerScreen(isHome: ...)` via `Navigator.push` (or showModal consistent with the existing settings navigation pattern).
- Widget test (extend settings test): both entries render; tapping navigates to the picker.

</tasks>

<verification>
- `flutter test test/widget/features/settings/` green (new tests).
- `flutter analyze` on changed files — no new issues.
- Full `flutter test` stays green (no regression; prefs value-type gained no required field — coord columns are nullable).
- `dart run build_runner build --delete-conflicting-outputs` only if a DAO/table change requires regen (Plan 01 already bumped the schema; if setters touch generated companions, regen).
</verification>

<threat_model>
## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-21-02-01 | Information disclosure | Home/Office coords (PII-adjacent) | mitigate | Coords live only in local Drift; NEVER logged or printed; not sent to any backend in this plan. |
| T-21-02-02 | Tampering | map centre read timing | accept | Centre is read only on explicit confirm; no external input crosses a trust boundary. |
</threat_model>

<success_criteria>
- SC#1 (LOC-01): user sets/changes Home & Office via the map picker; values persist across restarts; entries reflect saved/'Not set'. Proven by widget tests + the prefs setters.
- D-12 fixed-centre crosshair, read-on-confirm; D-13 no-Null-Island init.
- Additive: no behavioural change when nothing is set.
</success_criteria>

<output>
After completion, create `.planning/phases/21-home-office-locations-geofence/21-02-SUMMARY.md`
</output>
