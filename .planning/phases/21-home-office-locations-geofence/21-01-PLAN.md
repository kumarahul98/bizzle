---
phase: 21-home-office-locations-geofence
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - lib/config/constants.dart
  - lib/database/tables/user_preferences_table.dart
  - lib/database/tables/trips_table.dart
  - lib/database/daos/user_preferences_dao.dart
  - lib/database/database.dart
  - lib/features/trips/services/geofence_direction_resolver.dart
  - lib/features/tracking/providers/tracking_providers.dart
  - lib/features/tracking/services/tracking_service_controller.dart
  - lib/features/trips/providers/trip_management_providers.dart
  - drift_schemas/drift_schema_v6.json
  - test/generated_migrations/schema.dart
  - test/generated_migrations/schema_v6.dart
  - test/unit/database/migration_v6_test.dart
  - test/unit/features/trips/geofence_direction_resolver_test.dart
  - test/unit/features/tracking/persist_geofence_direction_test.dart
autonomous: true
requirements: [LOC-01, LOC-02]
user_setup: []

must_haves:
  truths:
    - "A v5 database (existing trips, no coord columns) upgrades to v6 without data loss: every trip row survives, `direction_source` defaults to `time`, and `home_lat/home_lng/office_lat/office_lng` on user_preferences read null (SC#5, D-01, D-02)."
    - "GeofenceDirectionResolver is a pure function that returns `to_office`/`to_home`/null with NO Drift, NO Riverpod, NO Geolocator-plugin-channel calls (uses only the static `Geolocator.distanceBetween` math) — fully unit-testable on the Dart VM (D-09)."
    - "END coord within 250m of exactly one of Home/Office labels by it; END outside BOTH returns null → time fallback; overlap (END inside both) tie-breaks via START; only-one-set never guesses the unset anchor (SC#2, D-04..D-08)."
    - "With NO Home/Office set the resolver returns null, so resolvedDirection collapses to the exact pre-Phase-21 `override ?? time` behaviour — purely additive (SC#5, D-09)."
    - "`resolvedDirection` precedence is `manualOverride ?? geofence(start,end,prefs) ?? time(start,cutoffs)` at finalize and in the live header/notification path (SC#3, D-09)."
    - "At finalize the controller decodes the polyline's first/last points, runs the geofence resolver, and writes `direction` + `direction_source` (geofence when matched, manual when the user overrode, else time) (SC#2, SC#3, D-10)."
    - "The manual write paths (Phase 17 quick toggle → editTrip, edit sheet → editTrip) stamp `direction_source = manual` so backfill (Plan 03) can never clobber a user's choice (SC#4, D-03)."
  artifacts:
    - path: "lib/features/trips/services/geofence_direction_resolver.dart"
      provides: "GeofenceDirectionResolver: pure const service; resolve(start, end, homeLat/Lng, officeLat/Lng, radiusMeters) → String? (kDirectionToOffice/kDirectionToHome/null)"
      contains: "class GeofenceDirectionResolver"
    - path: "lib/database/database.dart"
      provides: "schemaVersion 6; onUpgrade from<6 && to>=6 adds 4 coord columns on user_preferences + direction_source on trips"
      contains: "schemaVersion => 6"
    - path: "lib/database/tables/trips_table.dart"
      provides: "trips.directionSource text column, default kDirectionSourceTime"
      contains: "directionSource"
    - path: "lib/database/tables/user_preferences_table.dart"
      provides: "user_preferences home/office lat/lng nullable doubles"
      contains: "homeLat"
    - path: "drift_schemas/drift_schema_v6.json"
      provides: "v6 schema snapshot dumped via drift_dev schema dump"
      min_lines: 1
    - path: "test/unit/database/migration_v6_test.dart"
      provides: "SchemaVerifier v5→v6 migration test + data-survival + default-value assertions"
      min_lines: 60
    - path: "test/unit/features/trips/geofence_direction_resolver_test.dart"
      provides: "Exhaustive resolver tests covering D-04..D-08 (confident, outside-both, overlap tie-break, only-one-set, additive null)"
      min_lines: 80
    - path: "test/unit/features/tracking/persist_geofence_direction_test.dart"
      provides: "Finalize test: geofence match writes direction+source=geofence; no match writes source=time; override writes source=manual"
      min_lines: 60
  key_links:
    - from: "lib/features/tracking/services/tracking_service_controller.dart persistFinalizedTrip"
      to: "GeofenceDirectionResolver.resolve + decodePolyline endpoints"
      via: "decode trip.encodedPolyline first/last → resolve → write direction + directionSource"
      pattern: "GeofenceDirectionResolver|decodePolyline"
    - from: "lib/features/trips/providers/trip_management_providers.dart editTrip"
      to: "trips.directionSource = kDirectionSourceManual"
      via: "TripsCompanion directionSource Value on every editTrip write"
      pattern: "directionSource.*kDirectionSourceManual|kDirectionSourceManual"
    - from: "lib/features/tracking/providers/tracking_providers.dart resolvedDirection"
      to: "GeofenceDirectionResolver slotted between override and time"
      via: "override ?? geofence(start,end,prefs) ?? time"
      pattern: "GeofenceDirectionResolver|geofence"
---

<objective>
Lay the entire labeling engine for Phase 21: the v5→v6 Drift migration (Home/Office coords on user_preferences + `direction_source` on trips), a PURE `GeofenceDirectionResolver` implementing the full D-04..D-08 proximity policy, the precedence extension (`override ?? geofence ?? time`), the finalize wiring that decodes polyline endpoints and stamps `direction_source`, and tagging the existing manual-label write paths with `direction_source = manual`. This is the foundation Plan 02 (picker) and Plan 03 (backfill) both depend on (LOC-01, LOC-02).

Purpose: the geofence policy is correctness-critical and must be a pure, exhaustively-tested function so the same logic runs identically at finalize (D-10) and in the backfill (Plan 03, D-11). `direction_source` is the durable "who set this" record that makes the backfill safe (it re-labels only `!= manual` rows). The migration is strictly additive so every historical commute survives with `direction_source = time` and null coords — purely additive, SC#5.
Output: a v6 schema, the pure resolver + its exhaustive test suite, the extended precedence chain, finalize writing `direction_source`, manual paths tagged `manual`, and an automated migration test. NO settings UI and NO backfill yet — those land in Plans 02 and 03.
</objective>

<execution_context>
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/workflows/execute-plan.md
@/Users/coolman/Documents/Projects/bizzle/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/21-home-office-locations-geofence/21-CONTEXT.md

<interfaces>
<!-- Extracted from the codebase — implement against these, do not re-explore. -->

Current migration (lib/database/database.dart) — extend this EXACT guarded pattern.
schemaVersion is currently 5. Every branch is `if (from < N && to >= N)`:
```dart
@override
int get schemaVersion => 5;                  // → bump to 6
// ... existing from<2 .. from<5 branches unchanged ...
if (from < 6 && to >= 6) {
  // Phase 21 (D-01/D-02): additive-only v5 → v6.
  await m.addColumn(userPreferences, userPreferences.homeLat);
  await m.addColumn(userPreferences, userPreferences.homeLng);
  await m.addColumn(userPreferences, userPreferences.officeLat);
  await m.addColumn(userPreferences, userPreferences.officeLng);
  await m.addColumn(trips, trips.directionSource);
}
```
@DriftDatabase(tables: [Trips, SyncQueue, UserPreferences, TripBreaks], ...) — NO new table; only columns.

trips_table.dart column style — add (default constant from constants.dart):
`TextColumn get directionSource => text().withDefault(const Constant(kDirectionSourceTime))();`
DO NOT touch the existing `direction` column.

user_preferences_table.dart — add four nullable doubles (null = not set, single-row table):
`RealColumn get homeLat => real().nullable()();` (and homeLng, officeLat, officeLng).

UserPreferencesValue (lib/database/daos/user_preferences_dao.dart) is a required-all-fields value type
with FIVE sites that must each gain the four nullable double fields (homeLat/homeLng/officeLat/officeLng):
  1. the const constructor field list,
  2. `.defaults()` ctor (`homeLat = null`, etc.),
  3. `getOrDefault()` row map,
  4. `watch()` row map,
  5. `upsert()` companion (`homeLat: Value<double?>(value.homeLat)`, etc.).
Omitting any site won't compile (required fields). Mirror how reminderTime (a nullable field) is threaded.
NOTE: `_copyPrefs` in settings_screen.dart is touched by Plan 02 (picker), NOT here — leave it alone.

Existing finalize (tracking_service_controller.dart persistFinalizedTrip) currently computes:
```dart
final prefs = await _userPreferencesDao.getOrDefault();
const labeler = DirectionLabelService();
final autoLabel = labeler.label(trip.startTime.toLocal(), prefs.morningCutoffHour, prefs.eveningCutoffHour);
final direction = directionOverride ?? autoLabel;          // ← becomes override ?? geofence ?? time
```
Extend to: decode endpoints from `trip.encodedPolyline` (may be empty for <2-sample/manual), run the
resolver against prefs coords, then build:
  geofenceLabel = resolver.resolve(start, end, prefs.homeLat, prefs.homeLng, prefs.officeLat, prefs.officeLng, kGeofenceRadiusMeters)
  direction = directionOverride ?? geofenceLabel ?? autoLabel
  directionSource = directionOverride != null ? kDirectionSourceManual
                    : geofenceLabel != null ? kDirectionSourceGeofence
                    : kDirectionSourceTime
Write `directionSource: Value(directionSource)` into the TripsCompanion.insert.

resolvedDirection (tracking_providers.dart) currently: `override ?? DirectionLabelService().label(...)`.
It only has `startedAt` — there is NO live end coord during an active trip, so the LIVE header/notification
keeps the time fallback when no override (the geofence END coord only exists at finalize). Slot the geofence
in conceptually but the live path has no end coord, so resolvedDirection stays `override ?? time` AND the
authoritative geofence resolution happens at finalize (D-10). Document this explicitly in the method dartdoc:
"geofence needs the END coord which only exists at finalize; live preview uses time, finalize applies geofence".

decodePolyline (lib/shared/utils/polyline_codec.dart) — pure, returns `List<({double lat, double lng})>`.
Empty string → empty list. Use `.first` for start and `.last` for end; guard the empty list.

Geolocator.distanceBetween(lat1, lng1, lat2, lng2) → double meters. STATIC method, no plugin channel,
safe in pure unit tests (already used in trip_accumulator.dart line 222).

editTrip (trip_management_providers.dart) is the SINGLE manual write path for BOTH the Phase 17 quick
toggle (trip_detail `_handleDirectionChanged`) and the Phase 19 edit sheet. Add
`directionSource: const Value(kDirectionSourceManual)` to its TripsCompanion — every editTrip call sets
the direction manually, so every call stamps manual. (insertManualTrip keeps the DB default `time`; a manual
ENTRY has no GPS so it can't geofence, and the user did not pick a direction at create — leave it as-is per D-11.)

Schema-dump tooling (proven v1→v5):
  1. dart run drift_dev schema dump lib/database/database.dart drift_schemas/drift_schema_v6.json
  2. dart run drift_dev schema generate drift_schemas/ test/generated_migrations/
Regenerates test/generated_migrations/schema.dart with versions [1,2,3,4,5,6] + schema_v6.dart.

migration_v6_test.dart mirrors migration_v5_test.dart EXACTLY: SchemaVerifier(GeneratedHelper()),
`verifier.schemaAt(5)` + `v5.DatabaseAtV5(...)` to seed a v5 trip + prefs row via RawValuesInsertable,
`verifier.migrateAndValidate(db, 6)`, then assert the trip survives, reads direction_source='time',
and prefs coords read null.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: v6 schema (coords + direction_source) + migration test</name>
  <read_first>
    - lib/database/database.dart (schemaVersion, the five guarded onUpgrade branches, @DriftDatabase)
    - lib/database/tables/trips_table.dart (column style, withDefault, the existing direction column)
    - lib/database/tables/user_preferences_table.dart (nullable column style — reminderTime is the mirror for nullable)
    - lib/database/daos/user_preferences_dao.dart (UserPreferencesValue — all 5 sites; reminderTime as nullable mirror)
    - lib/config/constants.dart (Phase 17 direction block — append the Phase 21 block after it)
    - test/unit/database/migration_v5_test.dart (the exact convention to mirror for v6)
    - drift_schemas/ + test/generated_migrations/ (dump/generate outputs)
  </read_first>
  <behavior>
    - v5 trip row (no direction_source column) survives v5→v6 and reads direction_source='time'.
    - v5 prefs row survives and reads home_lat/home_lng/office_lat/office_lng = null.
    - migrateAndValidate(db, 6) validates the DDL diff against the generated v6 snapshot (no schema drift).
    - UserPreferencesValue round-trips the four new nullable doubles through upsert + getOrDefault.
  </behavior>
  <action>
    In lib/config/constants.dart, append a Phase 21 block (after the Phase 17/19 blocks; never replace existing values), all per D-02/D-05/D-12:
      - `kGeofenceRadiusMeters = 250.0` (double; the D-05 confident-match radius, fed to Geolocator.distanceBetween).
      - `kDirectionSourceManual = 'manual'`, `kDirectionSourceGeofence = 'geofence'`, `kDirectionSourceTime = 'time'` (the three `trips.direction_source` literals — D-02).
      - Settings copy constants reserved for Plan 02 are NOT added here (Plan 02 owns its own copy). Add ONLY the radius + the three source literals in this plan.
    Each with a dartdoc citing the decision.

    In lib/database/tables/trips_table.dart add `TextColumn get directionSource => text().withDefault(const Constant(kDirectionSourceTime))();` with a dartdoc per D-02: durable record of who set the direction (manual/geofence/time); default 'time' keeps every existing v5 row safe and correct (they were time-labeled); backfill (Plan 03) re-labels only rows where this != 'manual'. Do NOT modify the existing `direction` column.

    In lib/database/tables/user_preferences_table.dart add four nullable doubles mirroring the nullable column style: `RealColumn get homeLat => real().nullable()();`, `homeLng`, `officeLat`, `officeLng`. Dartdoc per D-01: null = not set; single-row table; PII-adjacent — never log these coords (T-21-03).

    Update lib/database/daos/user_preferences_dao.dart at ALL FIVE sites to thread the four new nullable doubles (mirror reminderTime — a nullable field): the value-class fields + constructor, the `.defaults()` ctor (all four = null), the `getOrDefault()` row map (row.homeLat, etc.), the `watch()` row map, and the `upsert()` companion (`homeLat: Value<double?>(value.homeLat)`, etc.).

    In lib/database/database.dart: bump `schemaVersion => 6`; add a guarded `if (from < 6 && to >= 6) { ... }` branch AFTER the from<5 branch (order matters so a v1..v5 install runs every branch in sequence) that runs five additive `m.addColumn` calls: homeLat, homeLng, officeLat, officeLng on userPreferences, and directionSource on trips. No table is created. Leave beforeOpen PRAGMA unchanged. Dartdoc per D-01/D-02/T-21-01: additive only, no UPDATE/DROP, existing rows safe.

    Run `dart run build_runner build --delete-conflicting-outputs` to regenerate database.g.dart + user_preferences_dao.g.dart. Then dump + regenerate the v6 snapshot:
      `dart run drift_dev schema dump lib/database/database.dart drift_schemas/drift_schema_v6.json`
      `dart run drift_dev schema generate drift_schemas/ test/generated_migrations/`
    Do NOT hand-edit generated files.

    Create test/unit/database/migration_v6_test.dart mirroring migration_v5_test.dart: open at v5 via `verifier.schemaAt(5)` + `v5.DatabaseAtV5(schema.newConnection())`, seed one prefs row (id=1, user_id) and one trip row (id, start/end, duration, total_paused_seconds, distance, direction, time_moving/stuck) via RawValuesInsertable (NO direction_source / coord columns exist at v5), close, then `AppDatabase(schema.newConnection())` + `verifier.migrateAndValidate(migratedDb, 6)`. Assert: (a) the trip survives (findById non-null, durationSeconds preserved), (b) `direction_source` reads 'time' (read via a customSelect on the migrated trip row, OR add a tripsDao read — assert the literal equals kDirectionSourceTime), (c) `getOrDefault()` returns null for all four coords. Add a fresh-install control test (NativeDatabase.memory()) asserting defaults: coords null, and a new trip persisted via the DAO defaults direction_source to 'time'.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && dart run build_runner build --delete-conflicting-outputs && dart run drift_dev schema dump lib/database/database.dart drift_schemas/drift_schema_v6.json && dart run drift_dev schema generate drift_schemas/ test/generated_migrations/ && flutter analyze lib/database/ lib/config/constants.dart && grep -q "schemaVersion => 6" lib/database/database.dart && grep -q "directionSource" lib/database/tables/trips_table.dart && grep -q "homeLat" lib/database/tables/user_preferences_table.dart && grep -q "1, 2, 3, 4, 5, 6" test/generated_migrations/schema.dart && flutter test test/unit/database/migration_v6_test.dart test/unit/database/migration_v5_test.dart test/unit/database/user_preferences_dao_test.dart</automated>
  </verify>
  <done>user_preferences gains four nullable coord doubles, trips gains direction_source (default 'time'), schemaVersion is 6 with a guarded from<6 branch, generated code + v6 snapshot regenerate cleanly, UserPreferencesValue threads the new fields at all five sites, and the v6 migration test proves v5→v6 applies with existing rows surviving and new columns defaulting safely — all green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pure GeofenceDirectionResolver + exhaustive D-04..D-08 tests</name>
  <read_first>
    - lib/features/trips/services/direction_label_service.dart (the pure-service style to mirror — const ctor, no Riverpod, no async)
    - lib/shared/utils/polyline_codec.dart (decodePolyline return shape — record (lat, lng))
    - lib/config/constants.dart (kDirectionToOffice, kDirectionToHome, kGeofenceRadiusMeters added in Task 1)
    - test/unit/features/trips/direction_label_service_test.dart (the test style to mirror)
    - lib/features/tracking/services/trip_accumulator.dart line ~222 (Geolocator.distanceBetween call shape)
  </read_first>
  <behavior>
    Resolver `resolve(start, end, homeLat, homeLng, officeLat, officeLng, radiusMeters)` returns String? — kDirectionToOffice, kDirectionToHome, or null. start/end are nullable records `({double lat, double lng})?`.
    - D-04 confident END→Office: end within radius of Office only → kDirectionToOffice.
    - D-04 confident END→Home: end within radius of Home only → kDirectionToHome.
    - D-06 outside both: end > radius from both → null.
    - D-07 overlap (end inside BOTH, Home & Office <2*radius apart): tie-break via START — start nearer Home → kDirectionToOffice; start nearer Office → kDirectionToHome; start null OR equidistant → null.
    - D-08 only Home set (office coords null): end near Home → kDirectionToHome; end not near Home → null (never guess to_office).
    - D-08 only Office set: end near Office → kDirectionToOffice; else null.
    - D-09 additive: both Home & Office null → null. end null (no polyline / <1 point) → null.
    - Boundary: distance exactly == radius is OUTSIDE (strictly within = `< radius`, D-06).
  </behavior>
  <action>
    Create lib/features/trips/services/geofence_direction_resolver.dart: a stateless `class GeofenceDirectionResolver` with a const constructor (mirror DirectionLabelService). Single method:
    ```
    String? resolve({
      required ({double lat, double lng})? start,
      required ({double lat, double lng})? end,
      required double? homeLat, required double? homeLng,
      required double? officeLat, required double? officeLng,
      double radiusMeters = kGeofenceRadiusMeters,
    })
    ```
    Implementation per D-04..D-09:
      - If end == null → return null (no anchor; D-09).
      - hasHome = homeLat != null && homeLng != null; hasOffice = officeLat != null && officeLng != null.
      - If !hasHome && !hasOffice → return null (additive; D-09).
      - Compute endNearHome / endNearOffice using `Geolocator.distanceBetween(end.lat, end.lng, anchorLat, anchorLng) < radiusMeters` (strict; D-06), each guarded by the corresponding hasHome/hasOffice (an unset anchor is never "near"; D-08).
      - Both near (overlap, D-07): if start == null → null; else compare `distanceBetween(start, home)` vs `distanceBetween(start, office)`; start strictly nearer Home → kDirectionToOffice; strictly nearer Office → kDirectionToHome; equal → null.
      - endNearOffice only → kDirectionToOffice. endNearHome only → kDirectionToHome.
      - Neither near → null (D-06 / D-08 fallback).
    PII guard (T-21-02): the resolver NEVER logs; no print/debugPrint; coords flow in and the label flows out, nothing is persisted or emitted here. Add a class dartdoc stating this is the single pure geofence policy used at finalize (D-10) AND in backfill (Plan 03, D-11), and that it does NO I/O — only the static Geolocator.distanceBetween math.

    Create test/unit/features/trips/geofence_direction_resolver_test.dart mirroring direction_label_service_test.dart. Use real-world coordinate fixtures (NOT 0,0 — T-21-03 example coords are fine, e.g. two points a few km apart so distanceBetween returns realistic meters). Cover EVERY behavior bullet above as a named test, including: confident END→Office, confident END→Home, outside-both→time-fallback(null), overlap tie-break both directions + equidistant→null + start-null→null, only-Home-set (near→to_home, far→null), only-Office-set (near→to_office, far→null), both-null→null (additive), end-null→null, and the exact-radius boundary (== radius is outside). Every assertion automated.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter analyze lib/features/trips/services/geofence_direction_resolver.dart test/unit/features/trips/geofence_direction_resolver_test.dart && flutter test test/unit/features/trips/geofence_direction_resolver_test.dart test/unit/features/trips/direction_label_service_test.dart</automated>
  </verify>
  <done>GeofenceDirectionResolver is a pure const service implementing D-04..D-09 exactly, with an exhaustive test suite proving every case (confident match, outside-both, overlap tie-break, only-one-set, additive null, radius boundary) — all green, no Drift/Riverpod/plugin-channel dependency.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Finalize wiring (decode endpoints, write direction_source) + precedence + tag manual paths</name>
  <read_first>
    - lib/features/tracking/services/tracking_service_controller.dart (persistFinalizedTrip — the autoLabel/directionOverride block + TripsCompanion.insert)
    - lib/features/tracking/providers/tracking_providers.dart (resolvedDirection method + its dartdoc)
    - lib/features/trips/providers/trip_management_providers.dart (editTrip TripsCompanion)
    - lib/features/trips/services/geofence_direction_resolver.dart (Task 2 output — the resolve signature)
    - lib/shared/utils/polyline_codec.dart (decodePolyline)
    - test/unit/features/tracking/tracking_notifier_direction_test.dart (finalize/persist test setup to mirror)
  </read_first>
  <behavior>
    - persistFinalizedTrip with a polyline whose END is within radius of Office writes direction=to_office AND direction_source='geofence'.
    - persistFinalizedTrip with a polyline whose endpoints are far from both writes the time-of-day label AND direction_source='time'.
    - persistFinalizedTrip with directionOverride non-null writes the override AND direction_source='manual' (override beats geofence).
    - persistFinalizedTrip with empty polyline (no endpoints) falls back to time AND direction_source='time' (geofence aborts cleanly).
    - editTrip (both call sites) stamps direction_source='manual' so a subsequent backfill leaves the row untouched.
  </behavior>
  <action>
    In lib/features/tracking/services/tracking_service_controller.dart persistFinalizedTrip: after reading prefs and computing `autoLabel`, decode `trip.encodedPolyline` via `decodePolyline` (import polyline_codec). Extract `start = points.isEmpty ? null : points.first` and `end = points.isEmpty ? null : points.last`. Run `const GeofenceDirectionResolver().resolve(start: start, end: end, homeLat: prefs.homeLat, homeLng: prefs.homeLng, officeLat: prefs.officeLat, officeLng: prefs.officeLng)` → `geofenceLabel`. Then:
      `final direction = directionOverride ?? geofenceLabel ?? autoLabel;`
      `final directionSource = directionOverride != null ? kDirectionSourceManual : geofenceLabel != null ? kDirectionSourceGeofence : kDirectionSourceTime;`
    Add `directionSource: Value(directionSource)` to the TripsCompanion.insert. Update the method dartdoc per D-09/D-10: precedence is override ?? geofence ?? time; direction_source records which path won. PII guard (T-21-02): the decoded endpoints are LOCAL variables consumed by the pure resolver and never logged or persisted as raw coords — only the resulting label + source persist.

    In lib/features/tracking/providers/tracking_providers.dart resolvedDirection: extend the dartdoc to state that the AUTHORITATIVE geofence resolution happens at finalize (D-10) because it requires the END coord, which does not exist mid-trip; the live header/notification preview therefore stays `override ?? time` (no behaviour change here). Do NOT add a Drift read of coords to this hot path. (If a future plan wants a live preview it can read the START coord only — out of scope here.)

    In lib/features/trips/providers/trip_management_providers.dart editTrip: add `directionSource: const Value(kDirectionSourceManual)` to the TripsCompanion in `tripsDao.updateTrip`. Update the editTrip dartdoc per D-03: every editTrip call is a user setting the direction (Phase 17 quick toggle via _handleDirectionChanged AND the Phase 19 edit sheet), so it stamps direction_source=manual — this is what guarantees backfill (Plan 03) never clobbers a user's choice (SC#4). insertManualTrip is NOT changed (keeps the DB default 'time' per D-11 — a manual entry has no GPS to geofence and the user did not pick a direction at create).

    Create test/unit/features/tracking/persist_geofence_direction_test.dart on AppDatabase(NativeDatabase.memory()) (mirror tracking_notifier_direction_test.dart setup): upsert prefs with Office/Home coords, build a FinalizedTrip with an encoded polyline whose decoded endpoints land near Office, call persistFinalizedTrip, read the trip back, assert direction==to_office AND directionSource=='geofence'. Repeat: endpoints far from both → time label + source 'time'; directionOverride set → override + source 'manual'; empty polyline → time + source 'time'. Use encodePolyline to construct the test polylines from chosen coords so the round-trip is realistic.
  </action>
  <verify>
    <automated>cd /Users/coolman/Documents/Projects/bizzle && flutter analyze lib/features/tracking/ lib/features/trips/providers/trip_management_providers.dart && grep -q "kDirectionSourceManual" lib/features/trips/providers/trip_management_providers.dart && grep -q "GeofenceDirectionResolver" lib/features/tracking/services/tracking_service_controller.dart && flutter test test/unit/features/tracking/persist_geofence_direction_test.dart test/unit/features/tracking/tracking_notifier_direction_test.dart</automated>
  </verify>
  <done>Finalize decodes polyline endpoints, applies override ?? geofence ?? time, and writes both direction and direction_source; editTrip stamps direction_source=manual at both manual call sites; resolvedDirection's dartdoc documents the finalize-only geofence resolution; the persist test proves geofence/time/manual/empty-polyline all write the correct direction + source — all green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| migration ↔ existing on-device data | A v6 upgrade runs against real user trip + prefs rows on first launch after update. A faulty migration could drop or corrupt commute history. |
| saved coords ↔ logs / sync payload | Home/Office lat/lng and a trip's decoded endpoints are PII-adjacent (they reveal where the user lives/works). They must never reach logs, crash reports, or any new sync field. |
| finalize input ↔ trips.direction | The geofence resolver produces a string written to `trips.direction`; a non-`to_office`/`to_home` value would corrupt stats grouping. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-21-01 | Tampering | v5→v6 migration of existing trips/prefs | mitigate | Migration is additive only (5× addColumn, no UPDATE/DROP). The migration_v6_test asserts an existing trip row survives, reads direction_source='time', and prefs coords read null — proving no data loss (D-01/D-02). |
| T-21-02 | Information disclosure | Home/Office coords + decoded trip endpoints | mitigate | The resolver does NO logging and takes/returns only primitives + the label; decoded endpoints are local variables in finalize, never logged or persisted as raw coords (only the label + source persist). Coord columns are stored locally in Drift; NO new sync field carries them (v0.3 sync is unchanged). Dartdocs on the coord columns and resolver state "PII-adjacent — never log". |
| T-21-03 | Tampering | trips.direction value from resolver | mitigate | The resolver returns ONLY kDirectionToOffice, kDirectionToHome, or null — never an arbitrary string. The exhaustive resolver test pins every branch's exact return value, so no out-of-domain string can reach trips.direction via the geofence path. |
| T-21-04 | Repudiation | who set a trip's direction | mitigate | direction_source records manual/geofence/time per write, so a later backfill can prove a row was user-set and skip it (the SC#4 "override sticks" guarantee). The persist + editTrip tests pin the source value on every path. |
</threat_model>

<verification>
- `dart run build_runner build` and the two `drift_dev schema` commands succeed and regenerate generated files + the v6 snapshot.
- `flutter test test/unit/database/migration_v6_test.dart` is green — v5→v6 preserves data, defaults are safe.
- `flutter test test/unit/features/trips/geofence_direction_resolver_test.dart` is green — every D-04..D-08 case proven.
- `flutter test test/unit/features/tracking/persist_geofence_direction_test.dart` is green — finalize writes correct direction + source for geofence/time/manual/empty-polyline.
- `grep kDirectionSourceManual lib/features/trips/providers/trip_management_providers.dart` confirms editTrip stamps manual.
- `flutter analyze` reports no new issues on touched files; the existing full `flutter test` suite stays green (UserPreferencesValue gained required fields threaded through every site).
</verification>

<success_criteria>
- SC#2 foundation + delivery: geofence labeling runs at finalize — END near Office → to_office, END near Home → to_home (D-04, D-10).
- SC#3: precedence is override ?? geofence ?? time, recorded durably in direction_source; no confident match → time fallback (D-06, D-09).
- SC#4 foundation: every manual write path stamps direction_source=manual so a user override is durable and backfill-proof (D-03).
- SC#5: with no Home/Office set the resolver returns null and behaviour is byte-for-byte the pre-Phase-21 time heuristic — purely additive (D-09); the v6 migration leaves every historical row as direction_source='time' with null coords.
- D-01/02/04..10 implemented exactly; v5→v6 migration proven safe by an automated SchemaVerifier test.
</success_criteria>

<output>
After completion, create `.planning/phases/21-home-office-locations-geofence/21-01-SUMMARY.md`
</output>
