import 'package:flutter/material.dart';

/// Light theme for the Traevy app.
///
/// Phase 1 ships the Material 3 defaults unchanged — Phase 7 (polish)
/// replaces this with a branded colour scheme.
final ThemeData lightTheme = ThemeData.light(useMaterial3: true);

/// Dark theme for the Traevy app.
///
/// Phase 1 ships the Material 3 defaults unchanged — Phase 7 (polish)
/// replaces this with a branded colour scheme.
final ThemeData darkTheme = ThemeData.dark(useMaterial3: true);
